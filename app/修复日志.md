> **2026-09-07 实施说明**：下方「2026-09-07 逐文件夹审查」第 1~5 批清单中的「待修」项已全部实施完毕（含 3 项按保守语义落地的待主人定方案），回归 173 passed、门禁通过；「语义确认」项与第 7 批（scripts/tests，不在 app 范围）除外。逐项状态见文末「实施记录」。

# 修复日志 — app/main.py

| 日期 | 级别 | 问题 | 修复 |
|---|---|---|---|
| 09-05 | P2 | lifespan 启动执行 ensure_indexes 冗余 DDL（Alembic 唯一入口原则例外） | 移除调用（索引已在基线迁移） |
| 09-05 | P2 | 连接池日志硬编码 min=10/max=30 与真实配置（env 默认 20/50）不符，误导排查 | 日志读 _POOL_CONFIG 实际值 |

# 修复日志 — deploy（补充）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | nginx.conf.lite | P2 | /metrics、/test-otel 经 location / 暴露公网，泄露接口路径/用量/错误类型 | nginx 精确匹配返回 404（nginx -t 已验证） |

# 审查发现 — 2026-09-07 逐文件夹审查·第1批（main/core/middleware）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P1 | core/memory_manager.py:48-56 | L1 缓存缺失并发回填用裸 rpush，同会话并发冷启动会把同一批消息写两遍，LLM 上下文重复（trim 只裁条数不去重） | 待修：回填改 Lua 原子 DEL+RPUSH 或 SET NX 回填锁 |
| P2 | main.py:142 / core/memory_manager.py:109,128 / core/audit.py:41 / core/task_manager.py:238 | asyncio.create_task 裸创建不持引用，CPython 弱引用可被 GC 中途回收 | 待修：模块级 set 持引用 + done callback 移除，一处工具函数全仓复用 |
| P2 | core/error_aggregator.py:30 | str(exc) 原文（httpx 异常带完整 URL，高德 key 在参数里）入 Redis 7 天并展示于 /api/admin/errors；sanitize_error_text 已存在未用 | 待修：入库前过 sanitize_error_text |
| P2 | core/stream_utils.py:172-190 | file_ids 数量无上限，每 id 2 次 Redis GET，恶意请求可放大 Redis 往返 | 待修：限制 file_ids 数量上限 |
| P2 | core/redis.py:57-62 | get_redis 并发首调无双检锁，可能双初始化连接池（lifespan 先启，概率低） | 待修 |
| P2 | main.py:231 | OTEL exporter 端点硬编码 http://tempo:4318，lite 部署无 tempo 时后台静默重试 | 待修：改环境变量 |
| P2 | core/config.py:142 | APP_ENV 用裸 os.getenv，与 BaseSettings 双轨 | 待修：纳入 Settings 字段 |
| P2 | core/sms.py:59,100 | ttl_minutes 形参被忽略，TemplateParam 硬编码 "5" | 待修：透传或删参 |
| P2 | core/task_manager.py:36 | _cancel_events 进程内字典依赖调用方 cleanup_event，异常路径漏清慢性泄漏 | 待修：routes 批次确认调用方后处理 |
| 语义确认 | main.py:43-75 | _ensure_admin_users 仅首次建号时用 ADMIN_PASSWORD，后续改 env 不生效（bootstrap-only） | 待主人确认语义 |
| 语义确认 | main.py:190-202 | init_pool/init_db 失败仅 warning 继续启动，DB 挂时网关在线但全业务 500（代码注释称有意降级） | 待主人确认语义 |

# 审查发现 — 2026-09-07 逐文件夹审查·第2批（routes）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P1 | routes/auth.py:96 vs 101 | 手机号登录防暴力锁键不一致：_check_login_lock 用原始输入（手机号），_record_login_failure 用解析后 username → 锁永远写在与检查不同的键上，手机号登录可无限试密码 | 待修：检查与落锁统一用同一键（如都用解析后名） |
| P1 | routes/map_api.py:210-254, 257-302 | /api/map/recommend 每请求一次 LLM 调用、/api/map/route 同，但完全绕过 ensure_chat_allowed/日 token 限额（限流只挂在 chat 路径）→ 登录用户可刷 LLM 配额；plan_route/recommend 的 key 获取方式不统一（get_deepseek_key vs 裸 os.getenv，2026-09-05 后池化=单 key，实为表述统一问题） | 待修：两处接入配额门禁为主，key 取法统一为辅 |
| P1 | routes/phone.py:121,184,297 | logger.info 打印完整明文手机号（kefa 红线：日志禁手机号；sms.py 已有 _mask_phone 未用） | 待修：统一过 _mask_phone |
| P1 | routes/phone.py:147,204 + routes/auth.py:77-87 | 手机号自动注册默认密码硬编码 123456789，而 /api/login 支持手机号+密码直登 → 知道手机号即可无短信登录所有未改密账号 | 待修：默认密码随机化或禁用默认密码密码登录（待主人定方案） |
| P2 | routes/oauth.py:143-144 | except 把 str(e) 未脱敏未转义拼进重定向 URL 反射给前端（可能含上游错误细节/URL） | 待修：映射为固定错误码 |
| P2 | routes/oauth.py:140 | access/refresh token 放 URL query（浏览器历史/访问日志/Referer 泄露面） | 登记：改 fragment 或一次性 code 需前端配合 |
| P2 | routes/observability.py:71-86 | /metrics、/test-otel 应用层无鉴权（lite 由 nginx 404 兜底，直连容器端口即绕过） | 待修：应用层加 admin 门禁或网络层隔离确认 |
| P2 | routes/phone.py:188-232 | phone_login 不校验 is_active，禁用账号可再签发 token（get_current_user 403 兜底，但用户缓存窗口 ≤5min） | 待修：登录路径补 is_active 检查 |
| P2 | routes/users.py:112-131 | /api/personas 无鉴权（暴露人格列表与 knowledge_base 路径）；/api/persona/switch 任何登录用户可改全局默认人格，影响所有未显式指定的请求 | 待修：switch 收敛 admin 或改按请求生效（待主人定） |
| P2 | routes/map_api.py:301,371,229-233 | bare except 会吞 BaseException；steps 为空数组时 [0] IndexError；recommend 死代码 url/headers；amap_route 无显式超时（默认 5s×5 连外调） | 待修 |
| P2 | routes/auth.py:137-158 | change_password 不吊销现存 refresh token，改密后旧会话最长 30 天仍有效 | 待修：改密时把用户全部 refresh jti 拉黑或短期失效 |

# 审查发现 — 2026-09-07 逐文件夹审查·第3批（services）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P0 | services/agent_tasks.py:57 定义 vs routes/v2.py:112/125/138 调用 | ensure_task_access(task, username, role) 三参定义被两参调用（task, current_user），运行时已复现 TypeError → 任务制对话 /result /cancel /resume 三端点全部 500 | 待修：调用改 ensure_task_access(task, current_user["username"], current_user["role"]) |
| P1 | services/chat_stream_ctx.py:221 + middleware/rate_limit.py | 日 token 计数器全仓无写入方（唯一调用点 inc_token=0）→ ensure_chat_allowed 的 daily_token 限额永不触发；任务路径/地图路径同样不计数 | 待修：finalize_answer 透传 usage 并累计，或改由 record_token_usage 顺带 INCRBY |
| P2 | services/chat_stream_ctx.py:86 | today 用本地 datetime.now()，rate_limit 的 TTL 按 UTC epoch 日切+注释声明统一 UTC（P3 #39），容器外时区漂移时日切错位 | 待修：统一 UTC 日期 |
| P2 | services/chat_stream_ctx.py:175 | 无 conversation_id 时 conv_id=conv_{user}_{秒级时间戳}，同秒并发两条无 id 请求共用一个会话，历史互相污染 | 待修：加 uuid 后缀 |
| P2 | services/agent_tasks.py:93-138 | 任务路径无 QPS/日配额（仅全局 50 槽位）；resume 不复查 is_quota_exhausted；create 未持久化 lang，resume 恒回中文 | 待修 |
| P2 | services/agent_tasks.py:173 | cancel_agent_task 终态集合漏 "timeout"，超时任务可被再置 cancelled（轻微） | 待修 |
| P2 | services/chat_fallback.py:55-79 | Flash 降级路径逐块 DFA 后无全文终检（主路径 runner/ReAct 有），跨块拼接敏感词在降级链可漏出 | 待修：流尾对累积文本 contains_sensitive |
| P2 | services/agent_tasks.py:182 | resume_agent_task 可变默认参数 user_perms=[]（只读未触发，规范问题） | 待修：改 None |
| P2 | services/chat_react.py:137 vs chat_fast_paths.py:175 | 工具参数兜底提取一个用 ctx.req.query 一个用 ctx.user_query（改写后），语义不一致 | 待修：统一 user_query |

# 审查发现 — 2026-09-07 逐文件夹审查·第4批（models + agents）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P1 | agents/document_parser.py:103-160,210-269 | PDF/DOCX 解析无页数/内嵌图片数/总时长上限：20MB 限制约束不了页数，1MB PDF 可含上万页/图，每页渲染 150dpi+OCR（OCR 全局 threading.Lock 串行）→ 单个恶意上传可占住 OCR 通道小时级，阻塞全部用户的文档解析，并占满 run_in_executor 默认线程池 | 待修：页数/图片数上限 + 解析 deadline，超限截断 |
| P2 | agents/tools.py:454-477 | _generate_embedding 硬编码 localhost/host.docker.internal 两个 Ollama URL，config.EMBEDDING_API_URL 配置存在但此路径不用 | 待修：读配置 |
| P2 | models/schemas.py:5-10 | ChatRequest.query 无 max_length，超大 query 直达 LLM（限流只管频率不管单请求体积）；file_ids 无上限（第1批已记 stream_utils 侧，schemas 是源头） | 待修：加长度/数量约束 |
| P2 | agents/router.py:341 / agents/runner.py:267 | user_perms: list \| None = [] 可变默认参数（当前只读未触发，规范问题，两处同源） | 待修：改 None |
| P2 | agents/orchestrator.py:361-398 | Phase1/Phase2 并行 N 路 LLM 调用不经 llm_semaphore，绕过全局 LLM 并发控制（stream_llm 路径受控，此路径不受控） | 待修：纳入信号量 |
| P2 | agents/tools.py:222-241 | _keyword_fill 用户关键词的 ILIKE 通配符 %/_ 未转义，可被构造成全表模糊匹配（性能面，非注入） | 待修：escape |
| P2 | agents/tools.py:38 | 天气 lives 为空数组时 lives[0] IndexError，落"天气查询失败"而非"暂无天气数据"（有异常兜底，仅文案精度） | 待修 |

# 审查发现 — 2026-09-07 逐文件夹审查·第5批（cdc + payment）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P0 | payment/refund.py:65-134 + routes.py:105-122 | 充值订单退款资金方向错误：原充值已给钱包 +amount（service._settle_recharge），退款又执行 _locked_wallet_change(+refund_amount) 再加一次 → 同一笔模拟充值双倍入账；且 /api/payment/refund 对所有登录用户开放、无频率限制（充值 95% 成功可重试）→ 「充值→退款→再充值」可无限刷余额。正确语义：充值单退款应扣回钱包(-)或禁止用户自退，仅消费单退款才应加回(+)（消费单因 amount 为负恰好被 _decide_refund 拦住，实际可退的只有充值单，即全是错误方向） | 待主人定语义后修 |
| P2 | payment/_ledger.py:264-280 | _record_transaction 函数体引用未定义的 order_no（NameError 地雷）；全仓无调用方，属死代码但被 re-export 为公开符号 | 待修：删除或补参数 |
| P2 | payment/service.py:508-524 | recover_stale_processing 扫 DB status='processing'，但全仓没有任何代码把订单写成 processing（该词只用于 Redis 幂等占位）→ 死代码，docstring 描述的恢复场景不存在 | 待修：删除或让渠道路径真正落 processing |
| P2 | payment/service.py:398 | 扣费"余额不足按余额扣"边界不一致：余额 0.01 扣 0.01 至 0，余额恰好 0 时条件 before_balance>0 不成立 → 扣全额转负；同为"不足"却两种结果 | 待主人确认语义 |
| P2 | payment/admin_routes.py:124-184 | /api/admin/reconcile 恒记 status='balanced'，只统计不核对差异，"对账"名不副实（模拟环境可接受） | 语义确认 |
| P2 | payment/deferred.py:111-113 | _settle_one 钱包不存在时 return False 永久重试，每维护轮刷一条 error（毒丸单无作废路径） | 待修：计数后作废 |
| 备注 | app/cdc 全部 | 未发现实质问题：leader 锁/续期失败即停、at-least-once 落盘、分区 DROP 水位守卫、checkpoint 单调保护、API admin 门禁均正确 | 通过 |

# 审查发现 — 2026-09-07 逐文件夹审查·第7批（scripts/tests/alembic/deploy/tools）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P1 | tests/test_deepseek_direct.py（import 期读 /app/.env、直连真实 DeepSeek API）等混入 tests/ 的一次性诊断脚本 | `pytest tests` 收集即 FileNotFoundError，整套测试无法运行（实测复现）；tests/ 还混有 diag_*.py、clear_and_rerun.py、xlsx/json 数据等非测试资产 | 待修：移 tests/manual/ 或 pytest.ini 加 testpaths/nhoarding 排除；156 个有效用例是资产，别被淹掉 |
| P2 | scripts/task_scheduler.py:110-118 | 越权校验可被 `python xxx.py` 形式绕过（command[0]="python" 无 .py 后缀无路径分隔 → 校验跳过）；startswith 前缀无 os.sep 保护（scripts 可匹配 scripts_x）——本地工具影响有限 | 待修：取 command 中首个 .py/.sh 参数校验 + sep 兜底 |
| P2 | scripts/retention_cleanup.py vs core/db_maintenance.py | 两套数据生命周期并存：maintenance 30 天压缩对话为 system 摘要（刻意保护），retention 90 天按 created_at 全删（含摘要）——实际历史寿命 90 天；另 --days 不带 --table 被静默忽略 | 语义确认：摘要是否应活过 90 天 |
| P2 | scripts/check_code_rules.py:23 vs 文档 | docstring 称默认检查 app/ mcp_assets/ scripts/，实际 DEFAULT_TARGETS=["app"]（文档漂移） | 待修 |
| L5 | tests/ 整体 | 156 个单测/集成用例且命名对应历史 P0/P1 修复，覆盖意识好；但 test_deepseek_direct 类脚本污染收集 + 无 CI 全量门禁（推送规则只要求"受影响模块 pytest"） | 建议：pytest.ini testpaths 限定 unit/integration |
| 备注 | alembic/ + tools/ | 迁移与工具 manifest 未发现实质问题（DDL 全走迁移、触发器校验 fail-loudly 已在 cdc/schema 验证） | 通过 |

# 审查发现 — 2026-09-07 逐文件夹审查·第8批（langgraph_lab / mcp_assets / static）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P2 | mcp_assets/servers/weather-demo/weather_server.py:70-77 | 与主链路同源的 lives[0] IndexError + 缺 weather/temperature KeyError 都没带过来——主 app 9-06 修的「港澳台暂无天气数据」修复未同步到 demo（copy 漂移第 3 处），且 {"error": str(e)} 把裸异常文本回给 MCP Client | 待修：对齐 fetch_weather_async 修复 |
| P2 | mcp_assets/servers/civil-code-rag/civil_code_server.py | 四步检索流水线是 app/agents/tools.py 的全量手工复制（文件头自称"自包含"是有意为之）：主库已演进为双路召回+RRF+本地 rerank，资产版仍是旧三步——漂移会持续扩大；另 L288-289 except: pass 静默（违反自家 silent-except 规范）、L256 __import__("json") 内联导入 | 登记：确认资产定位（快照式展示 vs 持续同步）后择一 |
| P3 | static/sw.js | 离线回退只缓存 '/' 旧 shell：部署后老客户端离线刷新会拿旧 index.html 引已失效 hash（在线访问一次自愈）；API/SSE 不拦截、资产缓存优先策略本身正确 | 可接受，登记 |
| P3 | mcp_assets/.../import_civil_books.py:36 | import 期 os.system 自动 pip install（本地脚本可接受，规范问题） | 待修 |
| 备注 | langgraph_lab/roundtable.py | 纯 mock 学习代码，无问题 | 通过 |

# 修复方案定稿 — 副本漂移（2026-09-07）

> 对应本日志第 8 批两条 P2（weather_demo 未同步修复、civil_code_server 流水线快照）及第 2 批 plan_route key 不一致、deploy 日志的 publish_lite.py（已删除解决）。
> 病根是同一个：同一段逻辑存在多份物理拷贝，修主份时副本不在视野里。以下两案按场景选用。

## 方案 A：抽共享层（首选，能 import 就用）

单一事实源，副本变薄壳。仓库内已有成功先例：资金原语拆 `_ledger.py` 后，充值/扣费/退款/延迟结算四条路径 import 同一份代码，乐观锁改行锁一处生效、从未漂移。

```python
# weather_demo 的正解：它本来就 import 主库的 _env.py（并非真自包含），
# 顺着把函数也 import 过来，文件瘦成工具声明薄壳——主库修复自动生效
from app.agents.tools import fetch_weather_async

@mcp.tool()
async def query_weather(city: str) -> dict:
    return await fetch_weather_async(city)
```

## 方案 B：快照标注 + CI 比对（拷贝必须独立存在时）

两件事：文件头写明快照身份（谁/何时/主实现在哪/已知差异）；比对测试把「静默漂移」变「构建期红灯」——主库一动、副本没跟就 fail，强迫二选一：重新同步，或有意识地更新标注。

```python
# tests/test_snapshots.py 最小实现：主实现一动、快照没跟就红
# （红不是坏事：强迫决策"重新同步"或"更新快照基线"）
import hashlib
from pathlib import Path

SNAPSHOTS = [  # (主实现, 快照, 快照基线说明)
    ("app/agents/tools.py", "mcp_assets/servers/weather-demo/weather_server.py",
     "2026-09-07 港澳台修复未同步"),
]

def test_snapshot_annotations_exist():
    for main, snap, _note in SNAPSHOTS:
        head = Path(snap).read_text(encoding="utf-8")[:800]
        assert "【快照标注】" in head, f"{snap} 缺少快照标注（漂移防线失效）"
```

## 案件分派

| 漂移点 | 采用 | 状态 |
|---|---|---|
| weather_demo / fetch_weather_async | A（改 import） | 待修（先落快照标注止血） |
| civil_code_server / 四步流水线 | B（资产要求自包含展示） | 注释已落地，CI 比对待加 |
| publish_lite.py | 删除 | 已解决（2026-09-07） |
| plan_route 裸 os.getenv | 并入第 2 批 P1 配额门禁一并修 | 待修 |

# 实施记录 — 2026-09-07 逐文件夹审查批次修复（第 1~5 批）

> 执行顺序：core(+main/middleware) → routes → services(+v2) → agents(+models) → cdc+payment，逐文件夹实施。
> 备份：D:/桌面/agent_gateway_backup/20260907_review_fixes/（改动前原文件，按仓库相对路径存放）。
> 回归：tests/unit/test_20260907_batch_fixes.py（18 用例）+ 全量 tests/unit 173 passed。
> 门禁：scripts/check_code_rules.py 通过（runner.run_agent_task 因本轮增量超 80 行，拆出 _release_task_resources）。

| 批次 | 位置 | 原级别 | 修复摘要 | 状态 |
|---|---|---|---|---|
| 1 | core/memory_manager.py 回填 | P1 | 回填改 Lua「列表为空才整批写入」原子脚本，并发冷启动败者自动跳过，不重不丢 | ✅ 已修 |
| 1 | 全仓裸 create_task | P2 | 新增 core/concurrency.spawn（模块级 set 持强引用 + done callback 移除 + 异常记日志）；本轮接入 main/memory_manager/audit/task_manager/chat_stream_ctx/llm_streaming/agent_tasks/runner 共 10 处 | ✅ 已修 |
| 1 | core/error_aggregator.py:30 | P2 | 入库前过 sanitize_error_text(fallback="(详情已脱敏)") | ✅ 已修 |
| 1 | core/stream_utils.py file_ids | P2 | build_file_context 截断上限 5（与 schemas 同值兜底） | ✅ 已修 |
| 1 | core/redis.py get_redis | P2 | asyncio.Lock 双检锁防并发双初始化 | ✅ 已修 |
| 1 | main.py OTEL 端点 | P2 | 改读 OTEL_EXPORTER_OTLP_ENDPOINT（默认 tempo 不变；置空跳过 exporter） | ✅ 已修 |
| 1 | core/config.py APP_ENV | P2 | 纳入 Settings.app_env 字段，删裸 os.getenv 双轨 | ✅ 已修 |
| 1 | core/sms.py ttl_minutes | P2 | TemplateParam 透传 str(ttl_minutes)，不再硬编码 "5" | ✅ 已修 |
| 1 | core/task_manager.py _cancel_events | P2 | run_agent_task finally 统一 cleanup_event（拆 _release_task_resources），异常/早退路径不再漏清 | ✅ 已修 |
| 2 | routes/auth.py 登录锁键 | P1 | 先 resolve_login_username 再检查/记账/清零，全程同一键 | ✅ 已修 |
| 2 | routes/map_api.py 配额门禁 | P1 | /recommend /route 接 ensure_chat_allowed + finally release_concurrent；/route key 取法统一 get_deepseek_key | ✅ 已修 |
| 2 | routes/phone.py 日志手机号 | P1 | 6 处日志统一 _mask_phone | ✅ 已修 |
| 2 | routes/phone.py 默认密码 123456789 | P1 | **决策落地**：默认密码随机化（secrets.token_urlsafe），密码登录对自动注册账号不再可用，走短信验证码登录；前端 zh/en 提示文案同步改为短信登录引导 | ✅ 已修 |
| 2 | routes/oauth.py str(e) 反射 | P2 | 固定 error=github_callback_failed，细节 logger.exception 进日志 | ✅ 已修 |
| 2 | routes/observability.py 门禁 | P2 | /metrics /test-otel 走 METRICS_TOKEN Bearer 门禁，未配置 fail closed（403 附配置提示；Prometheus 抓取需携带同值 Token） | ✅ 已修 |
| 2 | routes/phone.py is_active | P2 | phone_login 查询补 is_active，禁用账号 403 不再签发 token | ✅ 已修 |
| 2 | routes/users.py personas | P2 | /api/personas 加登录鉴权（前端 http 封装自带 JWT，无破坏）；/api/persona/switch 收敛 admin（前端为本地切换，不调此端点，无破坏） | ✅ 已修 |
| 2 | routes/map_api.py 杂项 | P2 | bare except→except Exception；steps 空数组 or [{}] 兜底；删 recommend 死代码 url/headers；amap_route/geojson 显式 MAP_API_TIMEOUT | ✅ 已修 |
| 2 | routes/auth.py 改密吊销 | P2 | 改密写 auth:pwd_changed:{username}（TTL=refresh 有效期），refresh 发现 iat 早于改密时刻即 401 | ✅ 已修 |
| 3 | routes/v2.py ensure_task_access | P0 | 三处调用补 (username, role)——/result /cancel /resume 三端点从全量 500 恢复 | ✅ 已修 |
| 3 | services/chat_stream_ctx.py 日 token 计数 | P1 | record_token_usage（全路径唯一 usage 计量点）顺带 update_daily_usage(inc_token)，daily_token 限额从此有写入方；日切 UTC | ✅ 已修 |
| 3 | services/chat_stream_ctx.py today | P2 | ensure_chat_allowed 切 UTC 日期（与 TTL 对齐口径一致） | ✅ 已修 |
| 3 | services/chat_stream_ctx.py conv_id | P2 | 无会话 id 改 uuid 后缀，同秒并发不再共用会话 | ✅ 已修 |
| 3 | services/agent_tasks.py 任务配额 | P2 | 新增 _task_quota_gate（QPS + 日 req/token，UTC 口径），create/resume 都过；resume 复查并计入日请求 | ✅ 已修 |
| 3 | services/agent_tasks.py lang | P2 | create 持久化 lang 到任务 hash，resume 透传原 lang（缺省 zh） | ✅ 已修 |
| 3 | services/agent_tasks.py timeout 终态 | P2 | cancel_agent_task 终态集合补 "timeout" | ✅ 已修 |
| 3 | services/chat_fallback.py 流尾终检 | P2 | Flash 降级累积全文，流尾 contains_sensitive 命中以安全文案收口（对齐主路径） | ✅ 已修 |
| 3 | services/agent_tasks.py user_perms=[] | P2 | 改不可变 ()——**按语义修正审查建议**：None 在 search_knowledge 语义是"不过滤"（fail-open），作缺省会把省略调用翻成宽权限；() 既除可变默认参隐患又保 fail-closed（router/runner 两处同改，旧测试钉住的 [] 断言同步更新为 ()） | ✅ 已修 |
| 3 | services/chat_react.py 工具兜底 query | P2 | 统一 ctx.user_query（与 chat_fast_paths 同口径） | ✅ 已修 |
| 4 | agents/document_parser.py 限额 | P1 | 页数 200 / 图片 50 / deadline 60s（协作式，页粒度检查），超限截断并追加标注；PDF 内嵌图/整页渲染/DOCX media 三条 OCR 通道全覆盖 | ✅ 已修 |
| 4 | agents/tools.py _generate_embedding | P2 | URL/模型改读 EMBEDDING_API_URL/EMBEDDING_MODEL，自动派生另一台主机名兜底 | ✅ 已修 |
| 4 | models/schemas.py 约束 | P2 | query/message max_length=65536；file_ids max_length=5（stream_utils 侧另有兜底） | ✅ 已修 |
| 4 | agents/orchestrator.py 信号量 | P2 | _call_deepseek_think 纳入 llm_semaphore（Phase1/Phase2 并行 N 路不再绕过全局并发控制） | ✅ 已修 |
| 4 | agents/tools.py ILIKE 转义 | P2 | %/_/\ 转义 + SQL ESCAPE '\' 声明，关键词不再可构造全表模糊匹配 | ✅ 已修 |
| 4 | agents/tools.py weather lives 空 | P2 | lives 空数组返回「{city} 暂无天气数据」，不再 IndexError 落"查询失败" | ✅ 已修 |
| 4 | agents/runner.py conv_id/计数 | P2 | conv_id 同步改 uuid 后缀；inc_used_questions 改 spawn | ✅ 已修 |
| 5 | payment/refund.py 退款方向 | P0 | **决策落地**：取「禁止用户自退充值单」方案（另一方案"扣回钱包"会在款已消费时造出负余额黑洞）；/refund 路由补按用户限频 5 次/小时。「充值→退款→再充值」刷余额回路关闭 | ✅ 已修 |
| 5 | payment/_ledger.py _record_transaction | P2 | 删除（NameError 地雷 + 全仓无调用方），service re-export 同步移除 | ✅ 已修 |
| 5 | payment/service.py recover_stale_processing | P2 | 删除，db_maintenance 调用点一并移除（全仓无路径写 processing，恢复场景不存在） | ✅ 已修 |
| 5 | payment/deferred.py 毒丸单 | P2 | 无钱包失败 Redis 计数，连续 5 轮作废（failed）止血留痕，不再每维护轮刷 error 永久重试 | ✅ 已修 |
| 5 | app/cdc 全部 | — | 上轮结论"未发现实质问题"，本轮无改动 | 通过 |

## 遗留与待人工确认（本轮不动）

| 项 | 说明 |
|---|---|
| main.py _ensure_admin_users bootstrap-only 语义 | 待主人确认（保持原状） |
| main.py init_db 失败降级启动语义 | 待主人确认（保持原状） |
| routes/oauth.py token 走 URL query | 需前端配合改 fragment/一次性 code，维持登记 |
| payment/service.py 余额恰好 0 时扣全额转负 | 语义确认（保持原状，登记） |
| payment/admin_routes.py reconcile 恒 balanced | 语义确认（模拟环境可接受） |
| scripts/retention 与 db_maintenance 生命周期口径 | 语义确认（摘要是否活过 90 天） |
| 第 7 批（scripts/tests/alembic/deploy/tools） | 不在 app 范围本轮未实施；其中「pytest tests 收集即失败」本轮已实测复现（tests 根目录混入直连脚本，collect 阶段挂起），建议下一轮优先处理 |
| /metrics 门禁部署配套 | full 部署如需 Prometheus 抓取，需在网关环境变量配置 METRICS_TOKEN 并在抓取端携带同值 Bearer |

# 复审轮 — 2026-09-07（主人指示：仅审查登记，不修复不跑测）

对当前工作树第 1~5 批修复 + 新增文件（web_search.py / secret_scan.py / 三批测试）做逐文件复审。发现按目录登记到各目录修复日志：app/core、app/agents、app/models（新建）、app/middleware、app/payment、app/routes、app/services、mcp_assets（新建）、scripts（新建）、tests（新建）、deploy、frontend。本轮零代码修改、零测试执行。

汇总：**P0 新增 0；P1 新增 1；P2 新增 9；P3 新增 5**。

- P1：core/audit 脱敏键表缺 "phone"——phone.py register/bind 审计仍落明文手机号（phone.py 注释声称已脱敏与实际不符）。
- P2：①router.py:400 / runner.py:176 裸 create_task(safe_set) 未迁 spawn；②llm_streaming.py:108 裸 create_task(deduct_token_cost)——资金路径，建议优先；③map_api 两 LLM 端点绕过 llm_semaphore；④routes/auth change_password 吊销标记写入无 try（Redis 故障→500 且吊销静默丢失）；⑤observability Bearer 比较非常量时间；⑥observability METRICS_TOKEN 未收编 config；⑦refund 充值单拦截黑名单式（建议白名单）；⑧agent_tasks 配额计数在幂等判定之前（重复提交重复计数）；⑨scripts/secret_scan 值字符类缺 _-（门禁假阴性）。
- P3：document_parser 截断标注计数两处小误差；refund 限频 INCR/EXPIRE 崩溃键；models/schemas 与 chat_stream_ctx 无 EOF 换行；fake_redis eval 子串分发脆弱。

待人工确认（新增）：①阿里云短信模板 {min} 变量是否接受任意分钟数（sms.py 透传修复的配套）；②mcp_assets weather 快照方案 A（共享 import）vs 方案 B（标注+自检，现状）拍板。

复审核实为实的关键声明：recover_stale_processing"全仓无路径写 processing"（该词仅作 Redis 幂等占位）；web_search.py 拆分行为等价纯移动；pwd_changed 吊销链路两端闭环（token 均带 iat、TTL 同源）；ensure_chat_allowed 后置门禁自行释放并发槽（map_api try/finally 无泄漏）；/api/personas 鉴权收敛不破坏前端（http 封装统一带 Bearer）；改写重试有完整测试；全仓 wc 复核无文件超 600 行。

# 全目录覆盖轮 — 2026-09-08（主人要求：本目录所有文件夹逐个审完；仅审查登记，未修复）

四路并行审查补齐了此前未覆盖的全部目录：alembic（13 迁移）、.github、deploy 余下文件、根目录基础设施（compose/nginx/prometheus 栈/CI/备份脚本）、scripts（105 个代码文件全量）、tests（62 个全量）、frontend（src 全量）、mcp_assets 余下 4 脚本、tools、langgraph_lab、prompts、static。每个目录的发现已落各自修复日志；根目录新建 [修复日志.md](../修复日志.md) 含**全目录覆盖表**（每个顶层目录的状态与结论，含不适用代码审查的运行时/资料目录）。本轮零代码修改、零测试执行。

**汇总：P0 新增 1；P1 新增 5；P2 新增 17；P3 新增 30+。**

- **P0（最高优先）**：`alertmanager.yml:15` 真实 QQ 邮箱 SMTP 授权码被 git 追踪、随基线提交 a80159d"含密钥清洗"漏洗进历史（已亲自核实 git ls-files + git log）——立即撤销授权码 + git filter-repo 清历史。
- **P1×5**：①`deploy/.server.env` 云服务器 root 明文密码落盘（未入库，改 SSH key）；②scripts 三文件硬编码 64-hex 签名密钥随基线进历史（已核实**非**现行 JWT_SECRET，需人工核对历史是否用过）+ 附伪造 admin token 代码；③`backup_daily.cmd` 路径乱码（0x3F）——**每日备份从未成功**；④prometheus.yml 无认证配置，/metrics 门禁上线后监控抓取全断；⑤alembic d9e0f1a2b3c4 downgrade 不成对（6 列只回滚 1）。tests 收集期黑名单 4 文件（`pytest tests/` 必炸的实锤清单）另计 P1（tests 日志）。
- **P2 亮点**：seed_project_knowledge 等 7 处真实手机号（含写入线上知识库）、check_code_rules 门禁在定时任务下**空转假通过**、slow_query_monitor 失败当健康、Dockerfile 把 tests/（含密码）打进生产镜像、Dockerfile.lite 的 static/assets 未入库（干净构建白屏）、deploy_aliyun.sh 必填未填仍报部署成功、me.json 真实手机号（待确认是否刻意）。
- **死代码**：tools/ 两子包 + app/core/plugin_loader.py（152 行）全仓零消费方，建议整体删除（待确认）。
- **通过**：frontend 无新 P0/P1/P2（locale 键集 244=244 一致）；tests/unit 18 文件质量高无摆设断言；static 与 frontend 是同一前端、sw.js 不缓存 API；迁移链单链无分叉；langgraph_lab、mcp_assets 余下脚本、根目录 ci.sh/start_all.sh 等通过。

下一步建议顺序：①撤销 SMTP 授权码+清历史（唯一 P0）→ ②backup_daily.cmd 修路径并实跑一次备份 → ③tests 收集期黑名单移出（否则回归测试跑不起来）→ ④prometheus 抓取配套 METRICS_TOKEN → ⑤其余 P2 按目录日志逐项清。

# P1 修复批 — 2026-09-08（主人指示：P1 全修 + alert.rules.yml；其余不动）

| 项 | 修复 | 验证证据 |
|---|---|---|
| prometheus.yml 无认证，抓取全断 | prometheus.yml 加全局 authorization（credentials_file）；compose 挂 ./monitoring/metrics_token；start_all.sh 启动前从 ../.env 再生成；METRICS_TOKEN 随机生成写入 ../.env（compose env_file 自动注入 4 个网关）；凭据文件 gitignore；.env.example 补条目 | 三份 YAML python yaml 解析通过 |
| alert.rules.yml:100 全宕不告警（主人点名） | 表达式改 `max(up{job="gateway"}) == 0 or absent(up{job="gateway"})` | YAML 解析通过 |
| backup_daily.cmd 路径 0x3F 乱码 | 整体重写为 ASCII 安全版，路径用 glob `cd /mnt/d/*/agent_gateway` | WSL 实测：`cd /mnt/d/*/agent_gateway && pwd` → /mnt/d/桌面/agent_gateway |
| scripts 三文件硬编码签名密钥 | test_voice.py / test_upload.py 改 `_env.get("JWT_SECRET")`，test_voice.ps1 改读 env；缺失即 fail-loudly | py_compile 通过；grep 旧值工作区零残留。遗留：历史是否用过该值待主人核对 |
| alembic d9e0f1a2b3c4 downgrade 不成对 | 补齐 5 列 DROP COLUMN IF EXISTS，与 upgrade 对称 | py_compile 通过（真库回滚未跑，留给空库往返冒烟） |
| tests 收集期瘫痪 | 6 个文件 git mv 改 check_* 前缀（4 个真连网络 + test_mapping*.py 实测拖死收集）；集成测试默认 skip（RUN_INTEGRATION=1 才跑）+ 密码走 _env | **`pytest tests/ --collect-only` 从 5 分钟+挂死 → 182 用例 1.82 秒，exit 0** |

**未修（按主人指示不做理会）**：全部 P2/P3（各目录日志维持待修/登记状态）。
**待主人操作**：①P0 SMTP 授权码撤销（QQ 邮箱后台，仅主人可做）+ 是否 git filter-repo 清历史待拍板；②deploy/.server.env 的 SSH 密钥化（服务器侧）；③核对 scripts 泄漏的 hex 值是否曾在线上用作 JWT_SECRET（是则轮换）。

## 发布记录 — 2026-09-08（主人指示：review 完成后推 GitHub + 云端）

按《推送规则.md》流程执行：(1) review 完成 → (2) 本地门禁（check_code_rules 无违规 / compileall OK / pytest tests/unit **177 passed**）→ (3) 提交 e161da3 合回 main，`git push origin main` 一次成功（Li-zner/agent-gateway-full 私仓）→ (4) 云端发布（lite 镜像 135.8MB，gw_publish.py：上传 161s → load → 重建网关容器 → /health 一次通过 → 镜像一致性校验通过）→ 保险动作 `alembic upgrade head` 干净（本次仅改 downgrade，链头未动，DB 已在 head）。
**P0 处置同步**：主人已在 QQ 邮箱后台作废旧 SMTP 授权码；新码入 .env（SMTP_AUTH_PASSWORD），alertmanager.yml 改 auth_password_file 文件注入，git grep 确认新码零入库。
**未推 public（规则第 6 步）**：按规则需主人确认「公网运行正常」后方可同步作品集仓，等待确认。

## 2026-09-10 修复：管理员种子阻断多实例启动

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | main.py、tests/unit/test_core_p1_fixes.py | P0 | `_ensure_admin_users` 固定查询 `username='admin'`，但插入配置账号 `133****8241`；4 实例启动时重复执行 INSERT，撞主键后 fail-closed 重启循环 | 按 `ADMIN_USERNAME` 改为单条 UPSERT；密码保持 bootstrap-only；移除额外 `Fengfeng` 引导；补回归测试 | 已修 |

## 2026-09-10 修复：生产依赖初始化 fail-closed

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | main.py | P1 | 生产环境 PostgreSQL 或 Redis 初始化失败时只记录 warning 并继续启动，网关表面在线但业务请求大面积失败，部署健康检查可能误判成功 | APP_ENV 非 test 时数据库或 Redis 初始化失败直接抛错拒绝启动；测试环境仍可显式降级；启动流程拆为可测试函数 | 已修 |

## 2026-09-10 修复：管理员账号登录失败与唯一性收敛

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | ../.env、本地 PostgreSQL | P0 | 权威配置口令与数据库中的管理员哈希不一致；启动种子只升级资料、不覆盖已有密码，导致目标账号登录 401 | 在不记录明文口令的前提下重置目标账号哈希、角色和启用状态；full 库收敛为唯一管理员账号；登录接口复验 200 | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | main.py:156、core/concurrency.py:34 | P1 | 关闭流程只等待手工保存的任务句柄，spawn 后台任务未统一排空，关闭期间可能丢失审计、缓存、计费和锁释放 | 增加全局任务注册表和带超时的 shutdown，完成后再关闭 Redis/PG | 待修复 |
| 09-11 | main.py:193 | P2 | 启动环境使用裸 os.getenv(APP_ENV)，与 config.py 经 .env 加载的配置源不一致 | 统一读取 config.APP_ENV，并定义运行环境枚举 | 登记 |

## 2026-09-24 P2 业务动作面上线：登记项（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-24 | payment/admin_routes.py:72-91 | P2 | 渠道开关已有直写端点（`POST /channels` 一次 UPDATE 同时改费率与 `is_active`），绕过动作控制面：无审批、无快照、无回滚记录，与新增的 `set_channel_active` 并存成旁路 | UPDATE 去掉 `is_active` 列；请求体带该字段一律 422 并指向中控台动作（不静默丢弃，避免老调用方误报成功）；口径由 4 条单测钉住 | 已修（同日收敛，全仓无调用方） |
| 09-24 | services/biz_action_handlers.py、routes/rag_admin_actions.py | P2 | 管理员账号目标只在执行期事务内拒绝，提议期不预查 role：提议与执行之间角色若变化（TOCTOU），仍以执行期那次 `SELECT ... FOR UPDATE` 为准 | 如引入用户管理端点（role 可写），提议期需同步预查并在裁决表留痕 | 登记（执行期兜底已覆盖，含"禁用自己"） |
| 09-24 | services/rag_actions.py:325 | P2 | 带 handler 的动作共用环境级串行锁 `{env}:__handler__`，业务写动作会与 RAG 动作互相排队；单环境队列吞吐上限 = 一个动作一次 | 业务动作量大或出现长事务时，改为按 target_type 分桶加锁 | 登记 |
| 09-24 | payment/admin_routes.py:24-36 | P2 | 收敛 `is_active` 后同类隐患仍在：`fee_rate/min_amount/max_amount` 带模型默认值，调用方省略即把线上配置重置（费率归零 = 免费交易），与刚修掉的"默认 True 悄悄重开渠道"同一形状 | 三字段改 `Optional` 并按提交的列动态拼 SET；本仓与该端点当前无调用方，改造窗口宽松 | 登记 |
