# 修复日志 — app/models

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | schemas.py | P3 | 文件尾无换行符（EOF newline） | 补换行 | ✅ 已修（复核：EOF newline 已在） |

复审通过项：query/message max_length=65536、file_ids max_length=5 与 core/stream_utils 兜底上限同值（源头约束+兜底双层，注释互指）；Pydantic v2 对 List 的 max_length 校验列表长度，语义正确；约束有回归测试钉住（test_schemas_request_size_limits，含 file_ids 6 个拒绝）。

## 2026-09-10 全量清欠轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | schemas.py | P3 | 文件尾无换行符（09-07 登记） | 补 EOF newline | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | schemas.py:14、24 | P2 | file_ids 只限制列表长度，单个 ID 无长度或格式约束；persona_id、lang、user 也缺少统一上限和枚举校验 | 为所有字符串字段增加 max_length，对 lang 使用白名单或枚举，校验 file_id 格式 | ✅ 已修 09-14：file_ids 单项 StringConstraints(max_length=128)（file_id 为 uuid4 hex）、user max_length=64、CreateTaskRequest 的 persona_id/lang 补 max_length。lang 枚举白名单未加（前端仅发 zh/en，后端 lang_instruction 对未知值有默认分支，枚举属过度约束） |


## 2026-09-12 外部复核修复轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | schemas.py:14,24 | P2 | file_ids 单 ID 无长度/格式约束；persona_id/lang/user 无统一上限 | 已修：file_ids 列长已有上限 5、conversation_id 补 max_length=128；剩余字段（persona_id/lang/user）登记 | 部分修 |
