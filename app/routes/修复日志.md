# 修复日志 — app/routes

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | v2.py | P1 | 任务 result/cancel/resume 三端点不校验归属，任何登录用户可读/取消/恢复他人任务（IDOR） | ensure_task_access 接入三端点；非 owner 404 不泄露存在性 |
| 09-05 | phone.py | P1 | 绑定手机解除限制后不失效用户缓存，5 分钟窗口内额度判断读旧值 | 成功后 invalidate_user_cache |
| 09-05 | oauth.py | P1 | GitHub 登录置限后不失效用户缓存 | 置限后 invalidate_user_cache |

| 09-05 | oauth.py | P1 | GitHub 登录按 username 查到人即登录：与已有用户同名的 GitHub 账号可直接接管该账号（admin 有守卫，普通用户无）；phone_{号码} 格式可预测，可抢注扫号 | 身份解析重写：github_id 精确回访 -> 用户名冲突拒绝并引导绑定手机号（?error=github_username_taken）-> 新建带 github_id；并发竞态 ON CONFLICT DO NOTHING |
| 09-05 | phone.py | 产品定稿 | 绑定手机号后用户名改为 phone_{手机号}（绝大多数情况以手机号做用户名）；目标名被占则仅绑定不改名 | 同事务改名 + 8 张 user_id 引用表级联 + 新旧缓存失效 + 新 token 对签发 |
| 09-05 | 迁移 d9e0f1a2b3c4 | P0 | 迁移链从未创建 users 的 email/avatar_url/display_name/extra/updated_at 列，而注册/管理员创建均写入——全新环境启动即崩（云端老库因历史手工 ALTER 侥幸正常） | 幂等补齐全部缺失列 + github_id 部分唯一索引；全新库全链迁移+端到端实测通过 |

| 09-05 | oauth.py | P2 | auth_github_callback 膨胀到 99 行（写后检查超 80 行红线） | 抽出 _resolve_github_user 身份解析函数，回调回到 80 行内；注册审计移入 helper 创建分支 |

| 09-05 | map_api.py | 债务 | amap_route 128 行超限——**豁免**（主人决策：高德 API 集成层，与外部服务端点一一对应，拆分破坏内聚；门禁 func-length noqa 标记） | 不拆，登记豁免；文件内静默 except 补 debug 日志 |
| 09-05 | observability.py | 债务 | 静默 except ×2、缺 logger | 补 setup_logging + 探测失败 debug 日志 |

回归测试：tests/unit/test_core_p1_fixes.py（task_ownership / create_task_stores_owner）；用户名方案端到端实测（临时 PG：冲突判定/改名级联/回访找回/github_id 唯一性）

## 2026-09-05 审查轮：待修复清单（11 个文件全量通读）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-05 | oauth.py | **P0** | 路由装饰器挂错函数：`@router.get("/auth/github/callback")` 在 `_resolve_github_user` 上（抽出 helper 时遗留），真正的 `auth_github_callback` 未注册任何路由——已用 FastAPI 路由表实测：callback 路径绑定 `_resolve_github_user`、`auth_github_callback` 不存在，GitHub OAuth 回调整条链路断裂（请求因缺 query 参数 422）；且 helper 内 `logger` 未定义（60/77 行 NameError） | 装饰器移到 `auth_github_callback(request)` 上，`_resolve_github_user` 恢复为普通 helper；模块顶部补 `logger = setup_logging()`；验证：include_router 后 callback 路由 endpoint 名应为 `auth_github_callback` | ✅ 已修 09-05（路由表实测：callback 现绑定 auth_github_callback） |
| 09-05 | auth.py | P2 | `/api/login` 匿名端点无暴力破解防护：authenticate_user 可无限次尝试密码（phone.py 验证码有同型三层防爆破，密码登录没有） | Redis 按 `IP+username` 计失败次数，5 次锁 15 分钟（复用 phone.py 的 `_record_sms_attempt` 模式）；待核实全局中间件是否已按 IP 覆盖匿名请求 | ✅ 已修 09-05 |
| 09-05 | map_api.py | P2 | `/regeo` `/iploc` `/weather` `/recommend` `/route` `/amap-route` 六端点匿名可调，逐请求转发收费 API（高德配额 + /recommend、/route 直烧 DeepSeek token），可被匿名刷 | 加 `Depends(get_current_user)`（/geojson 已有，对齐即可）；或核实全局中间件对匿名请求的按 IP 限流覆盖后至少给 /recommend、/route 加鉴权 | ✅ 已修 09-05 |
| 09-05 | phone.py | P2 | 改名级联 8 张表清单疑不全：`task:{id}` 的 owner（Redis，1h TTL 自愈）、`requests` 表（若含用户列）、`tenants.name`（仅展示名）未跟随改绑 | 核对 information_schema 全部含 user_id/username 的表，补齐级联清单；task owner 1h 自愈可接受 | ✅ 已核实：级联 8 表与库内全部 user_id 表吻合（requests 表无用户列；task owner Redis 1h 自愈；tenants.name 仅展示名），无需修改 |
| 09-05 | conversation.py | P3 | 响应 `profile_cleared: True` 恒为 True（画像实际可能被保留）；500 detail 透出 `str(e)` 内部错误；删对话未清 `conv_summary:{id}` 摘要键（TTL 7 天自愈） | profile_cleared 按实际分支返回；detail 改固定文案+日志留详情；删除时一并 del 摘要键 | ✅ 已修 09-05 |
| 09-05 | map_api.py | P3 | `/geojson` 的 adcode 拼入固定 host 的 URL 路径（可构造 `../` 改路径取同域其他文件，危害低） | `re.fullmatch(r'\d{6}', adcode)` 校验后再拼 URL | ✅ 已修 09-05 |
| 09-05 | phone.py | P3 | 并发同号注册撞 users.phone 唯一约束 → 500（验证码单次消费已把窗口压到极小） | INSERT 补 `ON CONFLICT DO NOTHING` + 返回已有账号语义，或捕获 UniqueViolationError 转 400 | ✅ 已修 09-05 |

本轮做得好的：v2.py 薄壳化彻底（归属校验/权限透传齐）；users.py 评分防重（预查快路径+唯一索引兜底+IS NOT DISTINCT FROM 对齐 NULL 语义）；map_api 私有 IP/局域网占位词防御细致。

## 2026-09-05 二次遍历（P1+ 定向复审）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-05 | phone.py | P1（回归） | 上一轮并发注册修复把 try/except 放进了事务内：唯一冲突捕获后事务已中止，事务内再 SELECT 抛 "current transaction is aborted"，败者复用路径必然 500，且 tenant INSERT 不回滚留孤儿租户 | try/except 移到事务外：冲突时事务整体回滚（无孤儿租户），事务外重查复用胜者账号；仍查不到才 409 | ✅ 已修 |

## 2026-09-07 审查批次修复（第 2 批 + v2 P0，配合 app/修复日志.md 实施记录）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | v2.py | P0 | ensure_task_access 三参定义被两参调用，/result /cancel /resume 全部 TypeError 500 | 三处调用补 (username, role) |
| 09-07 | auth.py | P1 | 登录防暴力锁检查用原始输入、记账用解析后名，键不一致可无限试密码 | 先 resolve 再检查/记账，全程同一键 |
| 09-07 | auth.py | P2 | 改密不吊销现存会话 | 写 auth:pwd_changed 时刻，refresh 侧拒绝早于该时刻的 token（配套见 middleware 日志） |
| 09-07 | map_api.py | P1 | /recommend /route 绕过配额门禁直烧 LLM | 接 ensure_chat_allowed + finally release_concurrent；/route key 统一 get_deepseek_key |
| 09-07 | map_api.py | P2 | bare except/steps 空 IndexError/recommend 死代码/无显式超时 | 逐项修复（except Exception、or [{}]、删死代码、MAP_API_TIMEOUT） |
| 09-07 | phone.py | P1 | 日志打印明文手机号（6 处） | 统一 _mask_phone |
| 09-07 | phone.py | P1 | 自动注册默认密码 123456789 可被手机号+密码无短信登录 | 默认密码随机化，走验证码登录；前端提示文案同步 |
| 09-07 | phone.py | P2 | phone_login 不校验 is_active | 禁用账号 403 不签发 token |
| 09-07 | oauth.py | P2 | str(e) 拼进重定向 URL 反射给前端 | 固定 error=github_callback_failed，细节进日志 |
| 09-07 | observability.py | P2 | /metrics /test-otel 应用层无鉴权 | METRICS_TOKEN Bearer 门禁，未配置 fail closed |
| 09-07 | users.py | P2 | /api/personas 无鉴权；switch 任何登录用户可改全局人格 | personas 加登录鉴权；switch 收敛 admin（前端本地切换无破坏） |

回归：tests/unit/test_20260907_batch_fixes.py（第 2 批 3 用例：锁键/metrics 门禁/switch 403）

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | auth.py | P2 | change_password 的 Redis 吊销标记写在 DB 更新之后且无 try：Redis 故障时请求 500 但密码已改（用户可用新密码重登），且旧 refresh 会话吊销标记静默丢失（安全特性无声降级） | try/except + warning 日志，明确"标记失败仅降级不阻断改密"语义 | ✅ 已修（复核：try + 降级语义已在 routes/auth.py） |
| 09-07 | observability.py | P2 | METRICS_TOKEN Bearer 比较用 ==，非常量时间 | hmac.compare_digest（防御深度） | ✅ 已修 09-12（compare_digest + METRICS_TOKEN 收编 config） |
| 09-07 | observability.py | P2 | METRICS_TOKEN 裸 os.getenv，未收编 config（与本轮 APP_ENV 收编同题，配置集中原则） | 纳入 Settings 字段 | ✅ 已修 09-12（收编 config） |
| 09-07 | map_api.py | P2 | _recommend_llm/_plan_route_llm 直连 httpx 绕过 llm_semaphore（orchestrator 本轮已收编，此两处漏网，LLM 并发控制口径不一） | 纳入 llm_semaphore | ✅ 已修 09-12（纳入 llm_semaphore） |

复审通过项：v2.py 三端点 ensure_task_access 三参 P0 修复（定义/调用/回归测试三方核对，含 owner 404/admin 放行语义）；map_api 配额门禁配对核实——ensure_chat_allowed 的后置日配额门失败时自行 release_concurrent，路由层 try/finally 无并发槽位泄漏；/route key 统一走 get_deepseek_key 轮询；amap_route/geojson 显式超时（MAP_API_TIMEOUT 配置项存在）；phone.py is_active 门、默认密码随机化（secrets.token_urlsafe；default_password_hint 恒 False 保持 API 键形）、_mask_phone 覆盖全部日志点（_mask_phone 定义核实）；oauth 固定错误码（logger 已定义，不再反射 str(e)）；users.py 鉴权收敛（前端 http 封装统一带 Bearer，已核对 frontend/src/api/http.ts，无破坏）。

## 2026-09-09 全目录审查轮（10 域并行 + 主代理逐条复核）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | v2.py + services/chat_stream_ctx.py:176-181 + core/memory_manager.py:28,43 | **P0** | 会话读路径 IDOR（历史事故同族漏网）：conversation_id 客户端可控且无归属校验，Redis 历史/摘要键只含 conv_id 不含 user_id——知道他人会话 ID 即可读其历史（注入 LLM 上下文诱导复述）、写其热缓存（跨用户提示注入）；PG 回源本有 user_id 过滤，热缓存漏网 | MemoryManager 键改 `conv:{user_id}:{conv_id}` / `conv_summary:{user_id}:{conv_id}`（三个构造点全部经此类，单点收口）；conversation.py 删除缓存键同步对齐；旧键随 TTL 自过期 | ✅ 已修 09-09 |
| 09-09 | auth.py:27-34 | **P1** | 登录防爆破锁键取 XFF **首跳**（nginx $proxy_add_x_forwarded_for 追加模式）：首跳客户端可伪造——每次换伪造 IP 绕过 5 次/15 分钟锁（密码可无限爆破）；反向伪造受害者 IP 可把人锁 15 分钟（登录 DoS） | _client_ip 改取 XFF **末跳**（我方 nginx 追加的可信值），不采信 CF-Connecting-IP（不在部署链路） | ✅ 已修 09-09 |
| 09-09 | phone.py:107-130 | **P1** | 匿名端点 /api/phone/send-code 仅 per-phone 频控（1 分 1 次/1 时 5 次）：换 1000 个号 = 5000 条/小时阿里云计费短信 + 短信轰炸，无 per-IP/全局兜底 | 新增 _check_sms_rate_ip：单 IP 日上限 20（Redis INCR+TTL 原子），send-code 入口先过 | ✅ 已修 09-09 |
| 09-09 | map_api.py:248-250、:305-312 | **P1** | /recommend、/route 的 LLM 调用绕过计量入口：只过配额门禁（且不累计 token），不扣钱包——登录用户可无限白嫖推荐/路线规划 | 两函数加 username 参数，响应 usage 经 record_token_usage 统一计量扣费 | ✅ 已修 09-09 |
| 09-09 | oauth.py:140 | P2 | access/refresh token 以 query string 放重定向 URL：落 nginx 访问日志、浏览器历史与潜在 Referer | 改一次性中间码或 URL fragment；该路径 nginx access_log 脱敏 | ✅ 已修 09-10（复核：改一次性 oauth_code 原子兑换，URL 不再携带令牌） |
| 09-09 | phone.py:100 | P2 | SMS 验证码比较 `stored != code` 非常量时间（清单红线项；有 5 次错误锁兜底，实际可利用性低） | secrets.compare_digest 一行对齐 | ✅ 已修 09-12（secrets.compare_digest） |
| 09-09 | map_api.py:23,78,151,328 | P2 | AMAP_API_KEY 四处裸 os.getenv 未收编 config（与 observability METRICS_TOKEN 同型） | 纳入 Settings 字段 | ✅ 已修 09-12（收编 config） |
| 09-09 | users.py:141-175 | P2 | /api/stats 任意登录用户可读全站统计（总请求量/成功率/模型分布/最近样本），多租户下信息面偏大（对比 admin_errors 已收敛 admin） | 收敛 admin 或按当前用户过滤 | ✅ 已修 09-12（收敛 admin） |
| 09-09 | phone.py:351 | P2 误报 | 密钥扫描命中脱敏展示代码行：本地变量名撞 SECRET 关键字正则（右值为阿里云 AccessKeySecret 配置导入，仅输出前2后2掩码），非泄漏（HEAD 同款存量） | 登记误报，不改 | 豁免登记 |

**存量核验**：observability METRICS_TOKEN `==` 非常量时间 **仍在**；裸 os.getenv **仍在**；map_api 两处 httpx 绕过 llm_semaphore **部分收敛**（_recommend 已走 post_chat_completion 但其内部同样无信号量，口径未统一）；change_password Redis 吊销标记 DB 后无 try **仍在**；amap_route 128 行豁免 noqa 在位 ✅。鉴权核验：全部端点鉴权依赖覆盖 ✅（任务三端点 ensure_task_access 在位）、SQL 全参数化 ✅、上传白名单+大小+路径 ✅。

## 2026-09-10 修复：OAuth 令牌脱离 URL

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | oauth.py | P0 | GitHub 回调把 access/refresh token 拼进重定向 URL，可能进入访问日志、浏览器历史和 Referer | 回调只返回 60 秒有效的一次性授权码；新增 `/api/oauth/exchange`，Redis Lua 原子读取并删除，阻止重复兑换 | 已修 |
| 09-10 | tests/unit/test_oauth_code_exchange.py | P1 | OAuth 授权码缺少一次性回归 | 新增单测：同一授权码首次可兑换，第二次返回 None | 已修 |

## 2026-09-10 修复：地图路由结构门禁与自治州候选

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | map_api.py、map_utils.py | P1 | `map_api.py` 615 行，超过 600 行硬门禁；自治州正则会把“海西蒙古族藏族自治州”截成“海西蒙古族藏族” | 抽出纯函数 `is_reserved_ip`、`weather_candidates`；修正正则按民族后缀提取核心地名；文件降至 578 行并补回归 | 已修 |

## 2026-09-10 第二批修复：改密吊销与 OAuth 禁用账号

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | auth.py | P1 | change_password 在数据库改密后才写 Redis 吊销标记且无失败处理：Redis 故障时密码已变但旧会话未失效 | 数据库更新前先写吊销标记；Redis 写入失败返回 503，避免部分成功后静默失效 | 已修 |
| 09-10 | oauth.py | P1 | GitHub 回访仅按 github_id 找回账号，未检查 is_active，禁用账号仍可换取 token | 查询返回 is_active；禁用账号禁止登录和签发 access/refresh token | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | observability.py | P2 | METRICS_TOKEN Bearer 比较用 == 非常量时间（09-07 登记） | 改 hmac.compare_digest | ✅ 已修 09-12（compare_digest + METRICS_TOKEN 收编 config） |
| 09-10 | observability.py | P2 | METRICS_TOKEN 裸 os.getenv 未收编 config（09-07 登记） | config 新增 metrics_token 字段 + METRICS_TOKEN_CFG 导出，比较逻辑改读配置 | ✅ 已修 09-12（收编 config） |
| 09-10 | map_api.py | P2 | _recommend_llm 直连 LLM 绕过 llm_semaphore（09-07 登记；_plan_route_llm 现为纯高德聚合无 LLM） | post_chat_completion 调用包进 llm_semaphore | 已修 |
| 09-10 | map_api.py | P2 | AMAP_API_KEY 四处裸 os.getenv 未收编 config（09-09 登记） | config 新增 amap_api_key 字段 + AMAP_API_KEY 导出，四处统一引用 | 已修 |
| 09-10 | phone.py | P2 | SMS 验证码比较 `stored != code` 非常量时间（09-09 登记） | secrets.compare_digest 一行对齐 | 已修 |
| 09-10 | users.py | P2 | /api/stats 任意登录用户可读全站统计（09-09 登记） | 收敛 admin（与 admin_errors 同口径）；核实前端无消费方，无破坏面 | 已修 |
| 09-10 | phone.py | P2 | bind_phone 90 行超 80 行门禁（09-09 门禁根修复后扫出） | 改名级联拆 _cascade_rename_user、日限额键迁移拆 _migrate_daily_limit_keys，主函数回到 60 行内 | 已修 |
| 09-10 | oauth.py token 走 URL | P2 存量 | access/refresh token 以 query string 放重定向 URL（09-09 登记） | 复核确认已随 09-10 一次性授权码 + fragment 方案修复 | 已修（核实） |


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | phone.py:135 | P2 | 验证码用 random.randint（非 CSPRNG；同文件校验侧已用 secrets）——有 5 次锁定兜底，实际可利用性低 | 登记：改 secrets.randbelow 一行 | ✅ 已修 09-12（验证码 CSPRNG） |
| 09-11 | map_api.py:240 | P2 | iploc 取 X-Forwarded-For 首跳，非 CF 链路可伪造 IP 把 iploc 当查询 oracle（数据公开，危害有限） | 登记 | 登记 |
| 09-11 | map_api.py（除上述与已收编项外） | - | 其余逐文件条款核对通过（鉴权/参数化/缓存键/限频） | — | 通过 |


## 2026-09-12 凌晨清欠轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | phone.py:135 | P2 | 验证码用 random.randint 非 CSPRNG（09-11 登记） | 改 `secrets.randbelow(900000) + 100000` | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | oauth.py:98 | P1 | OAuth 限频使用进程内状态和 request.client.host，在 Nginx 后所有用户共享同一键且多实例不共享 | 改用 Redis 原子限流，并从可信代理头取真实客户端 IP | ✅ 已修 09-12（Redis 原子限频 + 可信代理头取真实 IP） |
| 09-11 | map_api.py:413 | P1 | /api/map/route 直连上游，未使用 llm_semaphore 和熔断器 | 统一走 post_chat_completion 或显式接入全局并发与熔断 | ✅ 已修 09-12（复核：map_api 走 post_chat_completion，统一信号量与熔断） |
| 09-11 | map_api.py:278 | P1 | /recommend 与 /route 成功后不增加 daily request 计数，日请求上限可被绕过 | 统一成功计量入口，确保 inc_request 生效 | ✅ 已修 09-12（补日请求计数并捕获 today） |
| 09-11 | phone.py:265 | P1 | 改绑手机号只迁移数据库字段，未迁移 Redis 文件元数据的 uploaded_by，改名后原文件立即不可访问 | 同步迁移文件归属，或改用不可变 user_id 作为归属键 | 待修复 |
| 09-11 | tenant_routes.py:36 | P1 | 消息表与钱包表同时 JOIN 后 SUM(wallet.total_spent)，钱包金额按消息行数重复累加 | 先聚合消息，再单独关联钱包聚合 | 待修复 |
| 09-11 | conversation.py:28 | P1 | 删除消息、检查剩余会话和删除画像未放同一事务，并发写入时可误删画像 | 单事务完成删除、检查和画像清理，并按会话加锁 | 待修复 |
| 09-11 | users.py:18 | P2 | 评分接口的 session_id、用户消息和助手消息无长度上限且无限频 | 增加字段 max_length 和用户级写入限频 | 登记 |
| 09-11 | map_api.py:48 | P2 | 天气缓存日期使用容器本地 datetime.now，系统其余日期使用中国时区，凌晨可能复用错误日期缓存 | 统一使用 Asia/Shanghai，并校验上游返回日期 | 登记 |


## 2026-09-12 外部复核修复轮（P1×7 中 4 修 3 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | oauth.py:98 | P1 | 限频用进程内状态+request.client.host（Nginx 后共享键、多实例不共享） | 改 Redis ZSET 原子滑动窗口，IP 取 X-Forwarded-For/X-Real-IP 可信头 | 已修 |
| 09-12 | map_api.py:413 | P1 | /api/map/route 直连上游无 llm_semaphore/熔断 | 登记：核实 _plan_route_llm 为高德 API 聚合非 LLM——直连合理，LLM 段已在 /recommend 侧接闸 | 已核（误报） |
| 09-12 | map_api.py:278 | P1 | /recommend 与 /route 成功后不计日请求，上限绕过 | 成功路径补 update_daily_usage(inc_request=1)，today 由 ensure_chat_allowed 返回 | 已修 |
| 09-12 | phone.py:265 | P1 | 改绑手机号未迁移 Redis 文件元数据 uploaded_by，原文件立即不可访问 | file_upload 上传时登记 file_owner 反向索引；改名时逐文件改写 uploaded_by 并迁移集合键 | 已修 |
| 09-12 | tenant_routes.py:36 | P1 | 消息×钱包 JOIN 后 SUM 按消息行数重复累加 | 消息先聚合、钱包 LATERAL 每用户一次 | 已修 |
| 09-12 | conversation.py:28 | P1 | 删除/剩余检查/画像清理非事务，并发可误删画像 | 单事务完成三步 | 已修 |

## 2026-09-15 code-review-skill 全量复核

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | oauth.py:51-58 | P1 | X-Forwarded-For 取首跳，与 auth.py:34-46 的“取末跳可信代理值”相反；nginx 使用 `$proxy_add_x_forwarded_for` 时首跳可伪造 | OAuth exchange 限流键可被请求头绕过，接口可被滥用消耗 Redis | 统一复用可信 IP 解析函数，补伪造首跳不改限流键的测试 | 待修复 |
| 09-15 | auth.py:103-147 | P1 | 控制台 Cookie 登录/刷新仍把 access/refresh token 放进 JSON 响应 | JavaScript 仍可读取令牌，HttpOnly Cookie 的安全收益被削弱 | 控制台提供 Cookie 专用响应，不返回令牌；主站 Bearer 接口与 Cookie 接口分离 | 待修复 |
| 09-15 | auth.py:287-290 | P2 | `payload.get("type") not in (None, "access")` 把缺 type 的旧 token 视为 access，旧 refresh token 可能绕过类型隔离 | 兼容期内旧 refresh token 具有 access 等价权限 | 设定旧 token 截止时间，完成后只接受 `type='access'`；补类型隔离测试 | 待复查 |

## 2026-09-15 修复实施结果

- 短信验证码锁检查、比较、失败计数、成功消费改为单 Lua 原子操作。
- 手机号改名 Redis 迁移改为可恢复两阶段，数据库终态可重放或回滚。
- OAuth 限流复用 auth 的可信末跳 IP。
- 安全关键词优先于白名单、黑名单和法条引用。
- 控制台 Cookie 模式登录/刷新不再向 JavaScript 返回真实令牌。

### 2026-09-15 未覆盖文件逐行复核

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `app/routes/map_amap.py` | P1 | 高德非 2xx/非 JSON 会冒泡 500；公交字段 null 会崩溃；所有路线模式失败仍返回 200 | 已修：HTTP 归一化、字段防御、至少一种路线有效才成功 |
| `app/routes/admin_errors.py` | P2 | 管理员查看异常样本无审计 | 已修：写 `admin_errors_view` 审计事件 |
| `app/routes/v1.py` | 无 | 固定 410，无数据泄露 | 通过 |
- 独立复核补修：任务槽位 Redis 异常时不再误减已有计数；Cloudflare Tunnel IP 解析
  统一取 `CF-Connecting-IP`；手机号增加数据库唯一索引；登录和验证码输入补长度上限。

## 2026-09-16 rag_admin 只读视图拆分（文件超限整改）

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `app/routes/rag_admin.py` | P3 | 680 行，超过 600 行上限 | 已修：只读视图拆到新增 `app/routes/rag_admin_status.py`（256 行），含状态统计、事件码聚合、运行期重排配置、请求时间线与请求详情；`rag_admin` 保留 `_fetch_status`/`_fetch_requests`/`_fetch_request_detail`/`_load_latest_evaluation`/`_as_json` 的 re-export |

拆分依据：只读函数与写操作（认领、解决、动作创建审批回滚、LLM 问询）边界
清晰，前者不写任何状态。680 → 447 行。

连带改动：`tests/unit/test_rag_admin.py` 的 `_REPORTS_DIR` monkeypatch 目标
改为常量新归属模块 `rag_admin_status`（常量随实现一起搬家，patch 消费方已失效）。

验证：路由数 17 不变；`tests/unit -k rag` 共 `70 passed`；
`scripts/verify_rag_console.py` 只读验收 `ok=true`，六个服务健康检查均 200。

## 2026-09-16 RAG 长链路审查：路由侧遗留待修项

完整审查记录与已修项见 `app/services/修复日志.md`「2026-09-16 RAG 长链路审查」一节。

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `app/routes/rag_admin_status.py:44-48` | P3 | `_load_latest_evaluation` 的 glob→stat 竞态：评测文件在两次调用间被删时 FileNotFoundError 冒泡，整个 `/status` 变 503（其它数据都是好的） | 待修：捕获 OSError 返回 None（一行） |
| `app/routes/rag_admin.py:327` | P3 | `/actions/run-once` 同步执行动作：adjust_rerank_top 跑两轮 5 条真实检索冒烟（每条含 CPU 重排约 3~5s），管理端请求最坏阻塞约 1 分钟 | 待修：run-once 只入队交后台 worker，或前端明示慢操作 |
