# 修复日志 — app/agents

> 记录本目录 bug 修复（2026-09-04/05 会话）。级别：P0 核心/安全 | P1 边界/错误处理 | P2 质量。

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-04 | router.py | P0 | 法律/项目知识类问题被路由为简单任务后返回"未知工具" | 统一走 dispatch_tool（含知识库/联网工具） |
| 09-04 | router.py | P0 | 工具原始 JSON append 进结果缓冲，与回答正文一起展示给用户 | 工具结果只记日志，不进用户可见缓冲 |
| 09-04 | router.py | P1 | time 未导入，session_id 为空时 NameError 任务卡死 | 补导入 |
| 09-04 | router.py | P1 | 简单任务忽略 persona_id，直接读全局人格 | get_effective 按请求解析 |
| 09-04 | router.py | P2 | 天气简单任务把整句话传给地理编码 | 复用城市提取 |
| 09-04 | router.py | P2 | 裸 create_task(SemanticCache.set) 未捕获异常 | 复用 _safe_cache_set |
| 09-04 | runner.py | P1 | 主回答流绕过 DFA：内容先实时写入 Redis 后终检 | 逐块 check_stream，命中写安全文案并停止；终检兜底跨块 |
| 09-04 | runner.py | P1 | 取消草稿内容重复（缓冲已含 full_content 再拼一次） | 去除重复拼接 |
| 09-04 | runner.py | P1 | 工具参数 json.loads 无防护，坏参数炸整个任务 | 按工具失败处理走兜底链 |
| 09-05 | runner.py | P1 | 语义缓存拦截不识别 __EMPTY__ 占位，字面发给用户 | is_empty 检查放行 |
| 09-05 | runner.py | P1 | 人格按全局 current 解析，并发任务互相覆盖 | get_effective 按请求解析 |
| 09-04 | runner.py | P2 | 未用导入 jloads/set_cancelled、partial_answer 死赋值 | 清理 |
| 09-04 | memory.py | P1 | compress_message_history 把主 system（人格规则）压成摘要 | 主 system 固定保留首位 |
| 09-04 | orchestrator.py | P1 | Phase2 review 的 system prompt 绕过注入消毒 | phase1 意见入库即消毒 |
| 09-04 | orchestrator.py | P2 | analysis 非字符串时 .replace 崩溃 | str() 兜底 |
| 09-04 | tools.py | P1 | web_search 使用不存在的 DDGS 异步 API，主路径永不可用 | 装依赖；同步 DDGS 包 asyncio.to_thread；兜底去重抽函数 |
| 09-05 | tools.py | P2 | Instant Answer 异常 str 为空不可排查 | 错误补类型名 |
| 09-04 | routing_table.py | P2 | get_colloquial_map 的 def 行丢失（缩进体成死代码） | 恢复函数定义 |
| 09-04 | article_normalizer.py | P2 | 消保法分支不可达 + "消保法第25条"被改写成"消保法民法典第25条" | 删死分支；其它法律名紧跟条文不改写 |
| 09-04 | document_parser.py | P2 | 扫描件 PDF 回退把 PDF 路径喂给图片 OCR 必炸 | fitz 整页渲染后 OCR |
| 09-04 | law_mapping.py | P2 | 未用导入/函数内重复 import re | 清理 |
| 09-05 | runner.py + agent_tasks.py/v2.py | P0 | 任务路径对知识库检索硬编码 permissions=None（不过滤语义），普通用户经任务端点可越权检索 VIP 知识块（SSE 路径传的是真实权限） | agent_tasks 新增 task_user_perms（admin 不过滤，普通用户按权限组、缺省仅公开），创建/恢复入口经 runner→handle_simple_task 透传；任务入口默认 fail-closed |
| 09-05 | router.py | P1 | SemanticCache 只在函数体内局部导入，模块级 _safe_cache_set 引用必然 NameError 且被自身 except 吞掉，简单任务路径语义缓存静默不写入、防击穿锁空转 | 提升为模块级导入，删冗余局部导入 |
| 09-05 | routing_table.py | P1 | "未成年人"仅移出强制白名单不够，普通白名单仍放行，点名《未成年人保护法》的问题未拒答 | 黑名单新增该法条目（含网络保护条例）；民事行为类问题经强制/普通白名单照常放行 |
| 09-05 | runner.py + react_steps.py（新） | P2 | run_agent_task 324 行上帝函数；重建锁持锁不续期（renew_rebuild_lock 已实现无人调用）；缓存命中路径漏 cleanup_event、重复 save_user_message | 拆出 react_steps.py 步骤层（行为等价移动），编排层瘦身为全部 ≤80 行函数；新增 _renew_until_done 周期续期防长任务锁过期重复重建；命中路径补 cleanup_event；删冗余保存 |
| 09-05 | router.py | P2 | handle_simple_task 139 行 | 拆 _call_simple_tool / _format_via_llm |
| 09-05 | routing_table.py | P2 | route_query 103 行，mapped_query 回填逻辑重复 5 处 | 拆 _pass_result / _blacklist_result，行为等价 |
| 09-05 | tools.py | P2 | search_project_knowledge 90 行；web_search 限流计数字典每用户一键、永不清扫无界增长 | 拆 Embedding/关键词两个辅助函数；键数超 512 惰性清扫已过窗口键（活跃用户不受影响） |
| 09-05 | tools.py/sub_agents.py/document_parser.py/runner.py | P2 | 9 处裸 except（pass 无处理，AST 门禁静默类违规） | 换真处理：日志/赋默认值/continue |
| 09-05 | memory.py | P2 | generate_summary 空摘要（非异常）立即重试无退避；文件缺 EOF 换行 | 空摘要与异常同样指数退避（0.5s/1s）；补换行 |

| 09-05 | document_parser.py | P2 | startswith("[") 判定解析成功——正文以 [ 开头的文档（JSON 导出/引用块）被误标"提取失败" | 显式错误标记清单匹配 |
| 09-05 | sub_agents.py | P2 | JSON 兜底正则全局扫 "word:"，值内时间（12:30）/URL 被撕碎 | 锚定 { 或 , 之后的键位补引号 |

回归测试：tests/unit/test_agent_logic_fixes.py、test_agent_security_fixes.py、test_agent_p2_fixes.py、test_rebuild_lock.py（全量 97 passed）
门禁：scripts/check_code_rules.py app/agents 违规 13 → 0
备份：_backups/20260905_agents_review/（第一轮）、_backups/20260905_agents_p2/（第二轮）

## 2026-09-05 二次遍历（P1+ 定向复审）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-05 | react_steps.py | P2（回归） | react_steps 拆分时 `_finish_cancelled` 遗漏 `cleanup_event(task_id)`（原 runner 版有），取消路径的取消事件对象在进程字典中滞留泄漏（task_id 为 uuid 不复用，无功能影响） | 补回 cleanup_event | ✅ 已修 |

## 2026-09-05 第二轮：qwen 端点路由 + 错误信息卫生

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | router.py / orchestrator.py / sub_agents.py / memory.py / tools.py | P1 | 主力模型切 qwen 后各直连 DEEPSEEK_API_BASE 的调用点（意图分类/格式化/orchestrator/子Agent/历史压缩/重排）会 404 | 全部改走 config.llm_endpoint 按模型名路由；Flash 降级链（react_steps）保持 DeepSeek 直连不动 |
| 09-05 | runner.py / router.py | P2 | 任务异常 str(e) 与 "DEEPSEEK_API_KEY 未设置" 环境变量名直接进任务状态，前端轮询原样展示 | 异常过 sanitize_error_text；env 名改通用文案（细节进日志） |
| 09-05 | tools.py | P2 | 天气/联网搜索异常返回 {"error": str(e)} 带含 key 的完整 URL 到 tool_result 事件 | 改通用文案/异常类型名，细节进日志 |

回归：tests/test_llm_endpoint.py（qwen→百炼 / deepseek→官方 路由断言）

## 2026-09-06 第三轮：agent 流程复审（流式/任务双路径工具调用 + 历史压缩）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-06 | react_steps.py | P1 | `_tool_arg_error` 是同步函数，返回 dict 混进 asyncio.gather 当场 `TypeError: unhashable type`，坏参数炸整个任务——09-04 同名 P1 的修复在拆分时丢失 | 改 async def（与 chat_react 版对齐） |
| 09-06 | react_steps.py | P1 | `_handle_tool_step` 调 dispatch_tool 写死 `user_location=""`（参数收了却只喂圆桌），LLM 漏参时 _auto_args 拿不到定位，"我这边天气怎么样"拿整句话去地理编码 | 透传 user_location（与流式路径同一标准） |
| 09-06 | memory.py | P1 | compress_message_history 切点可落在 assistant(tool_calls) 与其 tool 消息之间，孤儿 tool 消息致下轮 LLM 请求 400、任务静默降级 Flash | 切点向左对齐到配对的 assistant（recent 开头是 tool 时逐条回扩） |
| 09-06 | runner.py | P1 | 任务路径文件注入内联读 Redis，绕过提示词注入消毒与 uploaded_by 属主校验（流式路径两道防线都有）——信任边界双标 | 统一走 build_file_context（username 透传做属主校验） |
| 09-06 | orchestrator.py | P2 | _brief_result 的 `result["route"]` 无 or {} 防护、`error[:50]` 假定 str，子代理返回 `{"route": null}` 时 AttributeError 圆桌静默降级 | 补 or {} 与 str() 兜底 |
| 09-06 | sub_agents.py | P2 | 首次 _call 只捕 JSON 类异常，httpx 网络错误/限流直接穿透调用方；重试分支反而捕了 httpx.HTTPError——首调不重试不合理 | 首调异常类型与重试分支对齐 |
| 09-06 | routing_table.py | P2 | 白名单"法条引用"含裸"第"/"条"近乎全匹配；法条正则前缀可选，"劳动合同法第39条"借 article_ref 绕过黑名单拒答 | 删裸字；前缀必须出现（无前缀真民法典问题走白名单/默认放行，结果不变） |
| 09-06 | runner.py | P2 | _INTERNAL_PATTERNS/_clean_reasoning 约 55 行死代码无任何调用方 | 删除，连带清理 re/datetime/get_redis/json 死导入；run_agent_task 顺带压回 80 行内 |
| 09-06 | router.py | P2 | _safe_cache_set 与 runner 重复实现（第三处 chat_stream_ctx 是裸 create_task） | 三处统一走 core.semantic_cache.safe_set |
| 09-06 | tool_definitions.py | P2 | TOOL_HANDLER_MAP 无引用 | 删除 |

回归：tests/unit/test_review_fixes.py（新增 react_steps 占位协程/对齐/定位透传、压缩配对 3 用例；旧"坏参数占位"用例为摆设断言——单工具全坏场景只断言 dispatch 未被调用，恰把 chat_react 错位锁进测试——已重写为坏+好混合并断言等长）

## 2026-09-06 第四轮：AI 层评测基建 + 引用溯源（配套 assets 见 tests/）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-06 | routing_table.py | P2 | 法条正则字符类缺"零"——「第一千零七十七条」匹配不上（被白名单"民法典第"前缀掩盖，未暴露）；上一轮收窄正则时引入 | 字符类补零，测试钉住 |
| 09-06 | tools.py | P2 | search_knowledge 结果不返回 chunk_key，回答引用无法溯源到库中具体块 | _recall_pg_trgm/_keyword_fill/_dedup 透传 chunk_key |

配套（非修复）：tests/civil_retrieval_labels.json（55 条法条标注 + 5 条诚实排除）+ tests/run_retrieval_eval.py（recall@5/10/20 + MRR + 延迟 + 方法分布，直连 DB 不花 LLM）+ tests/unit/test_retrieval_grounding.py。基线：recall@5/10/20=0.407/0.493/0.613，MRR=0.345，纯检索延迟 avg 47ms。

发现（待决策，未改）：低置信度查询走 LLM Rerank（qwen3.7-flash，20 候选×300字 prompt）在 HTTP_TIMEOUT_LONG=30s 内 100% 超时——线上每条此类查询白等 30s 后回退 trgm 排序，质量贡献为零；候选数/超时需调，或按补强指南 1.4 换本地 cross-encoder。

注：本轮 react_steps"dispatch_tool 透传 user_location"修复已被并行的 place_extract 重构取代（dispatch_tool 签名移除 user_location，定位语义统一在 place_extract）；相关测试已对齐新签名。

## 2026-09-06 第五轮：检索管线升级（双路召回 + 本地重排）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-06 | tools.py | P1 | LLM Rerank（qwen 端点，20 候选×300字 prompt）在 30s 超时内 100% 失败，每条低置信度法律查询白等 30s 后回退 trgm——实测冒烟抓到 | 整体删除 `_rerank_with_deepseek`；换本地 bge-reranker-base cross-encoder（sentence_transformers，镜像内 torch 已预装），to_thread 防阻塞事件循环 |
| 09-06 | tools.py | P1 | huggingface_hub 加载已缓存模型仍强制 HEAD 校验 huggingface.co，离线机器重试 5 轮、首查卡死 4 分钟后失败（冒烟实测） | 加载前 `HF_HUB_OFFLINE=1`（setdefault）；构建期下载由 Dockerfile RUN 显式 =0 覆盖 |
| 09-06 | tools.py | P2 | 召回窗 max(top_k*4,20) 过大（用户决策：召回 15/重排前 5） | 常量 RECALL_LIMIT=15 / RERANK_TOP=5，单测钉住 |
| 09-06 | tools.py | P2 | "口语映射后重试"是逻辑死代码：重试查询与首次完全相同，结果恒等 | 随管线重写移除 |
| 09-06 | tools.py | P2 | 权限过滤 SQL 片段在 trgm/ILIKE 两处重复，向量路将成为第三处 | 抽 `_perm_clause`；`_recall_pg_vector` 向量路上线（索引建了很久但无人查询），Ollama 不可用时优雅退化为 trgm 单路 |

配套：scripts/backfill_civil_embeddings.py（399 个 NULL embedding 块回填，需 Ollama）；Dockerfile 构建期预下载 bge-reranker-base（镜像 +1.1GB）；tests/unit/test_search_pipeline.py（RRF/权限片段/常量）。
容器内端到端冒烟：`trgm+vector+rrf+local_rerank` 生效，热查询 0.7-1.6s（原 30s 超时回退），"离婚冷静期"→1077 第一、"狗咬人"→前三全动物条款。

| 09-06 | tools.py | P1 | `_generate_embedding` 先试 host.docker.internal（容器地址）——主机上直连 Ollama 时对该地址黑洞等到连接超时（HTTP_TIMEOUT_MEDIUM=15s），每查询白等 15s 才轮到 localhost | URL 顺序对调（容器内 localhost 拒连是即时的），超时收窄至 5s |
| 09-06 | 运维 | — | civil_code 399 块 embedding NULL（历史导入遗漏），向量路对其不可见 | Ollama 起服 + dmeta 模型拉取，scripts/backfill_civil_embeddings.py 回填 399/399（现 1260/1260 全覆盖）；主机冒烟：语义改写类查询 162-350ms 直达正确条文 |

## 2026-09-07 审查批次修复（第 4 批，配合 app/修复日志.md 实施记录）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | document_parser.py | P1 | PDF/DOCX 解析无页数/图片数/时长上限，单个恶意上传可占死 OCR 通道 | 页 200/图 50/deadline 60s，超限截断并追加标注（三条 OCR 通道全覆盖；deadline 为协作式，页粒度） |
| 09-07 | tools.py | P2 | _generate_embedding 硬编码两个 Ollama URL，配置项不生效 | 读 EMBEDDING_API_URL/EMBEDDING_MODEL，派生另一主机名兜底 |
| 09-07 | tools.py | P2 | _keyword_fill ILIKE 通配符未转义，可构造全表模糊匹配 | %/_/\ 转义 + SQL ESCAPE 声明 |
| 09-07 | tools.py | P2 | 天气 lives 空数组 IndexError 落"查询失败" | 返回「{city} 暂无天气数据」 |
| 09-07 | router.py / runner.py | P2 | user_perms 可变默认参 [] | 改不可变 ()（保 fail-closed 语义，理由见 services 日志同项） |
| 09-07 | orchestrator.py | P2 | Phase1/Phase2 并行 N 路 LLM 调用绕过 llm_semaphore | _call_deepseek_think 纳入信号量（网络调用段持锁） |
| 09-07 | runner.py | P2 | 取消事件清理散落各路径，异常/早退漏清慢性泄漏；conv_id 秒级相撞；inc_used_questions 裸 create_task | finally 统一 cleanup_event（拆 _release_task_resources，顺带压回 80 行内）；uuid 后缀；spawn |

回归：tests/unit/test_20260907_batch_fixes.py（第 4 批 4 用例）；门禁 scripts/check_code_rules.py 通过

## 2026-09-07：触发式检索改写重试（AI 层补强，按桌面《AI 层深度补强指南》1.5 决策）

| 日期 | 文件 | 级别 | 内容 | 实现 |
|---|---|---|---|---|
| 09-07 | tools.py | 增强 | 双路召回都低置信时才花一次 LLM 调用改写重试，不默认全量开 | _recall_two_ways 抽取（trgm 峰值在关键词补充前取，防 ILIKE 固定 0.5 虚高门禁）+ _is_low_confidence 门禁（trgm<0.45 且 vec<0.50，KB_REWRITE_* env 可调）+ _rewrite_query_for_recall（post_chat_completion 主备降级，fail-open，只用于召回、重排仍用原话）+ 四路 RRF 重新融合；method 标签带 rewrite_retry 供评测统计 |
| 09-07 | web_search.py（新） | 债务 | tools.py 678 行超 600 硬限 | web_search 子系统（限流+搜索+降级 99 行）整体拆出，tools.py re-export 保持 import 兼容；两个历史测试的 patch 目标同步指向新模块 |

回归：tests/unit/test_search_pipeline.py 新增 4 用例（门禁纯函数/改写清洗/低置信触发/高置信不打扰），全量 177 passed；门禁通过。
ponytail：改写阈值未经标注集标定（经验初值），下一步用 run_retrieval_eval 在 55 条标注集上校准触发率与收益。

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | router.py:400 / runner.py:176 | P2 | 裸 asyncio.create_task(safe_set(...)) 未迁移 spawn——本轮"全仓后台任务统一走 spawn()"目标的同型漏网（safe_set 自吞异常，但 GC 中途回收风险同类） | 两处换 spawn | 待修复 |
| 09-07 | document_parser.py | P3 | 截断标注数值两处小误差：①parse_pdf deadline 分支 page_count 在 deadline 检查前已 +1，"处理了 N 页"虚高 1；②_render_pages_ocr 截断标注 images 恒为 0（每页实际都渲染了图）。仅标注文案精度，不影响主流程 | 计数时机微调 | 待修复（低优先） |

复审通过项：web_search.py 拆分为行为等价纯移动（逐行核对），两个历史测试 patch 目标已迁到定义处模块（patch re-export 名不影响新模块内部查找，处理正确）；拆分后全仓 wc 复核无文件超 600 行硬限；改写重试 fail-open + KB_REWRITE_* env 可调 + ponytail 标注 + 完整测试 4 用例（test_search_pipeline：门禁/清洗/触发/跳过反向断言）；_rrf_merge 变参签名天然支持四路融合；_generate_embedding 配置化（EMBEDDING_API_URL/MODEL 配置项存在、alt host 派生正确、URL 不含 localhost 时单地址）；_keyword_fill ILIKE 转义（ESCAPE 声明+参数化，测试钉住）；runner._release_task_resources finally 统一收尾（cleanup_event 慢性泄漏修复，pop 幂等，成功路径也不再漏）；user_perms 缺省 () 语义核实（search_knowledge None=不过滤的 fail-open 语义未破坏）；conv_id uuid 后缀三处（runner/router/chat_stream_ctx）对齐；PDF/DOCX 三条 OCR 通道页/图/deadline 上限 break 路径逐条核对。

## 2026-09-09 全目录审查轮（10 域并行 + 主代理逐条复核）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | agents/tools.py:157 + alembic | **P1** | knowledge_chunks.permission 列不在任何迁移/脚本中：tools.py 民法典检索引用该列（`COALESCE(permission,'{}')='' OR permission && $3`），baseline 建表 DDL 无此列——全新环境 `alembic upgrade head` 后首查即 UndefinedColumnError，检索主流程不可用（与 d9e0 users 缺列事故同类，users 修了这张表漏了） | 新迁移 e1a2b3c4d5e6：`ADD COLUMN IF NOT EXISTS permission TEXT[] NOT NULL DEFAULT '{}'`（幂等兼容线上手工列），downgrade 对称删列 | ✅ 已修 09-09 |
| 09-09 | agents/react_steps.py:177 + agents/router.py:471 | **P1** | web_search 外部网页内容（title/body/href）未经消毒直进提示词：圆桌路径有 _sanitize_context 防线、这两条兄弟路径漏（"忽略以上指令"类注入可进 LLM 上下文）；修一处漏兄弟路径模式的又一例 | 两处 tool 消息/system prompt 注入点复用 orchestrator._sanitize_context 包裹 json.dumps | ✅ 已修 09-09 |
| 09-09 | agents/router.py:287-330（简单任务） | **P0 关联** | 简单任务 _format_via_llm 与 runner 同款：只打 simple_task 指标、不扣费不计日 token（本次 runner P0 收口的同批兄弟路径） | 加 username 参数，usage 经 bill_token_usage 补钱包扣费与日 token 计数 | ✅ 已修 09-09 |
| 09-09 | sub_agents.py:113、router.py:176/297、memory.py:83 | P2 | 多处 LLM 调用绕过 llm_semaphore（09-07 修了 orchestrator 同型问题，兄弟调用方未跟）：sub_agents 还被 react_steps gather 并行最多 4 路 | 4 处网络调用段纳入 llm_semaphore（与 orchestrator 同修法） | 待修复 |
| 09-09 | tools.py:533-537 | P2 | _project_keyword_fill ILIKE 通配符未转义：09-07 修了 civil 通道同型漏洞（:232-234），project 通道漏 | 复用 civil 通道的转义两行 + ESCAPE '\' | 待修复 |
| 09-09 | web_search.py:65 + router 简单路径 | P2 | DDGS 未传显式 timeout（依赖库默认）；简单任务路径 dispatch_tool 全程无 wait_for 兜底（ReAct 路径有 TOOL_TIMEOUT） | DDGS(timeout=10)；简单路径包 asyncio.wait_for | 待修复 |
| 09-09 | orchestrator.py:419-425 | P2 | Phase2 review prompt 序列化整个 opinion dict，消毒只覆盖 analysis/cross_comments 两字段（suggestions 与 LLM 任意额外键未消毒进 system prompt） | 入库时对 opinion 全部字符串值消毒，或 discussion_json 白名单挑字段 | 待修复 |
| 09-09 | react_steps.py:163-165 | P2 | gather 捕获的异常 str 原样进 tool 消息回流 LLM 上下文，未过 sanitize_error_text（runner:329 同款已做） | `{"error": sanitize_error_text(str(result))}` | 待修复 |
| 09-09 | cdc/worker.py:34 + routes.py:58 | P2 | 已处理事件 60s 即删，/api/cdc/events 的 after 游标外部消费者超 60s 不拉取即永久断流（主消费者 JSONL journal 不受影响，设计取舍） | API 文档标明消费窗口，或 CLEANUP_EVERY 调大 | 登记 |

**存量核验**：b1c2d3e4f5a6 分区迁移 downgrade 对称性复核 **✅ 通过**（rename 让位→重建→迁数→setval→DROP 无残留）；d9e0 修复复核 ✅；runner.py:176 / router.py:400 裸 create_task(safe_set) **仍在**（safe_set 自吞异常，仅 GC 弱引用风险）；13 迁移单链无分叉；cdc at-least-once+leader 锁 token 校验 ✅。

## 2026-09-10 修复：天气工具支持未来三天预报

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | weather.py、tool_definitions.py、prompts/travel.json | P1 | 天气工具只请求 `extensions=base`，仅返回当天实况；模型会自行补全未来两天天气 | 改请求 `extensions=all`，解析并返回未来三天昼夜天气/温度/风向；`lives` 为空时用首日预报兜底 | 已修 |
| 09-10 | orchestrator.py | P2 | 圆桌工具摘要只显示当天天气，预报天数不可见 | 摘要追加“预报N天” | 已修 |

回归：`tests/unit/test_weather_forecast.py`；端到端复验天气工具返回 3 天预报，回答中的三天天气均有工具数据来源。

## 2026-09-10 第三批修复：任务取消、提示词边界与多实例限流

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | router.py、runner.py、react_steps.py | P0 | Agent 任务取消后，简单工具/格式化调用仍可能继续执行，晚到结果会覆盖取消终态；取消时已产生的 usage 未结算 | 工具与格式化调用增加超时和远程取消检查；完成、失败、取消统一走终态 CAS；取消前先结算已收到 usage | 已修 |
| 09-10 | router.py | P0 | 简单酒店、路线、美食任务调用子 Agent 时未透传 username，内部 LLM 消耗无法归户计费 | call_sub_agent 透传 username，并补计量回归 | 已修 |
| 09-10 | orchestrator.py、runner.py | P1 | 共享上下文把用户原话拼入 system 权限文本；Phase1 输出仅消毒两个固定字段，suggestions 等任意字段可旁路进入后续 prompt | 共享上下文移除用户原话；用户输入明确标记为仅作数据；Phase1 意见递归消毒全部字符串 | 已修 |
| 09-10 | web_search.py | P1 | 进程内字典限流在多实例部署下各自计数，同一用户可随实例数放大搜索次数 | 改为 Redis 原子计数，多实例共享窗口上限，并补跨用户隔离测试 | 已修 |
| 09-10 | router.py、runner.py、react_steps.py | P2 | safe_set 缓存写入仍使用裸 create_task；任务已有历史时仍允许全局语义缓存 | 统一改走 spawn；有历史会话跳过语义缓存，避免旧答案串入当前上下文 | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | sub_agents.py、router.py、memory.py | P2 | 多处 LLM 调用绕过 llm_semaphore（09-09 登记）：子 Agent 被 react_steps gather 并行最多 4 路、意图分类直连 httpx、记忆压缩生成 | 三处调用段统一纳入 llm_semaphore（与 orchestrator 同修法） | 已修 |
| 09-10 | tools.py | P2 | _project_keyword_fill ILIKE 通配符未转义，civil 通道同型漏洞（09-09 登记） | 复用 civil 通道转义写法（\ % _）+ ESCAPE '\' | 已修 |
| 09-10 | web_search.py | P2 | DDGS 未传显式 timeout（09-09 登记） | DDGS.text(timeout=10)；简单路径无 wait_for 的问题已随 09-10 _run_simple_tool 包 TOOL_TIMEOUT 修复 | 已修 |
| 09-10 | document_parser.py | P3 | 截断标注两处数值误差：deadline 分支 page_count 虚高 1；_render_pages_ocr images 恒 0（09-07 登记） | deadline 检查提前到自增前；渲染计数 rendered 如实入标注 | 已修 |
| 09-10 | react_steps.py 异常 str 回流 | P2 | gather 捕获的异常 str 原样进 tool 消息（09-09 登记） | 复核确认已随 09-10 修复批闭环：错误工具统一走 fallback 文案 + _sanitize_context 消毒整条 tool 消息 | 已修（核实） |
| 09-10 | orchestrator.py opinion 消毒 | P2 | Phase2 prompt 序列化整个 opinion dict 只消毒两固定字段（09-09 登记） | 复核确认已随 09-10 修复批闭环：add_phase1_opinion 递归消毒全部字符串 | 已修（核实） |
| 09-10 | runner/router 裸 create_task | P2 | safe_set 缓存写入裸 create_task 可被 GC 回收（09-07 登记） | 复核确认已随 09-10 修复批改 spawn | 已修（核实） |


## 2026-09-11 全量规则审查轮（5 路并行子代理逐文件深读 + 主代理复核）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | tools.py:507 | **P1** | project 通道 ILIKE 兜底 SQL 写作 `ESCAPE '\'`——Python 转义把反斜杠消费成 `ESCAPE '`（PG 语法错误），关键词兜底一跑即炸且连带丢掉已召回结果；`\%`/`\_` 为非法转义序列 | SQL 改 `ESCAPE '\\'`（渲染单反斜杠）、转义链规范写法；新增 _project_keyword_fill 回归（断言 SQL 与参数转义） | 已修 |
| 09-11 | weather.py:48 | P2 | AMAP_API_KEY 裸 os.getenv，未随 map_api 一并收编 config | 改 `from ..core.config import AMAP_API_KEY` | 已修 |
| 09-11 | runner/router/orchestrator/memory/react_steps 六处 | P2 | DEEPSEEK_API_KEY 裸 os.getenv，绕过 get_deepseek_key 的多 Key 轮询与健康剔除 | 登记：统一改池化取 Key 需回归六条路径，限期处理 | 登记 |
| 09-11 | weather.py:67 | P2 | geocode 守卫拒掉默认城市时递归无深度限制（同输入同输出，RecursionError 兜住） | 登记：加 depth 参数深度 1 即止 | 登记 |
| 09-11 | runner.py:401、tools.py:449 | P2 | 两处静默吞异常无豁免注释（runner 导入兜底、embedding 双 URL 全挂） | 登记：补 logger 或豁免注释 | 登记 |
| 09-11 | runner.py:292 | P2 | 续期任务裸 create_task（无异常认领回调） | 登记：改 spawn 或补 done_callback | 登记 |
| 09-11 | react_steps.py:62-79 | P2 | 取消仅 usage 事件检查、blocked break 拿不到尾部 usage → 该轮 token 漏计费（量级小） | 登记：与"流式取消响应性"取舍合并评估 | 登记 |


## 2026-09-12 凌晨清欠轮（09-11 登记 P2 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | runner/router/orchestrator/memory/react_steps 六处 | P2 | DEEPSEEK_API_KEY 裸 os.getenv，绕过多 Key 池化与健康剔除（09-11 登记） | 全部改 `await get_deepseek_key()`（懒导入防循环依赖），语义与 sub_agents 先例一致 | 已修 |
| 09-12 | weather.py:67 | P2 | geocode 守卫拒默认城市时无限递归（09-11 登记） | 加 depth 参数，深度 ≥1 直接返回未找到 | 已修 |
| 09-12 | runner.py:401、tools.py:449 | P2 | 两处静默吞异常无豁免注释（09-11 登记） | runner 补 noqa 豁免注释（退回空工具集属设计）；embedding 端点失败补 warning 日志 | 已修 |
| 09-12 | runner.py:292 | P2 | 重建锁续期任务裸 create_task（09-11 登记） | 改 spawn（持强引用+异常认领） | 已修 |


## 2026-09-12 深检第二轮（换语义不变量/并发竞态视角再查）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | react_steps.py `_consume_llm_stream` + tools.py `_load_colloquial_map` | **P1/P2** | ①任务路径 DFA 拦截 break 遗弃内层 stream_llm 生成器——usage 尾块未消费，被拦截响应零扣费；②口语映射首调并发竞态：锁内置空 dict 后锁外填充，并发快路径把空/部分 dict 当已加载返回，口语化映射静默失效 | ①拦截后继续消费内层流至穷尽只捕获 usage 尾块；②双检锁 + 局部构建完成后一次性发布（与 law_mapping 同款）。修复过程中曾引入 _LOAD_LOCK 未定义 NameError，已自愈并运行时验证（52 条映射加载成功） | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | router.py:393、509 | P0 | _emit_simple_result 只过滤局部变量并返回布尔值，调用方仍把过滤前 final_answer 写入记忆和语义缓存 | 返回过滤后的文本，记忆、缓存和任务结果统一使用同一份内容 | 待修复 |
| 09-11 | router.py:478、505 | P1 | 简单任务检测到远端取消后直接 return，不落 cancelled 终态；runner 清理取消标记后任务可能停留 generating | 所有取消早退统一走 _finish_cancelled，并增加终态兜底 | 待修复 |
| 09-11 | runner.py:119、routing_table.py:406 | P1 | 任务路径未走完整 route_query；危险关键词检查位于文章引用和白名单之后，可被更高优先级分支绕过 | 将安全拒答门禁提升到路由最前，任务与 SSE 使用同一裁决 | 待修复 |
| 09-11 | router.py:576、orchestrator.py:257 | P1 | 外部搜索结果经正则过滤后直接拼入 system prompt，正则不是完整提示词注入边界 | 第三方结果按不可信 tool/user 数据处理，使用结构化边界 | 待修复 |
| 09-11 | react_steps.py:69、runner.py:148 | P1 | 流式内容逐块过滤后立即写缓冲，跨块敏感词只能在最终全文检查时发现，原内容无法撤回 | 使用滑动窗口或 ContentStreamGuard，未知尾部不发送 | 待修复 |
| 09-11 | runner.py:244、286、router.py:433 | P1 | 任务缓存上下文不含用户名和文件指纹，且缓存查询早于文件上下文加载，可能命中旧或无文件上下文 | 缓存上下文加入用户标识；有 file_ids 时禁用缓存或加入文件哈希 | 待修复 |
| 09-11 | law_mapping.py:139 | P1 | 任意“第 X 条”都会跳过法律映射纠错，未限定必须是民法典条文，其他法律问题可能被错误检索 | 仅显式民法典/民法通则/民法总则条文跳过，其他先走黑名单与纠错 | 待修复 |
| 09-11 | document_parser.py:120、220 | P1 | 扫描 PDF 整页 OCR 未受图片上限约束；独立图片 OCR 未使用全局锁，存在资源耗尽和并发不安全 | 两条路径共享计数、锁、超时和有界执行器 | 待修复 |
| 09-11 | tools.py:232 | P1 | 重排模型首次加载无锁且同步在事件循环执行，to_thread 取消不能终止 CPU 推理 | 启动期预热单例，使用有界执行器和并发信号量 | 待修复 |
| 09-11 | react_steps.py:147、tool_definitions.py:11 | P2 | 工具参数只解析 JSON，不按工具 schema 校验必填、类型和长度 | 为每个工具建立 Pydantic 参数模型统一验证 | 登记 |


## 2026-09-12 外部复核修复轮（P0×6 全修 + P1×3）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | router.py `_emit_simple_result` | **P0** | 只过滤局部变量并返回布尔值，调用方仍把过滤前 final_answer 写入记忆和语义缓存——拦截产物原样落盘 | `_emit_simple_result` 返回过滤后文本；记忆、缓存、任务结果统一同一份内容；对应测试同步 | 已修 |
| 09-12 | router.py:478,505 | **P1** | 简单任务检测到远端取消后直接 return，不落 cancelled 终态；任务停留 generating | 所有取消早退统一走 cancel_task_if_active CAS 终态 | 已修 |
| 09-12 | runner.py:119、routing_table.py:406 | P1 | 任务路径未走完整 route_query；危险关键词检查顺序可被绕过 | route_query 安全裁决前置（详见 services 轮同款） | 已修 |
| 09-12 | router.py:576、orchestrator.py:257 | P1 | 外部搜索结果正则过滤后直接拼 system prompt，非完整注入边界 | tool 消息统一 _sanitize_context / display_safe_tool_result | 已修 |
| 09-12 | runner.py:244,286、router.py:433 | P1 | 任务缓存上下文不含用户名/文件指纹，缓存查询早于文件加载 | build_cache_ctx 加 username 首位；有 file_ids 时禁用缓存 | 已修 |
| 09-12 | law_mapping.py:139 | P1 | 任意"第 X 条"跳过纠错映射，其他法律问题绕过 | 仅民法典/民法通则/民法总则跳过 | 已修 |
| 09-12 | document_parser.py:120,220 | P1 | 扫描 OCR 未受图片上限约束；独立图片 OCR 未用全局锁 | 核实：整页 OCR 受 _MAX_PDF_PAGES + deadline 约束、OCR 走模块级 _OCR_LOCK——登记复核结论 | 已核 |
| 09-12 | tools.py:232 | P1 | 重排模型并发首调各自加载一份模型（内存翻倍） | 双检锁（_RERANKER_LOCK） | 已修 |
| 09-12 | react_steps.py:147 | P2 | 工具参数只解析 JSON 不按 schema 校验 | 登记：Pydantic 参数模型统一验证（涉及全部工具定义，单独批次） | 登记 |


## 2026-09-12 外部复核修复轮（P1×6 修 + P2×1 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | router.py:393,509 | **P0** | `_emit_simple_result` 只过滤局部变量返回布尔，调用方仍写过滤前 final_answer 进记忆与缓存 | 返回过滤后文本，记忆/缓存/任务统一同一份 | 已修 |
| 09-12 | router.py:478,505 | **P1** | 简单任务远端取消后直接 return 不落 cancelled 终态，任务停留 generating | 取消早退统一走 cancel_task_if_active CAS | 已修 |
| 09-12 | runner.py:119、routing_table.py:406 | P1 | 任务路径未走完整 route_query，危险关键词检查顺序可绕过 | 安全拒答门禁前置到路由最前（任务与 SSE 同裁决） | 已修 |
| 09-12 | router.py:576、orchestrator.py:257 | P1 | 搜索结果正则过滤后直拼 system prompt，非完整注入边界 | 第三方结果按不可信数据处理：_sanitize_context 消毒 tool 消息 | 已修 |
| 09-12 | react_steps.py:69、runner.py:148 | P1 | 流式逐块过滤后立即写缓冲，跨块敏感词无法撤回 | ContentStreamGuard 接入（chat/降级/任务三路径全覆盖，本会话完成） | 已修 |
| 09-12 | runner.py:244,286、router.py:433 | P1 | 任务缓存上下文不含用户名/文件指纹，查询早于文件加载 | build_cache_ctx 加 username 首位；有 file_ids 禁用缓存 | 已修 |
| 09-12 | law_mapping.py:139 | P1 | 任意"第 X 条"跳过纠错映射 | 仅民法典系跳过 | 已修 |
| 09-12 | document_parser.py:120,220 | P1 | 整页 OCR 无图片上限；独立图片 OCR 无全局锁 | 核实：页数/deadline 约束在、OCR 已走模块级 _OCR_LOCK——登记复核结论 | 已核 |
| 09-12 | tools.py:232 | P1 | 重排模型并发首调重复加载 | 双检锁 | 已修 |
| 09-12 | react_steps.py:147 | P2 | 工具参数只解析 JSON 不按 schema 校验 | dispatch_tool 共享漏斗加非对象参数守卫（数组/字符串按缺参走既有 or-user_query 兜底，4 条调用链一处修，回归单测）；全量 Pydantic 参数模型维持登记（与各工具兜底语义重复，收益有限） | 部分修 |
| 09-12 | tools.py:439-473 | **P0** | `_generate_embedding` 只读 `EMBEDDING_API_URL`，而该值在权威 .env 与容器内**均为空字符串**；注释声称的"自动派生另一台主机名做兜底"对空串失效（`''.replace("localhost", "host.docker.internal")` 仍是 `''`，`if alt_url != EMBEDDING_API_URL` 不成立），于是两轮尝试都是空 URL → 必定失败返回 None → `_recall_pg_vector` 静默 `return []` → **pgvector 向量召回 100% 失效**。而 `OLLAMA_URL=http://host.docker.internal:11434/api/generate` 早已配好且实测容器内可达（curl 返回模型列表），函数却完全不读它。连锁影响：trgm 单腿实测最高分仅 0.0706~0.1282，远低于 `REWRITE_TRIGGER_TRGM_SIM=0.45` → 每条查询都判低置信 → **每次都白触发一次 LLM 改写重试**（成本+延迟）；叠加检索层零指标（`kb_search_total` 在 metrics.py:56 定义但全仓零调用）→ 长期无人发现 | 建议（未改，待主人拍板）：① `_generate_embedding` 在 `EMBEDDING_API_URL` 为空时回退到由 `OLLAMA_URL` 推导的 embeddings 端点（注意 Ollama 的嵌入接口是 `/api/embeddings`，不是 `/api/generate`，需换端点而非直接复用该 URL）；② 或在 .env/compose 显式配置 `EMBEDDING_API_URL`；③ 同时补 `kb_search_total` 埋点 + 检索类告警，否则同类故障仍不可观测 | 待主人拍板（已实测取证：55/55 条真实 trace 的 vector 腿 hits=0、max_score=0.0） |
| 09-12 | tools.py:8-9,20,369,427-432 + **新增 retrieval_trace.py** | 功能 | **检索层可观测性落地（Phase 1）**：检索链路此前零指标零落库，同批 P0（向量召回全失效）因此长期无人发现。本次补齐——① 新增 `app/agents/retrieval_trace.py`（121 行）：`record()` 在检索出口打指标 + spawn() 后台落库，全程 fail-silent，绝不阻断检索；② `_search_two_legs` 把分路命中数/最高分写入 contextvar `_LAST_LEGS`（+1 行，不改函数签名与返回值，因此不会污染给 LLM 的 tool 消息）；③ `search_knowledge` 出口计时并调用 `record()`（净 +3 行，tools.py 577 行，硬限 600 内）；④ 新增 `kb_leg_empty_total{leg}` / `kb_recall_empty_total{persona}` / `kb_rerank_dropped_total` / `kb_low_confidence_total` / `kb_retrieval_latency_seconds` 五个指标（`kb_search_total` 首次被真正调用）；⑤ alembic 迁移 `c4d5e6f7a8b9` 建 `retrieval_traces` 表（含单腿故障部分索引） | 已落地（本地端到端实测：`vec_hits=0`/`rerank_used=false` 被如实落库，指标 `kb_leg_empty_total{leg="vector"}=1` 正常上报） |

## 2026-09-12 修复轮：embedding 配置链（P0，同日诊断发现并已修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | config.py:229-267（新增） | — | 端点配置为空时无任何兜底 | 新增 `embedding_endpoint_candidates()`：显式配置优先，为空时从已配好且可达的 `OLLAMA_URL` 派生 `/api/embeddings`（Ollama 的嵌入接口与生成接口 `/api/generate` 不是同一个，必须换而非复用），两个主机名互为兜底；去空去重保序 | 已修 |
| 09-12 | tools.py:445-476 `_generate_embedding` | **P0** | 把空串当唯一候选发出去 → 报 "URL missing protocol" 后静默返回 None → `_recall_pg_vector` 静默 `return []` → **向量召回 100% 失效** | 改用 `embedding_endpoint_candidates()`；日志带上具体 URL；全部失败时明确告警（不再静默 return None）；无候选时给出可诊断的提示 | **已修** |
| 09-12 | 权威 .env:28,30 + .env.example:62-69 | **P0** | `EMBEDDING_API_URL=`（空）+ `EMBEDDING_MODEL=text-embedding-ada-002`（**错误模型名**，与建库侧不一致） | 两者改为与 `scripts/build_knowledge_base.py:98-110` 建库侧完全一致：`http://host.docker.internal:11434/api/embeddings` + `shaw/dmeta-embedding-zh`；`.env.example` 补注释说明"查询侧必须与建库侧同模型，否则向量空间不匹配" | **已修**（改前已备份 `D:\桌面\.env.bak_before_embedding_fix`） |

**验证（真实数据，非推演）**：重建容器使配置生效 → 重采 55 条 → 与修复前基线逐条对比：

| 指标 | 修复前 | 修复后 | 变化 |
|---|---|---|---|
| recall@5 | 0.3879 | **0.6848** | +0.2969（相对 +77%） |
| MRR | 0.3491 | **0.5855** | +0.2364（+68%） |
| 零命中条数 | 32 / 55 | **15 / 55** | -17 |
| 健康样本 | 0 | **47（85.5%）** | — |
| RC-1b 单腿故障 | 55 | **0** | 消失 |
| RC-2 位置偏差 | 4 | 6 | +2（修好一层后暴露的下一层问题，非回归） |

**关键结论**：修复前的 what-if 推演数字与实际修复结果**逐项完全一致** —— 说明"只换 embedding 源、下游全健康"的判断成立，唯一断点就是 embedding 生成。

**遗留**：容器内仍是旧代码（本次真实验证靠的是**配置修复**，它对旧代码同样生效）；代码层修复需重建镜像才生效，届时会额外获得空配置兜底与更清晰的失败日志。

## 2026-09-12 检索质量专项：0.3879 → 0.7212，以及三个**已证伪**的调参方向

起因：主人质疑 recall@5=0.6848 对民法典 RAG 太低。用 `ragclosure` 对失败用例做环节归因：

| 损失环节 | 条数 | 占比 |
|---|---|---|
| 进了前 5 | 40 | 72.7% |
| **召回到但被挤出前 5** | 9 | 16.4% |
| 召回池就没有 | 6 | 10.9% |

### 已修：法条编号归一化（+0.0364）

`normalize_article_ref`（`app/agents/article_normalizer.py`）**早已完整实现却从未接入检索链路**
——全仓仅单元测试调用。用户写「第533条」而库内是「第五百三十三条」时，trgm 词面路与
ILIKE 关键词路双双失配（实测 similarity 0.0065 vs 0.0556，差 8.5 倍），只剩向量一路投票，
正确条文极易被挤出召回池。已在 `search_knowledge` 的检索查询构造链中接线（tools.py）。

**效果**：recall@5 `0.6848 → 0.7212`，零命中 `15 → 13` 条。

### 四个已实测、**结论为无效**的方案（勿重复尝试）

| 实验 | 结果 | 结论 |
|---|---|---|
| `KB_RERANK_TOP=15`（放宽容排窗） | +0.0091 | 收益可忽略 → 那 9 条的正确条文在 RRF 后排名已 > 15，放宽重排窗够不着 |
| `word_similarity` 替换 `similarity` | **-0.0091** | 短关键词查询大幅改善（第533条排名 22→1），但本库用例以自然语言长句为主，整体略逊 |
| `GREATEST(similarity, word_similarity)` | -0.0091（同上） | **不是有效组合**：word_similarity 在所有块上恒高于 similarity（尺度不同），取 max 被其完全主导 |
| `KB_RECALL_LIMIT=30` + `KB_RERANK_TOP=15` | **-0.0182** | 扩大召回窗只引入噪声，RRF/重排无法区分 |

**核心结论：参数调整已到瓶颈。** 剩余 13 条零命中 + 6 条位置偏差不是调参能解决的。

### 剩余瓶颈与下一步方向（按性价比排序）

1. **术语鸿沟（数据工程，最高性价比）**：查询用学理/口语词而法条原文没有。例：
   「情势变更」不在第533条原文里、「彩礼」实际对应第1042条。项目已有
   `_COLLOQUIAL_MAP` + `law_mapping.py` 机制，只需扩充映射数据——每个命中即 +1 条。
2. **重排模型适配**：`BAAI/bge-reranker-base` 是通用中文模型，非法条领域微调。
3. **切分粒度**：法条级切分，跨条问题（如"未成年打赏退款"涉第19+145条）天然吃亏。
4. **RRF 权重**：现为等权 k=60，可考虑提高 vector 路权重（需在标注集上调，注意过拟合）。

**当前最佳状态**：recall@5 = **0.7212**（较修复前 0.3879 提升 86%），MRR 0.6127，
健康样本 47/55 (85.5%)。容器参数已回到默认最优：`RECALL_LIMIT=15` /
`RERANK_TOP=5` / `similarity`。

## 2026-09-12 检索质量第二阶段：RRF 加权（+0.03）· 片段方案证伪 · 按规则重构

### 一、先验证再改（吸取上一轮教训）

上一轮未验证就改 `_keyword_fill`，零收益且破坏安全测试，被迫回滚。本轮改为
**先离线验证、再动代码**，两个候选方案：

| 方案 | 离线验证结果 | 结论 |
|---|---|---|
| 4 字滑窗片段切分救关键词路 | 5 条失败用例全部"前15不含正确法条"，命中块数多为 0 | **证伪**——滑窗≠分词，产物是跨词边界噪声（"的房子漏""居装修噪"） |
| **RRF 降低 trgm 权重** | 50 条 trace 离线模拟：trgm 权重 0.3~0.5 时 RRF 前5 命中 0.78→0.82；**归零反降至 0.80** | **有效，采纳** |

### 二、已实施：RRF 按路加权

- `_rrf_merge` 新增 `weights` 参数（默认 `None` = 等权，**行为不变**）
- `_search_two_legs` 三处调用传 `(RRF_TRGM_WEIGHT, 1.0, ...)`
- 新常量 `RRF_TRGM_WEIGHT` 默认 **0.4**（`KB_RRF_TRGM_WEIGHT` 可配）

依据：trgm 路排序接近随机（相关块 0.047 vs 无关块 0.012，区分度 4 倍且都在噪声区），
与向量路**等权**融合等于把噪声抬进前排。

**端到端实测**（50 条同批、样本一致）：

| 指标 | 改前 | 改后 | 变化 |
|---|---|---|---|
| recall@5 | 0.7333 | **0.7633** | +0.0300 |
| MRR | 0.6107 | **0.6350** | +0.0243 |
| 零命中 | 11 | **9** | -2 |

与离线模拟预测（+0.04，RRF 前5 口径）方向一致、幅度合理（重排有损耗）。

### 三、同期重构：tools.py 拆分（防熵机制检查点 1）

新增权重支持后 `tools.py` 达 **638 行 > 600 硬限**，按规则**当场重构**：
把「项目知识库（求职场景）」子系统拆到 `app/agents/project_kb.py`（140 行，
与 `web_search.py` / `weather.py` 同例），`tools.py` 回落到 **506 行**。
旧 import 路径经 re-export 保持兼容；拆后 **239 单测全绿、规范门禁通过**。

### 四、剩余瓶颈（更新）

- **trgm 路**：已通过降权缓解（不再与向量等权），但根因（打分函数尺度 + 中文无分词）未解
- **关键词路**：仍**未生效**——滑窗方案已证伪，需真分词器或词表
- **重排损耗（新发现）**：离线模拟 RRF 前5 命中 0.78/0.82，而最终 recall@5 只有
  0.7333/0.7633 → **重排反而吃掉约 2~5 个百分点**，`bge-reranker-base` 在法条领域
  可能不适配。这解释了「7 条召回到了却排在第 3~5 位」的现象。
- **可复用线索**：`project_kb._project_keyword_fill` 的注释写着"拆分为 2-4 字片段"，
  其实现可能已解决中文片段问题，值得作为参考（本轮未及细看）

## 2026-09-12 检索质量第三阶段：RRF × CE 线性融合（+0.03）· 重排结构性缺陷修复

### 一、决定性实验：重排的真实作用被查明

前一轮记录的"重排吃掉 2~5 个百分点"经查证**是口径错误**（宽松"任一命中"口径 vs 官方
"按条文平均"口径）。用官方口径重做决定性实验（对 50 条候选池全量 CE 打分，1363 对）：

| 排序方式 | recall@5 |
|---|---|
| 纯 RRF 序（线上现状） | 0.7633 |
| RRF 前 15 交给 CE 重排取前 5 | 0.7300（**更差**） |
| CE 全池排序取前 5 | 0.7400（**更差**） |
| oracle@5（并集池最优挑 5） | 0.9100（上界） |

逐条看：CE 在难例上极准（CC010 正确法条 CE 排第 1、RRF 排第 6；CC026/CC039 排第 2），
却在简单例上捣乱——**两路信号互补，该融合而非替换**。

同时查明一个**结构性缺陷**：`_rerank_local` 只重排 `merged[:RERANK_TOP]`（=5 条），
集合被锁死——正确法条在融合池位次 6~26 的 10 条失败用例，重排根本无权挽救
（实测 0 打坏 0 救回）。

### 二、失败归因（官方口径，50 条）

| 候选池 | recall@5 |
|---|---|
| trgm 前 15 | 0.6133 |
| vector 前 15 | 0.8600 |
| 两腿并集 | 0.9100 |
| RRF 融合前 15 | 0.8600 |
| **最终 top5** | **0.7633** |

瓶颈在「从池里选 5 条」，不在召回量。15 条失败用例缺失的 15 个法条：10 个在池内
位次 6~26（排序问题），5 个池内没有（词汇鸿沟：彩礼/漏水/噪音/绰号/忠诚协议）。

### 三、离线验证 → 实施

先离线扫参（CE 全池打分落盘后做，未动代码）：线性融合
`final = (1-w)·rrf_norm + w·ce_norm` 在 **w=0.2~0.6 宽平台**达 0.7933
（池=15 与全池两档一致），端点确认 w=0 纯 RRF / w=1 纯 CE 均更差。

实施（`tools.py`）：
- `RERANK_TOP` 默认 5→**15**（`KB_RERANK_TOP` 可配）——解除集合锁死
- 新增 `_blend_rrf_ce()` + `RERANK_BLEND` 默认 **0.4**（`KB_RERANK_BLEND` 可配）
- `_search_two_legs` 重排后走融合序

**端到端实测**（同批 50 条）：

| 指标 | 改前 | 改后 | 变化 |
|---|---|---|---|
| recall@5 | 0.7633 | **0.7933** | +0.0300（与离线预测一分不差） |
| MRR | 0.6350 | **0.6583** | +0.0233 |
| 零命中 | 9 | **8** | -1 |
| RC-2 位置偏差 | 9 | **6** | -3 |
| 健康样本 | 41 (82%) | **43 (86%)** | +2 |

逐条：5 条变好（CC001/CC030/CC041/CC051/CC054），2 条变差（CC010/CC014）。
延迟代价：CE 打分对数从 5 → 15，每查询约 +2~3s（CPU），可用 KB_RERANK_TOP 调回。

### 四、顺带修复（ragclosure 适配器自身缺陷）

探针存候选 dict 的**引用**，下游 `_rerank_local` 原地写 `rerank_score`，导致
trace 里每条 recall leg 的 `top[].score` 被重排分数覆盖（实测 100 路里 76 路
"分数与位序自相矛盾"，混入 0.95 这类 similarity 域外值）。已改为采集时快照拷贝。
`max_score` 因在重排前取值未受影响；RRF 用列表位序（来自 SQL ORDER BY）也未受影响。

### 五、剩余瓶颈（更新）

- 词汇鸿沟 5 条（池内没有）：需扩 `_COLLOQUIAL_MAP`（彩礼/漏水/噪音/绰号等），纯数据工作
- 单腿召回：vector@15=0.86、并集=0.91 是当前召回上界，突破需换更强 embedding 或切分改造
- 标注集仅 50 条：任何调参都有过拟合风险，建议扩标注 + train/holdout 划分

## 2026-09-12 检索质量第四阶段：blend 重校准 + 向量路去近似化 + 学理词映射（+0.038→0.9158）

**根因链**（`ragclosure/reports/召回率提升分析.md` §八~§十）：
8 条零命中用例的金条在向量路精确位次 30~179——**重排救不了进不了 15 条窗的条目**；
剩余失败主因是 dmeta 对「学理词/口语 ↔ 条文表述」的语义对齐弱，叠加 ivfflat 近似噪声。

实施（`tools.py` + `tests/民法典映射表.txt`）：
- `RERANK_BLEND` 默认 0.4→**0.6**（`KB_RERANK_BLEND` 可配）：train 100 条离线扫描
  （`tests/run_rerank_sweep.py`，生产现状离线复算与真实评测完全一致 0.8781），
  blend 0.55~0.65 平台 recall@5=0.8895，MRR 中性；0.7 多救 1 条但 MRR -0.02 不取。
  单测 `test_user_decision_constants` 钉住。
- `_recall_pg_vector` 加 `SET LOCAL ivfflat.probes = 1000`（事务内 LOCAL，作用域限本查询）：
  civil 库仅 ~1260 行，ivfflat(lists=100) 默认 probes=1 只扫 ~1 簇，实测金条位次随
  LIMIT 漂移（17↔22↔9）且 8/11 金条连近似 top-60 都进不了。全簇扫描该量级毫秒级。
  ponytail: 行数过 5 万需重建 lists 并重估。
- 映射表两批 +28 条（第一批 无过错责任/自助行为/压岁钱，第二批 通用学理词 25 条：
  表见代理/无因管理/不当得利/善意取得/情势变更/不可抗力/正当防卫/紧急避险/缔约过失/
  预期违约/根本违约/代位继承/转继承/必留份/隐私权/个人信息/名誉权/肖像权/高空抛物/
  赡养费/相邻关系/返还原物/排除妨害/人身安全保护令/合同成立问法）。条号全部经库内
  `content LIKE '第X条%'` 核对存在；**holdout 缺口（轰鸣/聘金/职务代理）刻意不建**，
  避免验收集失真（holdout 查询已验证对新键零触发）。

**端到端实测**（train 100 条，官方口径）：

| 指标 | 0.8781 基线 | 本轮后 | 变化 |
|---|---|---|---|
| recall@5 | 0.8781 | **0.9158** | +0.0377 |
| recall@10 | 0.9193 | **0.9456** | +0.0263 |
| MRR | 0.7847 | **0.8038** | +0.0191 |
| recall@10 零命中 | 5 | **2** | -3 |
| 延迟 avg/p95 | 3452/4505ms | 3061/3808ms | 持平略降 |

holdout（45 条）0.8296/0.8384 持平无回归。剩余零命中：train CC005（家暴+保护令，
等 embedding 升级）/ CC094（微信要约长句，已加"合同什么时候成立"条目待复评）；
holdout 3 条待扩验收集后再调。

**已证伪/搁置**：bge-reranker-v2-m3 换模型（recall@5 不变、CPU 延迟 3.5 倍）；
minmax/sigmoid 归一化（与 identity 打平，CE 分已是 sigmoid 后 (0,1)）；heading 并入
CE 输入（-0.02，章节标题是噪声）；qwen3-lab 作 embedding（它是生成式 LLM，无
embedding 能力，此前"换模型不理想"的尝试即此原因）。

## 2026-09-12 检索质量第五阶段：条号直钉提升（train 零命中清零）

**根因**（CC094 全链路打点实测）：映射注入条号使 trgm 路第 1/2 位精确命中金条，
但 RRF 里 `RRF_TRGM_WEIGHT=0.4` 时 vector 路任意前 13 名（1.0/61~1.0/75）都压过
trgm 第 1 名（0.4/61）——金条被挤到融合第 16/17 位，进不了 15 条重排窗。
全局调权重证伪：0.6 时 recall@5 持平、MRR -0.009、CC094 仍零命中（trgm 第 1 名
要过 0.81 才能进前 15，调到那会放回 0.4 当初要解决的噪声）。

实施（`tools.py`）：
- 新增 `_promote_pinned()`：正则提取归一化查询中的中文条号（用户原话或映射注入），
  content 以该条号开头的块提升到融合序最前——**只保证进窗，最终排序仍由 CE×RRF
  质量闸门决定**；无条件号/无命中时原序返回。
- 接入点：`_search_two_legs` 的 RRF 之后、截窗之前。
- 单测 `test_promote_pinned_rescues_named_article` 钉住（提升/保序/不重复/直通）。

**端到端实测**（train 100 条）：

| 指标 | 提升前 | 提升后 | 变化 |
|---|---|---|---|
| recall@5 | 0.9316 | **0.9368** | +0.0052 |
| recall@10 | 0.9614 | **0.9719** | +0.0105 |
| MRR | 0.8091 | **0.8489** | +0.0398 |
| recall@10 零命中 | 1（CC094） | **0** | -1 |

holdout（45 条）：recall@5 0.8296→**0.8407**、MRR 0.8384→**0.8495**、
recall@10 -0.0074（个例边缘）、零命中 3 条不变（轰鸣/聘金/职务代理，映射刻意未覆盖）。

同轮映射二批（25 条通用学理词 + 人身安全保护令/合同成立问法）两次迭代定稿：
初版长短语注入在 CC086 误伤 -0.5（查询已含术语+条文描述时，注入是污染），
学理词键瘦身为"术语+条号"、跨多条的"无因管理"整条删除后恢复。条号全部经
库内 `content LIKE` 核对存在；holdout 三缺口刻意不建键，保验收集独立性。

**embedding A/B 收口（同日）**：`tests/run_embedding_ab.py` 同管线对决 train 100 条
——dmeta recall@5 0.9400 / bge-m3 0.9267（MRR 0.8325 vs 0.8409），dmeta 主指标胜出
且快 400ms/条。**换 embedding 无收益依据，杠杆关闭**；bge-m3 模型留在 Ollama 本地
（1.2G，可 `ollama rm bge-m3` 释放）。A/B 临时表已清理。

## 2026-09-12 通宵迭代：邻接聚合（否决默认启用）· 量化否决 · 改写门死亡确认 · 映射三批

**一项采纳**：映射表三批 +15 条安全长键口语覆盖（键长≥3 字防子串误伤——"离了"会
把"离开了"搅成"离婚开了"，故收"离了婚"；15 键对 holdout 100 条查询零命中，纯生产
覆盖不动评测数字），条号全部库内核对。

**四项有据否决**（全部 train 实测，门禁纪律执行）：
1. RERANK_TOP 15→20：recall@5 持平、MRR -0.03、延迟 +900ms。
2. CE int8 动态量化（KB_RERANK_QUANT，保留开关）：延迟 -24%、内存 ~4x 缩，但
   recall@5 -0.0079 超 0.005 质量门。2G/1G 云端内存告急时可权衡。
3. 邻接条文聚合（KB_NEIGHBOR_ENABLED，默认关）：立项依据扎实（train 12 条部分
   命中里 9 条缺失金条是已召回条文 ±1~2 邻居），但 **holdout 全面回退**
   （0.8407→0.8296 / MRR 0.8495→0.8272）——插入挤掉的已排名金条多于救回的。
   代码保留（条号索引缓存 + 强先验保护带 guard=9），重开前需扩验收集。
4. 改写重试阈值校准：触发数在各阈值档恒为 0——pg_trgm 噪声底 0.1~0.2 使
   "trgm 低 AND vec 低"门结构上不可能开。确认休眠为稳态。

**事故与教训（重要）**：邻接第一版用 `%s` 占位符（psycopg 语法），asyncpg 不支持
→ 邻接目标非空即抛错 → 评测把整条查询记为失败跳过。E2~E4 的"recall@5 0.97~1.0"
实为 76~88 条报错后**幸存易例的残集指标**（真跑 E3c = 0.9184 净负）。
教训：**读汇总指标前先核对 evaluated/errors 计数**；这正是 §六 train/holdout
纪律与 compare.py"可比性检查"存在的原因。

终态默认配置：blend0.6 + 向量路去近似化 + 映射三批 + 条号直钉提升。
train 0.9368/0.9719/MRR~0.848/零命中 0；holdout 0.8407/0.8815/0.8495。测试 247 全绿。

## 2026-09-13 晨：法条结构化关联 —— 章扩召否决 · 答案层 top_k 破案（5→8）

**量化**（tests/analysis_linkage.py，34 个缺失金条）：与 top5 命中**同章**者 train
11/11、holdout 20/23；正文条号互引 0。法条的"纵横交错"是编/章制度结构，不是
条号直引——缺失金条早就在 30 条候选池里，排不进前 5 而已。

**尝试一章内余弦扩召**（_expand_chapter，KB_CHAPTER_EXPAND 默认关）：top1 定章 →
章内成员向量与查询余弦择优 3 条插池头。train 持平（0.9342 / MRR +0.011），
holdout 回退（0.8296 / 0.8228）。与 ±1 邻接同机制：基线已把多数金条排进前 5 时，
入窗式扩召的挤出伤害 > 救回收益。单测钉住余弦择优/去重/越权防御。

**采纳——答案层消费扩容**：答案 LLM 此前只看到 top5（stream_utils 未传 top_k 吃
默认值），而管线排了 15。同章正确条文排 6~10 位时被答案层白白截掉——多法条
"召回不全"一半是消费配额问题。已改 top_k=8（检索评测自带显式传参不受影响）。
存量即收益：@10 = train 0.9719 / holdout 0.8815。

## 2026-09-13 RAG 在线监测 MVP（④输出侧采集 + ⑤定时诊断循环，端到端跑通）

**架构**（延续「trace 存事实 / ragclosure 出结论」的职责切分）：
- 表：`answer_traces`（输出侧事实：query/answer/程序解析的引用编号/检索返回量快照/
  预留 feedback 列）+ `rag_verdicts`（诊断结论，(source,source_id,code) 唯一键）。
  迁移 d5e6f7a8b9c0。
- 采集：检索出口 `retrieval_trace.record()` 把事实快照进 contextvar → 同请求协程的
  `finalize_answer`（生成/缓存/法条拦截所有路径的唯一收口）spawn 落库 answer_traces。
- 诊断：`app/services/rag_monitor.py` 常驻循环（默认 300s，RAG_MONITOR_* 可配），
  anti-join rag_verdicts 取未处理行（每轮限 500），构造 ragclosure Trace 跑规则子集，
  ON CONFLICT DO NOTHING 幂等落表。
- 规则子集（诚实边界）：trace 只存数字与 chunk_key 未存块文本，**文本类规则
  （RC-2/3/5/7）不判**；当前判 RC-1 召回空 / RC-6 重排跳过 / RC-8 延迟超预算 /
  RC-4b 无依据作答 / RC-4c 引用越界。
- 挂载：lifespan 后台任务（同 maintenance/slowq 模式）+ Dockerfile COPY ragclosure/。
- 单测 7 例（引用解析/快照隔离/行转换/规则判定/jsonb 回归）；全量 255 绿。

**踩坑**：asyncpg 读 jsonb 默认返回 JSON 字符串（未注册 codec），`isinstance(v, list)`
恒假 → 健康行被误判 RC-4b。冒烟逐行对 DB 才抓到。已修 `_as_key_list` 兼容两种形态
+ 回归单测。教训：jsonb 往返必须显式测，不能默认 list 进 list 出。

**端到端冒烟**（一次性容器）：迁移 → cycle1（存量窗口行 0 verdict）→ 造 2 条答案
trace（正常/无依据）→ cycle2 精确产出 1 条 RC-4b（正常行零误报）→ 清理冒烟数据。
真实流量接入待服务重建后自然发生（采集默认开）。

**下一步**（监测平台化，待拍板）：Grafana verdict 面板、低危自动修复白名单、
alertmanager 通知带 evidence/action、LLM 复述层（API 接入大模型解释 verdict）。

## 2026-09-13 监测平台化第一层：verdict 指标告警 + Admin API（status/ask）

- 指标：`rag_verdicts_total`（code×severity，metrics.py）——rag_monitor 落库后同步
  打点，Alertmanager 直接对结论速率告警，无需 PG exporter。alert.rules.yml 新增
  规则 16（high 级结论 >0 即告警：无依据作答/引用幻觉/召回全空）与规则 17（medium
  持续 >5 条 info 级）。
- `GET /api/admin/rag/status`：近 24h verdict 按 code 统计 + 近 20 条明细（evidence/
  action/patch 全量，人工介入窗口）+ 检索侧量级（24h 检索量/空召回/重排降级/p95）
  + 答案侧量级。仅管理员（get_current_user + role 校验，同 admin_errors 模式）。
- `POST /api/admin/rag/ask`：实时监测摘要（≤6000 字 JSON）注入上下文，LLM 回答
  运维提问。冒烟实测正确读出样本 verdict（"高危异常 1 条"）。
- 踩坑两个（冒烟抓到）：① DeepSeek flash 先输出 reasoning_content，max_tokens=800
  被推理吃光 content 为空 → 提到 2000 + 空回复显式 502；② 完整摘要推理在
  HTTP_TIMEOUT_SHORT(5s) 内跑不完 → 改 MEDIUM(15s)。
- 单测 +3（鉴权 403/空问题 422/指标注册），全量 258 绿。冒烟数据已清理。

**MVP 链路现状**：采集（retrieval_traces/answer_traces）→ 诊断（rag_monitor 循环）
→ 存储（rag_verdicts）→ 指标（rag_verdicts_total）→ 告警（规则 16/17）→
人工窗口（/status）→ LLM 问询（/ask）。服务重建镜像后全链自动生效。

## 2026-09-13 监测平台第二层：每日 LLM 巡检报告 + Grafana 面板

- 表 `rag_daily_reports`（report_date 主键，一天一份，迁移 e6f7a8b9c0d1）。
- `rag_monitor_loop` 每轮后调 `ensure_daily_report()`：当天无报告 → 复用 status
  统计注入 LLM 生成晨报（健康/诊断要点/是否需人工介入，300 字内）；失败不落行
  下轮重试，report_date 唯一防重复。冒烟实测 FIRST=1 / SECOND=None（幂等正确），
  报告内容数据准确且主动追问了"答案量 0 是采集未上线还是真无流量"。
- API `GET /api/admin/rag/daily-report`：当天无报告时现场补生成一次。
- Grafana：运行中实例密码已改、无 provisioning，**不碰凭据**；面板 JSON 落
  `monitoring/grafana/rag_monitor_dashboard.json`（5 面板：结论速率/高危累计/
  中危累计/检索量与空召回/延迟分位），用户 UI 导入时选现有 Prometheus 数据源即可。
- 踩坑：import 路径（rag_admin 在 routes 不在 services）、无 id 列的表 RETURNING id。
  全量 258 绿。

## 2026-09-13 监测 MVP 功能漏洞补全轮（自查 5 处）

1. **monitor 停机漏诊**（高）：_fetch_pending 原带时间窗口（间隔×3），停机超过窗口
   期的未诊断 trace 永远漏诊。去掉窗口——anti-join rag_verdicts 已保证幂等、
   LIMIT 500 分批追账（存量 2476 行约 5 轮追完，之后纯增量）。
2. **表无限增长**（高）：answer_traces / rag_verdicts 无清理机制。补
   _daily_housekeeping（answer 7 天=RAG_TRACE_RETENTION_DAYS、verdicts 30 天=
   RAG_VERDICT_RETENTION_DAYS），挂每日报告成功路径（一天一次幂等触发）。
3. **多轮 tool call 检索事实丢失**（高）：同一回答内 LLM 多次调用检索时，
   answer trace 原来只关联最后一轮。set_last_retrieval 改合并语义：keys 去重
   累计（上限 50）、count/latency 累加、rounds 计数、保留首轮 uid 与首问。
4. **指标虚计 + 静默吞错**（中）：ON CONFLICT DO NOTHING 下 metrics 无条件 inc
   可能虚计 → RETURNING 1 判真插入；finalize_answer 的裸 except pass → debug 留痕。
5. **trace 关联缺失**（结构）：加迁移 f7a8b9c0d1e2——retrieval_traces.trace_uid
   （请求级 UUID）、answer_traces.retrieval_uid（contextvar 带回，多轮保留首轮）、
   rag_verdicts.query_snippet（人工窗口直接可读，免跳原表）。冒烟验证
   LINKED=True；单测 4 例补齐（合并语义/首轮 uid/key 名回归/query 摘要）。

**踩坑**：uid key 名不一致（record 传 trace_uid，消费方读 retrieval_uid）→
静默丢关联，冒烟逐行对 DB 抓到。教训：跨模块 dict 契约必须单测钉住 key 名。
全量 262 绿。

## 2026-09-13 监测 MVP 第二轮自查（"再查"）：1 个高价值发现 + 3 处加固

1. **幻觉引用检测结果无统计落点**（高价值，非 bug 而是断链）：chat_fast_paths 的
   find_ungrounded_citations（回答条文号 vs 全部依据文本的**内容级**比对，比
   RC-4c 编号越界强一档）结果只进 logger.warning，"幻觉引用率统计"承诺了但没有
   落点。修复：answer_traces 加 ungrounded_citations 列（迁移 g8b9c0d1e2f3），
   fast path 检测处经 set_ungrounded_citations contextvar 带入 answer trace，
   监测引擎可在线统计幻觉引用率。
2. **trace_uid 生成位置**：原在第一个 try 内，若 answer_trace import 段失败则
   UnboundLocalError 使整条检索 trace 丢失。提前到函数顶部独立生成。
3. **username 截断**：answer_traces.username VARCHAR(64)，超长 username 会炸
   INSERT（静默丢行）。补 [:64]。
4. **生产镜像带 trace 数据**：Dockerfile COPY ragclosure/ 会把 traces/（真实
   query 的 jsonl）与 reports/（诊断报告）带进镜像。.dockerignore 排除两目录。

**复查划掉一个误报**：rerank_used NULL 透传原实现已正确（None 判断在位）。
确认无漏采路径：全部 5 个回答终点（fast path×2 / core×3）都收敛于
finalize_answer。全量 263 绿，迁移已应用，冒烟 UNGROUNDED 落表 + UID 关联双通。

## 2026-09-13 监测 MVP 第三轮自查（多实例/数据污染视角）

1. **每日报告多实例竞态**（高）：4 个 gateway 容器同跑 monitor 循环，
   ensure_daily_report 的"查-生成-落行"无互斥 → 4 份并发 LLM 调用（报告行
   ON CONFLICT 幂等不重复，但成本 ×4）。修：pg_try_advisory_lock 抢占，
   未抢到本轮让位；锁在 finally 释放。
2. **评测流量污染监测统计**（高）：评测/采集/分析脚本走真实 search_knowledge，
   每轮 95 条混进 retrieval_traces（晨报检索量、空召回率、verdict 全被冲刷）。
   修：run_retrieval_eval / run_rerank_sweep / run_embedding_ab /
   analysis_linkage / analysis_neighbor_bucket / collect_rewrite_signals
   统一 RETRIEVAL_TRACE_ENABLED=0（import app 前设置）。
3. **ask 上下文字符截断**（中）：[:6000] 硬截可能截出非法 JSON。改结构化瘦身：
   recent_verdicts 限 10 条、evidence 截前 3 条、丢 patch 全文（LLM 判断不需要）。
4. **已知盲区（文档化，非本轮修）**：① ReAct 路径（chat_react）无内容级幻觉
   检测（fast path 才有）——其 ungrounded_citations 落 NULL；② answer 行 query
   是用户原话、retrieval 行是映射后查询（fast path 传 mapped query）——uid 关联
   不受影响，排查时注意。③ 多实例 verdict 指标已由 RETURNING 判真插入防双计。

ask 端点瘦身上下文复测通过（LLM 正确识别"回答数为 0 且 p95 偏高"并给核查建议）。
全量 263 绿。

## 2026-09-13 监测 MVP 第四轮自查（池化/毒丸视角）+ 第五轮（收敛判定）

4. **每日报告锁残留 → 永久停摆**（高）：会话级 pg_try_advisory_lock 在连接归还池时
   不被 asyncpg reset 清理——实例关停恰逢 unlock 前则锁随连接残留，此后 try_lock
   永远拿不到（除非池重建），报告停摆。改事务级 pg_advisory_xact_lock +
   conn.transaction() 包裹（COMMIT/ROLLBACK 自动释放，取消安全）。
5. **毒丸行饿死队列**（高）：run_cycle 行循环整体一个 try，单行诊断抛错（数据形状
   异常）会跳过同批剩余行；anti-join 只排除已落 verdict 的行，毒丸行永不被排除 →
   ORDER BY id LIMIT 500 每轮从同一批开始，后面的行永久饿死。修：行级 try 隔离，
   失败行落 RC-ERR 标记 verdict（进 anti-join 排除集 + low 级指标留痕），落库失败
   另行 catch 下轮重试。_persist_verdicts 拆出独立函数。

第五轮扫过：连接池嵌套借还（事务内 _fetch_status 借第二连接，池 max=4 无死锁）、
RC-ERR 不触发告警（low 级）、verdict 参数位序、空 verdict 列表、循环异常继续——
无新发现。**判定：功能性漏洞已收敛**；剩余风险需真实流量暴露（LLM 输出漂移、
多实例时钟、长尾数据形状），建议部署观察一周后复盘。

## 2026-09-13 监测 MVP 第六轮自查（部署失败模式 + 调用面收口）

1. **监测三表未入 init_db 关键表清单**（部署级）：重建镜像后若忘跑 alembic，
   服务照常启动（关键表都在），但采集与监测循环静默 warning 空转——监测系统
   自己发生了它要消灭的"静默失效"。修：answer_traces / rag_verdicts /
   rag_daily_reports 补进 init_db 必需表检查（fail loudly 拒绝启动，与 CDC 同款）。
2. **调用面收口确认**：全部 9 个回答终点（fast path ×2 / chat_stream_core ×3 /
   chat_react ×3 / map_api ×1）均收敛 finalize_answer，answer trace 无漏采；
   agent_tasks 仅注释提及检索、不实际调用。map_api 的 travel 回答会进
   answer_traces（retrieval 字段 NULL，RC-4b 不会误判，persona 列可过滤）。
3. alembic 迁移链 d5e6→e6f7→f7a8→g8b9 线性无分叉，upgrade head 依次通过。

第六轮结论：启动失败模式已闭环。全量 263 绿。

## 2026-09-13 第七轮（参数防呆）+ 终止判定

- RAG_MONITOR_INTERVAL_SEC=0 → 无 sleep 死循环打库、BATCH=0 → 永不处理：
  钳到下限 60s / 50。
- 第七轮其余扫描（alembic 并发执行、_persist 失败语义、contextvar 跨请求残留、
  环境开关一致性）无新发现。**终止判定：连续两轮无功能性新发现，自查结束。**
  剩余风险需真实流量暴露，部署观察一周后复盘。

## 2026-09-13 第八轮：逐行审查（主人要求"一行行查"）—— 5 处新发现

前七轮按视角抽查，本轮逐行全文重读四个新模块，抓到：

1. **【高危】缓存命中行 RC-4b 误报**：rag_monitor L97 `or 0` 把
   retrieval_returned_count=NULL（语义=未检索：缓存命中/法条拦截）压成 0 →
   returned=[] → RC-4b 把正常缓存回答判成"无依据作答"。生产开语义缓存后
   高危告警 16 会刷屏。修：diagnose_row 对 NULL 显式跳过依据类规则。
   七轮视角抽查全漏，逐行才抓到——冒烟也没抓到因为没测缓存路径。
2. **【中】ask 端点 NameError**：改结构化瘦身时删了 `import json as _json`，
   改完没复测 → _json 未定义必 500。补 import 并复测通过。
3. **【中】status 端点 evidence/patch/stats 恒为空/原始字符串**：jsonb 字符串
   坑第三处变体（monitor 转换层修了，rag_admin 输出层漏了）→ _as_json 统一。
4. **【中】清理依赖 LLM 存活**：housekeeping 挂在报告成功路径——LLM 欠费则
   过期清理停摆。解耦为 maybe_daily_housekeeping（内存日期标记，独立触发）。
5. **【低】retrieval_traces 清理从未挂载**：purge_expired 定义后零调用方，
   7 天保留只是注释（表已 2476+ 行无限涨）。收敛进 _daily_housekeeping。
   另：record() 内 uid 双重生成死代码清理。

教训沉淀：① jsonb 往返三层（写入/monitor 读取/API 读取）都要测，漏一层废一层；
② 改完必须重跑冒烟（NameError 就是"改完没测"直接产生）；③ 逐行与视角抽查
互补——视角抽查快但漏语义级坑（NULL 双义），逐行慢但兜底。
全量 264 绿（新增缓存命中回归单测）。

## 2026-09-13 第九轮：剩余文件逐行（实验脚本/迁移/主链函数/挂载日志）

- 实验脚本 env 顺序：os import 先于 setdefault ✓（曾担心 NameError）。
- 迁移四件逐行：链 c4d5→d5e6→e6f7→f7a8→g8b9 线性；uq_rag_verdict 唯一键与
  ON CONFLICT 对齐；部分索引（zero_return）谓词正确；UUID 列与 asyncpg 原生
  uuid 对象匹配；downgrade 顺序正确。
- tools.py 三个新函数逐行：_promote_pinned 前缀匹配无"第四十八/第四百八十"类
  包含误伤；_expand_chapter 死变量 chapter 清理（换 heading 空值守卫）；
  _blend_rrf_ce 的 rerank_score 缺省路径经 rerank 窗口保证。
- main.py 监测启动日志硬编码"300s"改引 INTERVAL_SEC 实值。
- 单测 fixture 列名与真实表列逐一核对一致；RC-1/RC-6/RC-8/RC-4b/RC-4c 触发
  条件与规则实现逐条人工复核。
- 并发正确性再确认：verdict 指标 RETURNING 防双计在 4 实例下成立；RC-ERR
  标记行走同一 _persist_verdicts 幂等路径。

第九轮无功能性新发现（3 处小清理 + 2 处确认）。累计九轮、14 处修复。
全量 264 绿。自查终止——剩余风险区间需真实流量。

## 2026-09-13 部署实施：监测 MVP 全量上线（4 实例）

- 镜像重建（compose 无 build 段，历史上就是手工 docker build）→ 验证镜像内容
  （ragclosure 代码进、traces/reports 被 dockerignore 挡住）。
- **部署现场抓到第九轮漏网的启动崩溃**：_initialize_runtime 内部对
  _start_background_tasks 的解包仍是 5 变量（改签名时 grep 了两个 return 却漏了
  调用点解包行）→ gateway crash-loop。修复重建后 4 实例全部 healthy。
  教训：改函数签名要 grep **调用点解包**，不能只 grep return。
- 滚动重启 4 实例全部 healthy；监测循环 4 实例并发启动；第一轮存量追账
  （500 行/轮）+ anti-join 幂等验证（第二轮新 500 行、verdict 恒定无重复）。
- 存量三表数据全是白天的开发/评测产物（评测 trace 混流触发 22 条 RC-8 历史噪音）
  → truncate 三表，生产从部署时刻零起点积累干净数据。
- 当前状态：链路空转待真实流量；晨报明早（UTC 日切）自动生成；
  Grafana 面板 JSON 待主人 UI 导入。

## 2026-09-13 中控台上线（rag_dashboard，127.0.0.1:13150）

- 单文件 `rag_dashboard.py`（FastAPI + 内嵌 HTML，零前端构建），**容器化**入
  compose（rag_dashboard 服务，宿主直连 PG 的 asyncpg SSL 握手时好时坏，
  改走 gateway_net 内网名稳定）。设计为独立观测者：直连 PG 读监测四表 +
  HTTP 探活 4 实例 /health + 直调 DeepSeek 问询——不 import 线上代码。
- 页面：实例探活卡（4 绿点）+ 9 项健康指标卡 + verdict 明细表（severity 着色/
  evidence/action）+ 今日晨报 + LLM 提问框；15s 自动刷新。
- 踩坑三个（冒烟抓到）：① 宿主 .env 路径硬编码在容器内 FileNotFoundError →
  改 env 注入优先、文件 fallback；② 模块级 @postgres:→127.0.0.1 改写无差别
  执行，容器内连自己的 5432 → 改写收敛进宿主 fallback 分支；③ 探活目标同理，
  容器内必须走服务名:10086（127.0.0.1 是容器自己）——统一 INSTANCE_TARGETS
  按 _IN_CONTAINER 选择。④ _slim_status 抽公共函数时顺手修 NameError。
- ask 端点实测通过（LLM 正确指出"检索量 0 与晨报口径不一致"——由此顺手把
  daily_report 快照从问询上下文剔除，避免口径混淆）。
- 访问：http://127.0.0.1:13150 （浏览器打开即用，15s 自动刷新）

## 2026-09-14 阻塞1 修复 + 部署（回应《线上RAG中控台-后续实施细节》）

文档《线上RAG中控台-后续实施细节》阻塞1 指控**属实且比描述更严重**：
_ensure_daily_report_locked 的 INSERT 在 `if not report.strip(): raise` 之后、
同缩进（第四轮 advisory lock 拆函数时改坏，第九轮逐行与部署均漏）。后果双杀：
① 每日报告永不持久化；② 每 300s 循环白烧一次 LLM 调用（生成后丢弃）。

修复：INSERT 归位 + 回归测试 2 例（非空落库/空报告 raise 且不落库，
patch chat_support 与 _fetch_status 双桩）。第八轮的 app 侧修复同步在镜像内。

部署：重建镜像 → 4 实例滚动重启全部 healthy → 存量三表 truncate（清掉评测
混流数据）→ 链路零起点空转待流量。UTC 日切（北京 08:00）后自动出 09-14 晨报。
Grafana 面板 JSON 待主人导入。文档阻塞2（重复扫描）确认属实待 Phase 2 队列化；
阻塞3（中控台无鉴权+meta refresh 冲突）确认属实待修。

## 2026-09-14 全量只读审计：Agent 主链新发现

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-14 | router.py:405 | **P0** | `_emit_simple_result()` 调用异步 `finish_task()` 时曾漏 `await` | 简单任务结果已写入 Redis，但状态永久停在 `generating` | 改为 `await finish_task(...)`，补状态断言回归 | ✅ 已修（上轮改，本轮回归验证 23 passed） |
| 09-14 | react_steps.py:235,284 | **P0** | `record_token_usage` 原先只在断连异常分支导入 | Flash 降级正常结束时 `UnboundLocalError`，回答成功但漏计费/漏指标 | 函数入口统一导入，正常与断连路径共用计费函数 | ✅ 已修（同上） |
| 09-14 | runner.py:280 | P1 | `_build_cache_ctx()` 原先位于 `try/finally` 外 | 上下文构建异常会绕过 `fail_task` 和资源清理，任务停在 `pending` | 移入 `try`，异常统一走 fail/finally | ✅ 已修（回归 test_runner_cache_context_error_fails_and_cleans） |
| 09-14 | react_steps.py:208、runner.py:196 | P1 | 任务路径 Flash 降级只写结果缓冲，不保存记忆/缓存 | 降级回答后追问失忆，缓存无法复用 | 在降级收尾统一保存 memory 和 safe_set | ✅ 已修：`_run_react_loop` 的 max_steps/降级/工具超时三种 break 收尾统一改走 `_finish_text_answer`（终检+记忆+缓存+finish 一处收口） |
| 09-14 | chat_react.py:124 | P1 | SSE ReAct 主模型失败后直接转发 `fallback_chain`，未 `finalize_answer` | 会话历史、日请求计数、RAG answer trace 缺失 | 将降级输出聚合后与正常路径共用 finalize | ✅ 已修：新增 `_fallback_and_finalize`（`accumulate_sse_text` 聚合最终文本 → finalize_answer，write_cache=False 防残缺答案入缓存） |
| 09-14 | chat_react.py:249 | P1 | SSE 圆桌构造 `AgentOrchestrator(ctx.req.query)` 未传 `username` | 圆桌 Phase1 LLM 调用全部漏计费 | 传 `username=ctx.username` | ✅ 已修 |
| 09-14 | runner.py:266 起 | P1 | 任务路径未调用 `begin_request/finish_request`，无请求级 span | `/v2/chat/tasks` 请求无法进入控制台请求级监测 | 在任务入口建立请求 trace，在简单/ReAct/降级收尾统一 finish | ✅ 已修（主人决策 #3A）：run_agent_task 入口 begin_request + mark_route(task/task_simple/task_react)，`_finish_text_answer` 补 answer span，finally 统一 finish_request(status=outcome)，幂等安全 |
| 09-14 | runner.py:335 | P2 | 任务人格提示词直接 `.format()`，不像 SSE 路径使用 `safe_format_prompt` | 配置缺占位符时任务异常，SSE 路径正常 | 复用 `safe_format_prompt` | ✅ 已修 |
| 09-14 | router.py:436 | P1 | 简单任务 LLM 格式化失败时回退原始工具 JSON | 用户可见内部结构，且 JSON 可能被写入语义缓存 | 改为固定用户文案或结构化降级视图 | ✅ 已修：新增 `_fallback_tool_view` 可读降级视图（字段: 值行式 + 错误友好文案，内部 `_` 字段不外泄），不再回退原始 JSON |
| 09-14 | sub_agents.py:128,151,160 | P1 | 上游返回结构缺 `choices` 时抛 `KeyError`，重试捕获列表未包含 | 子 Agent 单次异常可打穿任务 | 捕获 `KeyError/IndexError` 并走统一重试/错误返回 | ✅ 已修：两级 except 元组补 KeyError/IndexError；回归 test_sub_agent_malformed_structure_retries（空 choices 走重试） |
| 09-14 | tools.py:490 | P1 | `_rerank_local` 只保护模型加载，未捕获 `_predict` 推理异常 | 重排运行时异常会打断检索，而不是降级 RRF | `try/except` 包裹 `to_thread(_predict)`，失败返回 `[]` | ✅ 已修：失败记 warning 返回 []，与"模型不可用回退 RRF"同语义 |
| 09-14 | react_steps.py:162、chat_react.py:177 | P2 | 工具超时后取消子任务但未统一 await 回收 | 取消竞态下可能短暂留存任务与告警日志 | 超时分支取消后 `await gather(..., return_exceptions=True)` | 已核（复核结论）：两处均为 `asyncio.wait_for(gather)` 形态——超时时 wait_for 内部 `_cancel_and_wait` 已取消并等待内层 gather 全部子任务完成，无残留；显式 cancel 循环是冗余无害，不加代码 |

### 同日结构拆分（门禁当场拆，不隔夜）

| 项 | 说明 |
|---|---|
| tools.py → kb_expand.py | tools.py 经检索质量五阶段迭代达 765 行（存量违规，本轮门禁暴露）。拆出「扩召子系统」：`_promote_pinned`（条号直钉）、`_article_index`/`_expand_neighbors`（±N 邻接，默认关）、`_cosine`/`_expand_chapter`（章内余弦，默认关）、`_apply_neighbor_guard`（保护带）→ tools.py 回落 600 行内；旧名 re-export，调用点零改动，test_search_pipeline 的 patch 目标同步指向 kb_expand（web_search/project_kb 先例）。拆分顺带修复 schema.py 暴露的既有别名冲突：`RecallLeg.from_dict` 的块列表别名混入 `"hits"`，`{"hits": 3}` 输入直接 TypeError（hits 与计数语义撞车，已从列表别名移除） |
| router.py → simple_task.py | router.py 加降级视图后 622 行。拆出「格式化/收尾原语」：`_build_formatter_messages`/`_format_via_llm`/`_load_file_ctx`/`_safe_history`/`_emit_simple_result`/`_fallback_tool_view`/`_save_simple_task_artifacts` → 旧名 re-export，`handle_simple_task` 与历史测试零改动 |
| runner.run_agent_task 83→80 内 | 拆出 `_enter_cache_gate`（缓存门 + 续期任务组装）、`_prepare_task_run`（取密钥/建消息/落用户消息，密钥缺失内聚 fail_task+退额度）、`_fail_task_sanitized`（剥指纹 fail + 未产出退额度） |
| chat_react._stream_react 86→80 内 | 降级收尾抽 `_fallback_and_finalize` |

回归：tests/unit/test_20260914_audit_fixes.py（新增 14 例：幂等超时 409/额度三态/锁回收/spawn 排空/未产出退款/RecallLeg 异常归一/探针腿故障/监测腿重建/回填判定/RC-4d/降级聚合/降级视图/子Agent重试/claim 冲突）；全量 pytest 295 passed；规范门禁无违规。

## 2026-09-15 修复实施结果

- 简单任务失败按 Redis 真实终态回传 outcome，未产出时退还试用额度。
- 任务 fallback、max_steps 和敏感词拦截结果不再写语义缓存。
- 工具结果进入模型前统一递归消毒并加不可信数据边界。
- 任务流和降级流统一跨块敏感词守卫。
- 独立复核补修：主模型断流后的降级内容重置结果缓冲，取消任务按真实 Redis 终态
  回传 outcome，任务侧写入 answer trace，简单任务工具结果改用不可信边界包装。
- 验证：后端全量 `348 passed`；前端 `38 passed`；生产构建通过。
## 2026-09-16 重排层拆分（文件超限整改）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-16 | `tools.py` | P3 | 742 行，超过 600 行上限 | 重排层拆到新增 `kb_rerank.py`（228 行）：`_get_reranker`、`_rerank_local`、`_blend_rrf_ce`、`_select_rerank_candidates`、`_is_low_confidence`、`_clean_rewrite`、`_rewrite_query_for_recall`，以及 `RERANK_MODEL`/`RERANK_QUANT`/`REWRITE_TRIGGER_*` 与 `_RERANKER` 状态；tools.py 保留原地 re-export，`tools.*` 调用路径不变 |

拆分依据：静态分析确认该块只依赖上述常量与 `logger`，不引用召回侧
`_recall_pg_*`/`_keyword_fill`，因此 `tests/unit/test_search_pipeline.py` 对
`tools_mod` 上召回函数的 monkeypatch 仍然生效。`tools.py` 742 → 540 行。

验证：`test_search_pipeline.py`、`test_retrieval_grounding.py`、
`test_20260907_batch_fixes.py`、`test_20260912_deep_sweep.py`、
`test_20260911_rule_review.py` 共 `54 passed`。

遗留：`_search_two_legs` 130 行，超过函数 80 行上限。该函数是召回主链路，
含并发、诊断埋点与降级分支，拆分需单独立项，本轮未动。

## 2026-09-16 超长函数拆分（函数 80 行上限整改）

上一条遗留已处理；同轮扫出 `router.py::handle_simple_task` 也超限。

| 日期 | 位置 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-16 | `tools.py::_search_two_legs` | P3 | 130 行，超过函数 80 行上限 | 拆出 `_recall_and_fuse`（70 行，双路召回 + 低置信改写重试 + RRF 融合 + 埋点）与 `_expand_and_guard`（22 行，邻接/章级扩召 + 固定条保护）；`_search_two_legs` 降到 59 行 |
| 09-16 | `router.py::handle_simple_task` | P3 | 85 行，超过函数 80 行上限 | 拆出 `_build_simple_answer`（民法典确定性通道 / 通用单次格式化 + 缺 key 收口）；`handle_simple_task` 降到 73 行，签名不变（测试断言 `user_perms` 默认值为 `()`） |

### 同轮抓到的真实回归（重要）

重排层拆分时 `kb_rerank.py` 漏了 `import asyncio`，`_rerank_local` 里的
`await asyncio.to_thread(_predict)` 抛 `NameError`，被该函数既有的
`except Exception` 降级分支吞掉，**单测全绿但线上精排静默失效**。

- 发现路径：拆分后对容器内真实调用 `search_knowledge`，
  `method` 由 `trgm+vector+rrf+local_rerank` 退化为 `trgm+vector+rrf`。
- 修复：补 `import asyncio`；复跑后 `method` 恢复
  `trgm+vector+rrf+local_rerank`。
- 全仓 `python -m pyflakes app scripts` 未定义名扫描归零。
- 补回归测试 `test_rerank_local_wiring_survives_module_split`：走真实
  `asyncio.to_thread` 路径，漏导入即 `out == []` 断言失败。已验证摘掉
  `asyncio` 时该测试会失败（返回 `[]`），加回后返回 `['b', 'a']`。

教训：模块拆分后不能只跑单测——降级分支会把缺导入转成静默质量损失。凡改动
含 `except` 兜底的路径，必须补一次真实调用验证。

验证：`check_code_rules.py app mcp_assets scripts` 通过；后端单测
`378 passed`（失败 3 项为聊天链路既有夹具问题，与本次无关）；容器内真实
`search_knowledge("合同解除的条件")` 返回 5 条、method 含 `+local_rerank`；
`_probe_health({"target":"gateway"})` 返回 200/passed。
