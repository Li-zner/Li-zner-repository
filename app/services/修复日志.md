# 修复日志 — app/services

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | agent_tasks.py | P1 | 任务端点越权（配合 routes/v2 修复）：新增 ensure_task_access；create_task 传 owner | owner 贯穿创建/恢复 |
| 09-05 | chat_react.py | P1 | answer_chunk 先实时发出后做 DFA 检查，敏感内容已到前端 | 逐块 check_stream 命中即止；拦截分支跳过工具、落盘安全文本、不写缓存 |
| 09-05 | chat_fast_paths.py | P1 | 简单/多Agent 两条快速通道同款"先流式后过滤" | 两条循环改逐块过滤 |
| 09-05 | chat_fast_paths.py | P2 | today_cn 迁移时模块级导入缺失（NameError 风险） | 补顶部导入 |
| 09-05 | chat_stream_ctx.py | P1 | 请求路径 pm.switch 改全局人格，并发串号 | get_effective 按请求解析 |

| 09-05 | chat_stream_core.py | P2 | rebuild_lock_gate 重读缓存直发无 DFA 检查（与 serve_from_cache 不一致，旧毒缓存可漏出） | 重读路径补 contains_sensitive |

回归测试：tests/unit/test_core_p1_fixes.py、test_agent_logic_fixes.py

## 2026-09-05 审查轮：待修复清单（11 个文件全量通读）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-05 | chat_react.py | **P1** | `_execute_react_tools` 两处 `json.loads(tc["function"]["arguments"])` 无防护（139-144 行）：LLM 偶发非法 JSON 参数 → 异常打穿 async generator → 整条 SSE 流无 answer_complete 直接断流。runner 任务路径同类问题已修（`_tool_arg_error` 占位走降级链），SSE 路径漏掉 | 仿 runner：解析失败构造 `{"error": f"{func_name} 参数不是有效 JSON"}` 占位结果进 `_build_tool_messages` 降级链，事件名 args 处同防护 | ✅ 已修 09-05（占位 error 走 _build_tool_messages 降级链，回归测试 test_review_fixes） |
| 09-05 | chat_fallback.py | P3 | `fallback_chain` 异常分支把 `yielded` 重置为 False：Flash 中途失败时已发出的部分内容后会再补一条 answer_complete 兜底文案（内容叠加/截断感）。runner 版 `_fallback_chain` 不重置 | 异常分支保留 yielded 真值，仅零产出才发兜底终结事件 | ✅ 已修 09-05 |
| 09-05 | chat_stream_core.py | P3 | `rebuild_lock_gate` 锁等待仅 0.3s 单次重读，与任务路径（15s/30 次）不对称——行为各自闭环，登记口径差异 | 可选：统一为任务路径同款等待循环 | 可不修 |
| 09-05 | chat_stream_ctx.py | P3 | `ensure_chat_allowed` 日配额切日用本地 `datetime.now()`，而 `update_daily_usage` 的 TTL 对齐用 UTC `time.time()`——容器时区非 UTC 时切日口径轻微错位（60s 缓冲已兜底大部分） | 切日统一 UTC | ✅ 已修 09-05 |
| 09-05 | reasoning_guard.py | P3 | "不要提"/"工具超时" 等宽泛指纹会整段抑制正常思考（产品取舍，非 bug） | 登记；若误杀率高再收窄指纹 | 可不修 |

本轮做得好的：chat_generate 门禁顺序与 finally 清理完备（ctx 未生成也释放并发槽位）；快速通道逐块 DFA 与任务路径同标准；reasoning_guard 预编译正则；chat_support Key 轮询剔除+全失效重置。

> 实施说明（09-05）：chat_fallback 修复在异常且已有产出时补发 [DONE] 终结事件（防前端悬挂），零产出仍走原兜底文案路径。

## 2026-09-05 第二轮：qwen 主力切换配套——思考泄露拦截

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | reasoning_guard.py | P1 | 实测泄露：模型复述输出约束（"不要使用 emoji"/"使用中文（简体）"/文件模板规则）与元结构词（意图识别/身份设定/约束检查）直通前端；逐 chunk 检测可被流式切块绕过；无 key 形状检测 | 补约束复述/元结构/供应商指纹词；新增 ReasoningStreamGuard（24 字符尾部重叠拼接跨块检测 + 强指纹命中整流粘性抑制）；sk- Key 形状正则（负向环视，\b 在中文后失效） |
| 09-05 | llm_streaming.py | P1 | stream_llm_throttled 是 reasoning 展示唯一出口但裸转发——快速通道（简单/多Agent）整段无过滤 | 接入 ReasoningStreamGuard（react 路径原有 sanitize_reasoning 保留，双重生效） |
| 09-05 | chat_fast_paths.py | P2 | tool_result 事件把工具异常 str(E) 原样发前端（httpx 异常带含 key 的完整 URL） | 统一过 sanitize_error_text |
| 09-05 | reasoning_guard.py | P3 | ponytail: 跨块检测靠尾部重叠，可捕获 ≤(重叠+1) 字符指纹的两块切分；3 块以上极端长指纹理论可绕过 | 已注释升级路径（整段缓冲按行送检） |

回归：tests/test_reasoning_guard.py（真实泄露原文回归 + 跨块切块 + key 形状 + persona 豁免）

## 2026-09-06：agent 流程复审（快速通道/流式编排清理）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-06 | chat_react.py | P1 | 工具参数非法 JSON 被 continue 跳过不进 parsed：tool_results 与 tool_calls 按下标错位（坏调用拿别人的结果）+ 末尾 tool_call 缺 tool 消息（下轮 LLM 请求 400 降级 Flash）；配套回归测试是摆设断言未拦住 | 坏参数占位进 parsed 保持下标对齐；测试重写为坏+好混合断言等长与占位 |
| 09-06 | chat_react.py | P2 | try/except 包 create_task 只能捕同步错误，后台扣费 Task 异常无人认领（RuntimeWarning），"仅记 warning"注释名不副实 | done_callback 统一记日志 |
| 09-06 | chat_stream_core.py | P2 | civil reject 路径不 finalize_answer：拒答问答不进对话历史、用量不累计（law_mapping_gate 同类拦截有收尾） | 补 finalize_answer（同一标准） |
| 09-06 | chat_stream_core.py | P2 | civil 流程 route_query 每请求跑两遍（civil_route_gate + law_mapping_gate 各一次） | law_mapping_gate 复用 ctx.civil_route_match |
| 09-06 | chat_fast_paths.py | P2 | 简单/推荐快速通道 isinstance(tool_result, Exception) 死分支（dispatch_tool 只返回 dict 或抛异常）；天气分支 `city=""; if not city:` 恒真残留 + 函数内重复导入 | 删除清理，CITIES 提升到模块级导入 |
| 09-06 | chat_stream_ctx.py | P2 | finalize_answer 裸 create_task(SemanticCache.set)，后台写入异常无人认领 | 切 core.semantic_cache.safe_set |
| 09-06 | chat_support.py | P2 | post_chat_completion 三处 mark_key_result 漏 await——协程被丢弃，Key 健康计数静默失效（map_api 路径） | 补 await |
| 09-06 | llm_streaming.py | P2 | ensure_answer 的 write_cache 死参数（无调用方传入） | 删除 |

回归：tests/unit/test_review_fixes.py 4 passed（含重写后的对齐用例）

## 2026-09-06：引用溯源（检索依据透出）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-06 | chat_fast_paths.py | P2 | 民法典回答引用法条全靠 LLM 自觉，检索依据不进 prompt 约束也不校验，幻觉引用无法发现 | search_knowledge 有数据时注入「引用溯源要求」（条文号必须来自检索结果+末尾输出依据清单）；新增 find_ungrounded_citations 校验（log-only 记 warning，不阻断回答，供幻觉引用率统计） |

## 2026-09-07 审查批次修复（第 3 批，配合 app/修复日志.md 实施记录）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | agent_tasks.py | P1 | 日 token 计数器全仓无写入方，daily_token 限额永不触发 | record_token_usage（唯一 usage 计量点，llm_streaming）顺带 update_daily_usage(inc_token)，UTC 日切 |
| 09-07 | agent_tasks.py | P2 | 任务路径无按用户 QPS/日配额；resume 不复查试用额度；lang 未持久化 | 新增 _task_quota_gate（QPS+日 req/token）create/resume 都过；lang 落任务 hash 并透传 |
| 09-07 | agent_tasks.py | P2 | cancel 终态集合漏 "timeout" | 补 timeout |
| 09-07 | agent_tasks.py | P2 | resume user_perms 可变默认参 [] | 改不可变 ()（None 在 search_knowledge 语义是"不过滤"，会 fail-open，故不按字面采纳） |
| 09-07 | chat_stream_ctx.py | P2 | today 本地时间与 UTC TTL 口径错位；conv_id 秒级时间戳同秒相撞；finalize 两处裸 create_task | UTC 日期；uuid 后缀；换 spawn |
| 09-07 | chat_fallback.py | P2 | Flash 降级无全文流尾终检，跨块拼接敏感词可漏出 | 累积全文，流尾 contains_sensitive 命中以安全文案收口 |
| 09-07 | chat_react.py | P2 | 工具参数兜底提取用 req.query，与 chat_fast_paths 的 user_query 语义不一致 | 统一 user_query（旧测试桩同步补 user_query 属性） |

回归：tests/unit/test_20260907_batch_fixes.py（第 3 批 4 用例：三参签名/配额门禁/token 接线/timeout 终态）

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | llm_streaming.py:108 | P2 | 裸 asyncio.create_task(deduct_token_cost(...)) 未迁移 spawn——资金路径（扣费流水），GC 中途回收=漏扣；本轮 spawn 迁移的同型漏网，涉及钱建议优先 | 换 spawn | 待修复 |
| 09-07 | agent_tasks.py | P2 | _task_quota_gate 的 inc_request=1 发生在幂等复用判定之前：10 秒内重复提交即使命中幂等复用也重复计日请求数（主链路在 finalize 后计数，两路径口径不一） | 计数移到幂等 miss 之后 | 待修复 |
| 09-07 | chat_stream_ctx.py | P3 | 文件尾无换行符（既有） | 补换行 | 待修复（低优先） |

复审通过项：daily token 计数接线核实（update_daily_usage 签名 inc_request/inc_token 匹配；UTC 日切与门禁读取同口径；回归测试断言 150 真实累加）；_task_quota_gate 门序正确（配额门在 _try_reserve_task_slot 之前，失败不占后台槽位）；cancel timeout 终态（终态集合与其余状态枚举核对）；chat_fallback [DONE] 改 break 后流尾终检必达（_full_text 空安全；sf 在循环前已定义，无 NameError）；chat_react ctx.user_query 字段存在（chat_stream_ctx.py:62）、旧测试桩同步补齐；conv_id/UTC/spawn 与其他模块三处对齐。

## 2026-09-09 全目录审查轮（10 域并行 + 主代理复核；计量域由支付域代理交叉验证）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | agent_tasks.py:90 注释 + agents/runner.py:215-223 | **P0** | Agent 后台任务路径（/v2/chat/tasks 全量流量）**完全不接计量扣费入口**：runner 每轮只打 v2_task Prometheus 指标，从不 deduct_token_cost 也不计日 token；grep 全仓 deduct/record 调用点仅 SSE 聊天两处；且 agent_tasks.py:90 注释"LLM 消耗由 record_token_usage 统一累计"为假——任务用户零扣费 + daily_token 限额对任务永不生效（"旁路漏扣费一个月"历史教训同类再现） | llm_streaming 抽 bill_token_usage（日 token+扣费、不含指标防双计、走 spawn），runner 计量块调用 | ✅ 已修 09-09 |
| 09-09 | chat_fallback.py:87-89 + :103-108 | **P1** | fallback_flash 中途失败异常在本层被吞 → fallback_chain 的 [DONE] 补发分支成死代码：Flash 已产出部分内容后中途异常，流以**无终结事件**结束，前端悬挂（违反 answer_complete+[DONE] 必发） | flash 层跟踪已产出标记：中途失败已产出则补 [DONE]，未产出静默返回交 chain 空流兜底；103 裸 except 补日志 | ✅ 已修 09-09 |
| 09-09 | agent_tasks.py:226（resume） | **P1** | resume 缺 402 试用额度门禁（create 有 ：128）：09-07 修复日志称"resume 不复查试用额度已修"，实际 _task_quota_gate 只补了 QPS/日配额——quota_limited 用户持一个已取消任务即可无限 resume 烧 LLM，绕过试用上限 | resume 路径（routes/v2.py resume_task）补 is_quota_exhausted 402 检查 | ✅ 已修 09-09 |
| 09-09 | chat_react.py:258 + llm_streaming.py:131-132 | **P1** | 客户端断连时扣费不执行：计量点全在流正常结束之后，断连 → generator 被关闭 → record_token_usage 不跑——上游 token 已耗但零扣费、daily_token 限额可被"断连重连"刷过 | 修复条件：断连路径按已收字节估算补扣，或 usage 块到达即计 + 保底上游排空；涉流式核心改造，本轮登记设计后实施 | ✅ 已修 09-09：主链（fast-path/ReAct）断连时上游流未关闭，spawn 排空任务接管消费按真实 usage 补计费；两条 Flash 降级直连 httpx 无法排空，退化按已产出文本估算（2字符≈1token）；3 个回归用例（tests/unit/test_20260909_batch_fixes.py），186 用例全绿 |
| 09-09 | chat_fallback.py:37-86 | **P1** | fallback_flash 无任何计量扣费：不读 usage、不扣钱包、连 token 指标都没有——主模型失败后用户拿到的完整 Flash 回答全部免费（chat_react:98 / chat_stream_core:250 兜底必经） | 请求加 stream_options include_usage，流尾走 record_token_usage | ✅ 已修 09-09 |
| 09-09 | agents/sub_agents.py:127-136、agents/orchestrator.py:315-324、agents/tools.py:351-354、agents/memory.py:82-95 | **P1** | 工具内部/圆桌/检索改写/记忆压缩的 LLM 调用全部只进 Prometheus、不扣钱包不计日 token（均由用户请求触发） | 修复条件：统一收口 chat_support.post_chat_completion 内部计量（需线程化 username，涉及 6 调用方签名）；或按业务显式登记"内部调用不扣费"豁免——待主人拍板口径后实施 | ✅ 已修 09-09（主人拍板计入计费：子Agent/圆桌/改写/压缩/分类/任务降级六处收口，见根日志拍板执行记录） |
| 09-09 | chat_fallback.py:103 | P2 | 裸 `except Exception:` 静默吞异常，无日志无 noqa（随 P1 [DONE] 修复一并处理） | 补 logger.warning | ✅ 已修 09-09（随 [DONE] 修复一并） |
| 09-09 | chat_react.py:276-279 + safety_filter.py:223 | P2 | 跨块拼接敏感词只剩"发后终检"窄口：check_stream 逐块独立检测无跨块 carry，纯文本路径终检命中后先前已发出的块无法撤回（历史"先流式后过滤"事故同型残留） | stream 层维护块尾 overlap 缓冲（同 ReasoningStreamGuard 方案）后送检再发 | 待修复 |
| 09-09 | chat_react.py:152-155、chat_fast_paths.py:181,236-247 | P2 | tool_result 事件未过 DFA 直发前端：web_search 等第三方内容/错误串绕过安全过滤（answer 路径同块均先滤） | tool_result 对外文本字段过 contains_sensitive/sanitize_error_text | 待修复 |
| 09-09 | llm_streaming.py:135-140 | P2 | answer_via_models 单模型中途失败换下一个：已流出部分回答与下一模型回答拼接重复 | 仅"未产出任何 answer 块"的失败才换模型，已出块转 fallback_chain | 待修复 |
| 09-09 | agent_tasks.py:205-209 | P2 | cancel TOCTOU：终态检查与 update_status 之间任务可刚完成，completed 被无条件覆盖为 cancelled（已扣费任务被标 cancelled 且可再 resume，重复消耗） | update_status 改条件写（仅 pending/generating 可迁移） | 待修复 |
| 09-09 | agent_tasks.py:213-257 | P2 | resume 无幂等占位（create 有 claim_idempotency）：连点并发建多个新任务重复跑 LLM | 复用 claim_idempotency（键加 resume 后缀） | 待修复 |
| 09-09 | llm_streaming.py:89-97 | P2 | 计量指标 model 标签硬编码 DEEPSEEK_MODEL：flash/qwen 降级实际发生的 token 全记主模型名下，监控口径失真 | 透传实际 model_try 作标签 | 待修复 |

**存量核验**：llm_streaming:108 裸 create_task(deduct) —— 本轮随 bill_token_usage 重构**改走 spawn**（✅ 本轮修复）；agent_tasks 计数先于幂等复用判定 **仍在**（:131 先于 ：135，幂等命中也重复计日请求）；SSE 事件序 13 条终止路径其余全部核实齐全；坏 JSON 占位不错位 ✅；门禁顺序/槽位锁释放 ✅。

## 2026-09-10 修复：快速通道历史丢失

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | chat_fast_paths.py | P0 | 简单聊天/推荐快速通道调用模型时只传 system + 当前问题，已构建的会话历史未进入模型上下文，同窗口追问失忆 | 新增 `_messages_for_fast_path`，保留 `ctx.messages` 中的文件上下文和历史，再追加当前问题 | 已修 |
| 09-10 | chat_stream_core.py | P0 | 有上下文的追问仍可能被语义缓存跨轮/跨窗口串答 | 有历史时跳过缓存与重建锁；首轮无历史时才允许全局缓存 | 已修 |

## 2026-09-10 第二批修复：Agent 任务生命周期

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | agent_tasks.py | P0 | 简单酒店/路线/美食任务丢 username，子 Agent token 不计量 | call_sub_agent 透传 username，并补回归测试 | 已修 |
| 09-10 | agent_tasks.py、router.py、runner.py、react_steps.py | P0 | 取消后简单任务仍继续执行并覆盖 completed；取消时 usage 不结算 | 工具/格式化加超时与取消检查；终态 CAS；取消 usage 先结算再收尾 | 已修 |
| 09-10 | agent_tasks.py | P1 | 配额在幂等判断前计数；resume 无幂等；调度失败双重释放槽位 | 幂等命中后计数；resume 加幂等占位；槽位释放收敛到单一异常路径 | 已修 |

## 2026-09-10 第三批修复：系统提示词边界与 LLM 熔断

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | chat_stream_ctx.py、runner.py | P1 | 系统提示词拼接用户原始问题，用户可控文本获得系统指令权限；存在提示词注入提升风险 | 系统提示词不再包含用户原话，用户输入作为普通消息数据传递；任务路径同步收敛 | 已修 |
| 09-10 | chat_support.py | P1 | 非流式 LLM 调用未经过熔断器，上游持续故障时仍反复打满超时 | post_chat_completion 接入按模型隔离的 get_breaker，与流式出口统一保护 | 已修 |


## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | llm_streaming.py | P2 | 计量指标 model 标签硬编码 DEEPSEEK_MODEL：flash/qwen 降级实际 token 全记主模型名下（09-09 登记） | record_token_usage/_spawn_drain_bill 增加 model 参数并由 answer_via_models 透传实际 model_try，缺省回落主模型 | 已修 |
| 09-10 | llm_streaming.py | P2 | answer_via_models 单模型中途失败换下一个，已流出回答与下一模型回答拼接重复（09-09 登记） | 已出块即失败收尾不换模型；仅"零 answer 块"的失败才降级下一模型 | 已修 |
| 09-10 | llm_streaming.py | P2 | 裸 create_task(deduct_token_cost) 资金路径未走 spawn（09-07 登记） | 复核确认已随 09-09 修复批改 spawn + done_callback | 已修（核实） |
| 09-10 | agent_tasks.py | P2 | _task_quota_gate 计数在幂等判定前，重复提交重复计日请求数（09-07 登记） | 复核确认已随 09-10 修复批调整顺序（先幂等复用后计数） | 已修（核实） |
| 09-10 | chat_react.py + chat_fast_paths.py | P2 | tool_result 事件未过 DFA 直发前端：web_search 等第三方内容/错误串绕过安全过滤（09-09 登记） | chat_support 新增 display_safe_tool_result（深拷贝 + sanitize_error_text + DFA 命中替换），四处 tool_result 事件消毒；喂 LLM 的原对象不受影响 | 已修 |
| 09-10 | chat_react.py + chat_fast_paths.py + safety_filter.py | P2 | 跨块拼接敏感词只剩"发后终检"窄口：check_stream 逐块独立无跨块 carry（09-09 登记，"先流式后过滤"事故同型残留） | safety_filter 新增 ContentStreamGuard（块尾保留最长词-1 字符缓冲拼下块再送检，流尾 flush 补检，含 hold=0 切片陷阱处理），_stream_react/simple/recommend 三条流式路径接线；收尾终检保留兜底 | 已修 |
| 09-10 | chat_stream_ctx.py | P3 | 文件尾无换行符（09-07 登记） | 复核确认本工作区该文件已带 EOF newline | 已修（核实） |


## 2026-09-10 收尾深检（主人指示逐文件复查）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | chat_fallback.py | **P1** | `_flash_sse` 计费块误位于流尾终检拦截分支的 `return` 之后——不可达死代码。后果：Flash 降级链**正常路径交付完整回答却零扣费**（09-09"降级链补计量"修复实际未生效），拦截分支同样漏计 | 计费提前到终检/收口之前，正常与拦截两路径均按真实 usage 计费 | 已修 |
| 09-10 | chat_react.py | P2 | tool_result SSE 事件直发前端未消毒（本轮漏网的第 4 处；此前只在 chat_fast_paths 修了两处） | 补 display_safe_tool_result；frame 内原对象仍供 LLM 上下文 | 已修 |


## 2026-09-11 全量规则审查轮（本轮最重要发现）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | chat_react.py + chat_stream_ctx.py | **P0** | ReAct 主路径丢失当前用户问题：`_assemble_messages` 组装 [system+文件+历史+提示] 后从未追加 user 消息（旧版 v2.py@04d25a02:946 有 `messages.append(user_query)`，纯移动重构丢失）。凡走 react_loop 的请求（civil 人格/多域意图/fast path 降级），LLM 在没有当前问题的上下文上作答。此前未暴露因主流量走 fast path（自行追加）且旧单测断言的恰是缺失状态 | react_loop 入口处追加（每请求恰一次，fast path 不经此函数互不重复）；旧断言按正确语义更新；新增 react_loop 用户消息回归 | 已修 |
| 09-11 | chat_fast_paths.py:242,342 | P1 | DFA 拦截后 finalize_answer 默认 write_cache=True——"安全前缀+安全文案"钉进语义缓存（ReAct 同场景已用 write_cache=False） | 两处改 `write_cache=not _stream_interrupted` | 已修 |
| 09-11 | chat_fallback.py | P1 | DFA 拦截分支 return 早于计费块——拦截路径零扣费；且拦截发生在流中途时 usage 尾块未到，纯 `if _usage` 仍不可达 | 拦截分支：有 usage 用真实值，否则按已产出文本估算（与断连口径一致）；正常路径真实 usage 计费不变 | 已修 |
| 09-11 | chat_fallback.py | P2 | Flash 正常结束只发 [DONE] 无 answer_complete（拦截分支有），前端语义不对称 | 登记：待核实前端是否以 [DONE] 兜底后统一 | 登记 |
| 09-11 | llm_streaming/chat_fallback/map_api | P2 | record_token_usage 四处未传 model=（flash/降级应答记主模型名下）；endpoint 恒标 v2_chat | 登记：参数化 endpoint 并逐调用方补 model | 登记 |
| 09-11 | chat_stream_core.py:150,216 | P2 | 重建锁/并发租约续期任务裸 create_task（存 ctx 且 finally cancel，GC 风险低，但违反全仓 spawn 约定） | 登记：改 spawn | 登记 |
| 09-11 | agent_tasks.py:255-261 | P2 | resume 先 launch 后 save_idempotent：save 抛异常时 except 再释放槽位 → 同一任务双释放，超限放行 | 登记：save_idempotent 挪到 launch 之前 | 登记 |
| 09-11 | chat_stream_core.py:260-267 | P2 | answer_complete+[DONE] 后 finalize_answer 抛异常会经 except 再进 fallback_chain，在 [DONE] 后叠加第二段回答（需 save/扣费故障注入才触发） | 登记：except 前检查 ctx.saved_normally | 登记 |


## 2026-09-12 凌晨清欠轮（09-11 登记 P2 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | llm_streaming.py + chat_fallback/map_api/react_steps | P2 | record_token_usage 缺 model=/endpoint 参数，降级与地图应答记主模型名下（09-11 登记） | 加 endpoint 参数（默认 v2_chat）；chat_fallback 两处补 model=FLASH、react_steps fallback 同、map_api 传 model=data['model'] + endpoint='map_recommend' | 已修 |
| 09-12 | chat_fallback.py | P2 | Flash 正常结束只发 [DONE] 无 answer_complete（09-11 登记 C-F10；已核实前端有空内容兜底不悬挂，属语义对齐） | 正常结束补 `answer_complete(_full_text)` | 已修 |
| 09-12 | chat_stream_core.py | P2 | 重建锁/并发租约续期任务裸 create_task（09-11 登记） | 两处改 spawn | 已修 |
| 09-12 | chat_stream_core.py | P2 | answer_complete+[DONE] 后 finalize_answer 抛异常会叠加第二段回答（09-11 登记 C-F11） | except 分支检查 ctx.saved_normally，已送达则跳过降级 | 已修 |


## 2026-09-12 深检第二轮补充

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | chat_react.py `_stream_react` | **P1** | DFA 拦截 break 遗弃 `stream_llm_throttled` 生成器——usage 尾块未消费即弃，被拦截响应零扣费（fast path 无此问题：其断连排空挂在 GeneratorExit 传播链上，而 break 是正常退出不经过） | blocked 分支调 `_drain_bill_blocked`（排空剩余流拿真实 usage 补计费，正常结束不双计），拆为模块级助手满足 80 行门禁 | 已修 |
| 09-12 | app/core/sms.py | P2 | `send_admin_notification` 零调用方 stub 且假成功（永远 return True 从不发送） | 已删除 | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | chat_fallback.py:98 | P0 | 逐块调用无状态 check_stream 后立即发送，跨块敏感词在流尾检查前已到前端；实测“诈”“骗”分块均放行 | 改写为 ContentStreamGuard，确认词边界安全后再发送并在流尾 flush | 待修复 |
| 09-11 | chat_stream_core.py:93 | P1 | 真实语义缓存命中只发送并置 finished，不调用 finalize_answer，历史与日请求计数缺失 | 缓存命中复用正常收尾入口，或明确补齐记忆和计量 | 待修复 |
| 09-11 | chat_fast_paths.py:240、chat_stream_core.py:262 | P1 | 先发送 answer_complete 和 [DONE]，再执行 finalize_answer；收尾异常时主编排会在终止事件后继续输出 | 收尾状态先确定，任何终止事件后禁止再次产出 | 待修复 |
| 09-11 | chat_fallback.py:142 | P1 | 上游返回空内容时仍发送 answer_complete 和 [DONE]，上层把已产出误判为成功并跳过兜底文案 | 仅非空全文才标记成功，空结果交降级链处理 | 待修复 |
| 09-11 | chat_react.py:199 | P1 | 流式 ReAct 工具结果未经统一消毒即回填模型，外部网页可提示注入 | 所有第三方工具结果按不可信数据包装消毒后再入上下文 | 待修复 |
| 09-11 | agent_tasks.py:122、131 | P1 | 幂等键未包含 persona_id/file_ids/lang；日请求和试用额度先扣，后台槽位满时额度不回滚 | 幂等键纳入全部执行参数；先占槽位再扣额度或提供补偿 | 待修复 |
| 09-11 | file_upload.py:90 | P2 | 解析失败把原始异常作为 text_content 返回；元数据 json.loads 无异常保护 | 用户侧固定文案，内部细节只记日志；元数据做结构校验 | 登记 |


## 2026-09-12 外部复核修复轮（P0×1 + P1×4 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | chat_fallback.py:98 | **P0** | 逐块无状态 check_stream 后立即发送，跨块敏感词已到前端（实测"诈""骗"分块均放行） | 降级链改 ContentStreamGuard（行缓冲+尾 flush），与主路径同款 | 已修 |
| 09-12 | chat_stream_core.py:93 | P1 | 真实缓存命中不调 finalize_answer：历史缺失、日请求计数缺失 | 命中后 finalize_answer(write_cache=False) 收口 | 已修 |
| 09-12 | chat_fast_paths.py:240、chat_stream_core.py:262 | P1 | 先发终止事件再 finalize，收尾异常时 [DONE] 后继续输出 | saved_normally 后跳过降级；fast path 拦截产物 write_cache=False | 已修 |
| 09-12 | chat_fallback.py:142 | P1 | 空全文仍发 answer_complete 成功事件，上层跳过兜底文案 | 空全文不发成功事件，交 fallback_chain 兜底 | 已修 |
| 09-12 | chat_react.py:199 | P1 | 工具结果回填模型未经消毒，外部网页可注入 | _build_tool_messages 经 _sanitize_context | 已修 |
| 09-12 | agent_tasks.py:122,131 | P1 | 幂等键缺 persona/file_ids/lang；额度先扣后槽位满不回滚 | 幂等键纳入全部执行参数；槽位满回滚 used_requests（quota.rollback_used_questions） | 已修 |


## 2026-09-12 外部复核修复轮（P0×1 + P1×4 全修 + P2×1）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | chat_fallback.py:98 | **P0** | 逐块无状态 check_stream 后立即发送，跨块敏感词流尾检查前已到前端（实测"诈""骗"分块放行） | 降级链接入 ContentStreamGuard（行缓冲+流尾 flush），与主路径同款 | 已修 |
| 09-12 | chat_stream_core.py:93 | P1 | 真实缓存命中只发送置 finished，不 finalize——历史与日请求计数缺失 | 命中后 finalize_answer(write_cache=False) 收口 | 已修 |
| 09-12 | chat_fast_paths.py:240、chat_stream_core.py:262 | P1 | 先发终止事件再 finalize_answer，收尾异常时终止事件后继续输出 | saved_normally 置位先于 yield、异常后跳过降级（chat_stream_core C-F11 同源） | 已修 |
| 09-12 | chat_fallback.py:142 | P1 | 空全文仍发 answer_complete+[DONE]，上层误判成功跳过兜底 | 空全文不发成功事件交兜底链 | 已修 |
| 09-12 | chat_react.py:199 | P1 | 工具结果回填模型未消毒，外部网页注入提示词 | _build_tool_messages 内容经 _sanitize_context | 已修 |
| 09-12 | file_upload.py:90,183 | P2 | 解析失败把原始异常当 text_content；元数据 json 无保护 | 用户侧固定文案（原始异常只进日志）+ get_user_file 损坏 meta 按 404（ValueError/UnicodeDecodeError/非 dict）；build_file_context 同源守卫跳过坏文件；单测回归 | 已修 |

## 2026-09-14 全量只读审计：服务链新发现

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-14 | chat_stream_core.py:144-163 | P1 | 防击穿锁等待后重新命中缓存的分支未 `finalize_answer` | 该路径漏会话历史、日请求计数和 RAG 收尾 | 与首次缓存命中统一 finalize，`write_cache=False` | ✅ 已修：重读命中分支补 `finalize_answer(write_cache=False)`，与 serve_from_cache 同口径 |
| 09-14 | chat_stream_core.py:297-311 | P1 | 顶层异常降级只转发 fallback SSE，未 finalize | 用户收到答案但 trace 仍记失败，历史和统计缺失 | 降级输出聚合为最终文本后统一 finalize | ✅ 已修：`_fallback_events` 经 `accumulate_sse_text`（chat_fallback 新增，answer_complete 覆盖/answer_chunk 追加）聚合最终文本后 finalize_answer（write_cache=False）+ saved_normally 置位 |
| 09-14 | quota.py:60-85、chat_stream_ctx.py:112 | P1 | 试用额度依赖数据库异常时返回 `False`，调用方抛 402 | 数据库故障被误报为“免费额度用尽” | 返回三态（可用/额度耗尽/依赖异常），异常映射 503 | ✅ 已修：core/quota 新增 `QuotaDependencyError` 三态（fail-closed 不变），ensure_chat_allowed 捕获后释放并发租约并抛 503 |
| 09-14 | agent_tasks.py:130-165 | P1 | 任务槽位满/建任务失败时曾漏归还试用额度，普通用户曾用全局旧限额 | 免费次数被吞、任务与 SSE 限额口径不一致 | `quota_reserved` 统一回滚；统一 `_get_limits(role)` | ✅ 已修（上轮改，本轮回归验证通过） |
| 09-14 | task_manager.py:377 | P1 | 幂等映射后台异步写入，而并发请求等待窗口仅 1 秒 | 高负载下可能重复创建任务 | 改为 async 直写并 await 后再启动任务 | ✅ 已修（上轮改；本轮续修超时冲突 409，见 core 日志） |
| 09-14 | agent_tasks.py:27-29 | P1 | 任务并发上限 `_task_active_count` 是进程内计数 | 多实例部署时全局上限失效，实例数 x 50 | 迁移到 Redis 共享计数/租约 | ✅ 已修：改 Redis INCR/DECR 原子计数（`task:active_slots`，超限原子回滚，TTL 自愈；Redis 故障 fail-open + warning），4 实例共享全局 50 槽 |
| 09-14 | memory_manager.py:182-202 | P1 | 对话冷备 PG 写入失败只记日志，无重试/outbox | Redis TTL 到期后永久丢失该轮长期记忆 | 增加失败重试队列或持久化 outbox | ✅ 已修（文件在 app/core/，明细见 core 日志）：3 次退避重试；持久 outbox 维持登记 |

## 2026-09-15 code-review-skill 全量复核

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | agent_tasks.py:78-99,143-186,262-311 | P1 | `_task_quota_gate` 先累加日请求，后续槽位满、建任务失败或恢复失败只回滚试用额度和槽位，日请求计数不回滚 | 用户没有获得任务却被永久计一次请求，反复失败可耗尽日限额 | 成功后确认计数，或给失败路径提供原子回滚；确保与成功路径互斥 | 待修复 |
| 09-15 | rag_actions.py:38-48 + frontend/src/views/RagConsoleView.vue:152-166 | P1 | `probe_health` 默认探测 `127.0.0.1:10086`，但动作运行在独立 worker 容器（监听 13151）；控制台提交空参数 | “健康探测”动作通常直接失败，半自动修复第一步不可用；任意 URL 还构成管理端 SSRF 面 | 默认改内网服务名或显式配置；限定白名单域名/路径；前端给出目标选择与结果 | 待修复 |
| 09-15 | chat_stream_core.py:108-115,148-154,274-280 | P1 | 缓存穿透占位、防击穿重读成功、API Key 缺失三条已发送终态事件的分支未调用 `finalize_answer` | 用户收到完整回答，但会话历史、日请求计数和 RAG answer trace 可能缺失 | 所有 terminal 分支统一走 finalizer，先置 `saved_normally` 再收口 | 待修复 |
| 09-15 | retrieval_trace.py:95-101、answer_trace.py:120-125、rag_request_trace.py:178-183 | P2 | `spawn()` 同步创建任务失败时，已经构造的协程未 await/close，会留下协程泄漏告警 | 极端关停/无事件循环场景出现未认领协程，降低可观测噪声质量 | 捕获创建异常时关闭协程，或把协程所有权收口到 `spawn()` | 待修复 |

## 2026-09-15 修复实施结果

- 任务日请求改为 Redis 原子预留，失败路径显式回滚。
- 增加单用户后台任务并发上限，全局槽位与用户槽位同时释放。
- API Key 缺失、锁不可用等已发送终态分支统一走 finalize。
- RAG 健康探测限制为内网目标白名单和固定健康路径。
- 缓存命中、降级、空结果和主动拦截结果按完整性决定是否写缓存。

### 2026-09-15 未覆盖文件逐行复核

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `app/services/rag_incidents.py` | P1 | verdict 规则重放更新后 incident 的严重度/建议未刷新 | 已修：新增 `refresh_incident`，冲突 verdict 更新后同步刷新 |
| `app/services/rag_request_content.py` | P2 | JSONB 标量字符串被拆成字符、answer trace 排序不稳定、retrieval trace 无上限、chunk 顺序丢失 | 已修：统一 text list、created_at+id 排序、LIMIT 50、按召回顺序重排 |

## 2026-09-15 Batch 2 告警与修复闭环演练

本地演练通过：RC-1/RC-6/RC-8/RC-4b/RC-4c 五类规则、
worker 租约恢复、L1 审批执行、Prometheus 规则语法与单元测试、
常驻 worker 的 `rag_verdicts_total` 指标链。

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | rag_actions.py:285、rag_monitor.py:392 | P2 | incident 解决后不回滚未执行动作，`waiting_approval` / `proposed` / `queued` 会永久残留 | 控制台持续显示待处理；审批被终态事件拒绝，只能人工取消 | incident 进入 resolved/closed 时，同事务取消未执行动作并审计 | 已修 |

## 2026-09-15 L1 自动修复生产加固

本地真实闭环：`adjust_rerank_top=5` 经策略 `auto_allowed` 自动排队，
独立 worker 领取并执行，采样命中 `3/5 -> 3/5`，平均耗时约
`30926ms -> 27092ms`，结果成功落库。

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-15 | `rag_policy.py` | P0 | 冷却查询返回历史截止时间后用 `bool()` 判断，冷却永远不会过期 | SQL 只返回 `now()` 仍位于冷却窗口内的记录，并按环境隔离 | 已修 |
| 09-15 | `rag_actions.py` | P0 | 同一 `action_key` 可被两个执行器并发运行，配置可能交错覆盖 | worker 使用 PostgreSQL 会话级 advisory lock，持锁执行并落库 | 已修 |
| 09-15 | `rag_rerank_remediation.py` | P0 | 基线采集失败时回滚会误写静态默认值，可能覆盖已有运行期配置 | 未捕获基线时回滚改为 no-op；执行后阶段异常保留真实基线 | 已修 |
| 09-15 | `rag_actions.py` | P1 | 审批不重新校验动作启用、环境和冷却状态 | 审批时重新执行策略裁决，并更新审批决策关联 | 已修 |
| 09-15 | `rag_actions.py` | P1 | 未知动作已抢占后取注册表失败，会占住队列反复重试 | 注册表读取纳入失败处理，未知动作直接写终态 | 已修 |
| 09-15 | `rag_action_audit.py` + 迁移 | P1 | 基线、验证和回滚只存在动作 JSON，缺少独立查询时间线 | 新增 `rag_action_verifications`，按 attempt 保留 baseline/postcheck/rollback | 已修 |
| 09-15 | `rag_incidents.py` | P2 | incident 解决后未执行动作永久残留 | 解决或关闭 incident 时同事务取消 `proposed/waiting_approval/queued` 动作 | 已修 |
| 09-15 | `RagConsoleView.vue` + `RagActionDetail.vue` | P1 | 控制台无法查看策略、基线、验证和回滚证据 | 增加动作审计详情面板与中文状态展示 | 已修 |

验证证据：

- RAG 相关单元测试 `65 passed`。
- 前端 `vue-tsc --noEmit` 与生产构建通过。
- 数据库实测：过期冷却返回空、有效冷却返回时间、错环境 worker 不领取任务。
- 数据库实测：同一动作锁被占用时第二执行器获取失败，释放后可重新获取。
- 快速 L0 `probe_health` 动作执行成功，验证时间线可从管理 API 查询。
- `scripts/verify_rag_console.py` 只读验收通过，迁移版本
  `u1e2f3a4b5c6`，四个网关和控制面健康检查均为 200。

## 2026-09-16 中控台事件排序、推荐修复与人工回滚

南提出三项中控台诉求：未处理高风险事件置顶、已处理事件最新在前、补齐
自动/半自动修复链路、误提交可回退。本轮补齐后端支撑。

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-16 | `rag_incidents.py:133` | P2 | 事件列表无运营优先级排序，未处理与已处理混杂，高危事件可能沉底 | `list_incidents` 改为：未处理优先；未处理内按严重度（critical>high>medium>low）再按 `last_seen_at` 倒序；已处理按 `COALESCE(resolved_at, closed_at, last_seen_at)` 倒序 | 已修 |
| 09-16 | `rag_incidents.py:109` | P3 | API 未透出 `closed_at`，前端无法按真实处理时间排序已处理事件 | `_incident_row` 增加 `closed_at`；新增 `_optional_value` 兼容 asyncpg Record 与测试用 dict，避免缺列时 KeyError | 已修 |
| 09-16 | `rag_recommendations.py` | P1 | 事件只有文本建议，没有任何可执行的自动/半自动修复链路 | 新增确定性推荐映射：`RC-1`/`RC-1b`/`RC-6` → L0 `probe_health`，`RC-8` → L0 `rerun_diagnosis` 与 L1 `adjust_rerank_top`（走审批），其余回落人工建议 | 已修 |
| 09-16 | `rag_monitor.py:372` | P1 | 新 verdict 落库后不创建任何动作，自动修复永远不触发 | `_persist_verdicts` 对新事件调用 `auto_plan_l0_actions`；该调用在既有事务内，L1/L2/L3 只返回建议不落库；历史回填不自动建动作 | 已修 |
| 09-16 | `rag_admin.py:357` | P1 | 控制台拿不到推荐修复，也无法发起人工回滚 | 新增 `GET /incidents/{id}/recommendations` 与 `POST /actions/{id}/rollback`，均限管理员并写管理审计 | 已修 |
| 09-16 | `rag_actions.py:399` | P1 | 动作一旦执行成功就无法回退，误操作只能直接改配置 | 新增 `rollback_action`：仅 `status='succeeded'` 且注册了回滚处理器的动作可回滚，行级锁内执行并落 `rollback_result`；`_action_row` 输出 `rollback_available` | 已修 |

自动/半自动链路分层（与 L1 自动修复加固一致）：

- L0 只读动作：`evaluate_action` 判为 `auto_allowed` 后状态 `queued`，由
  `rag_console_worker` 的 `rag_action_loop` 每 30 秒领取执行。
- L1 可逆动作：默认 `waiting_approval`，需人工点「批准」；仅当
  `RAG_AUTO_REMEDIATION_ENABLED=1` 且不在冷却期才自动排队。
- L2/L3：只生成提案或建议，永不自动执行。

验证证据：

- `tests/unit/test_rag_recommendations.py`、`test_rag_actions.py`、
  `test_rag_incidents.py`、`test_rag_monitor_core_fixes.py` 共 `34 passed`。
- RAG 相关单测全量 `70 passed`。
- `scripts/verify_rag_console.py` 只读验收 `ok=true`，服务健康检查
  gateway/gateway2/gateway3/gateway4/rag_console/rag_console_worker 均 200。

## 2026-09-16 动作处理器拆分（文件超限整改）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-16 | `rag_actions.py` | P3 | 611 行，超过 600 行上限 | 内置处理器拆到新增 `rag_action_handlers.py`（107 行）：`_probe_health`、`_rerun_diagnosis`、`_clear_exact_cache` 及探测白名单 `_probe_targets`/`_validate_probe_url`/`_resolve_probe_url`；rag_actions.py 保留 re-export，`rag_actions._resolve_probe_url` 等测试调用路径不变 |

拆分依据：探测白名单与 SSRF 校验集中在处理器模块，动作注册表、策略、审批、
执行与回滚仍留在 `rag_actions.py`。611 → 519 行。

验证：`tests/unit -k rag` 共 `70 passed`；`_REGISTRY` 四个动作键、
`_resolve_probe_url({})` 返回 `('gateway', 'http://agent_gateway:10086/health')`
与拆分前一致。

## 2026-09-16 快速通道埋点容错（清掉最后 3 个红灯）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-16 | `chat_fast_paths.py:266` | P2 | `attributes={"model": ctx.selected_model}` 直接取属性：ctx 由调用方构造，缺字段即抛 `AttributeError`，被 `simple_fast_path` 的兜底分支吞掉，快速通道整体退化为降级回复 | 改 `getattr(ctx, "selected_model", "")`，与同函数内 `getattr(ctx, "username", "")` 同口径；埋点缺标签不打断主流程 |

真实影响：`test_civil_grounding_contract`、`test_stream_safety_contracts`
共 3 个用例命中该分支（夹具 ctx 无 `selected_model`），此前长期红灯。生产
`ChatStreamCtx` 有默认值，故线上未暴露；但任何以轻量对象构造 ctx 的调用方
都会静默降级。

验证：后端单测 `381 passed`（全绿，无失败，此前固定 3 失败）。

## 2026-09-16 RAG 长链路审查（检索→回答→监测闭环贯通性）

本轮审查范围：入口 `chat_generate` → 检索采集（retrieval_trace/tools）→ 回答采集
（answer_trace/finalize_answer）→ 三张 trace 落库 → monitor 消费 → 动作执行 → 控制台
join，逐环节核对数据契约与 asyncio 上下文传递。

### 已修

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-16 | `rag_request_trace.py` + `answer_trace.py` | **P1** | ReAct 链路把检索放在 `create_task`+`gather` 子任务执行（`chat_react.py:209`、`react_steps.py:172`），`_LAST_RETRIEVAL` contextvar 的重绑定不会传回父任务 → `answer_traces.retrieval_returned_count/keys/retrieval_uid` 恒 NULL → monitor 对 NULL 行跳过 RC-4b/4c（防缓存误报的正确防御，副作用是盲区），RC-4d 也无数据。快路径（await 同任务）不受影响 | 检索事实容器改挂请求 trace 对象：`_RequestTrace.retrieval_facts` 共享 dict，`request_retrieval_state()` 访问器；begin_request 在父任务先建立、子任务按引用共享同一对象，原位修改父任务可见。contextvar 保留为无请求上下文回退。合并语义（keys 去重累计/count/latency 累加/首轮 uid/上限 50）逐行保留。不需要 bump RULESET_VERSION（存量 NULL 行重放无新结论） |
| 09-16 | `answer_trace.py` | P2 | `answer_traces.username` 落明文，与 `rag_request_traces.username_hash` 的不可逆摘要策略矛盾 | `_persist` 复用 `_username_hash`；全库无消费方读该列明文，改哈希后两表可按哈希关联 |

回归：`tests/unit/test_rag_monitor.py` 新增 `test_retrieval_snapshot_crosses_task_boundary`
（子任务写快照、父任务断言可见，旧代码必挂）；既有 3 个 set_last_retrieval 测试改用
`last_retrieval_snapshot()/reset_last_retrieval()`。RAG 单测 `59 passed`，导入链冒烟 OK。

### 待修复清单

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-16 | `rag_action_handlers.py:95` / `semantic_cache.py:219` | **P1** | `clear_exact_cache` 多实例下清不掉用户侧缓存：动作 worker 跑在 rag_console_worker 进程，`invalidate_exact` 只清本进程 LRU + PG 行；各 gateway 实例 L0 先于 PG 命中（semantic_cache.py:107），坏答案持续命中到 TTL。动作回报 `deleted=1, passed=true` 但故障未消除 | Redis pub/sub 广播失效，或 L0 条目带版本号、get 时与 Redis 比对。动 semantic_cache 读取主路径，需单独设计 | 待修 |
| 09-16 | `rag_monitor.py:532-557` | P2 | `ensure_daily_report` 在 DB 事务 + advisory lock 内做 `_fetch_status()`（二次借池连接）和 LLM HTTP 调用（HTTP_TIMEOUT_MEDIUM 级）；LLM 慢/挂时 worker 每循环占连接到超时，池紧张时 `_fetch_status` 的 acquire(5s) 也失败，报告当天反复生成不出 | 锁内只查 existing；取状态 + LLM 移到事务外；最后短事务 INSERT | 待修 |
| 09-16 | `rag_monitor.py:63,90,327` | P2 | RULESET_VERSION 字典序比较：`"2026-09-15.10" < "2026-09-15.2"`，序号超过 9 时规则重放静默失效（`_fetch_pending` 的 `>=` 误判已处理、`_record_processed` 的 WHERE 不再更新） | 改数字版本或日期+补零；需一并考虑库里已存字符串版本的比较语义 | 待修。**过渡约束：序号个位数（1~9）** |
| 09-16 | `chat_stream_core.py:42-47` / `chat_stream_ctx.py:256` | P3 | `_tracked_stage` 的包裹 span（react/simple_fast_path/cache 等）在 `finalize_answer→finish_request` 置空上下文之后才记录，被 `record_span` 静默丢弃——成功请求的主流程整段耗时 span 永远缺失（内层 retrieval/rerank/answer span 不受影响） | span 在 finalize 前打点，或挂请求对象延迟落库；动 finish_request 生命周期需评估全部收尾路径 | 待修（仅可观测性损失） |
| 09-16 | `rag_recommendations.py:5-10` | P3 | `_L0_MAP` 缺 RC-1a（双腿全挂、最严重形态）的自动探测映射，只有手工建议；RC-1/1b/6/8 都有 | 补 `("RC-1a", ("probe_health", {"target": "worker"}, ...))`；先确认当初是否刻意 | 待修（待确认） |
| 09-16 | `rag_admin_status.py:44-48` | P3 | `_load_latest_evaluation` 的 glob→stat 竞态：评测文件在两次调用间被删时 FileNotFoundError 冒泡，整个 `/status` 变 503（其它数据都是好的） | 捕获 OSError 返回 None（一行） | 待修 |
| 09-16 | `rag_admin.py:327` | P3 | `/actions/run-once` 同步执行动作：adjust_rerank_top 跑两轮 5 条真实检索冒烟（每条含 CPU 重排约 3~5s），管理端请求最坏阻塞约 1 分钟 | run-once 只入队交后台 worker，或前端明示慢操作 | 待修 |
| 09-16 | `rag_actions.py:319` | P3 | `rollback_action` 手动回滚无异常兜底：`spec.rollback` 抛异常（如 Redis 挂）→ 500；事务回滚后动作保持 succeeded 可重试，与自动路径 `verify_or_rollback` 的 catch 行为不一致 | 仿 verify_or_rollback 包 try/except，失败结果落 rollback_result | 待修 |
| 09-16 | `chat_react.py` 回答生成处 | P3 | `set_ungrounded_citations` 只在快路径调用（`chat_fast_paths.py:330`），ReAct 终答不调 → RC-4d（内容级幻觉引用）在 ReAct 路径无数据 | ReAct 终答处接 `find_ungrounded_citations`（需全部依据文本，注意多轮 tool call 的依据合并） | 待修（功能扩展，非缺陷） |

本轮做得好的：request_uid 三表串联全链路有效（子任务拷贝上下文仍能取到）；三张 trace
落库全部 spawn 后台化、失败静默不阻断；`rerank_used` NULL 透传与 `_fetch_status` 的
`FILTER (WHERE NOT rerank_used)` 口径一致；`mark_first_token` 字符串匹配与 `sse()`
json.dumps 分隔符兼容；客户端断连时 CancelledError 不被降级链吞掉、trace 仍落 failed。
