# 项目协议（人类填写）

> 填好后 AI 据此实现。所有 `[待定]` 必须先问再动。

## 1. 项目标识

- 项目名：`[待定]`
- 一句话定位：`[待定]`（例：面向 C 端的旅行规划 AI 助手）
- 技术栈偏好：`[待定]`（默认 FastAPI + PostgreSQL + Redis + Docker）

## 2. 端口分配表

| 服务 | 宿主机端口 | 容器内端口 | 备注 |
|---|---|---|---|
| 主实例 app | `[待定]`（建议 8081） | 8000 | |
| 扩展实例 app2/3/4 | `[待定]` | 8000 | |
| Nginx 入口 | `[待定]`（建议 8090） | 80 | |
| PostgreSQL | 不对外 | 5432 | 仅内网 |
| Redis | 不对外 | 6379 | 仅内网 |
| Prometheus | `[待定]` | 9090 | 如启用监控 |

> 注意：Windows WSL2 下部分 localhost 端口会被 wslrelay 拦截（实测 10091），
> 被拦就换端口。

## 3. 协议规范

- 接口风格：`REST`（默认）/ `GraphQL` / 其他：`[待定]`
- 长响应：`SSE`（默认）/ WebSocket：`[待定]`
- 版本前缀：`/api/v1/`
- 认证方式：`Bearer JWT`（默认）/ Session / 其他：`[待定]`
- 统一响应格式：`{"success": bool, "data": ..., "error": {"code", "message"}}`（默认）
- 错误码规范：`模块_错误` 全大写（默认）

## 4. 端点清单（写核心端点，AI 据此实现）

| 端点 | 方法 | 鉴权 | 说明 |
|---|---|---|---|
| `/health` | GET | 无 | 存活探针 |
| `[待定]` | | | |
| `[待定]` | | | |
| `[待定]` | | | |

## 5. 需要 AI 特别注意的约束

- `[待定]`（例：某些接口需要幂等键 / 某些字段必须脱敏 / 时区要求）
