# 修复日志 — tests

## 2026-09-07 复审轮（仅审查登记，未修复）

复审通过项：fake_redis 新增 lrange/lpush/ltrim/expire/eval(EXISTS 分支)——EXISTS 对空列表返回 0 与 Redis"空列表自动删除"真实语义一致；web_search 拆分后两个历史测试的 patch 目标全部迁到定义处模块（patch re-export 名不影响新模块内部查找，处理正确）；test_20260907_batch_fixes.py 14 用例逐一核对为真断言（patch 均带 try/finally 恢复、断言真实行为非摆设）；test_search_pipeline 改写重试 4 用例覆盖新功能核心路径（含"高置信不得触发改写"反向断言）；test_secret_scan 正反两向 + 退出码覆盖；test_review_fixes 测试桩同步补 user_query 属性。

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | unit/fake_redis.py | P3 | eval 以 "EXISTS" in script 子串分发 Lua 分支：未来其它含 EXISTS 的脚本会误入回填分支 | 分发改用脚本内独特标记串 | ✅ 已修（复核：分发改脚本内独特标记） |
| 09-07 | diag_cc051.py / ragas/ragas_evaluation.py.bak 删除 | 关闭 | 影响面核实：全仓无存活引用；ragas 主文件 ragas_evaluation.py 仍在，run_ragas_with_key.py / run_ragas_with_env.py / update_ragas_prompt.py 的引用不受影响 | — | 已关闭 |

## 2026-09-08 全目录覆盖轮（62 个代码文件全量审查；仅登记，未修复）

### 收集期黑名单（`pytest tests/` 必炸的根因清单，主日志遗留项的实锤）

机制：pytest.ini 的 testpaths=tests/unit 仅在无参数时生效；显式 `pytest tests/` 时 python_files=test_*.py 仍生效，tests 根下 4 个文件 import 即执行网络调用/读文件。

| 文件 | 模块级行为 | 级别 |
|---|---|---|
| tests/test_cc019_direct.py:7 | import 即 `httpx.Client(timeout=60)` POST localhost:10092 登录+流式问答（真实 ADMIN_PASSWORD） | P1 |
| tests/test_mapping_live.py:7 | 同上，5 条流式问答循环 | P1 |
| tests/test_new_features.py:7 | 同上，2 条流式问答 | P1 |
| tests/test_deepseek_direct.py | import 读 /app/.env（Windows 必 FileNotFound）；容器里则用真 key 发真实 DeepSeek 调用 | P1 |

建议：移出 tests/ 收集路径（改 check_*.py 或挪 scripts/），或加 pytest.importorskip/探活守卫。子代理原判 P0，按"不影响生产运行、属测试基建瘫痪"定 P1（高优先）。

> ✅ 已修 09-08：6 个文件 git mv 改名为 `check_*` 前缀——不再匹配 `test_*.py` 收集模式，pytest 不再收集（文件内容未动，手工直跑行为不变）。前 4 个是模块级真连网络；后 2 个是 `check_mapping.py` / `check_mapping_local.py`（原 test_mapping*.py，模块级 `reload_mapping()` 拖死收集——二分实测：全量收集从 5 分钟+挂死变为 **182 用例 1.82 秒收集完成**；它们本就是零断言 print 脚本，属收集外对象，内容仍是 P2 登记项）。
> 验收：`pytest tests/ --collect-only -q` → 182 collected，exit 0；integration 集成测试默认 skip 生效。

### 其余发现

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | integration/test_payment_idempotency.py:10-17 | P1 | integration 标记不会自动 skip，裸跑 `pytest tests/` 真连 localhost:10090 必红；且硬编码 Admin@**** | ✅ 已修 09-08：默认 skip（显式 `RUN_INTEGRATION=1` 才执行）+ admin 密码改 `_env.get("ADMIN_PASSWORD")` 读取 | ✅ 已修 |
| 09-08 | test_llm_endpoint.py、test_reasoning_guard.py | P2 | 自检脚本冒充测试：import 时跑完全部 assert（收集 0 用例） | 包进 test_ 函数或改名 | ✅ 已修 09-12（改造为真测试并纳入 testpaths） |
| 09-08 | test_mapping.py、test_mapping_local.py、test_routing.py | P2 | 零断言 print 脚本（摆设测试的极端形态）；前两者硬编码 D:\桌面 绝对路径 | 改造为真测试或删除 | ✅ 已修 09-12（旧摆设文件已删除/改造） |
| 09-08 | run_deploy.py、deploy_mapping.py、clear_and_rerun.py、diag_cc051_v2.py、diag_cc001.py、add_civil_cases.py、generate_civil_cases.py、update_ragas_prompt.py | P2 | 运维/破坏性脚本混入 tests/：直跑即部署、删评测缓存、**docker exec 清空 PG semantic_cache 表**、覆写源码/数据文件、联网抓取，均无 main 守卫 | 移 scripts/ 或加守卫；diag_cc051_v2 的 DELETE 需人工确认语义 | ✅ 已修 09-12（5 个移维护目录 + 3 个原地加显式确认守卫；断链修复） |
| 09-08 | run_ragas_with_key.py:12、check_ragas.py:4 | P2 | 引用已迁移/已改名的文件（ragas_evaluation.py 已在 tests/ragas/ 下；报告文件名不符），直跑必 FileNotFound | 修路径或删除 | ✅ 已修 09-12（复核：引用目标均在，可达） |
| 09-08 | test_mapping.py 等 11 处 | P3 | 硬编码 D:\桌面\agent_gateway 绝对路径，换机即失效 | Path(__file__) 相对定位 | 登记 |
| 09-08 | unit/test_agent_logic_fixes.py:155-169 | P3 | 复制品测试：断言测试内自建的 judge() 副本而非 document_parser 真实行为；:155 有死代码 | 改调真实入口 | 登记 |
| 09-08 | unit/test_p0_p1_fixes.py:26、test_sliding_counter.py:27、test_core_p1_fixes.py:97 | P3 | 直接对共享模块属性赋值打桩不还原，跨文件污染、依赖测试顺序 | 统一 monkeypatch | 登记 |
| 09-08 | run_eval_with_key.py:20、ragas/run_ragas_with_env.py:26 | P3 | exec(open().read()) 执行脚本，traceback/覆盖率失真 | 改 import 调 main() | 登记 |
| 09-08 | tests/ 根 6 个评测产物（xlsx/json/txt） | P3 | 已入 git，仓库卫生 | .gitignore | 登记 |

复审通过项：tests/unit 18 个文件全读——真断言、FakeRedis、离线，无摆设断言（except 仅用于"先抛后断"合法场景）；conftest.py 干净（只设 env）；所有被测源码符号逐一核实存在；全 tests/ 无真实 sk- 密钥。

## 2026-09-09 全目录审查轮

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | test_orchestrator_context_compression.py + pytest.ini:2 | **P1** | 双重失效：①测试仍按旧实现调用（build_shared_context user_location 参数、DiscussionBoard 两参构造），实现演进后从未跟上，HEAD 上必失败；②pytest.ini testpaths=tests/unit 把它和 test_secret_scan/test_snapshots 三个真测试排除在收集外，CI 永远跑不到，带病无人知（本次 `pytest tests` 显式跑才暴露） | 测试对齐现实现（单参构造 + 现行断言）；pytest.ini testpaths 显式追加 3 个真测试文件；回归：183 用例全绿 | ✅ 已修 09-09 |
| 09-09 | unit/test_20260907_batch_fixes.py:48 | P1 关联 | backfill 并发测试断言旧 Redis 键名 `conv:c1`——IDOR 修复（键含 user_id）后同步对齐为 `conv:u:c1` | 随 IDOR 修复一并更新 | ✅ 已修 09-09 |
| 09-09 | run_eval_with_key.py:8、run_civil_eval.py:8-14、ragas/run_ragas_with_env.py:12-18 | P2 | 运行器指向仓库根 `.env`——该路径现为 09-06 迁移后的**空目录**，os.path.exists 对目录返回 True → open() 抛 PermissionError 启动即崩（真实 .env 在仓库上一级） | 三处统一改用根 _env.py；删除根残留空 .env 目录 | ✅ 已修 09-12（改 is_file 判断 + 取上一级权威 env；根空目录删除） |
| 09-09 | tests/test_llm_endpoint.py 等 3 个 + test_routing.py | P2 | 半假测试：模块级 assert 零 test 函数（import 即跑完）/零断言纯 print 顶着 test_ 名字（本会话安全钩子禁 Bash 改名，git mv check_* 未执行） | 改名 check_* 或补 test 函数外壳 | ✅ 已修 09-12（改造为真测试并纳入 testpaths） |

**存量核验**：13 个 check_*.py 零断言 **仍在**（改名后不收集，口径一致）；运维/破坏性脚本混入 **仍在 8 个**（diag_cc051_v2 清 semantic_cache 全表、add/generate_civil_cases 覆写 test_cases.yaml、update_ragas_prompt 覆写源码等，均无 main 守卫，直跑即破坏）；引用已改名文件 **仍在 2 处**（run_ragas_with_key→tests/ragas_evaluation.py 已迁走、check_ragas→旧 xlsx 名）。

## 2026-09-10 修复：管理员与地图工具回归

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | unit/test_core_p1_fixes.py | P0 | 管理员种子按错误用户名查询，多实例启动循环缺少回归 | 新增配置用户名 UPSERT 断言 | 已修 |
| 09-10 | unit/test_weather_forecast.py | P1 | 自治州天气候选缺少真实地名回归 | 新增 IP 判定与“海西蒙古族藏族自治州 → 海西”断言 | 已修 |
| 09-10 | run_evaluation.py | P2 | 评测提示与输出含非必要 emoji | 改为 `[正确]`、`[错误]`、`[注意]` 等纯文本标签 | 已修 |

## 2026-09-10 历史修复回归批次

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | integration/test_payment_idempotency.py、test_llm_endpoint.py、test_secret_scan.py | P1 | 模型名、密钥扫描和模拟支付幂等缺少与线上配置一致的回归口径 | 模型名同步 deepseek-flash；密钥扫描捕获编码错误改替换模式；支付幂等按现行账号配置；全量测试通过 | 已修 |
| 09-10 | unit/test_agent_task_lifecycle.py、unit/test_agent_security_fixes.py、unit/test_agent_p2_fixes.py | P0 | 任务取消竞态、多实例取消、Redis 限流、提示词边界缺少完整回归 | 覆盖 CAS 终态、取消 usage 结算、Redis 限流、递归消毒；Agent E2E 增跨窗口与话题切换场景 | 已修 |
| 09-10 | unit/test_logging_secrets.py、unit/test_oauth_code_exchange.py、unit/test_chat_fast_paths.py、unit/test_chat_stream_core.py | P1 | HTTP 日志泄漏、OAuth 授权码重放、快速通道历史丢失和跨轮缓存串答缺少回归 | 补对应单测；前端构建和 23 个 Vitest 用例通过 | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | unit/test_reasoning_guard.py（新增） | **P1** | tests/test_reasoning_guard.py 为 33 处模块级 assert 的自检脚本且不在 pytest.ini testpaths——09-10 03:39 跨块泄露回归**实际从未进 CI**（09-08 登记"自检脚本冒充测试"的严重后果实证） | 全部 10 个场景改写为标准测试函数收入 tests/unit（自动收集），git rm 原脚本 | ✅ 已修 09-12（改造为真测试并纳入 testpaths） |
| 09-10 | tests/test_llm_endpoint.py | P2 | import 即跑完 assert 的自检脚本、收集 0 用例（09-08 登记） | 改为 3 个标准测试函数；QWEN_API_KEY 改 monkeypatch 钉配置模块导出值（Settings 已实例化，env 补写不生效） | 已修 |
| 09-10 | pytest.ini | P2 | testpaths 未含改造后的真测试 | 增补 tests/test_llm_endpoint.py、tests/test_routing.py | 已修 |
| 09-10 | tests/test_routing.py | P2 | 零断言 print 脚本（09-08 登记；test_mapping/test_mapping_local 此前已不在仓库） | 改造为真测试：10 组路由裁决断言 + 拒绝路径文案断言，入 testpaths | ✅ 已修 09-12（旧摆设文件已删除/改造） |
| 09-10 | run_deploy/deploy_mapping/clear_and_rerun/diag_cc051_v2/diag_cc001/update_ragas_prompt.py | P2 | 运维/破坏性脚本混入 tests/：直跑即部署、删评测缓存、docker exec 清空 PG semantic_cache 全表、覆写源码（09-08 登记） | 5 个移入 scripts/maintenance/（Mimosa 扫描下重写：字面量参数列表、RUN_DEPLOY/DEPLOY_MAPPING/ALLOW_DESTRUCTIVE 显式确认守卫）；add_civil_cases/generate_civil_cases/diag_cc001 原地加 ALLOW_DESTRUCTIVE 守卫；09-12 勘误：diag_cc001 其后经 git rm 删除（一次性诊断，内容在 git 历史），update_ragas_prompt 亦补同款确认门 | ✅ 已修 09-12（5 个移维护目录 + 3 个原地加显式确认守卫；断链修复） |
| 09-10 | run_ragas_with_key.py、check_ragas.py | P2 | 引用已迁移/已改名文件，直跑必 FileNotFound（09-08 登记） | 路径改指 tests/ragas/ 下现存文件 | 已修 |
| 09-10 | run_eval_with_key/run_civil_eval/ragas/run_ragas_with_env.py | P2 | 运行器指向仓库根空 ".env" 目录，open() 抛 PermissionError 启动即崩（09-09 登记） | 前两者改走根 _env.py；第三者去掉根路径候选；删除根残留空 .env 目录 | ✅ 已修 09-12（改 is_file 判断 + 取上一级权威 env；根空目录删除） |
| 09-10 | integration/test_migrations.py（新增） | P3 | 迁移链零测试：无"空库 upgrade head → downgrade base"冒烟（09-08 登记） | 新增 integration 标记冒烟测试：一次性临时库上全链 upgrade/downgrade/重放，无库自动跳过 | 已修 |
| 09-10 | unit/fake_redis.py | P3 | eval 以 "EXISTS" 子串分发 Lua 分支（09-07 登记） | 改用脚本内独特标记 RPUSH | ✅ 已修（复核：分发改脚本内独特标记） |
| 09-10 | chat-store.test.ts | P2 联动 | regenerate 死代码删除后其用例失引用 | 用例改走 deleteQA + send 路径（分支切换语义不变） | 已修 |

**核验**：pytest 默认集合 227 passed（含新增 reasoning guard 11 例 + llm_endpoint 3 例 + routing 2 例 + 迁移冒烟跳过逻辑）；vue-tsc 0 错；vitest 23 passed。


## 2026-09-10 收尾深检补充

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | unit/test_core_p1_fixes.py | P2 | 管理员种子测试夹具使用真实历史口令字面量，撞推送门禁"已知泄漏"黑名单（secret_scan 逐行扫描修复后暴露） | 夹具改占位值（monkeypatch 场景本不依赖真实口令） | 已修 |
| 09-10 | unit/test_reasoning_guard.py | P3 | sanitize 夹具 sk- key 形状撞门禁规则 | 改用门禁认可的 sk-your-example 占位形状（sanitize 对任意 sk- 形状照剥，断言语义不变） | 已修 |


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | integration/test_migrations.py | P2 | skipif 只查 import 可用不查 PG 连通性——库未启动时 error 而非 skip | 登记：加 asyncpg 连通性探测 | ✅ 已修（复核：判定函数已含 asyncpg 连通性探测） |
| 09-11 | unit/test_20260911_rule_review.py（新增） | P1 配套 | 本轮 P0/P1 修复回归：react 用户消息追加、project 通道 ESCAPE+转义、Flash 拦截计费、CDC 停机 txid 水位 | 已修 | 已修 |


## 2026-09-12 凌晨清欠轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | integration/test_migrations.py | P2 | skipif 只查 import 不查 PG 连通性，库未启动时 error 而非 skip（09-11 登记） | _db_tooling_available 补 asyncpg 连通性探测，失败转 skip | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | integration/test_migrations.py:34 | P1 | pytestmark 在 _admin_dsn 定义前调用 _db_tooling_available，NameError 被吞后 skipif 永久为 True，迁移链测试从未执行 | 将 skip 判定移到辅助函数定义之后，或只捕获连接异常 | ✅ 已修 09-12（求值序修复，skip 判定真实生效） |
| 09-11 | integration/test_payment_idempotency.py:46 | P1 | 显式启用集成测试时使用生产白名单禁止的 balance 充值渠道，并会实际改动钱包/订单数据 | 改用允许的模拟渠道和独立测试库，finally 清理测试数据 | ✅ 已修 09-14：改白名单内模拟渠道，显式走下单→支付→入账链路 |
| 09-11 | check_new_features.py:1 等 check_*.py | P2 | 多个诊断脚本不会被 pytest 收集，主要只有打印无断言，形成测试覆盖假象 | 转成标准 test_*.py 真断言，或迁到 scripts/diagnostics 并退出码判定 | 登记 |
| 09-11 | ragas/ragas_evaluation.py:486 | P2 | 裸 except 返回 None，会吞掉中断和编程错误，评测失败被当正常无结果 | 捕获明确异常并记录失败原因 | 登记 |
| 09-11 | unit、integration 各高风险路径 | P1 | 缺少跨块过滤、CDC 并发 checkpoint/分区删除、负金额退款、账号隔离、多窗口刷新、上传切换人格等回归 | 为已确认缺陷逐项增加最小回归和端到端场景 | 待修复 |


## 2026-09-12 外部复核修复轮（P1×2 修 + P2×2 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | integration/test_migrations.py:34 | P1 | pytestmark 求值早于 _admin_dsn 定义，NameError 被吞 → skipif 恒真，**迁移链测试从未真正执行**（09-12 引入、09-12 自愈） | _admin_dsn 移前定义；skipif 求值序修正 | 已修 |
| 09-12 | integration/test_payment_idempotency.py:46 | P1 | 集成测试用白名单禁止的 balance 渠道并实际改钱包/订单 | 登记：改模拟渠道+独立测试库+finally 清理（需测试库资源，单独批次） | 登记 |
| 09-12 | check_new_features.py 等 | P2 | check_*.py 无断言只有打印，覆盖假象 | 登记：转真断言或迁 diagnostics（批量） | 登记 |
| 09-12 | ragas/ragas_evaluation.py:486 | P2 | 裸 except 返回 None 吞中断 | 登记：捕获明确异常并记录失败原因 | 登记 |
| 09-12 | unit/integration 高风险路径 | P1 | 缺跨块过滤/CDC 并发/负金额退款/账号隔离/多窗口回归 | 已修：跨块过滤（ContentStreamGuard 三路径+回归）、CDC 并发 checkpoint（pair 读取+回归）、负金额退款（消费单可退断言）、账号隔离（storageKey+回归）；多窗口刷新留待前端批次 | 已修（大部分） |
| 09-12 | integration/test_migrations.py:17 | P1 | REPO_ROOT 取 parents[1] 指向 tests/，alembic script_location 解析到不存在的 tests/alembic——skip 修通后强制运行必败，迁移冒烟实际从未跑过 | 改 parents[2]；本地 DATABASE_URL 打通实测全链通过；CI（code-rules.yml）加 pgvector/pgvector:pg15 服务容器常跑迁移冒烟 | 已修 |
| 09-12 | run_retrieval_eval.py:27-33 | P1 | `.env` 加载分支用 `Path.exists()` 判断后直接 `read_text()`——仓库根的 `.env` 实为**空目录**（实测 exists=True / is_file=False / is_dir=True），读取抛 `PermissionError [Errno 13]`（Windows）/ `IsADirectoryError`（Linux），**脚本一运行即崩**；且即便不崩，权威 .env 实际位于仓库上一级（`../.env`），原路径也读不到真实配置。本脚本是检索层 recall@k/MRR 的唯一硬证据来源，崩了等于检索质量长期无基线 | 建议（未改，待修复）：把 `BASE / ".env"` 改为按 `is_file()` 判断并优先取 `BASE.parent / ".env"`，与 `_env.py` 的权威路径口径对齐 | 待修复（由 ragclosure 采集真实 trace 时顺带发现并实测复现） |
| 09-12 | run_retrieval_eval.py:35-36,106 | P1 | **`TOP_K=20` 形同虚设，recall@10/@20 是假指标**：`app/agents/tools.py:_search_two_legs` 用 `top_candidates = merged[:RERANK_TOP]`（`RERANK_TOP` 硬编码 5，tools.py:219/394）先截断候选，最终 `results` 最多 5 条，**与传入的 top_k 无关**。于是 `score_case` 里的 `results[:k]` 在 k=5/10/20 时窗口完全相同 → 实测三个值同为 0.3879，`recall@10 == recall@20 == recall@5`。后果：报告会让人误以为"召回窗口放大到 10/20 指标不变"，掩盖"从未真正评估过 10/20 窗口"的事实；且以此做改造前后对比时，任何只影响 6~20 位的改动都测不出来 | 建议（未改）：① 把 `RERANK_TOP` 提为环境变量可配，评测时按 TOP_K 传值；② 或在评测脚本显式标注实际窗口上限（`effective_k = min(TOP_K, len(results))`）并在报告中提示，避免继续输出假指标 | 待修复（由 ragclosure 对照口径时实测发现） |
| 09-12 | unit/test_p0_p1_fixes.py（journal/checkpoint 组） | P1 | **测试隔离缺陷，全量跑结果不稳定**：`test_journal_rotation_no_file_per_write`、`test_checkpoint_never_rewinds`、`test_journal_prunes_expired_files` 单独跑必过，但全量 `pytest` 时随机失败——且**每次失败的用例不同**（实测 `pytest` 全量 fails A+B；`pytest tests/unit` fails C；单独跑全过）。这些用例直接操作真实 `cdc_journal/` 目录与 checkpoint 文件，彼此共享文件系统状态，执行顺序一变即互相干扰。后果：真正的回归信号会被随机失败淹没（本轮改检索埋点时就差点被误判为改动引入——已用 `--ignore=ragclosure` 排除法实测确认无关） | 建议：journal/checkpoint 用例改用 `tmp_path` 隔离目录；或加 autouse fixture 在用例前后重置 `cdc_journal/`；根因修法是让日志目录经参数注入而非模块级常量 | 待修复（既有问题，与 09-12 检索埋点改动无关） |

## 2026-09-12 修复轮（3 条已修 + 1 条核实）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | run_retrieval_eval.py:24-37 | P1 | `.env` 用 `exists()` 判断后直接 `read_text()`；仓库根 `.env` 实为空目录 → 脚本一运行即崩，检索评测长期无基线 | 改为按 `is_file()` 判断 + 优先取 `BASE.parent/.env`（与 `app/core/config.py` 的权威路径口径一致）；已验证顶层导入成功、`DATABASE_URL` 正常加载 | **已修** |
| 09-12 | run_retrieval_eval.py:35,106 等 | P1 | `TOP_K=20` 形同虚设，`recall@10/@20` 恒等于 `recall@5`（假指标） | 根因修在 `app/agents/tools.py`：`RECALL_LIMIT`/`RERANK_TOP` 改为环境变量可配（默认值不变、行为等价）；本脚本新增"实际有效窗口"提示，并在报告中写入 `effective_window` / `rerank_top` 字段 | **已修** |
| 09-12 | unit/test_p0_p1_fixes.py（CDC 组） | P1 | 测试隔离缺陷：全量跑随机失败、单独跑必过、**每次失败的用例都不同** | 根因是**依赖删除成功**——原用固定目录名 + `finally: rmtree(ignore_errors=True)`，删除失败静默无感，残留的 journal/checkpoint 让下次读到脏数据（实测断言读到上次留下的 `20260912.jsonl.11` 与 `checkpoint=120`）。改为 `_fresh_cdc_dir()` 提供**系统临时目录 + 唯一名字**，不依赖删除；另加 autouse fixture 尽力清理历史遗留 | **已修**（连跑 3 次稳定 15 passed；全量 **239 passed / 0 failed**） |
| 09-12 | unit/test_p0_p1_fixes.py | P2 | 受限沙箱会拦截对工作区 `.tmp/` 下文件的删除，`prune_old_files` 的 `os.remove` 抛 OSError 被自身 except 吞掉，表现为"一个都没删" | 随上条规避（测试目录移出工作区 `.tmp`）。**这不是产品缺陷**：`prune_old_files` 在系统临时目录实测 `removed=1`，实现正确 | 已核 |
