# 修复日志 — .github

## 2026-09-08 全目录覆盖轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | workflows/code-rules.yml:9-15 | P3 | workflow 无 permissions 收敛（默认 token 权限偏宽）；actions 按主版本 tag 引用未钉 SHA | 顶层加 `permissions: contents: read`；checkout/setup-python 为官方 action，tag 引用可接受 | ✅ 已修 09-14：顶层 permissions: contents: read（官方 action 按主版本 tag 维持可接受口径） |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | workflows/code-rules.yml:19 | P1 | CI 只检查 app 结构并编译，不运行测试、密钥扫描，也不覆盖 scripts/tests/deploy，当前违规仍全绿 | 增加全量结构门禁、secret scan、后端测试和部署配置语法校验 | ✅ 已修 09-12（单测/密钥扫描/迁移冒烟/YAML+sh 校验全量门禁化）；09-14 顺带移除 tools.py 的 --skip-files（拆分后达标，跳过项清零）。tests/ 未纳入结构门禁：存量 check_* 诊断脚本 22 处静默告警，专项清理后再接入 |


## 2026-09-12 外部复核修复轮（P1×1 修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | workflows/code-rules.yml:19 | P1 | CI 只查 app 结构并编译——不跑测试/密钥扫描/部署配置校验，违规仍全绿 | 门禁扩展：check_code_rules 覆盖 app+mcp_assets+scripts、compileall app+scripts+tests、离线单测、密钥扫描、YAML/sh 语法校验 | 已修 |

## 2026-09-15 code-review-skill 全量复核

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | workflows/code-rules.yml:42-50 | P1 | CI 只运行 `tests/unit`，未运行根目录的 `tests/test_secret_scan.py`、快照/路由/端点测试，也没有前端 `vitest`/`vue-tsc`/构建步骤 | 根测试与前端回归不进入 PR 门禁，历史“全绿”不等价于全仓绿 | 分别增加根测试、前端测试/类型检查/构建、前端 production dependency audit；迁移测试保持 PG 服务容器 | 待修复 |
| 09-15 | scripts/check_code_rules.py:101-104 | P2 | 结构门禁未覆盖 `ragclosure/`，新增 adapter 已出现 85 行函数而门禁仍通过 | 规范债务从主目录转移到未扫描目录 | 把 `ragclosure` 纳入结构门禁，或明确将其定为独立子项目的门禁范围 | 待修复 |
