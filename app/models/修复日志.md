# 修复日志 — app/models

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | schemas.py | P3 | 文件尾无换行符（EOF newline） | 补换行 | 待修复（低优先） |

复审通过项：query/message max_length=65536、file_ids max_length=5 与 core/stream_utils 兜底上限同值（源头约束+兜底双层，注释互指）；Pydantic v2 对 List 的 max_length 校验列表长度，语义正确；约束有回归测试钉住（test_schemas_request_size_limits，含 file_ids 6 个拒绝）。
