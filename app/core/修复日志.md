# 修复日志 — app/core

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-04 | audit.py 相关 | P1 | audit_logs 表无任何迁移建表，审计静默全失败 | 新增迁移 c2d3e4f5a6b7 建表+索引 |
| 09-05 | persona_manager.py | P1 | 全局 current 被请求路径 switch，并发串人格 | 新增 get_effective（只读解析）；switch 仅限显式端点 |
| 09-04 | safety_filter.py | P1 | 白名单全文一票豁免：输出含任一白名单词即绕过全部敏感词检查 | 同句豁免（命中词所在句含白名单词才豁免，豁免后继续扫） |
| 09-04 | memory_manager.py | P1 | 对话历史 ORDER BY created_at 无决胜列，同事务双写时间戳相同致乱序 | 补 id DESC 决胜列 |
| 09-05 | memory_manager.py | P2 | trim 三步（llen/lrange/ltrim）并发丢消息 | Lua 原子化（_TRIM_LUA） |
| 09-05 | memory_manager.py | P2 | get_profile 90 天比较用本地 now()，时区漂移 | UTC naive 统一 |
| 09-05 | quota.py | P1 | used_requests 自增不失效 5 分钟用户缓存，试用 20 次上限可绕过 | 自增即失效缓存（invalidate_user_cache）；键前缀单源化 |
| 09-05 | semantic_cache.py | P2 | L2 命中改写 query_hash 致原查询精确键丢失/行易主 | 只加命中计数不改 hash |
| 09-05 | semantic_cache.py | P2 | 每次命中全量 UPDATE 写放大 | 1/10 抽样计数 |
| 09-05 | db_maintenance.py | P2 | 命中>=2 的缓存条目永不清理 | 180 天硬过期 |
| 09-05 | db_maintenance.py | P2 | 三处 cutoff 用本地时间，时区漂移 | 统一 _utc_naive_now |
| 09-05 | cache_warmup.py | P2 | 20 条"请稍候"占位假预热（声称的覆盖机制不存在）+ ctx 不匹配永不可达 | 重写：仅预热真实答案，按匿名 ctx 键写入 |
| 09-05 | sms.py | P2 | 日志明文打印验证码/手机号 | 验证码前2位掩码、手机号 3+4 掩码 |
| 09-05 | plugin_loader.py | P2 | manifest 可借 importlib 加载任意模块 | handler 限定 tools.* 前缀 |
| 09-05 | constants.py | P2 | strftime %A 输出英文星期，与中文助手不一致 | 新增 today_cn（4 处调用点统一） |
| 09-05 | task_manager.py | P1 | 任务不存 owner，端点无归属校验（越权读取/取消/恢复） | create_task 落 owner 字段 |

| 09-05 | db_maintenance.py | P2 | ensure_indexes 启动+每日执行冗余 DDL，违反 Alembic 唯一入口（A19/A23）；索引基线迁移里全有 | 删除函数与全部调用点 |
| 09-05 | task_manager.py（补充） | P1 | 详见 app/routes 日志：owner 字段落库 | 同上 |

| 09-05 | db_maintenance.py | 债务 | compress_old_conversations 87 行超限（豁免名单历史债） | 拆出 _compress_one_conversation（单会话压缩，返回条数） |
| 09-05 | core 多文件 | 债务 | 机器门禁（模板 check_code_rules.py）首次扫描：core 内多处静默吞异常 | 逐处补 logger.debug 说明原因；logging.py 用自身 getLogger |

机器门禁：scripts/check_code_rules.py（自可复用资产模板安装，增强 noqa 支持）+ CI 工作流 .github/workflows/code-rules.yml
回归测试：tests/unit/test_core_p1_fixes.py、test_cdc_partitioning.py

## 2026-09-05 审查轮：待修复清单（全量通读 24 个文件，sms.py 按主理人交代跳过）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-05 | db_maintenance.py | **P1** | 压缩摘要下一轮维护被删：摘要 INSERT 的 created_at 用 last_msg（必然 < cutoff），下一轮分组查询再次选中且 msg_count=1，走"单条消息直接删除"分支把摘要本身 DELETE——30 天前对话历史两个维护周期后永久丢失，删除无日志 | 分组查询与单条删除分支都排除已压缩记录：`AND role != 'system'`（或 `content->>'type' != 'compressed'` 更精确）。不推荐摘要 created_at 写当前时间（只推迟 30 天）。补回归测试：连跑两轮 compress，断言摘要存活 | **待修复** |
| 09-05 | safety_filter.py | P2 | contains_sensitive 超时 500ms 返回 False（fail-open）；5000 字截断 + O(n²) 下对抗性长文本可逼近超时绕过，且放行不可见 | 超时分支补 Prometheus 计数器 `safety_filter_timeout_total`（可观测性）；fail-closed 需权衡误伤，暂不建议 | ✅ 已修 09-05 |
| 09-05 | safety_filter.py | P2 | filter_text 不做白名单同句豁免（与 contains_sensitive 行为不一致），且全仓库零调用 | 删除 filter_text；find_first 保留（check_stream 内部依赖），docstring 注明无豁免语义 | ✅ 已修 09-05 |
| 09-05 | stream_utils.py | P2 | build_file_context 按 file_ids 直取内容不校验属主（IDOR 形状；uuid4 不可猜 + Redis 1h TTL 缓解） | 上传时 meta 写 owner，读取时与 current_user 比对，不匹配跳过 | ✅ 已修 09-05 |
| 09-05 | semantic_cache.py | P2 | L1 命中 1/10 抽样累加 hit_count，与 clean_stale_cache 的 `hit_count < 2` 判冷口径漂移：热门条目计数停在 0~1，60 天后可能被误清（重建兜底，损失为重建成本） | 抽样改 SQL 侧 `hit_count + CASE WHEN random() < 0.1 THEN 1 ELSE 0 END`（保持写放大优化）并调清理阈值语义 | ✅ 已修 09-05 |
| 09-05 | agent_rules.py | P3 | 整模块零调用（已核实）；中文 `\b` 正则不生效、`"安全"` 等宽泛词误判技术任务 | 确认无预留用途后整文件删除 | ✅ 已修 09-05 |
| 09-05 | logging.py | P3 | setup_logging 每次调用清空重加 root handlers，模块 import 反复重置，会误伤自定义 handler | 加 `_configured` 幂等守卫 | ✅ 已修 09-05 |
| 09-05 | safety_filter / persona_manager / plugin_loader | P3 | 惰性单例无锁，并发首调双构建（良性，最终赋值胜出） | 登记即可；要消除则 asyncio.Lock 或 import 急初始化 | 可不修 |
| 09-05 | task_manager.py | P3 | append_result 的 `_result_len` 计数器无消费方 | 删除，或接入轮询端点做真实进度 | ✅ 已修 09-05 |
| 09-05 | error_aggregator.py | P3 | `_RECENT_KEY` 列表无 TTL（有 LTRIM 100 上限，影响极小） | 写入补 `expire(_RECENT_KEY, 7*86400)` | ✅ 已修 09-05 |

审查范围：24 个文件全读；sms.py 未审（主理人交代）。做得好的：quota 原子条件自增+缓存失效、claim_idempotency 原子占位、TRIM_LUA 原子裁剪、semantic_cache 三级+上下文指纹、config fail-loudly、password bcrypt_sha256 迁移、plugin_loader tools.* 信任边界。

> 实施说明（09-05）：semantic_cache 采用"无条件累加"而非日志所列 CASE 抽样——抽样计数正是口径漂移根因，无条件累加语义最直白；filter_text 已整方法删除；agent_rules.py 整文件删除。

## 2026-09-05 第二轮：qwen 主力切换 + 人格分化

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | config.py | P1 | 主力模型切 qwen3.7-flash（百炼端点）后，全项目共用 DEEPSEEK_API_BASE 的直连调用会 404 | 新增 qwen_api_base/qwen_api_key 配置 + llm_endpoint(model, key) 按模型名路由端点与密钥 |
| 09-05 | stream_utils.py | P1 | stream_llm 直连 DEEPSEEK_API_BASE；百炼流式中间块带 usage:null 使 `"usage" in data` 分支 AttributeError | 改走 llm_endpoint 路由；判断改 data.get("usage") |
| 09-05 | safety_filter.py | P2 | 任务状态/工具错误把 str(e) 原样给前端（httpx 异常带完整 URL，高德 key 在 URL 参数里） | 新增 sanitize_error_text：剥 URL 与 sk- Key 形状，空文案给兜底 |
| 09-05 | persona_manager.py | P2 | 默认人格为 unified（旅游+民法典混合体）；人格已分化，混合人格废除 | 删除 prompts/unified.json；默认人格切 travel；前端"旅行模式"按钮发 unified 经 get_effective 回落默认，行为自洽 |

## 2026-09-06：语义缓存写入兜底统一 + 工具分发参数兜底

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-06 | semantic_cache.py | P2 | 缓存写入兜底在 runner/router 各复制一份 _safe_cache_set，chat_stream_ctx 却裸 create_task——三处同型逻辑 | 抽模块级 safe_set，三处调用方统一 |
| 09-06 | stream_utils.py | P2 | dispatch_tool 的 web_search 分支空 query 不回退 user_query（LLM 漏传参搜出空查询） | 补 `or user_query` |

回归：tests/unit/test_agent_logic_fixes.py::test_router_safe_cache_set_resolves_cache（适配 safe_set 新接线）

## 2026-09-07 审查批次修复（第 1 批，配合 app/修复日志.md 实施记录）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | memory_manager.py | P1 | 缓存缺失并发回填裸 rpush，同会话并发冷启动重复写两遍 | 回填改 Lua「列表为空才整批写入」原子脚本 |
| 09-07 | concurrency.py（新工具） | P2 | 裸 create_task 弱引用可被 GC 中途回收 | 新增 spawn()（持引用+异常记日志），core 内 4 处接入 |
| 09-07 | error_aggregator.py | P2 | str(exc) 原文（含带 key 的 URL）入 Redis 7 天 | 入库前过 sanitize_error_text |
| 09-07 | stream_utils.py | P2 | file_ids 数量无上限放大 Redis 往返 | build_file_context 截断上限 5 |
| 09-07 | redis.py | P2 | get_redis 并发首调无双检锁 | asyncio.Lock 双检 |
| 09-07 | config.py | P2 | APP_ENV 裸 os.getenv 与 BaseSettings 双轨 | 纳入 Settings.app_env |
| 09-07 | sms.py | P2 | ttl_minutes 形参被忽略 | TemplateParam 透传 str(ttl_minutes) |

回归：tests/unit/test_20260907_batch_fixes.py（第 1 批 4 用例）

## 2026-09-07 复审轮（主人指示：本轮仅审查登记，不修复不跑测）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | audit.py | P1 | 脱敏键表（password/token/code/secret/api_key）缺 "phone"：routes/phone.py register/bind 的 audit detail 仍落明文手机号——phone.py 注释声称"手机号脱敏入审计与日志"与实际不符（kefa 红线：日志禁打印手机号；审计经 tenant_routes 可查） | audit() 脱敏键表补 "phone"（值 3+4 掩码），一处修全仓受益 | 待修复 |
| 09-07 | sms.py | 待人工确认 | TemplateParam 透传 ttl_minutes 后，阿里云短信模板变量 {min} 需接受任意分钟数（当前唯一调用方 phone.py:121 用默认 5，行为中性） | 阿里云模板侧确认无需改版 | 待确认 |

复审通过项：spawn 工具（持引用/自移除/异常记日志，test_spawn 用例覆盖）；memory_manager 原子回填 Lua（fake_redis EXISTS 对空列表返回 0 与 Redis"空列表自动删除"真实语义一致）；redis 双检锁；db_maintenance 删 recover_stale_processing（"全仓无路径写 processing"声明已核实——该词仅作 Redis 幂等占位，非订单状态）；error_aggregator 入库前脱敏；stream_utils file_ids 上限（与 schemas 同值 5）；config app_env 收编（BaseSettings 字段无 env_prefix，映射 APP_ENV 验证通过）；task_manager/audit 的 spawn 接入。

## 2026-09-08 全目录覆盖轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | plugin_loader.py（152 行） | P2 | 全仓零调用方的死模块（含 get_plugin_loader），而 Dockerfile:43 仍 COPY tools/ 进生产镜像——manifest 加载任意模块的历史风险面虽已收敛（tools.* 前缀），但整套插件机制无人使用 | 随 tools/ 目录一并删除（详见 tools/修复日志.md，删除待主人确认） | 待人工确认 |

## 2026-09-09 全目录审查轮（10 域并行 + 主代理复核）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | semantic_cache.py:172-179 | **P1** | DFA 拦截的安全文案会被写入语义缓存并钉成该 query 的固定答案（60 天）：set() 只挡空值和 DEEPSEEK_FALLBACK_MESSAGE，不挡 sf.safe_message；五处调用点（chat_react 终检/runner/router/chat_fast_paths×2）拦截后照常 finalize_answer(write_cache=True)，同文件 chat_react.py:266 流中拦截已正确 write_cache=False，证明属遗漏非设计；命中计数累加还会躲过 clean_stale_cache | SemanticCache.set 单点加安全文案守卫（对比 get_filter().safe_message 跳过写入），一处修全仓五调用点 | ✅ 已修 09-09 |
| 09-09 | db_maintenance.py:99 + :193 | **P1** | ①分组查询与明细查询之间无事务/锁，并发维护时明细可返回 0 行 → `msg_rows[0]` IndexError → 外层 except 中断当日全部后续维护（画像清理/缓存清理/待结算补偿全跳过），双实例还会各插一条重复摘要；②maintenance_loop 启动即跑、无分布式锁，与"多实例共享"拓扑矛盾（对比 slow_query_watch.py:25 有 Redis 锁） | `if not msg_rows: return 0` 前置守卫 + 循环套 Redis NX 锁（对齐 slow_query_watch 模式） | ✅ 已修 09-09 |
| 09-09 | safety_filter.py:183 | P2 | 拦截词写入全局单例字段 `_last_triggered`，跨请求共享可变状态（人格串号同类形状）；grep 核实 triggered_word 属性当前无外部调用方，属潜伏 | 删除该字段/属性（check_stream 返回值已含该信息） | 待修复 |
| 09-09 | task_manager.py:37 | P2 | 任务取消信号是进程内 dict：任务 ID 在 Redis 全局可见，但 is_cancelled 只查本进程——多实例下取消请求落到别的实例时后台任务白跑完（烧 LLM token，结果不受影响） | 取消标记落 Redis，is_cancelled 改读 Redis | 待修复 |
| 09-09 | memory_manager.py:145-159 | P2 | 滚动摘要压缩"读旧→生成→覆盖写"，两路并发各自基于同一旧摘要生成，后写者覆盖前者溢出内容（原文在 PG，仅摘要完整性受损） | _compress_overflow 走 spawn 串行队列或按 conv_id 加短锁 | 待修复 |
| 09-09 | stream_utils.py:187 | P2 | 文件属主校验 `if username and ...`：username 为空时整体失效（fail-open 默认参数）；当前 4 处调用方均传认证 username，未构成实际 IDOR，但信任边界默认值应 fail-closed | username 必填去默认值，空即跳过注入 | 待修复 |
| 09-09 | semantic_cache.py:83 | P2 | 画像指纹仅取 SHA-256 前 8 hex（32bit）：同人格 ~10^5 用户量级生日碰撞概率≈1，碰撞对之间画像隔离失效（个性化回答互串） | 指纹取 12-16 位 | 待修复 |
| 09-09 | sms.py:68-81 | P2 | 无阿里云密钥时"演示模式"返回 True 并在服务端弹 PowerShell 打印验证码：生产密钥缺失/被清时用户收不到短信但业务按成功流转，无告警区分（公网 fail-open） | 按 app_env 门控演示模式，生产缺配置直接 raise | 待修复 |
| 09-09 | agents/runner.py:176、agents/router.py:400 | P2 | 后台写缓存 safe_set 仍用裸 create_task（safe_set 自吞异常故无未认领异常问题，但弱引用可被 GC 中途回收，chat_stream_ctx.py:225 同款已改 spawn） | 两处改 spawn | 待修复 |

**存量核验**：audit.py:27 脱敏键表缺 "phone"（09-07 登记 P1）**仍在**；plugin_loader.py 死模块（09-08 登记）**仍在**（全仓零调用方）；sms.py TemplateParam 忽略 ttl_minutes **已修**（:101-102 已透传）；本域无豁免清单外 except pass；全部文件 ≤283 行。

## 2026-09-10 修复：会话缓存隔离与 HTTP 日志脱敏

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | semantic_cache.py、chat_stream_ctx.py、chat_stream_core.py | P0 | 语义缓存未按会话隔离，追问可命中别的窗口旧答案 | 首轮保留全局缓存；检测到历史后跳过缓存读取/重建/写入；任务路径缓存上下文加入会话指纹 | 已修 |
| 09-10 | logging.py | P0 | httpx INFO 日志输出完整请求 URL，高德 Key 等 query 凭据被写进日志 | 将 `httpx`/`httpcore` 日志级别压到 WARNING，新增回归测试 | 已修 |

人工动作：本次定位过程中，本地测试日志曾出现高德 API Key。应立即轮换高德 Key，并清理旧容器日志。

## 2026-09-10 第二批修复：任务状态、额度、限流与熔断

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | task_manager.py | P0 | 取消与完成并发时，晚到完成结果可覆盖 cancelled | 新增 finish/fail/cancel 条件写 Lua；仅 pending/generating 可迁移终态 | 已修 |
| 09-10 | quota.py | P0 | 试用额度完成后才自增，并发与断连可绕过 | 新增 reserve_used_questions，执行 LLM 前数据库原子预留并失效用户缓存 | 已修 |
| 09-10 | semantic_cache.py、chat_stream_ctx.py | P0 | 跨窗口共享缓存导致旧答案串入，实时天气也会命中旧缓存 | 缓存上下文加入会话指纹；有历史时跳过缓存 | 已修 |
| 09-10 | rate_limit.py | P1 | 并发槽位是固定 30 秒计数，释放不校验持有者 | Redis Hash token 租约；释放校验 token，长请求后台续租；fail-open 不产生可释放槽位 | 已修 |
| 09-10 | circuit_breaker.py、stream_utils.py、chat_support.py | P1 | 熔断器未接生产链路；成功不重置连续失败；取消探针会放开多探针 | CLOSED 成功清零；HALF_OPEN 取消回 OPEN；统一流式/非流式 LLM 出口接入熔断 | 已修 |

## 2026-09-10 第三批修复：模型配置与跨目录归属说明

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | config.py | P1 | 默认模型仍为过时的 `deepseek-v4-flash`，与当前 DeepSeek 可用模型名和部署示例不一致，可能导致上游 400 | 默认主力与降级模型统一改为 `deepseek-flash`，并同步测试与部署示例 | 已修 |

归档说明：`rate_limit.py`、`circuit_breaker.py`、`auth.py` 位于 `app/middleware`，详细问题与验证已在 `app/middleware/修复日志.md` 归档；本目录保留跨域历史记录不删除。

## 2026-09-10 复查修复：流式主路径计量补 include_usage

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | stream_utils.py:55-63 | **P1** | `stream_llm` 请求体缺 `stream_options.include_usage`——OpenAI 兼容流式接口默认不回 usage 尾块，v2_chat/_stream_react 与任务 ReAct 的真实计量扣费（record_token_usage/bill_token_usage）、以及本日新增的"取消时按 usage 结算"（react_steps._consume_llm_stream）在主路径全部拿不到用量，恒按 0 处理。旁证：两条 httpx 直连降级路径（chat_fallback.py:67、react_steps.py:238）均已显式声明该参数，唯独共享主路径漏配；单测替身 mock 掉 stream_llm 故全绿未拦截 | payload 增加 `stream_options: {"include_usage": True}`；新增 tests/unit/test_stream_usage_flag.py（伪造 SSE 客户端断言 payload 带该参数且 usage 尾块透传为 usage 事件） | 已修 |

P2 登记（本轮复查，不修）：①流式任务取消响应性——旧代码逐事件检查取消可立即断流，新代码在 usage 尾块才检查（与"取消前先结算 usage"绑定，状态经 CAS 即时翻转，仅上游流多跑一段）；②refund.py 幂等键在结算落库后写 Redis 失败的极端窗口存在重放重退可能——订单状态机与部分退款累计防超额已兜底上限。

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | audit.py | P1 | 脱敏键表缺 "phone"：register/bind 的审计 detail 落明文手机号（09-07 登记） | 脱敏键表补 phone（3+4 掩码，短值全掩码） | 已修 |
| 09-10 | safety_filter.py | P2 | `_last_triggered` 写全局单例字段跨请求共享（09-09 登记），triggered_word 属性无外部调用方 | 删除字段与属性（check_stream 返回值已含 triggered_word；单测同步改用返回值） | 已修 |
| 09-10 | stream_utils.py | P2 | build_file_context 属主校验 `if username and ...` fail-open（09-09 登记） | 改 fail-closed：username 为空整体跳过注入 | 已修 |
| 09-10 | semantic_cache.py | P2 | 画像指纹仅 8 hex/32bit，~10^5 用户量级生日碰撞概率≈1（09-09 登记） | 画像指纹对齐会话指纹升至 16 hex（旧缓存条目自然过期，无兼容负担） | 已修 |
| 09-10 | sms.py + routes/phone.py | P2 | 无阿里云密钥时演示模式恒开：生产缺配置用户收不到短信但业务按成功流转（09-09 登记） | 演示模式按 APP_ENV 门控（test/dev/local/development），生产缺密钥返回 False；send-code 路由同步改 503 明确失败，不再谎报"已发送（演示模式）"。注意：本机演示模式需显式 APP_ENV=dev | 已修 |
| 09-10 | memory_manager.py | P2 | 滚动摘要"读旧→生成→覆盖写"并发各自基于同一旧摘要，后写者覆盖前者溢出内容（09-09 登记） | 按会话 _summary_locks 串行化（排队等锁后重读最新摘要，两批溢出都不丢）；ponytail 注明多实例间仍 last-writer-wins | 已修 |
| 09-10 | db_maintenance.py | P1 复核 | "压缩摘要下一轮被删"（09-05 登记） | 复核确认此前已修：分组查询/单条删除/明细查询三处均带 role != 'system' 排除，日志滞后 | 已修（核实） |

**维持登记**：流式取消响应性取舍与 refund 幂等极端窗口（见 09-10 复查条目）。


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | sms.py | P2 | 演示模式 Popen 把 phone/code 内插 PowerShell 命令串（调用点已白名单+仅 test/dev 可达，函数自身零防护属埋雷）；:138 管理员通知日志落明文 ADMIN_PHONE | 登记：删 Popen 或加数字白名单；日志改 _mask_phone | 登记 |
| 09-11 | audit.py | P2 | 脱敏为精确键名匹配，token变体/嵌套 dict 不脱敏 | 登记：改子串匹配+一层递归（需先枚举调用方 detail 键名确认无实害） | 登记 |
| 09-11 | circuit_breaker.py | P2 | HALF_OPEN 单探针无身份校验：CLOSED 期在途调用被取消会把 HALF_OPEN 翻回 OPEN，与原探针并存双触达 | 登记：探针持唯一 token，回迁仅持有者可发起 | 登记 |
| 09-11 | rate_limit.py | P2 | 并发租约过期判定用客户端 time.time()（QPS 窗口用 Redis TIME），多实例时钟漂移时租约提前逐出/滞留；TTL 120 硬编码 | 登记：过期判定移入 Lua 用 Redis TIME | 登记 |
| 09-11 | task_manager.py:95-102 | P2 | update_status 无 pending/generating 前置校验，是绕过终态 CAS 的旁路（当前调用方仅合法迁移） | 登记：函数内白名单 status 或删除改走 CAS | 登记 |
| 09-11 | task_manager.py:37,285 | P2 | _cancel_events 全局 dict：跨实例取消时条目在非生成实例永不清理（缓慢泄漏） | 登记：取消事件改 Redis-only 或加 TTL/清扫 | 登记 |
| 09-11 | semantic_cache.py:155 | P2 | 注释"回填 L0+L1"与实现（仅 L0）矛盾 | 登记：改注释 | 登记 |
| 09-11 | safety_filter/place_extract | P2 | SAFETY_FILTER_MESSAGE/WORDS_FILE、DEFAULT_CITY 裸 getenv 未收编 config（非密钥） | 登记：并入 _Settings | 登记 |
| 09-11 | quota.py | P2 | inc_used_questions 零调用方死代码（与 reserve_used_questions 双轨并存，形状同"计量旁路"事故） | 登记：删除（连同其单测） | 登记 |


## 2026-09-12 凌晨清欠轮（09-11 登记 P2 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | sms.py | P2 | 演示模式 Popen 命令内插埋雷 + 管理员通知日志明文手机号（09-11 登记） | 删 Popen 弹窗只留日志；通知日志改 _mask_phone | 已修 |
| 09-12 | audit.py | P2 | 脱敏精确键名匹配，变体键/嵌套 dict 漏脱敏（09-11 登记） | 改子串匹配（password/token/code/secret/api_key/credential）+ 递归一层；phone 3+4 掩码保留 | 已修 |
| 09-12 | circuit_breaker.py | P2 | HALF_OPEN 探针无身份：旁观者取消会翻回 OPEN 致双探针（09-11 登记） | 探针持 uuid token：_cancel_probe/_record_success/_record_failure 仅持有者生效 | 已修 |
| 09-12 | rate_limit.py | P2 | 并发租约过期判定用客户端 time.time()；TTL 120 硬编码（09-11 登记） | 获取/续期 Lua 均改 Redis 服务端 TIME；LEASE_TTL_SECONDS 常量收编；假 Redis 测试替身同步 | 已修 |
| 09-12 | task_manager.py | P2 | update_status 是终态 CAS 旁路；_cancel_events 跨实例泄漏（09-11 登记） | update_status 白名单 pending/generating（终态走 finish/fail/cancel）；request_cancel 不再写本进程 Event（跨实例信号纯走 Redis 标记，泄漏源消除） | 已修 |
| 09-12 | semantic_cache.py | P2 | 注释"回填 L0+L1"与实现矛盾（09-11 登记） | 注释改正 | 已修 |
| 09-12 | safety_filter/place_extract | P2 | SAFETY_FILTER_MESSAGE/WORDS_FILE、DEFAULT_CITY 裸 getenv（09-11 登记） | 收编 _Settings 并导出；调用方改引 config | 已修 |
| 09-12 | quota.py | P2 | inc_used_questions 零调用方死代码（09-11 登记） | 连同其单测删除（reserve_used_questions 为唯一计量入口） | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | semantic_cache.py:74、services/chat_stream_ctx.py:192 | P0 | 缓存上下文只含人格、画像指纹和调用方可控的会话指纹，不含不可变用户标识；画像相同的两个账号复用 conversation_id 时可命中彼此缓存 | 将 username 或不可变 user_id 加入缓存上下文，并覆盖 L0/L1/L2 键与重建锁 | 待修复 |
| 09-11 | task_manager.py:95 | P0 | update_status 仅限制目标状态为 pending/generating，没有校验当前状态；任务取消后仍可被写回 generating，取消失效并继续消耗 token | 使用 Lua 按当前状态执行合法迁移，终态不可回到 active | 修复不完整 |
| 09-11 | quota.py:60、services/agent_tasks.py:131 | P1 | 试用额度先持久化扣减，随后才申请后台槽位；槽位满返回 429 时额度不回滚 | 先占槽位再预留额度，或将两个资源纳入同一幂等补偿边界 | 待修复 |
| 09-11 | task_manager.py:73 | P1 | get_task 的 timeout 只写返回对象，不回写 Redis；后台任务仍会继续运行并可能晚到写 completed | 增加 CAS 超时终结和生成实例取消信号，超时应成为真实终态 | 待修复 |
| 09-11 | task_manager.py:293 | P1 | request_cancel 写 Redis 失败只记 warning，不返回失败且无本地兜底；取消接口可能被当作成功但任务继续执行 | 将写入结果返回给路由，失败时明确报错；保留跨实例取消语义 | 待修复 |
| 09-11 | main.py:156、core/concurrency.py:34 | P1 | 关闭流程只等待手工句柄，spawn 创建的缓存、审计、计费和续期任务未统一排空；单个异常还可能中断后续资源关闭 | 新增全局任务注册表和带超时的 shutdown，再关闭 Redis/PG | 待修复 |
| 09-11 | safety_filter.py:113、157、206 | P1 | contains_sensitive/find_first 只扫描前 5000 字符；安全上下文按整句子串豁免，长文本尾部及“法律词+危险指令”仍可绕过 | 分段扫描完整输出，流式路径使用滑动窗口；白名单改为严格模板匹配 | 待修复 |
| 09-11 | stream_utils.py:118、services/chat_stream_ctx.py:143 | P1 | 上传文件只做固定正则替换后拼入 system 提示词，改写表达可绕过提示词注入防护 | 文件内容按不可信 user/tool 数据传递，正则只作辅助 | 待修复 |
| 09-11 | audit.py:29、54 | P2 | _scrub 不递归列表，列表内敏感值可进入审计；审计 spawn 无界，无背压 | 递归序列类型脱敏；采用有界队列、批量写入和丢弃告警 | 登记 |
| 09-11 | config.py:155、main.py:193 | P2 | 密钥只校验非空，不校验长度和熵；main.py 使用裸 APP_ENV，与 config 的配置源不一致 | 密钥增加最小长度/占位符检查；统一读取 config.APP_ENV | 登记 |


## 2026-09-12 外部复核修复轮（P0×2 + P1×5 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | semantic_cache.py:74 + chat_stream_ctx:192 | **P0** | 缓存上下文不含不可变用户标识——画像相同的两账号复用 conversation_id 互串缓存 | build_cache_ctx 加 username SHA-256 指纹为首位分量，runner/chat_stream_ctx 同步传参 | 已修 |
| 09-12 | task_manager.py:95 | **P0** | update_status 只校验目标态不校验当前态——取消后可写回 generating | 改 Lua 当前态 CAS：仅 pending→generating，终态拒绝回 active | 已修 |
| 09-12 | task_manager.py:73 | P1 | get_task 的 timeout 只写返回对象不回写 Redis | 登记：需 CAS 超时终结+生成实例取消信号（涉跨实例语义，单独批次） | 登记 |
| 09-12 | task_manager.py:293 | P1 | request_cancel 写 Redis 失败只告警，取消被当成功 | 登记：返回写入结果给路由（涉接口契约变更，单独批次） | 登记 |
| 09-12 | main.py:156、concurrency.py:34 | P1 | spawn 任务未统一排空，关闭期丢审计/缓存/计费 | 登记：全局任务注册表+带超时 shutdown（架构级，单独批次） | 登记 |
| 09-12 | safety_filter.py:113,157,206 | P1 | 只扫前 5000 字符+整句子串豁免可绕过 | 登记：分段扫描+严格模板匹配（安全语义重设计，单独批次） | 登记 |
| 09-12 | stream_utils.py:118 | P1 | 上传文件固定正则替换非完整注入边界 | 登记：按不可信数据传递（架构级，单独批次） | 登记 |
| 09-12 | quota.py:60、agent_tasks.py:131 | P1 | 额度先扣后槽位满不回滚 | 已在 services 轮修复（rollback_used_questions） | 已修 |
| 09-12 | audit.py:29,54 | P2 | _scrub 不递归列表；spawn 无界 | 登记（子串+递归已覆盖主键，列表项为低频） | 登记 |
| 09-12 | config.py:155 | P2 | 密钥只校验非空不校验长度熵；裸 APP_ENV | 登记：最小长度+config.APP_ENV 统一 | 登记 |

## 2026-09-12 外部复核批次 B/C 追加（工具参数守卫 / 文件元数据守卫）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | stream_utils.py:158 | P2 | dispatch_tool 对 LLM 产出的非对象参数（数组/字符串）args.get 直接 AttributeError，react/router/fast_paths/chat_react 4 条调用链全暴露 | 共享漏斗 isinstance 守卫：非 dict 一律按缺参走 auto_tool_args 兜底；回归单测 | 已修 |
| 09-12 | stream_utils.py:197 | P2 | build_file_context 对损坏 file meta 的 json.loads 无防护，单条坏数据炸整个聊天请求 | 损坏/非对象 meta 按无文件跳过（与 file_upload.get_user_file 404 守卫同款语义）；回归单测 | 已修 |

## 2026-09-14 全量只读审计：核心状态与异步边界

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-14 | task_manager.py:236-261 | P1 | `claim_idempotency` 等待 1 秒超时后返回“未占位” | 创建方卡顿时仍可并存第二个任务 | 超时返回冲突/处理中状态，或延长等待并续约 | ✅ 已修：超时返回 `CLAIM_IN_PROGRESS` 哨兵；agent_tasks 两个调用方映射 409"相同请求正在创建中"（Redis 异常仍返回 None 尽力而为）；回归 test_claim_idempotency_timeout_returns_in_progress |
| 09-14 | memory_manager.py:18-24 | P2 | `_summary_locks` 进程内字典无清理 | 会话数增长形成慢性内存占用 | 有界锁池或按完成时间回收 | ✅ 已修：`_KeyedLock` 引用计数（refs>0 不回收，最后一位使用者退出即从字典移除；取锁与计数间无 await，事件循环内原子）；回归 test_summary_lock_reclaimed_after_last_user |
| 09-14 | quota.py:83-85 | P1 | DB 异常与额度耗尽共用 `False` | 用户看到 402，故障被误判为业务额度 | 引入结构化错误类型并映射 503 | ✅ 已修：新增 `QuotaDependencyError`，`reserve_used_questions` DB 故障抛出（fail-closed 不变）；chat_stream_ctx/agent_tasks 三个调用方映射 503；回归 test_reserve_quota_dependency_error |
| 09-14 | concurrency.py:34-46 | P1 | `spawn` 只在 done callback 记日志，无全局排空 | 进程关闭时审计、缓存、计费任务可能未完成 | 全局任务注册表 + 超时 shutdown drain | ✅ 已修：`drain_background_tasks(timeout)`（宽限期优雅等待→超时取消→2s 收尾）挂入 main.py lifespan 关闭序，位于 Redis/PG 连接池关闭之前（审计/缓存/计费收尾依赖连接可用）；回归 test_drain_background_tasks_waits_and_cancels |

### 附：memory_manager.py:182-202 冷备重试（服务日志登记项，文件在本目录）

PG 冷备写失败原先只记 warning——Redis 热缓存 TTL 到期后该轮长期记忆永久丢失。已补有限重试（共 3 次，1s/2s 退避，spawn 任务内 sleep 不阻塞主流程）。ponytail: 仍非持久 outbox——进程在重试窗口内崩溃仍会丢，彻底解决需落盘 outbox + 独立投递循环（维持登记，专项批次）。

## 2026-09-15 修复实施结果

- `task_manager` 超时 CAS 失败后重读真实终态；终态幂等映射用 Lua 原子替换。
- `memory_manager` 摘要成功后才裁剪热历史，画像更新失败不再重放记忆写入。
- 画像 JSONB 兼容字符串读回；连接池 acquire 全量增加超时。
- 维护循环单次 Redis 异常不再终止后台协程。
- `audit` 列表对象递归脱敏；超大单个流式块按窗口检查敏感词。

### 2026-09-15 未覆盖文件逐行复核

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `app/routes/__init__.py` | P1 | 包导入即加载 v1/v2/map_api，控制台进程会被业务路由依赖链拖入 | 已修：移除包级 eager import |
| `app/core/db.py` | P1 | 多进程默认连接池 min=20/max=50，连接数按进程线性放大；缺少 SQL 命令超时 | 已修：默认降为 min=2/max=10，增加 `db_command_timeout`，close_pool 用 finally 清引用 |
| `app/core/password.py` | P2 | 损坏哈希抛异常导致登录 500；legacy 判断与升级 SQL 口径不一致 | 已修：UnknownHashError 转 False，`pwd_context.identify()` 判定 legacy |
| `app/core/jfast.py` | P2 | orjson 分支静默忽略 stdlib 参数，行为随环境漂移 | 已修：传入兼容参数时回退 stdlib |
| `app/core/readiness.py` | P2 | 泄露依赖异常类型，DB/Redis 串行探测 | 已修：日志内部记录，返回稳定 error；两项并行 |
| `app/core/security_headers.py` | P2 | setdefault 可被下游覆盖安全头 | 已修：安全头强制覆盖 |
| `app/core/auth_cookies.py` | P2 | X-Forwarded-Proto 仅接受单值 https | 已修：支持逗号分隔代理链中存在 https |
| `app/core/otel.py` | P2 | OTEL 导入失败无原因日志 | 已修：记录异常类型 |
- 独立复核补修：维护步骤逐个隔离；摘要锁等待取消时归还引用计数；记忆写入增加
  `message_uid` 幂等键，避免提交确认丢失后的整批重试重复插入。

## 2026-09-15 Qwen 思考模式导致首包延迟

| 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|
| `config.py`、`stream_utils.py` 等 | P1 | `qwen3.7-flash` 默认输出大段 `reasoning_content`，简单民法典问题实测 657 个 reasoning tokens、约 9.5 秒后才输出正文；线上请求出现 20 秒以上首包 | 新增 `LLM_ENABLE_THINKING=0` 配置和统一请求选项函数，qwen 对话、分类、摘要、地图等调用默认关闭思考 | 已修 |
| `chat_fast_paths.py` | P2 | 快速通道只有总首包，无法区分检索与模型耗时 | 新增 `generation` span，记录模型耗时、思考字符数和正文长度 | 已修 |

实测结果：

- 同一问题关闭思考前：首个正文约 `9569ms`，reasoning 2293 字。
- 关闭思考后直连：首个正文约 `462~787ms`，总耗时约 `0.9~1.1s`。
- 网关端到端热态：首个正文约 `2120~2508ms`，总耗时约 `2.1~2.5s`。
- 网关重启后首个请求约 `7.8s`，其中约 `5.3s` 是本地 CrossEncoder 冷加载，
  不是每次都发生的稳定延迟。
