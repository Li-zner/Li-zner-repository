# 修复日志 — app/cdc

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-04 | worker.py | P1 | cdc_events 无限增长（#15 只删已处理事件，表本体仍累积） | 按月 RANGE 分区 + worker 分区维护：补建未来分区 + 仅 DROP max(id)<=处理水位的过期分区（安全网，绝不丢未归档审计数据）；DEFAULT 分区只告警 |

迁移：alembic/versions/b1c2d3e4f5a6_cdc_events_monthly_partition.py（单事务原子重建，真实 PG 回环实测）
回归测试：tests/unit/test_cdc_partitioning.py

## 2026-09-05 审查轮：复审结论

5 个文件全量通读，**未发现新问题**。worker（leader 锁 token 校验/续期失败即退选、at-least-once 落盘、分区白名单正则防拼接、DROP 前 max(id)<=水位不变量、DEFAULT 分区只告警）、journal（跨天原子切换、fsync 批量、checkpoint 单调保护+临时文件原子替换、滚动序号防增殖注释清晰）、routes（admin 门禁+refresh_checkpoint 防冻结）、schema（fail loudly 提示跑迁移）——设计与实现一致，质量最高的一册。

## 2026-09-09 全目录审查轮

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | worker.py:34 + routes.py:58-62 | P2 | 已处理事件 60s 即删：/api/cdc/events 的 after 游标外部消费者超 60s 不拉取即永久断流（主消费者 JSONL journal at-least-once 不受影响，属设计取舍） | API 文档标明消费窗口，或 CLEANUP_EVERY 对外可配并调大 | 登记 |

**过检**：at-least-once + id 去重、leader 锁 token 校验 + 续期失败退选、句柄持锁 close、DROP 前水位不变量、分区名白名单正则 ✅。

## 2026-09-10 修复：提交顺序水位与 durable journal

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | alembic/versions/a8b9c0d1e2f3_cdc_events_txid.py、worker.py | P0 | BIGSERIAL 按 id 推进会漏掉后提交的较小 id | cdc_events 增加 txid；触发器和 worker 改按 `(txid,id)` 消费，并等待事务低于快照 xmin | 已修 |
| 09-10 | journal.py、worker.py | P0 | journal 缓冲未 fsync 就推进 checkpoint/cleanup | 新增 append_batch：整批写入并 fsync 后才推进水位 | 已修 |
| 09-10 | scripts/cdc_acceptance.py | P1 | 验收基线使用 MAX(id)，历史 journal 会被误算 | 基线改序列 last_value；主流程拆分满足结构门禁 | 已修 |

## 2026-09-10 第二批修复：journal 权限、checkpoint 水位与保留策略

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | journal.py | P1 | journal 文件和目录使用默认权限，同机其他用户可能读取 CDC 业务数据；旧文件长期累积无清理策略 | journal 文件权限收紧为 0600、目录为 0700；新增按 CDC_JOURNAL_RETENTION_DAYS 清理旧文件 | 已修 |
| 09-10 | journal.py、worker.py | P1 | checkpoint 只保存 last_id，重启或跨事务恢复时无法按事务顺序继续；批次落盘与水位推进边界不够明确 | checkpoint 同时保存 last_txid；批次写入并 fsync 成功后才推进 last_id/last_txid | 已修 |

验收：CDC 1000 条变更 3.02 秒全部落盘，A1/A2/A3/A4/A6 全部通过。

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | worker.py:243 | P0 | 过期分区仅用 MAX(id) <= last_id 判定可删，但消费顺序是 (txid,id)；低 id、高 txid 的未处理事件可能随分区一起被 DROP | 删除前校验分区内不存在大于 (last_txid,last_id) 的事件 | 待修复 |
| 09-11 | journal.py:63、183、258 | P0 | last_id 与 last_txid 分两次读取文件，并发替换 checkpoint 时可交叉得到新 txid + 旧 id 的混合水位，跳过事件 | 一次读取同一 JSON，原子返回水位对；初始化、刷新、保存比较复用该函数 | 待修复 |
| 09-11 | journal.py:183、218、worker.py:325 | P0 | checkpoint 的读比较写不是跨进程原子；所有实例共用固定 .tmp 文件，非 leader 退出也会写 checkpoint | 仅 leader 可写；使用唯一临时文件加工件锁或 Redis CAS | 待修复 |
| 09-11 | routes.py:48 | P1 | 事件 API 仍使用 id > after，没有 txid 游标和快照条件，外部消费者会漏掉晚提交的小 id 事件 | 返回 txid，游标改为 (txid,id)，应用 worker 同款可见性条件 | 待修复 |
| 09-11 | schema.py:23 | P1 | 启动校验只检查表、函数和触发器名称，不检查 txid 列、分区结构和触发器函数版本 | 校验关键列、非空约束、分区键和迁移版本，旧 schema fail loudly | 待修复 |


## 2026-09-12 外部复核修复轮（P0×3 全修 + P1×2 修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | worker.py:243 | **P0** | 过期分区 DROP 仅用 MAX(id) <= last_id 判定，"低 id 高 txid"未处理事件随分区丢失 | DROP 前按 (txid,id) 完整游标 EXISTS 校验未处理事件 | 已修 |
| 09-12 | journal.py:63,183,258 | **P0** | last_id 与 last_txid 分两次读文件，并发 replace 可交叉出"新 txid+旧 id"混合水位跳过事件 | 新增 load_checkpoint_pair 单次读取原子返回水位对；init/refresh/save 比较统一复用 | 已修 |
| 09-12 | journal.py:183,218、worker.py:325 | **P0** | checkpoint 读比较写非跨进程原子；固定 .tmp 多实例互覆；非 leader 退出也写 checkpoint | tmp 文件加 pid 唯一化；run/stop 落盘前校验 _lock_ok（仅 leader 写） | 已修 |
| 09-12 | routes.py:48 | P1 | 事件 API 用 id > after 游标漏晚提交小 id 事件 | 登记：外部消费者 API 返回 txid + (txid,id) 游标（涉 API 契约变更，单独批次） | ✅ 已修 09-14：events 接口新增 after_txid/after_id 复合游标（与 worker 同款 txid<xmin 可见性条件），响应带 txid 与 next cursor；旧 after 参数兼容保留并在文档标明断流窗口 |
| 09-12 | schema.py:23 | P1 | 启动校验不查 txid 列/分区结构/触发器函数版本 | 登记：校验关键列与迁移版本（涉启动语义，单独批次） | ✅ 已修 09-14：ensure_schema 补三查（txid 列存在 / cdc_events 为 RANGE 分区 / cdc_capture 函数源含 txid_current），旧 schema fail loudly 指向 alembic upgrade head |

## 2026-09-15 修复实施结果

- CDC leader 锁增加独立续期任务，长批次清理和分区维护期间不会静默过期。
- 续期失败立即停止消费并重新竞选，避免双写窗口。
- 验证：后端全量 `348 passed`；CDC 相关迁移链通过。

## 2026-09-15 Batch 1 本地单实例

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-15 | schema.py:60 | P1 | `asyncpg` 将 PostgreSQL `partstrat` 的内部 `"char"` 返回为 `bytes`，代码却与字符串 `"r"` 比较，导致已是 RANGE 分区表的数据库仍被判定为旧 schema，CDC worker 永久重试 | 比较前统一将 `bytes` 解码为字符串 | 已修 |

验证：`ensure_schema` 对本地 PostgreSQL 直连通过；`tests/unit/test_cdc_partitioning.py` 4 passed；单实例网关启动后 CDC worker 成功成为 leader，原先每 5 秒重复的 schema 警告消失。
