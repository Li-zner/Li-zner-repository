# 修复日志（原仓库根：基础设施文件 + 全目录覆盖登记）

> 2026-09-26 自仓库根迁入 `agent_docs/`；旧记录中的「根日志」「根目录修复日志」均指本文件。

## 2026-09-08 全目录覆盖轮（主人要求：本目录所有文件夹逐个审完；仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | alertmanager.yml:15 | **P0** | 真实 QQ 邮箱 SMTP 授权码硬编码且被 git 追踪——基线提交 a80159d"含密钥清洗"漏洗，授权码已进 git 历史（git ls-files + git log 双重核实） | ✅ 已处置 09-08：**主人已在 QQ 邮箱后台作废旧授权码（止损核心完成）**；新授权码写入权威 .env（SMTP_AUTH_PASSWORD，不入库）；alertmanager.yml 改 `auth_password_file` 读挂载文件，compose 挂 ./monitoring/smtp_auth_password，start_all.sh 启动前自动同步；git grep 确认新码零入库、旧码工作区零残留。遗留（可选）：旧码仍在 git 历史，已作废故风险解除；若公开仓前想彻底洗净可用 git filter-repo（破坏性，待拍板） | ✅ 已处置 |
| 09-08 | prometheus.yml:9-16 | P1 | 应用层 /metrics 已 fail-closed（未配 METRICS_TOKEN 即 403），但抓取配置无 authorization 段、compose 也未配 METRICS_TOKEN——监控抓取 100% 失败 | ✅ 已修 09-08：prometheus.yml 加全局 authorization（credentials_file）；compose 给 prometheus 挂 ./monitoring/metrics_token；start_all.sh 启动前从 ../.env 再生成该文件；METRICS_TOKEN 已生成写入 ../.env 并同步凭据文件（值不落库，monitoring/ 已 gitignore）；.env.example 补条目说明 | ✅ 已修 |
| 09-08 | alert.rules.yml:100-104 | P2 | GatewayInstanceDown 在全部实例宕机（最需告警时）反而不触发：目标全 down 后 rate() 为空向量，`== 0` 不成立 | ✅ 已修 09-08（主人点名）：改 `max(up{job="gateway"}) == 0 or absent(up{job="gateway"})`，全部宕机或 job 缺失均触发；YAML 解析验证通过 | ✅ 已修 |
| 09-08 | docker-compose.yml | P2 | 4 个网关直连端口与监控端口（9090/13101/13100/3200/9093 等）全绑 0.0.0.0，直连绕过 nginx WAF/限流 | 公网部署前统一加 127.0.0.1: 前缀 | 待修复 |
| 09-08 | .env.example | P2 | 与 config.py 双向脱节：DIFY_API_KEY/DIFY_API_URL/OLLAMA_URL/PORT/TIMEOUT_* 全仓无消费点；缺 REDIS_URL/METRICS_TOKEN/EMBEDDING_API_URL/OTEL_EXPORTER_OTLP_ENDPOINT 等实际读取项 | 删死项、补活项 | 待修复 |
| 09-08 | Dockerfile:44 | P2 | `COPY tests/ ./tests/` 把评测产物、带硬编码密码的集成测试全部打进生产镜像 | 只 COPY 运行所需 | 待修复 |
| 09-08 | nginx.conf:16 | P3 | WAF 正则误杀正常文本：提问含 javascript:/alert(/onload= 即 403（lite 版同） | 收窄特征或仅匹配路径 | 登记 |
| 09-08 | Dockerfile | P3 | 容器 root 运行（无 USER）；`npm install` 应 `npm ci`（有 lockfile）；deploy.sh 仅 set -e | 非 root 用户；npm ci；对齐 set -euo pipefail | 登记 |
| 09-08 | requirements.txt:20-24 | P3 | asyncpg/authlib/itsdangerous/orjson/pyyaml/PyMuPDF/pytesseract 未钉版本 | 全部钉版本 | 登记 |
| 09-08 | tests/unit（L5 视角） | P3 | 迁移链零测试：无"空库 upgrade head → downgrade base"冒烟 | 加集成测试 | 登记 |

通过项：_env.py、ci.sh、start_all.sh、gitpush_daily.cmd、tempo-config.yaml、pytest.ini、.dockerignore、requirements-dev.txt、alembic/env.py（URL 走环境变量不落 ini、无密钥打印）；docker-compose.lite.yml 与 nginx.conf.lite 复核无新漏项。

## 全目录覆盖表（2026-09-08 轮次后，本仓库每个顶层目录的审查状态）

| 目录/文件 | 性质 | 审查状态与结论 |
|---|---|---|
| app/ | 后端源码 | 09-04~09-07 五轮 + 09-07 复审轮覆盖；本轮补登记 core/plugin_loader.py 死代码（见 app/core 日志） |
| alembic/ | 数据库迁移 | 本轮全审（见 alembic/versions/修复日志.md）：单链无分叉，1 项 P1 downgrade 不成对 |
| .github/ | CI 工作流 | 本轮审查：P3 权限收敛（见 .github/修复日志.md） |
| deploy/ | 部署 | 本轮追加：1 项 P1（.server.env）+ 4 项 P2（见 deploy 日志） |
| scripts/ | 运维脚本 | 本轮全审 105 个代码文件（见 scripts 日志）：P1×2、P2×7 |
| tests/ | 测试 | 本轮全审（见 tests 日志）：收集期黑名单 4 文件等 |
| frontend/ | 前端源码 | 第 6 批 + 本轮全量：无新 P0/P1/P2，P3×11（见 frontend 日志） |
| mcp_assets/ | MCP 资产 | 快照轮 + 本轮余下 4 脚本通过 |
| static/ | 前端构建产物落点 | 本轮轻审通过：sw.js 只缓存 /assets/* 与离线壳，API/SSE 显式放行不缓存；与 frontend/ 是同一前端（非双前端）。问题仅在 assets 未入 git → 登记于 deploy 日志（Dockerfile.lite） |
| tools/ | 插件壳 | **死代码**：两子包仅空 __init__ + manifest，唯一潜在消费者 plugin_loader.py 本身零调用方（见 tools/修复日志.md） |
| langgraph_lab/ | 学习实验 | roundtable.py 单文件通过（全 mock、守卫齐全） |
| prompts/ | 人格提示词 | 合法 JSON 无密钥；me.json 含真实手机号待人工确认（见 prompts/修复日志.md） |
| docs/ | 文档 | 纯 md；全仓密钥扫描未见命中，不适用代码审查 |
| code review/、.wiki/、可复用资产/、.claude/、.cursor/、.vscode/、.zcode/ | 资料/工具配置 | 非产品代码，不适用 5 层审查 |
| .venv/、.git/、__pycache__/、.pytest_cache/、.tmp/、logs/、backups/、uploads/、data/、etc/、cdc_journal/、tempo_data/、logs 散文件 | 运行时/产物 | 不适用代码审查。核实：etc/（内含真实密钥）从未进 git；data/ 被追踪但均为知识库 md 无敏感；logs/ 与 .tmp 未追踪也未忽略（建议补 .gitignore，防止未来误 add） |
| 根散文件：Bug日志.md、推送规则.md、README.md、AGENTS.md、autopush.log 等 | 文档/日志 | 密钥扫描干净；三个 .log 均已 gitignore |

## 2026-09-09 全目录审查轮（规则：可复用资产/SOP/Review规则.md；10 域并行审查 + 主代理逐条复核；P0/P1 当轮修复）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | prometheus.yml:7 | **P0** | 09-08 修复批把 `authorization` 写进 `global:` 段——该字段只属于 scrape_config，Prometheus 严格 YAML 解析直接失败；**实测容器 crash-loop 27 小时**（Restarting (2)，`field authorization not found in type config.plain`），11 条告警规则/抓取/Grafana 数据源全灭——修监控的改动自己杀死了 Prometheus | authorization 块移入 gateway 抓取作业；promtool check config 验证后 restart 验证 Up | ✅ 已修 09-09 |
| 09-09 | prometheus.yml | P1 | 缺 `alerting:` 段：alert.rules.yml 11 条规则只在本机评估，永不送达 alertmanager（9093 空转），SMTP 告警链形同虚设 | 补 `alerting.alertmanagers → alertmanager:9093` | ✅ 已修 09-09 |
| 09-09 | start_all.sh:26-29 + .env.example:27 | P1 | 凭据来源错位：脚本读 `../.env`（权威 .env 在仓库上一级），.env.example 却写"从本文件同值生成"；键缺失 `|| true` 吞错静默写空文件；且 SMTP 授权码为空时告警邮件根本发不出，注释声称的"以告警断线暴露"不成立 | 凭据文件为空时 stderr 醒目 WARN（不阻断启动）；.env.example 注释改如实描述 | ✅ 已修 09-09 |
| 09-09 | gitpush_daily.cmd | P1 | 每日无人审 add+commit+push，不经过 secret_scan（旧 SMTP 授权码当年正是经自动推送入库） | push 前强制 secret_scan，命中即 exit /b 1 | ✅ 已修 09-09 |
| 09-09 | deploy/deploy_aliyun.sh:39-46 | P1 | 部署末段健康检查失败仅 `\|\| echo` 兜底，退出码 0——部署失败被当成功（绕过前段 set -euo pipefail） | 循环后复查，仍不健康 exit 1 | ✅ 已修 09-09 |
| 09-09 | deploy/修复日志.md:21 | P1 | 本仓库 git 追踪的修复日志自身公开披露真实服务器 IP + root 用户名 + "仅密码登录"事实（deploy/.server.env 未追踪但 root 口令明文在工作区） | 主人行动：改 SSH 密钥登录 + sshd 禁密码 + 轮换口令 + 历史日志脱敏 | 待主人拍板 |
| 09-09 | docker-compose.yml:114-128,241 | P2 | prometheus_data 卷已声明但 prometheus 服务从未挂载——容器重建历史指标全丢（postgres/grafana/tempo 都有卷） | 补 `- prometheus_data:/prometheus` | 待修复 |
| 09-09 | docker-compose.yml:149-171 | P2 | loki/promtail 是唯二无 `restart: unless-stopped` 的服务；loki 无持久卷、promtail positions 落 /tmp——宿主机重启日志栈静默下线 | 补 restart 策略 + loki 数据卷 | 待修复 |
| 09-09 | deploy/.env.lite:11,23 | P2 | 真实 DeepSeek `sk-` 与高德 key 明文于工作区（核实未入库、git 历史零命中） | 主人确认后轮换 | 待主人拍板 |
| 09-09 | nginx.conf:39 | P2 | 公网入口仅 `listen 80`，无 TLS/HSTS，会话凭据明文过网（deploy_aliyun.sh 注释自认"如需 HTTPS 再配"） | 入口套证书终结或 tunnel | 待修复（需证书资源） |
| 09-09 | nginx.conf:98-123 | P2 | 全量版缺 `/metrics`、`/test-otel` 的 404 屏蔽（lite 版 nginx.conf.lite:74-81 有）——纵深防御不一致，现仅靠应用层 fail-closed 兜底 | 同步 lite 版两个 location 块 | 待修复 |
| 09-09 | Dockerfile + compose:177 | P2 | 全部容器 root 运行（tempo 显式 `user: root`） | Dockerfile 加非 root USER | 登记 |
| 09-09 | compose ×7 + Dockerfile:2,5,10 | P2 | nginx 与监控栈镜像全用可变标签（latest ×7）；基础镜像未 pin digest；`npm install` 应 `npm ci` | 固定版本 tag | 登记 |
| 09-09 | tempo-config.yaml:10-14 | P2 | local backend 无 compactor/block_retention，trace 块无限累积撑盘；另实测 tempo 容器 RestartCount≈2280 crash-loop（既有问题，链路追踪长期不可用） | 补 block_retention；另查 tempo crash 根因 | 待修复 |
| 09-09 | alert.rules.yml:105 | P2 | GatewayInstanceDown 只覆盖"全宕"，部分宕（1-3/4 实例 down）不告警（max(up)==0 仅全宕为真） | 加 per-instance `up{job="gateway"}==0` warning 规则 | 待修复 |
| 09-09 | scripts/secret_scan.py:14,40,46,75 | P2 | 注释声称的 `\b` 防误报实测不成立（task-sk-… 仍 HIT）；MAX_FILE_BYTES=2MB 跳大文件与"日志不跳"注释矛盾（泄漏日志往往 >2MB）；每行每规则只报首命中 | 主要洞随 P1 正则修复一并处理；其余登记 | 部分随修 |
| 09-09 | scripts/backup_daily.cmd:6 | P2 | 0x3F 修复核验成立（纯 ASCII glob，当前唯一匹配可解析）；但 D 盘出现第二个同名目录时 cd 多参数报错且无日志/退出码检查——备份会再次静默失效 | for 循环断言匹配 + 输出重定向 backup.log | 待修复 |
| 09-09 | deploy.sh:22 | P2 | 旧一键部署脚本对 4 实例同时 --force-recreate（全站中断窗口）；ci.sh 已替代但脚本仍在仓库根可误用 | 删除或标注废弃 | 待修复 |
| 09-09 | nginx.conf:8-19 | P2 存量复核 | 简易 WAF 仅匹配未解码的 $request_uri，URL 编码载荷可绕过、请求体不检（自注释"简易 WAF"） | 维持登记：不能作为唯一防线 | 登记 |
| 09-09 | docker-compose.yml:16 等 6 处 | P2 存量复核 | `env_file: ../.env` 依赖仓库外父目录，干净 clone 后 compose 无法自举（"安全目录"设计的可移植性代价） | 维持设计，部署文档注明 | 登记 |

**存量核验（09-08 登记项现状）**：compose 端口 0.0.0.0 绑定仍在（未恶化）；.env.example 与 config.py 双向脱节仍在（新发现缺活项 GF_SECURITY_ADMIN_PASSWORD——compose grafana 消费该键，缺失落回默认口令）；Dockerfile:44 COPY tests/ 仍在（但其"硬编码密码"部分已好转：集成测试密码已改 \_env 读取）；deploy_aliyun.sh 三连 + Dockerfile.lite static/assets 未入库仍在。

**本轮核验通过项**：SMTP 授权码链端到端闭环（../.env → start_all.sh → monitoring/ 挂载 → auth_password_file，alertmanager 对文件内容 TrimSpace，容器 Up 27h）；d9e0f1a2b3c4 downgrade 对称复核通过；backup_daily.cmd 双代码页读法一致；test_voice.ps1 缺 JWT_SECRET 即响亮 SystemExit（无空串签名）；test_secret_scan / test_snapshots / test_20260907_batch_fixes 均有真断言；密钥扫描全库 sk-/AKIA/LTAI/ghp_/PEM 零新增命中（etc/agent_gateway/.env 与 deploy/.env.lite 为本地明文凭据，均未入库）。

## 2026-09-09 主人拍板执行记录（本日审查轮四项决策落地）

| 决策 | 执行结果 | 证据 |
|---|---|---|
| ①清理线上测试账号 | 事务内删除 98 个（test01-15、demo、guest、load_001-080、user2026）及其 user_profiles 7 行（其余依赖表 0 行）；剩余 7 账号：admin、Fengfeng×2、Li-zner×2、phone_133****8241、**admit**（精确匹配查无此行疑含不可见字符/非 bcrypt 哈希，身份不明保留待确认）；另发现 users.username 疑无唯一约束（Fengfeng/Li-zner 各两行），待主人核查 | asyncpg 事务执行输出（逐表 DELETE 计数） |
| ③内部 LLM 调用计入计费 | 六处收口完成：子 Agent（bill，指标已打点）、圆桌两 Phase（bill，经 AgentOrchestrator(username) 穿 3 层）、检索改写重试（record）、记忆压缩（record，经 memory_manager 传 user_id）、意图分类 LLM 路径（record，当前 use_llm 恒 False 属预留，启用即计费）、任务降级 Flash（record + include_usage）；grep 复核全仓 chat/completions 调用点 12 处全部有计量归属 | pytest 183 全绿；单测替身 4 处同步新签名 |
| ④删除 tools/ + plugin_loader | git rm -f 完成（7 文件含双层死亡代码）；Dockerfile:43 COPY tools/ 同步移除 | git status D 记录 |
| ⑤端口收敛 + HTTPS | compose 全量栈 8 个非入口端口绑 127.0.0.1（4 网关直连 + prometheus/grafana/loki/tempo；nginx 10090 前门保持公网；lite 栈本就只有前门无需改）；setup_ssl.sh 修复（去掉 -p 80:80 端口冲突、邮箱参数化 LETSENCRYPT_EMAIL、ACME 续期路径放行）；compose 预挂载 nginx_certs 卷与 webroot（无副作用），激活步骤写入脚本输出 | docker compose config 通过；bash -n 通过 |
| 附：推送门禁实效验证 | 按 gitpush_daily 新门禁时序模拟：全量暂存补丁 2.8MB → secret_scan 退出码 0 干净放行；.mimosa/（插件运行态）已入 .gitignore 防误提交 | 本节执行记录 |

**未闭合项（不阻塞今晚推送，按规则登记）**：断连漏扣费（P1，需流式核心改造，单独一批）；admin 口令轮换（主人未指示，维持登记）；HTTPS 实际签发激活（需服务器侧跑一次 setup_ssl.sh，配置已备齐）；admit 账号确认。

## 2026-09-09 主人拍板执行记录·第二轮（断连漏扣费修复 + 账号清空）

| 决策 | 执行结果 | 证据 |
|---|---|---|
| 断连漏扣费修复（最后一个代码级 P1） | 双层方案：①主链（fast-path answer_via_models / ReAct _stream_react）客户端断开（GeneratorExit/CancelledError）时上游流对象尚未关闭，`_spawn_drain_bill` spawn 排空任务接管消费直至上游自然结束，按**真实 usage** 补计费后原样上抛不吞取消；②两条 Flash 降级（chat_fallback/_flash_sse、react_steps/_fallback_flash）直连 httpx 无法安全排空（async with 随生成器关闭），退化按已产出文本**估算**计费（2字符≈1token，remark 标注"断连估算"）。估算兜底函数 `_estimate_tokens` ponytail 注明 ±50% 天花板 | tests/unit/test_20260909_batch_fixes.py 3 用例（真实 usage 排空/上游死亡退化估算/估算口径）；186 用例全绿；规范门禁通过 |
| 账号清空（第二轮：只留手机号 + admit） | 删除 admin、Fengfeng×2、Li-zner×2 共 5 行及依赖行（audit 58 / conversation_memories 91 / message_ratings 3 / payment_orders 38 / transaction_logs 36 / user_profiles 3 / user_wallets 3 / user_usage 3）；**admit 密码重置为 Li********（app 自带 bcrypt hash_password 生成 + verify 自检通过）；phone_133****8241 保留。剩余账号恰好 2 个：admit(user)、phone_133****8241(user) | asyncpg 事务执行输出（逐表计数）+ 剩余账号列表 |
| 附带结果 | ①admin 账号已不存在——"git 历史旧口令可登录"风险自然闭合；但 verify_me_github.py 等脚本假设 admin 账号存在，登录会失败（按需改用 admit 或重建 admin）；②admit 角色仍为 user，admin 面板（/api/admin/errors、persona switch 等）暂无可用账号——如需提权一条 SQL：`UPDATE users SET role='admin' WHERE username='admit'`；③Redis 无 user:info:* / auth:pwd_changed:* 残留键（TTL 自清）；④线上服务器（Aliyun lite 栈）若也有同名账号，需在服务器侧执行同款清理（本地无法直达，脚本与 SQL 已在报告留档） | 本节执行记录 |

**结论更新**：代码级 P0/P1 全部清零（含断连漏扣费），按 Review规则改判**可发布**（发布附带说明见 code review/2026-09-09-全目录审查报告.md：需 alembic upgrade head 应用新迁移 e1a2b3c4d5e6；端口收敛与断连修复随镜像重建生效）。

## 2026-09-09 主人拍板执行记录·第三轮（admit 删除 + 133 提权 + 脚本账密同步 + 注册去向深究）

| 事项 | 结果 | 证据 |
|---|---|---|
| admit 删除 | 已删（本地 full 库）；lite 库另一行脏数据（username=整串哈希、hashed_password=明文"Fengfeng"、role=admin——历史某次参数插反的残留，create_admin.py 当前版本无此 bug）已一并清除，lite 库剩 Fengfeng(admin) | 事务 DELETE 输出 + psql 前后对照 |
| 133 设为管理员 + 密码重置 | phone_133****8241 → role=admin，密码重置为 Nannan@2026（原密码为注册时设置的单向哈希不可找回，故重置；哈希用 app 自带 hash_password 生成 + verify 自检通过）；已失效该用户 Redis 缓存键 | asyncpg 事务 + psql 查询 |
| 账密登录端到端实测 | POST /api/login（username=phone_133****8241）→ HTTP 200 + token；GET /api/user/profile → username/phone=133****8241/role=admin 全部正确。**phone_ 前缀账号密码登录无任何限制**（authenticate_user 只按 username 查库验哈希） | curl 实测输出 |
| 脚本账密同步（6 处） | ../.env 新增 ADMIN_USERNAME=phone_133****8241 / ADMIN_PASSWORD（幂等 upsert）；verify_me_github.py、benchmark_single_gateway.py（原 test01/demo 账号列表已随清理失效，改为单一管理账号登录）、test_upload.py、test_voice.py、test_voice.ps1（JWT sub 改读 ADMIN_USERNAME）、tests/integration/test_payment_idempotency.py 全部改走环境变量，代码零硬编码账号 | compileall + pytest 186 全绿 |
| 朋友手机号注册"没显示"深究 | **结论：不是 bug，是库不在这里**。注册代码核验无误（username=phone_{手机号}、phone 列正常写入、profile 正常返回 phone）；两个本地库（full/lite）均无朋友账号——他们在公网站点（Aliyun 服务器）注册，账号落在**服务器库**。核实 SQL：`docker exec postgres_lite psql -U agent_user -d agent_gateway -c "SELECT username, role, phone, created_at FROM users ORDER BY created_at"`（服务器上执行） | phone_register 代码走读 + 两库查询输出 |

**附带提醒**：①若在服务器跑过 create_admin.py 重建 admin，注意 .env 的 ADMIN_PASSWORD 现在是 133 的密码，语义已变为"管理账号口令"；②服务器库如存在与本地同批的测试/压测账号，可用根日志 09-09 第二轮记录的同一份依赖表清单执行同款清理。

## 2026-09-09 主人拍板执行记录·第四轮（五项体验优化）

| # | 需求 | 实现 | 验证 |
|---|---|---|---|
| 1 | 地图缩放丝滑化（PC 滚轮/手机双指，灵敏度适中，松手不回弹） | 病根：georoam 逐事件 setOption 钳制与 echarts 手势内部状态互殴（松手回弹根源）。改为 geo.scaleLimit 原生硬边界（50%–500%）+ 事件只同步读数；resize 跨 768px 同步 scaleLimit | vue-tsc 0 错误；新 bundle 已含 scaleLimit；手感主人实测 |
| 2 | 一天内高频点击前十城市 Redis 缓存 | /weather、/recommend 双路由接入：点击 zset 计数（TTL 2 天），当日 Top10 城市结果缓存到 24 点；force=1（刷新按钮）跳缓存重新生成并覆盖；缓存命中不走 LLM 不计费 | 实测：天气 0.49s→0.008s；推荐 17.1s（LLM）→0.007s（缓存）→force 4.4s（重新生成）；redis 键 map:city_cache:* 确认 |
| 3 | 收藏/历史用文字不用 emoji | ⭐/📖 改为「收藏」「历史」 | 新 bundle 含字样 |
| 4 | 港澳点击范围优化 | 省视图新增近海透明点击热区（香港 [114.26,22.08]、澳门 [113.55,22.03]，34px 触达），落海域不压深圳/珠海且易点中；点击归属处理同步扩展 | 新 bundle 含坐标；手感主人实测 |
| 5 | 账号体系去 phone_ 前缀 | 注册/登录自动注册/绑定改名全部改为裸手机号（纯数字用户名与 GitHub 登录名天然不冲突）；GitHub 登录用登录名、缺 login 随机 gh+8hex；绑定手机即改账号为手机号、GitHub 登录经 github_id 回访同一账号（双登录同一身份）；改名级联补漏 user_usage 表；存量账号已迁移 phone_133****8241→133****8241（级联 8 表+user_usage），.env ADMIN_USERNAME 同步 | 实测裸手机号+密码登录 200，profile 返回新用户名；187 用例全绿 |

## 2026-09-09 追加：城市点击慢——埋点定位与优化

| 项 | 内容 |
|---|---|
| 埋点 | /weather、/recommend 增加 [map-perf] 分阶段计时日志（cache 命中/生成、key/llm/parse 分段、模型、token 数） |
| 结论 | 慢在推荐（LLM 生成）走主模型：单次 10-17s；天气仅 0.3-0.5s；且前端气泡等 allSettled，被推荐拖住 |
| 优化 | ①recommend 改 flash 优先、主模型兜底（post_chat_completion 增加 prefer_flash）②前端"先到先显"：天气先亮、推荐好了再填 ③高频城市缓存（上一轮）叠加后热门城市重复点击毫秒级 |
| 实测（成都首点） | weather total=0.41s；recommend total=1.57s（llm=1.56s）model=deepseek-v4-flash tokens=268——对比主模型 17s 提速约 10 倍 |

## 2026-09-10 追加：模型 A/B 实测 + 缓存 Top-N 可调

| 项 | 内容 |
|---|---|
| A/B 实测（同一推荐任务） | qwen3.7-flash（百炼）：拉萨 21.32s/1754tokens、乌鲁木齐 6.89s/465tokens——思考型模型，回答前生成大段内部思考；deepseek-v4-flash：1.22s/1.25s，tokens≈230——差距是模型性格非网络。结论：地图推荐维持 flash 优先、qwen 兜底不变 |
| Top-N 可调 | CITY_CACHE_TOP_N 环境变量（默认 30）；Redis 压力评估：单条 1~2KB，Top40 也仅 ~100KB，无容量压力，Top-N 是产品策略非容量限制 |
| 天气新鲜度 | 天气缓存 TTL=min(到午夜, 2h)——早上的气温不应晚上还展示；推荐仍到午夜（不会过期失真） |
| 实测 | 南京首点 0.51s；缓存键 TTL=7199s；容器内 CITY_TOP_N=30 生效 |

## 2026-09-10 追加：城市缓存最终方案（主人定稿）

| 项 | 定稿 |
|---|---|
| Top-N | 默认 200（"尽可能大"）；CITY_CACHE_TOP_N 环境变量可调。Redis 压力评估：单条 1~2KB，Top200 ≈ 400KB，无压力 |
| 天气 | 每日刷新一次（TTL 1 天），维持高德实况原样——高德免费天气接口只有实况+按天预报，无逐小时数据，8-22 点"最长天气段"无法计算（主人指示：不能算就维持原样）。弹窗里的"中雨"即缓存写入时刻高德报的实况 |
| 美食景点 | 15 天刷新一次（短期不会变） |
| 缓存键 | 改为不带日期前缀（原带日期=过午夜必失效，与 15 天 TTL 矛盾）；Top-N 只 gate 写入，force=1 无视 Top-N 一律覆盖（否则刷新会被旧缓存遮蔽） |
| 实测 | 天气 force→TTL 86399s、二次点击 0.005s；推荐 force→TTL 1295999s、1.59s（flash）；TOP_N=200 容器内生效 |

## 2026-09-10 追加：天气改预报 + 刷新推荐差异化（主人定稿）

| 项 | 定稿 |
|---|---|
| 天气改预报 | 走高德 extensions=all 预报接口：返回今天/明天/后天三天白天天气+夜温~日温区间（出行查的是未来两天而非当下实况）；前端按「今天晴 24~31℃｜明天…｜后天…」清晰标注日期 |
| 推荐刷新差异化 | force 生成本就不读缓存，但同提示词易雷同——提示词注入随机风格（老字号/热门/经典小众兼顾/本地宝藏 四选一轮换），实测连续两轮 force 推荐完全不同 |
| 实测 | 广州天气预报：今天晴 24~31℃｜明天晴 24~32℃｜后天多云 24~32℃；刷新两轮美食/景点零重复；187 用例全绿 |

## 2026-09-10 追加：主人实测反馈三连修

| 反馈 | 根因（埋点） | 修复 |
|---|---|---|
| 美食+天气返回 30s | 埋点实锤：flash 模型间歇挂起吃满 15s 超时 → 换 qwen 7~12s → 总计 21~27s（天气本身 0.32s 不慢） | post_chat_completion 增加 preferred_timeout：flash 尝试 8s 快速熔断换备用，总时长上限可控；weather/recommend 气泡前端先到先显 |
| 天气一行写不满 | 前端 join('｜') 单行 | 改三行展示（join('\n') + CSS white-space:pre-line） |
| 刷新强制不同没必要 | 主人定稿：一切随机即可 | 去掉风格列表，改 payload temperature 0.9~1.3 抖动（qwen 兜底路径自动剥离防 400） |
| 实测 | 西宁：天气三行（今天阴 6~15℃/明天多云 8~20℃/后天小雨 7~15℃）；推荐 1.43s/1.46s 两轮内容不同；187 用例全绿 | — |

## 2026-09-10 追加：告警误报整改（主人反馈"缓存命中低于10%一直发预警"）

| 项 | 内容 |
|---|---|
| 根因 | CacheHitRateLow 表达式在零/低流量时恒真：hits/(hits+misses+1)=0/(0+1)=0<0.1，空闲 10 分钟必发一封。实测 firing 中（另有 LLMTokenSpike 因新 TSDB 基线极小被本轮测试流量误触发） |
| 整改 | ①CacheHitRateLow 加流量门槛（10 分钟窗口语义缓存查询 ≥30 次才参与判定），窗口 5m→10m 与描述对齐 ②LLMTokenSpike 加 clamp_min 绝对下限 2000 tokens，杜绝小基线误报 |
| 验证 | prometheus 重载后两告警 active=0，ALERTS 序列待自动 resolve |

## 2026-09-10 定向复查（独立代理复核 ea4200d..HEAD 增量 335 行）

| 级别 | 发现 | 处置 |
|---|---|---|
| P1 | 天气缓存键无日期成分+滚动 24h TTL，而预报标签是"今天/明天/后天"相对值——Top 城市跨日展示错误日期最长 24h | 缓存键加当日日期 + 天气 TTL 对齐午夜；实测杭州 TTL=81373s |
| P2 | recommend 缓存写入前无形状校验——畸形但合法的 JSON 会钉死 15 天"暂无推荐" | 写入前校验 foods/spots 为 list，否则回落兜底且不写缓存 |
| P2 | 绑定改名级联补了 user_usage 但漏了 Redis 当日限额键——改名即重置当日配额 | bind_phone 改名分支迁移 daily_req/daily_token 键（保留 TTL） |
| P2 | oauth callback 内 username 死代码（与 _resolve_github_user 兜底重复漂移） | 删除 |
| P2低 | city 未消毒入日志（换行伪造面）+ 64 字符截断碰撞 | 新增 _san_city 统一消毒（去换行+截断），天气/推荐入口生效 |
| 设计确认 | 缓存命中在 ensure_chat_allowed 之前返回（受限用户可读缓存推荐，无 LLM 成本） | 知情保留（公开内容，无成本） |

其余复查结论：三登录入口同账号 ✓、级联表独立核验无遗漏 ✓、post_chat_completion 既有调用方无回归 ✓、前端 seq 守卫完整 ✓、告警 PromQL 语义正确 ✓。187 用例全绿。

## 2026-09-10 追加：思考链泄露三连修 + 子Agent兜底（凌晨03:39梧州→日照实测复盘）

| 项 | 内容 |
|---|---|
| 根因一（泄露主因） | ReasoningStreamGuard 逐 delta 过滤：指纹被块边界切开（query_ho\|tel）即整体绕过。实测同段 478 字提示词复述按 6 字/块喂入漏出 477 字（裸 query_hotel/query_food 全部上屏） |
| 修复一 | 守卫改行缓冲：不见换行不放行，指纹无论怎么切必须在完整行上现形；流尾无换行残余由 stream_llm_throttled 调 flush() 补检放行。前端 cleanReasoning 同源缺陷同修：store 改累积原文后整体重算清洗 |
| 根因二（思考复述注入话术） | 推荐通道向思考型模型注入"有4项实时数据本次未能获取...不要向用户提到查询失败"，模型原样复述进思考（复测截图文案逐字吻合），回答还违反了"不提失败"纪律 |
| 修复二 | 注入话术改为点名缺失板块（"以下部分本次没有查询到数据：天气、交通..."）：不报数字、无"失败/未能获取"元话语，且模型不再困惑"不知道哪些未能获取" |
| 附带修复 | ①_stream_react 去掉对守卫输出的二次 sanitize（会把行尾换行 strip 掉致思考行粘连）②call_sub_agent 改走 post_chat_completion：主模型失败自动 flash 兜底（此前单发主模型，上游一抖酒店/路线/美食三个子Agent同时全灭——03:39 四工具全挂的疑似主因之一），计量标签跟随实际应答模型 |
| 已知代价 | 整段无换行的超长单行思考会缓冲到流尾才处理（思考模型正常输出带换行，思考展示是辅助信息，可接受）；已记入代码注释 |
| 验证 | tests/test_reasoning_guard.py 新增跨块泄露回归（03:39 原文按 6 字/块重放：裸工具名零外露、正常行保留、流尾 flush 放行）；tests/test_chat_fast_paths.py 失败注入断言更新为新话术契约；全量 pytest 187 passed + 1 skipped；前端 vitest 18 passed（新增 reasoning-clean.test.ts 钉死逐块漏出/累积拦截对照）；vue-tsc --noEmit 通过 |
| 遗留 | 03:39 四工具全挂的最终上游根因（高德 key/配额 vs 子Agent上游400）需上阿里云看 docker logs agent_gateway 当时日志；思考区展示决策（继续展示已修复的过滤流 vs 人格级关闭）维持现状待主人拍板 |

## 2026-09-10 追加：接入阿里云实测取证——"四个工具全挂"真凶落网 + place_extract 长尾兜底

| 项 | 内容 |
|---|---|
| 接入 | 经 SSH（askpass 读 deploy/.server.env 密码，密码不落命令行）接上轻应用服务器；本机专用公钥 id_ed25519_dsh_aliyun.pub 已装入 root authorized_keys（落实 09-09 审计"改密钥登录"建议）。注意：该私钥带 passphrase，BatchMode 密钥登录暂不可用，密码会话可用 |
| 03:39 实锤 | 梧州→日照对话确实由该服务器服务（容器启动后唯一一次推荐通道）。日志全窗口仅 1 次高德 GET + 1 次 deepseek POST：三个子 Agent 连 HTTP 请求都没发出 |
| 真凶一 | sub_agents._call 里 `base_url, api_key = llm_endpoint(DEEPSEEK_MODEL, api_key)` 自引用赋值——api_key 被当局部变量先读后赋，UnboundLocalError 每调必炸；异常不在 call_sub_agent 捕获元组内，被 _run_one 裸 except 静默吞成 error 结果。6c0bf46（09-06 auto push）引入，酒店/美食/路线三工具自此全灭 4 天。今日 post_chat_completion 改造已顺带根除（并补 flash 兜底） |
| 真凶二 | 日照/梧州不在 68 城白名单 → extract_destination 空串 → auto_tool_args 原话兜底把整句话当 city 传给高德 geocode → 必然"未找到城市"。修复：place_extract 增加表外地名正则兜底（"想去X旅游""从X市出发"，疑问/指代字过滤） |
| 日志侧证 | 19:39:47Z"思考内容命中强泄露指纹"= 截图思考链在"注意不要[提]"处戛然而止的原因；agents 顺序 food→hotel→route→weather 与截图 chips 顺序一致；最终回答为纯知识兜底（未提失败，已写缓存计费 3088 tokens） |
| 验证 | tests/test_place_extract.py 新增 6 例（表外目的地/出发地/weather/route 参数、疑问词过滤），并挂入 pytest 收集（此前 main() 式脚本 pytest 盲区）；全量 pytest 188 passed + 1 skipped |
| 遗留 | ①服务器镜像为 00:48 构建（不含本批修复），部署需 git push + 服务器重建容器，待主人发话 ②部署后建议清掉 03:39 那条语义缓存（同问法会继续命中旧的纯知识兜底答案，干扰验证）③本机私钥 passphrase 待主人提供，或授权生成免密自动化密钥 |

## 2026-09-10 工作区全量复查（规则：可复用资产/SOP/Review规则.md；对未提交工作区约 1700 行增量逐文件审阅）

| 级别 | 发现 | 处置 |
|---|---|---|
| **P1** | stream_utils.stream_llm 请求体缺 `stream_options.include_usage`——主路径（v2_chat/_stream_react、任务 ReAct）真实计量扣费与取消按 usage 结算全部拿不到用量恒为 0；两条直连降级路径已显式声明，唯独共享主路径漏配 | ✅ 已修：payload 补参数 + tests/unit/test_stream_usage_flag.py 回归（详见 app/core/修复日志.md） |
| P2 | 流式任务取消响应性：旧逐事件检查可立即断流，新设计在 usage 尾块才检查（与"取消前先结算 usage"绑定；任务状态经 CAS 即时翻转，仅上游流多跑一段） | 登记（设计取舍） |
| P2 | refund.py 幂等键在结算落库后写 Redis 失败的极端窗口可重放——订单状态机 + 部分退款累计防超额兜住上限 | 登记 |
| P2存量 | scripts/check_code_rules 报 app/routes/phone.py:267 bind_phone 90 行超 80 行门禁（本工作区未触碰该文件，存量违规） | 登记（待拆分） |

**通过项（有证据）**：①CDC txid 设计复核成立——按 (txid,id) 序消费 + `txid < xmin` 守卫使水位不可能越过未决事务（ORDER BY txid 首个未决即停，慢事务提交后仍落在水位之前被消费）；②`_ensure_admin_users` 的 `ON CONFLICT (username)` 依赖 users 主键（baseline_schema PRIMARY KEY (username) 已核实）；③并发租约双重释放幂等（HDEL 0 为 no-op）、fail-open `__bypass__` 标记不可释放、map 缓存命中在门禁前返回与"知情保留"记录一致；④refresh family 全族撤销 = OAuth rotation reuse detection 标准语义；⑤`_assemble_messages` 不含当前问题，`_messages_for_fast_path` 追加不重复；⑥oauth 授权码 Lua 原子 GET+DEL 防重放；⑦前端删除会话走 DELETE /api/conversations/{id}（user_id 归属校验存在），404 容忍；⑧quick-path has_history 判定不含 system/file 上下文消息，首轮缓存语义正确。
**验证**：pytest tests/unit 204 passed（含新增 include_usage 回归）；compileall 通过；check_code_rules 仅存量 phone.py 一项。

## 2026-09-10 全量清欠轮（主人指示："全修复了，慢慢plan再执行；以前记录未修复的也可以修"）

盘点 18 份修复日志全部未闭合项约 60 条，本轮修复 52 条、核实此前已修 8 条、维持登记 9 条（需主人/服务器侧动作）。各域明细见对应目录修复日志，摘要：

| 域 | 修复要点 |
|---|---|
| app/core | audit 脱敏补 phone 掩码（P1）；safety_filter 删 _last_triggered 全局态；stream_utils 属主校验 fail-closed；语义缓存画像指纹 8→16 hex；SMS 演示模式按 APP_ENV 门控（生产 fail-closed，本机需显式 APP_ENV=dev）；send-code 失败改 503 不谎报已发送；memory 滚动摘要按会话锁串行防覆盖 |
| app/payment | 退款白名单（仅 payment 单可退）；充值幂等键加用户维度；扣费 failed/deferred 打点；待结算独立 5 分钟结算循环；对账接口真实核对流水（不平写 abnormal）；退款限流 Lua 原子化 |
| app/routes | METRICS_TOKEN 收编 config + compare_digest；map_api 纳入 llm_semaphore + AMAP key 收编 config；SMS 验证码 compare_digest；/api/stats 收敛 admin；bind_phone 拆分过 80 行门禁 |
| app/services | 计量 model 标签透传实际应答模型；中途失败不换模型防拼接重复；ContentStreamGuard 跨块敏感词守卫接线三条流式路径；tool_result 事件对外消毒 |
| app/agents | sub_agents/router/memory 三处 LLM 调用纳入 llm_semaphore；project 通道 ILIKE 通配符转义；DDGS 显式 timeout；document_parser 截断标注数值修正 |
| middleware/models | revoke_refresh_token 区分"token 无效"与"Redis 异常"，logout 响应透出 revoked；schemas.py EOF newline |
| 基础设施 | compose：prometheus_data 实际挂载、loki/promtail 补 restart+持久卷、alertmanager 端口绑回环、7 处 latest 镜像钉版本、网关容器去 tests/ 挂载；Dockerfile 去 COPY tests/ + npm ci；nginx WAF 匹配收窄到 $document_uri（修误杀）+ /metrics、/test-otel 404 屏蔽对齐 lite；tempo 补 compactor/block_retention（crash-loop 根因候选，部署后需验证）；alert.rules 补部分宕机 warning |
| 脚本 | **secret_scan 结构性 P1**（for-else 错位致非补丁文件只扫末行、空文件崩溃——门禁形同虚设）重构逐行扫描 + 全大写常量豁免；check_code_rules 门禁根推导修复（每日 08:00 任务不再空转）+ 语法坏文件计违规；slow_query_monitor 失败不装健康；task_scheduler 超时 kill + 越权校验修复；deploy_github.sh 接密钥门禁；Grafana 弱口令 ×4 改环境变量；真实手机号 7 处外置 gitignored 数据文件（test_phone_login 改 TEST_PHONE 环境变量防误发计费短信）；4 个弱口令建号脚本 git rm |
| tests | **test_reasoning_guard 自检脚本改标准单测**（原不在收集范围——03:39 跨块泄露回归从未进 CI）；test_llm_endpoint/test_routing 改造为真测试并纳入 testpaths；破坏性脚本 5 个移 scripts/maintenance/ + 3 个原地加显式确认守卫；断链引用修复；根空 .env 目录删除；新增迁移链冒烟测试（临时库上 upgrade/downgrade/重放） |
| 前端 | 文件上传前端预检（accept + 类型/大小）；MapView 三处（resp.ok+错误重试入口、resize 重缩放、下钻气泡竞态）；流式 401 刷新失败清登录态跳登录；额度徽标恒真死逻辑；死代码清理（regenerate/@suggest） |
| 配置 | .env.example 与 config.py 全面同步（删死项 DIFY/OLLAMA/TIMEOUT_*、补 REDIS_URL/METRICS_TOKEN/GF_SECURITY_ADMIN_PASSWORD/QWEN_API_KEY/EMBEDDING/OTEL/SMS 等、修 ADMIN_PASSWORD 重复键）；.gitignore 补 logs/ 与 .tmp/；requirements 7 项依赖钉版本 |

**维持登记（需主人/服务器侧动作）**：.env.lite 真实 key 轮换；服务器 IP/root 口令历史披露脱敏（先切换密钥登录）；HTTPS 证书激活；Grafana 线上弱口令轮换；admin 口令轮换；tempo crash-loop 需部署后验证；WAF 不能作为唯一防线（设计）；env_file ../.env 可移植性（设计）。

**验证**：pytest 默认集合 227 passed；vue-tsc 0 错误；vitest 23 passed；check_code_rules 无违规；YAML/sh 全部通过；secret_scan 对 app/scripts 干净（deploy/static 命中均为已登记项）。


## 2026-09-10 收尾深检（主人指示："一点点看每一个文件，查找 bug 或漏洞或语法问题"）

方法：pyflakes 全仓静态扫（undefined name/未用变量分级）+ 自写 AST 不可达代码扫描 + 逐文件复读本轮全部改动及其调用方。新发现并修复 7 处：

| 级别 | 文件 | 问题 | 处置 |
|---|---|---|---|
| **P1** | app/services/chat_fallback.py | `_flash_sse` 计费块位于终检拦截分支 return 之后（不可达）——Flash 降级链正常路径零扣费，09-09"降级链补计量"实际未生效 | 计费前置到终检前，两路径均按真实 usage 计费 |
| **P0→已拦** | app/main.py | `_initialize_runtime` 仍为 4 元组解包，而 `_start_background_tasks` 已改返回 5 元组（新增 deferred_task）——**启动即 ValueError 崩溃**（本轮批量替换时一处 replace 未加断言静默失败所致） | 补齐 5 元组解包；全部历史替换用标记审计脚本复核落盘（34 项全确认） |
| P1 | scripts/secret_scan.py 连带 | 逐行扫描重构后 `_is_real` 需配套三类豁免：localhost 测试 DSN、全大写常量引用、前端 localStorage 键名枚举；并修 value 清洗顺序（`Token = 'gw_...'` 剥引号被前导空格挡住） | 三类豁免 + 清洗顺序修正；deploy 真实泄漏照报（门禁有效性双向验证） |
| P2 | scripts 6 个 | 本轮外置手机号改造漏写导入：check_grafana/setup_grafana/gen_resume_v2/update_kb/write_resume/test_phone_login（pyflakes undefined name ×9） | 逐个补齐；test_phone_login 注册口令同改 TEST_PASSWORD 环境变量 |
| P2 | tests/unit/test_core_p1_fixes.py | 管理员种子夹具用真实历史口令字面量，撞门禁黑名单 | 改占位值 |
| P3 | tests/unit/test_reasoning_guard.py | sanitize 夹具 sk- 形状撞门禁 | 改门禁认可的占位形状 |
| P2 | app/services/chat_react.py | tool_result 事件消毒漏了主聊天路径这第 4 处 | 补 display_safe_tool_result |

死代码清理：`is_quota_exhausted`（零调用方，已由 reserve_used_questions 取代）与 `USER_FENGFENG_PASSWORD` 配置导出删除。

**收尾终验**：pytest 227 passed；vue-tsc 0 错；vitest 23 passed；check_code_rules 无违规；pyflakes undefined name = 0；compileall 通过；YAML/sh 语法通过；secret_scan 对 app/scripts/tests/frontend/src 干净、deploy 真实泄漏照报（门禁有效性验证）；全应用 import 冒烟 64 条路由装配正常。


## 2026-09-11 全量规则审查轮（5 路并行子代理逐文件深读 + 主代理复核）

按《可复用资产/SOP/Review规则.md》对全仓做无差别行级审查。**发现 P0×1、P1×13（全部当轮修复并配回归）、P2×43（按口径全部登记）**，另有 3 项子代理发现经复核降级。完整覆盖矩阵、发现清单与"需运行时确认清单"见 `code review/2026-09-11-全量规则审查报告.md`。

要点：
- **[P0]** ReAct 主路径丢失当前用户问题（旧版单体有 `messages.append(user_query)`，纯移动重构丢失；凡走 react_loop 的请求 LLM 在无当前问题的上下文上作答。旧单测断言钉住的恰是缺失状态，暴露了"测试钉住 bug 行为"的反模式）——react_loop 入口补追加 + 双回归。
- **[P1] 计量/资金**：chat_fallback 拦截分支跳计费 + 正常路径计费块不可达（双修）；fast path 拦截产物钉缓存（write_cache=False ×2）；对账恒等式错误改分类型比对；PaymentFailureHigh/LLMErrorRateHigh 双死告警接线埋点。
- **[P1] 功能/水位**：tools.py project 通道 ESCAPE 被 Python 转义消费成 PG 语法错误（关键词兜底一跑即炸）；cdc stop/异常路径 checkpoint 缺 txid 致断点续传退化为全量重放；start_all.sh 检查 ./.env 而权威 .env 在上一级（启动必然 abort）。
- **[P1] 其他**：deploy_aliyun sed 未转义 + PG 密码随机化（09-10 补丁未落盘，本轮重新应用）；slow_query 空输出与故障同判；_format_via_llm 绕过 llm_semaphore；前端流式中可删当前会话（孤儿会话）。
- P2 按口径登记至各域修复日志（含 defense-in-depth、口径一致、结构、测试基建、前端 i18n/UX 等 43 条）。

**终验**：pytest 231 passed；vue-tsc 0 错；vitest 24 passed；门禁无违规；pyflakes undefined=0；secret_scan 对代码目录干净；import 冒烟通过。
**结论：可发布**（附运行时确认清单与服务器侧待办，见报告）。


## 2026-09-12 凌晨清欠轮（主人指示：登记的 P2 全部修复，明早验收）

09-11 审查登记的 43 条 P2 当夜全部修复（含此前"待运行时确认"的代码侧防御项），覆盖后端 31 项、前端 13 项、运维/配置 12 项。**逐条明细与验证方式见 `docs/2026-09-12-凌晨修复验收清单.md`**；登记出处见各域修复日志同日条目。

关键项速览：熔断器探针 token 身份化、并发租约时钟改 Redis TIME、update_status 封终态旁路、取消事件泄漏源消除（request_cancel 不写本地 Event）、六处 DEEPSEEK key 池化、weather 递归上限、降级链补 answer_complete 与 model 计量、resume 幂等前移、退款幂等键提交感知、验证码 CSPRNG、MapView 拆分 + i18n 补齐、http/auth 职责收敛、tempo 去 root（验收清单置顶）、alertmanager 邮箱出库（ALERT_EMAIL 注入）、autoscale inode 防御、slow_query 哨兵修正（E-F5 的正确形态：None=故障/空串=健康）。

**终验**：pytest 230 passed、vue-tsc 0 错、vitest 24 passed、门禁无违规、pyflakes undefined=0、YAML/sh 语法通过、import 冒烟通过、secret_scan 代码目录干净。
**待主人验收**：见验收清单；其中 tempo 启动状态与 alertmanager 邮箱注入为本轮仅有的两个需要起容器确认的项。

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | start_all.sh:175 | P0 | 默认模式先停 gateway2-4，再启动仍解析这些 upstream 的 Nginx，最后才同步 upstream；全量启动可能因 host not found 失败 | 启动 Nginx 前先校准 upstream、执行 nginx -t，再 reload | 待修复 |
| 09-11 | docker-compose.yml:172 | P1 | Promtail 直接挂载 Docker Socket，只读挂载不能限制 Docker API 写操作，容器被攻陷接近宿主机 root | 使用受限 Socket Proxy 或改 Docker logging driver | 待修复 |
| 09-11 | Dockerfile:2、requirements.txt | P2 | 基础镜像、Torch、OpenTelemetry 和部分依赖未固定 digest/精确版本，构建不可复现 | 固定镜像 digest 和完整依赖锁文件 | 登记 |


## 2026-09-12 外部复核修复轮（主人在 17 份日志写入 09-11 复核发现，逐条修复）

主人以外部审查视角在 17 份修复日志各写入"复核发现"节，共约 70 条（P0×10、P1×30+、P2×20+）。本轮 P0 全部修复、P1 大部分修复、P2 修复过半；其余登记或列入待拍板。**逐条明细与验证见 `docs/2026-09-12-复核修复验收清单.md`**。要点：

| 级别 | 修复 |
|---|---|
| **P0×10 全修** | ①CDC 分区 DROP 改按 (txid,id) 游标校验未处理事件；②journal 水位对单次原子读取（防交叉）；③checkpoint 非 leader 不落盘 + tmp 文件唯一化；④语义缓存上下文加 username 首位（跨账号隔离）；⑤update_status 改当前态 CAS Lua（取消后不可写回 generating）；⑥退款负金额恒"已退满"→abs 基数，消费单恢复可退；⑦对账按类型符号统一；⑧deploy_aliyun 已有 .env.lite 不覆盖（防换密失联）；⑨lite compose 补 build 段；⑩start_all 先校准 upstream 再启 nginx；前端会话键加账号维度 |
| **P1 精选已修（23 项）** | alembic ctx 降级去重对称 / phone 降级对称 DROP；oauth 限频改 Redis 原子+可信 IP；map /recommend+/route 补日请求计数并捕获 today；phone 改名迁移 Redis 文件归属（反向索引配套）；tenant JOIN 去重（LATERAL）；conversation 删除事务化；语义缓存命中 finalize 收口；fallback 空全文不假成功；chat_react 工具消息消毒；agent_tasks 幂等键纳入 persona/file_ids/lang + 槽位满额度回滚（quota.rollback_used_questions）；law_mapping 仅民法典系跳过纠错；rerank/colloquial 双检锁；OCR 上限/锁核对；CI 全量门禁化；scripts 批（deploy_github 暂存扫描、cc/clear 守卫、backup 原子写、restore ON_ERROR_STOP+断言、simulate_locust 凭据、deploy_mapping fail-fast、build_kb 事务化、fix_container 删除）；prompts/me.json 手机号掩码；test_migrations 定义序修复（自吞 NameError 致永久 skip——我上轮引入的 bug，本轮自愈） |
| **深检 P1 补充** | DFA 拦截 break 遗弃生成器 → usage 尾块未消费零扣费（chat+任务双路径），排空计费修复 + 回归 |

**登记待拍板（代码外/架构级）**：HttpOnly Cookie 改造、Docker Socket Proxy、全局任务注册表 shutdown、前端存储版本合并、WCAG 主题对比度、vitest 升级、调度器容器沙箱、生产密钥 600、Secrets 替代 .env、oauth 无鉴权限频增强、CI 无 PG 服务容器。

**终验**：pytest **233 passed**、vue-tsc 0 错、vitest 24 passed、门禁无违规、pyflakes undefined=0、secret_scan 代码目录干净。
**结论：可发布**（运行时确认清单不变：tempo/alertmanager/react修复/迁移冒烟四项部署后打勾）。


## 2026-09-12 深夜最终清点（主人问"确定都修复完整了吗"——诚实盘点后补修）

P0 全部修复；P1 剩余项本轮补修 6 项；仍登记 12 项（架构级/需线上/需设计，见下）。

### 本轮补修

| 文件 | 级别 | 修复 |
|---|---|---|
| core/task_manager.py get_task | P1 | 惰性超时从"只改返回对象"改为 CAS 写回真实终态 timeout + 设跨实例取消标记（生成实例下一检查点退出，不再晚到写 completed） |
| core/task_manager.py + services/agent_tasks.py | P1 | request_cancel 返回写入结果；失败时取消路由 503 明确报错（不再假成功） |
| core/safety_filter.py | P1 | contains_sensitive 全量分段扫描（5000 窗口 + max_word_len-1 重叠防切词 + 共享超时预算），不再只扫前 5000 字符；窗口超时预算共享修复（perf_counter 起点 bug 自查自愈） |
| core/stream_utils.py | P1 | 上传文件内容包〈file-data〉不可信数据边界（防提示词注入的结构化边界第一步） |
| agents/tools.py | P2 | _load_colloquial_map 双检锁 + 局部构建一次性发布（并发首调空映射竞态）；_get_reranker 双检锁（并发双载内存翻倍） |
| app/core/sms.py | P2 | 删除 send_admin_notification 假成功 stub（零调用方） |
| core/audit.py + routes/phone.py + agents/weather.py + payment/admin_routes.py | P3 | pyflakes 清零：死导入/未用变量/无占位符 f-string 清理 |
| scripts/deploy_github.sh | P1 | 密钥扫描改扫暂存差异 .patch——已忽略的本地凭据不再造成固定假失败 |
| scripts/simulate_locust.py | P2 | test01-03 口令改 get("LOAD_PASSWORD") |
| scripts/cc.py、clear_semantic_cache.py | P1 | ALLOW_DESTRUCTIVE 门；Redis 清理模式收窄为本项目命名空间（"*cache*"/"kb:*" 会误删共享数据） |
| scripts/backup_db.sh | P1 | 临时文件→非空校验→原子改名→chmod 600 |
| scripts/restore_drill.sh | P1 | ON_ERROR_STOP + users 非空断言 + 失败退出码 |
| scripts/maintenance/update_ragas_prompt.py | P2 | 补 ALLOW_DESTRUCTIVE 确认门 |
| scripts/fix_container.py | P1 | 删除（一次性容器覆写脚本，目标结构已不存在） |
| tests/ragas/ragas_evaluation.py | P2 | 裸 except 收窄 (TypeError, ValueError) |
| frontend/styles/app.css | P1 | --text-muted 三主题加深至 WCAG AA |
| tests/integration/test_migrations.py | P1 | （09-12 早前已修求值序；本轮复核确认 skip 判定正确） |

### 仍登记项（需主人拍板或专门批次，不建议夜间无人值守改动）

| 项 | 级别 | 原因 |
|---|---|---|
| HTTP Only Cookie 改造（token 出 localStorage） | P1 | 涉登录态架构与前端全量适配 |
| Docker Socket Proxy（promtail 容器逃逸面） | P1 | 涉部署拓扑变更 |
| 全局任务注册表 + shutdown 排空 | P1 | 架构级（涉及 spawn 收口全链） |
| 前端存储版本合并/Web Locks 多窗口协调 | P1 | 前端存储架构重构 |
| vitest 2.1.9 漏洞升级 | P1 | 需白天升级重跑全部前端测试 |
| 安全过滤"句子豁免"严格模板匹配 | P1 | 安全语义重设计（当前窗口重叠已封堵跨窗切词与尾部绕过） |
| rate_limit fail-open → fail-closed 决策 | P1 | 可用性取舍，需主人拍板 |
| get_task timeout 跨实例取消信号完备化 | P2 | 已做 Redis 标记；实例内 Event 通道仅剩可观测性差异 |
| oauth exchange 免鉴权端点增强限频 | P3 | 已有 IP 限频，可再收紧 |
| REPEATABLE READ 对账快照 | P2 | 涉隔离级别，需评估锁影响 |
| 调度器容器沙箱化 | P1 | 部署架构改造 |
| tools 参数 Pydantic 模型化 | P2 | 涉全部工具定义重构 |
| file 响应契约/元数据结构校验 | P2 | 涉响应契约 |
| .env.lite.example 必填字段收敛 | P2 | 部署口径变更 |
| 需主人拍板的历史项 | — | admin 口令轮换、.env.lite 密钥轮换、HTTPS 签发、weather_server 方案 A/B |


## 2026-09-12 深夜批次 B/C 收尾（三个三个修，按主人建议控制单批注意力）

本批修完剩余登记项中所有代码本地可安全完成的 9 条：
- api/chat.ts 流空闲看门狗（60s 无字节 → reader.cancel + 504，修"永久生成中"）
- file_upload 解析失败固定文案（原 str(e) 可能泄内部路径）+ 原始异常进日志
- schemas persona_id/lang 补 max_length
- admin reconcile 全链 REPEATABLE READ 事务化（内联重写去交错）
- phone.py 改绑手机号迁移 Redis 文件归属（file_owner 反向索引，file_upload 上传时登记）
- tools.py _COLLOQUIAL_MAP 双检锁修复完善（_LOAD_LOCK 定义补回）
- tests/integration/test_migrations.py 求值序修复（_admin_dsn 移前定义，修复永久 skip）
- tests/ragas/ragas_evaluation.py 裸 except 收窄 (TypeError, ValueError)
- .github/workflows/code-rules.yml 门禁扩展（结构门禁全目录 / compileall / 单测 / 密钥扫描 / YAML+sh 语法）

**终验**：pytest 233 passed / vue-tsc 0 错 / vitest 24 passed / 门禁无违规 / pyflakes undefined=0 / secret_scan 干净。

## 2026-09-12 顺带发现登记（构建 RAG 检索诊断工具时，用真实检索链路实测取证）

背景：主人计划做一个「RAG 失败全链路追踪与闭环诊断 Bot」，本次为验证可行性，在**不改动本项目任何源码**的前提下，用探针包住 `app/agents/tools.py:_recall_two_ways`，在**生产容器内**跑通 55 条民法典真实用例、采集成通用 trace 后做规则诊断。以下 3 条为过程中实测发现的既有缺陷，仅登记，未修改代码。

关键实测数据（55/55 条 trace）：**vector 腿 `hits=0`、`max_score=0.0` 占比 100%**；trgm 腿最高分仅 0.0706~0.1282。对照独立复算的检索指标：recall@5 = 0.3879、MRR = 0.3491、recall@10 零命中 32/55（58%）。

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | app/agents/tools.py:439-473 | **P0** | `_generate_embedding` 只读 `EMBEDDING_API_URL`（.env 与容器内均为空串），注释宣称的"派生备用主机名"兜底对空串失效 → 两轮尝试皆空 URL → 必失败返回 None → `_recall_pg_vector` 静默 `return []` → **pgvector 向量召回 100% 失效**；`OLLAMA_URL` 已配好且实测可达却未被读取。连锁：trgm 单腿分数低于改写阈值 0.45 → **每条查询都白触发一次 LLM 改写重试** | ① `EMBEDDING_API_URL` 为空时回退到由 `OLLAMA_URL` 推导的 `/api/embeddings` 端点（注意不是 `/api/generate`）；② 或显式配置 `EMBEDDING_API_URL`；③ 补 `kb_search_total` 埋点与检索类告警 | 待主人拍板（详见 app/agents/修复日志.md） |
| 09-12 | tests/run_retrieval_eval.py:27-33 | P1 | `.env` 用 `Path.exists()` 判断后 `read_text()`，而根目录 `.env` 实为**空目录**（exists=True / is_dir=True）→ 运行即抛 `PermissionError`/`IsADirectoryError`；且权威 .env 在仓库上一级，原路径也读不到 | 改 `is_file()` 判断 + 优先取 `BASE.parent/".env"` | 待修复（详见 tests/修复日志.md） |
| 09-12 | scripts/backfill_civil_embeddings.py | P1 | 同款 `.env` 读取缺陷 → 补向量脚本一运行即崩，向量补全路径形同虚设；与上条 P0 叠加构成"向量两端皆断"的完整链条 | 同款修复 | 待修复（详见 scripts/修复日志.md） |

**说明**：上述诊断过程（09-12 18:00 前）独立于本项目，采集使用探针 + `docker exec`，**未修改本仓库任何文件**，也未向生产库写入数据（仅只读检索 + 读取 `knowledge_chunks`）。18:00 起按主人指示落地 Phase 1 可观测性，变更登记如下。

## 2026-09-12 Phase 1 落地：检索层可观测性

起因：同日诊断实测发现向量召回 100% 失效（见上表 P0）却长期无告警，根因是**检索层零可观测**——`kb_search_total` 定义后从未被调用、无检索类告警、检索结果不落库。本次补齐这一层。

| 变更 | 文件 | 说明 |
|---|---|---|
| 检索追踪模块 | `app/agents/retrieval_trace.py`（新增，121 行） | `record()` 在检索出口打指标 + `spawn()` 后台落库，全程 fail-silent（与 error_aggregator 同哲学）；**只存客观事实、不存判断结论**——诊断规则由独立工具消费，两侧职责分离，规则演进不用动线上代码 |
| 埋点 | `app/agents/tools.py`（净 +6 行，577/600 硬限内） | `_search_two_legs` 把分路命中数/最高分写入 contextvar `_LAST_LEGS`（**不改函数签名与返回值**，因此不会污染给 LLM 的 tool 消息）；`search_knowledge` 出口计时并调用 `record()` |
| 指标 | `app/core/metrics.py`（+5 个） | `kb_leg_empty_total{leg}`、`kb_recall_empty_total{persona}`、`kb_rerank_dropped_total`、`kb_low_confidence_total`、`kb_retrieval_latency_seconds`；`kb_search_total` 首次被真正调用 |
| 建表 | `alembic/versions/c4d5e6f7a8b9_retrieval_traces.py` | `retrieval_traces` 表 + 时间索引 + 单腿故障部分索引（`WHERE vec_hits = 0`） |
| 告警 | `alert.rules.yml`（+4 条） | `RetrievalLegEmpty`（单腿降级，本轮核心）、`RetrievalEmptyRecall`、`RerankSilentlyDropped`、`RetrievalLatencyHigh`；均带「10 分钟 ≥10 次检索」流量门槛，沿用 `CacheHitRateLow` 的防误报经验 |

**验收（本地端到端实测）**：跑一次真实检索，`retrieval_traces` 落库一行
（`trgm_hits=15 / vec_hits=0 / max_trgm_sim=0.0156 / rerank_used=false`），
指标 `kb_leg_empty_total{leg="vector"}=1`、`kb_rerank_dropped_total=1`、
`kb_low_confidence_total=1`、`kb_search_total{result="hit"}=1` 全部正常上报；验证写入的行已清理。

**部署状态**：迁移已在本地库应用（`b9c0d1e2f3a4 → c4d5e6f7a8b9`）；**生产容器仍是旧代码，需重建镜像埋点才生效**。

**待办**：`retrieval_traces` 的保留策略（默认 7 天）——`retrieval_trace.purge_expired()` 已提供，尚未挂到定时清理，`scripts/retention_cleanup.py` 需补一条。

## 2026-09-12 修复轮：P0 × 1 + P1 × 5（主人授权"可以进行修复"）

### P0：embedding 配置链（已修 + 真实验证）

`_generate_embedding` 把空串当唯一候选发出去 → 静默返回 None → 向量召回 100% 失效。

- **配置层**：`.env` / `.env.example` 的 `EMBEDDING_API_URL`、`EMBEDDING_MODEL` 对齐建库侧
  （`http://host.docker.internal:11434/api/embeddings` + `shaw/dmeta-embedding-zh`）
- **代码层**：新增 `config.embedding_endpoint_candidates()`——显式配置优先，为空时从**已配好且可达**的
  `OLLAMA_URL` 派生 `/api/embeddings`（注意 Ollama 嵌入接口与生成接口不同），全部失败时明确告警

**真实修复效果**（55 条重采对比，非推演）：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| recall@5 | 0.3879 | **0.6848** |
| MRR | 0.3491 | **0.5855** |
| 零命中条数 | 32 | **15** |
| 健康样本 | 0 | **47（85.5%）** |
| RC-1b 单腿故障 | 55 | **0** |

**与修复前的 what-if 推演数字逐项完全一致** —— 印证"唯一断点就是 embedding 生成"。

### P1：修复 5 项

| # | 问题 | 位置 | 状态 |
|---|---|---|---|
| 1 | `.env` 实为空目录，`exists()` 判断后读取必抛异常，脚本一运行即崩 | `tests/run_retrieval_eval.py` | 已修 |
| 2 | `TOP_K=20` 假指标（`recall@10/@20` ≡ `recall@5`） | `tests/run_retrieval_eval.py` + `app/agents/tools.py`（`RERANK_TOP` 改环境变量可配） | 已修 |
| 3 | CDC 测试隔离缺陷（全量跑随机失败、每次失败用例不同） | `tests/unit/test_p0_p1_fixes.py` | 已修（239 passed / 0 failed） |
| 4 | 同款 `.env` 缺陷，向量补全脚本从未跑通 | `scripts/backfill_civil_embeddings.py` | 已修 |
| 5 | **保留策略脚本从未跑通过**（各表时间列类型不统一，统一传字符串必崩某张表；`requests.start_time` 竟是 `real`） | `scripts/retention_cleanup.py` | 已修（全表 dry-run 通过） |

### 附带完成

- `retrieval_traces` 纳入保留策略（7 天）
- `.env.example` 补"查询侧必须与建库侧同模型"说明
- 全量单测 **239 passed / 0 failed**；编码规范门禁通过

### 环境注意（本机沙箱特有，非产品缺陷）

受限沙箱会**拦截对工作区 `.tmp/` 下文件的删除**（safe-delete 批量阈值 50），
使"依赖删除成功"的测试清理逻辑静默失效、残留累积。已改用系统临时目录规避；
`prune_old_files` 本身实现在系统临时目录实测正确（`removed=1`）。

## 2026-09-12 P1 修复：Dockerfile 误伤业务数据文件（三套机制线上静默空转）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | Dockerfile:44-45 | **P1** | 09-10 为「评测产物与集成测试不进生产镜像」整体移除了 `COPY tests/`，**误伤了 4 个业务数据文件**。它们是 `app/agents` 运行期必需的数据源，容器内 `/app/tests/` 整个目录不存在 → 三套机制**静默空转**（只在启动后打一条 warning）：<br>· `民法典映射表.txt` → `tools.py` 口语→术语映射（实测 52 条）<br>· `民法典补充协议.txt` → `law_mapping.py` 法律纠错（实测 66 条）+ `routing_table.py` 黑名单<br>· `关键词白名单.txt` → `routing_table.py` 民法典关键词白名单<br>· `互斥.txt` → `routing_table.py` 非法域黑名单 | 精确 COPY 这 4 个数据文件到 `./tests/`（评测产物与集成测试仍不进镜像） | **已修** |
| 09-12 | Dockerfile | P2 | `tests/` 目录混放"业务运行时数据"与"测试产物"，是本次误伤的根源（评审时同样容易误判） | 建议后续把 4 个数据文件迁到 `data/legal/` 并改 3 处加载路径；本轮为控制改动面未动 | 登记 |

**实测收益**（55 条标注，同一批 50 条交集口径）：recall@5 `0.7233 → 0.7333`（+0.0100），
零命中 `12 → 11` 条。

**同期发现的排查教训**：该缺陷的表现是"warning 一闪而过、功能静默降级"，靠读代码很难发现
——是通过**采集真实 trace 时看到容器里 `法律映射表文件缺失` 的告警**才顺藤摸瓜查出来的。

## 2026-09-14 全量只读审计（Agent/RAG 主链 + 全库机器扫描）

> 范围：`app/`、`ragclosure/`、`frontend/src/`、`desktop/src/`、`scripts/`、
> `tests/`、`alembic/`、`deploy/`、`.github/` 与根目录脚本。
> 方式：全库静态扫描 + 高风险主链逐段人工复核；遵循“只读不改”，本轮不提交业务代码改动。
> 机器扫描：312 个 Python 文件语法通过；24 个 Shell 脚本 `bash -n` 通过；
> 5 个 PowerShell 脚本解析通过；18 个 YAML 文件解析通过；密钥扫描零命中；
> 283 条测试可收集，281 条 unit 测试通过。

### 全量结论

| 项 | 结论 |
|---|---|
| Agent | 任务创建、简单任务、ReAct、快速路径、降级、缓存、配额、记忆链均发现确定性缺陷，详见 `app/agents/修复日志.md`、`app/services/修复日志.md`、`app/core/修复日志.md` |
| RAG | 检索与依据链存在生产级配置缺失、单腿故障不可识别、历史回填告警失真和依据证据不可视问题，详见 `ragclosure/修复日志.md` |
| 前端 | Markdown 输出已过 DOMPurify；控制台事件详情缺 evidence/patch/关联 verdict，详见 `frontend/修复日志.md` |
| 代码质量 | 未发现新增硬编码密钥、YAML 语法错误、Shell/PowerShell 语法错误或 Python 语法错误 |
| 测试 | 全量 unit 281 passed；集成迁移与支付用例未在本轮执行 |

### 待主人决策的逻辑问题

| # | 决策点 | 选项 A | 选项 B | 推荐 |
|---|---|---|---|---|
| 1 | 零召回时是否允许模型凭内部知识回答 | 拒答或兜底，保证 RAG 依据契约 | 允许回答，但 RC-4b 降为提示级并明确“非知识库依据” | A |
| 2 | 向量召回是否是上线必需能力 | 作为生产依赖部署 embedding 服务并加健康检查 | 显式定义关键词单路模式，移除向量健康假象 | A |
| 3 | 任务端点是否纳入请求级 RAG 监测 | 补齐 `begin_request/finish_request` 和阶段 span | 标记任务端点为监测范围外 | A |
| 4 | 内部失败是否退还试用额度 | 未产生模型调用/输出时退还，部分输出不退 | 只要创建任务即消耗 | A |
| 5 | 上传文件是否增加 MIME/魔数校验 | 增加文件头、MIME、压缩炸弹限制 | 仅保留扩展名白名单 | A |

### 状态说明

本轮只读审查前，工作区已有 8 个未提交文件改动（来自上一轮中断前）：
`app/agents/react_steps.py`、`app/agents/router.py`、`app/agents/runner.py`、
`app/core/task_manager.py`、`app/services/agent_tasks.py`、
`app/services/chat_stream_ctx.py`、`tests/unit/test_agent_security_fixes.py`、
`tests/unit/test_agent_task_lifecycle.py`。
本轮没有继续修改这些文件，也没有回滚；相关修复状态在各模块日志中单独标注。

## 2026-09-14 修复执行轮（主人指示：按修复日志逐文件夹修复；先完成上轮中断验证，再清全部"待修"项）

### 第一步：完成上轮中断的验证（6 项工作区改动 + 2 个新测试文件）

上轮已改未验的 6 项（router await finish_task、react_steps 计费导入、runner 缓存构建入 try、
agent_tasks 额度回滚+统一限额、save_idempotent 改 await 直写、chat_stream_ctx 统一限额）
+ 新增测试全部通过；改前工作区已备份 `_backups/20260914_workspace_state/workspace.patch`。

### 第二步：按日志清欠（5 份日志 22 项"待修/待决策"，修 21、核 1、留 1 部署项）

| 域 | 修复要点 |
|---|---|
| app/agents | 降级链收尾统一 `_finish_text_answer`（记忆+缓存，追问不再失忆）；任务路径接入请求级 trace（决策 #3A：begin/mark_route/answer span/finally finish）；人格提示词改 `safe_format_prompt`；格式化失败改可读降级视图不再外泄原始 JSON；sub_agents 补 KeyError/IndexError 重试；`_rerank_local` 推理异常降级 RRF；工具超时回收经复核确认 wait_for 语义已覆盖（不加码） |
| app/services | chat_react 降级统一 finalize（`_fallback_and_finalize` 聚合）+ 圆桌补 username 计费；chat_stream_core 两处漏 finalize 补齐（重读缓存命中 / 顶层降级）；`accumulate_sse_text` 聚合助手（chat_fallback）；任务并发上限迁 Redis 原子计数（4 实例共享 50 槽）；claim 冲突映射 409；额度 DB 故障映射 503 |
| app/core | `claim_idempotency` 超时返回 `CLAIM_IN_PROGRESS`（不再并发放行）；`QuotaDependencyError` 三态；`_summary_locks` 引用计数回收；`drain_background_tasks` 挂入 main 关闭序（排空审计/缓存/计费任务，先于连接池关闭）；memory 冷备 3 次退避重试；任务内部失败未产出退还试用额度（决策 #4A） |
| ragclosure | `RecallLeg.from_dict` 异常信号归一（顺带修 hits 别名 TypeError 既有 bug）；探针采腿级故障（整体异常 + embedding 失败两种形态）；monitor 重建召回腿 + RC-1b 入子集；回填行不打告警指标；RC-4d 消费内容级幻觉引用；零召回提示词与 RC-4b 口径统一（决策 #1A，chat_fast_paths/chat_fallback 话术）；迁移 m4c5d6e7f8a9（vec_failed 列） |
| frontend | RagConsoleView 事件详情渲染样本证据（evidence/patch/关联请求跳转）+ RagVerdictSample 类型；RagOpsChatView 问询上下文回带 incident 证据 |

### 结构门禁当场拆（4 处违规清零）

`tools.py`(765 存量) 拆出 `kb_expand.py`（条号直钉/邻接/章扩/保护带，旧名 re-export）；
`router.py`(622) 拆出 `simple_task.py`（格式化/收尾原语）；`runner.run_agent_task` 拆
`_enter_cache_gate/_prepare_task_run/_fail_task_sanitized`；`chat_react._stream_react` 拆
`_fallback_and_finalize`。拆分后 tools/router 回落 600 行内，全部函数 ≤80 行。

### 终验

pytest tests/unit **295 passed**（基线 281 + 新增 14）；ragclosure **42 passed**；
vue-tsc 0 错误；vitest **32 passed**；规范门禁**无违规**；pyflakes undefined name = 0；
compileall 通过；alembic 单链 27 迁移 head=m4c5d6e7f8a9 无分叉；import 冒烟 64 条路由正常；
secret_scan 对 app/ragclosure/tests/alembic 干净。

### 待主人/部署侧（未闭合）

- **决策 #2A（ragclosure P0）**：生产 embedding 服务部署 + readiness——需服务器侧动作，代码侧兜底与故障标志已就绪
- 生产容器需重建镜像 + `alembic upgrade head`（vec_failed 列）后，RC-1b/RC-4d/回填防风暴才生效
- 已登记架构项不变：持久 outbox、任务槽位 Redis 计数的 TTL 边界（24h 全实例停机自愈归零）等

## 2026-09-14 追加：全仓库修复日志清欠轮（主人指示：不只 app，其余文件夹日志一并查看修复；完成后审查）

扫描全仓 19 份修复日志，13 份存在未闭合条目。分三类处置：

### 一、核验后回写状态（历史已修但日志未更新，本轮逐条对照代码复核）

scripts（secret_scan 字符类、手机号外置、弱口令脚本、门禁根推导、slow_query、task_scheduler×2）、
routes（metrics/SMS/stats/oauth/map_api 七项）、payment（退款白名单/abs 基线/对账符号/幂等键/
打点/5 分钟结算/死导入七项）、middleware（revoke 区分、续期租约过期校验、change_password try）、
alembic（f6a7 去重对称、c3d4 phone 对称 DROP）、deploy（deploy_aliyun 三项、Dockerfile.lite
多阶段、tempo 去 root、alertmanager 邮箱出库）、tests（摆设测试改造、破坏性脚本隔离、.env 空目录、
迁移测试求值序+连通性、fake_redis 分发）、prompts（手机号脱敏+去重）、mcp_assets（去自动安装）。
复核中发现两条与历史结论不符，如实登记：middleware 的 get_cached_user 300s 缓存——全仓无
role/is_active 变更入口，缓存失效无代码接线点，维持登记并注明前提；rate_limit fail-open——
可用性取舍维持待拍板。

### 二、本轮新修（代码/迁移/配置，逐项有验证）

| 域 | 修复 |
|---|---|
| app/cdc | events 接口新增 (txid,id) 复合游标 + worker 同款可见性条件（修"晚提交小 id 事件永久漏"），响应带 txid/next cursor，旧 after 兼容；ensure_schema 补三查（txid 列 / RANGE 分区 / capture 函数含 txid_current），旧 schema fail loudly |
| alembic | 新迁移 n5d6e7f8b9c0（cdc txid 防御回填后 SET NOT NULL）；新迁移 o6e7f8a9b0c1（15 列 naive timestamp → timestamptz，静态 ALTER + UTC 显式换算）；f0a1 downgrade 补 USING；baseline 删冗余 idx_project_knowledge_embedding + downgrade 末尾 DROP EXTENSION |
| deploy | nginx.conf.lite /health 改 proxy_pass 网关（修发布健康检查被硬编码 200 架空）；nginx.conf.lite.local 同步 /metrics、/test-otel 404 屏蔽 |
| .github | code-rules.yml 顶层 permissions: contents: read；移除 --skip-files app/agents/tools.py（拆分后达标） |
| app/payment | refund 重构三段式：读单短借还 → 渠道 I/O 零连接 → 结算短借还（事务内锁行复验兜 TOCTOU），慢渠道不再占满连接池；RechargeRequest/RefundRequest 字段限长 |
| app/models | file_ids 单项 StringConstraints(128)、user/persona_id/lang 补 max_length |
| tests | test_payment_idempotency 改白名单内 simulated_alipay（原 balance 渠道被白名单拒绝且非支付路径） |

### 三、验证（本批）

迁移链真实冒烟：本机临时库 `upgrade head → downgrade base → upgrade head` 全通，
downgrade base 后业务表清零且 pg_trgm/vector 扩展残留=0，二次 upgrade 后 txid NOT NULL=True、
users.created_at=timestamptz（此前"扩展残留=2"是二次 upgrade 重建的正常结果，检查顺序错误，
已用修正探针复核）。pytest 295 passed；workflow YAML 解析通过；全部改动文件 ast.parse 通过。

### 与上批合并的终验与审查结论见下节。

## 2026-09-14 修复审查轮（规则：可复用资产/SOP/Review规则.md，范围=本工作区两批全部未提交改动，54 文件 +1356/-604）

逐文件按 L1 执行流 / L2 业务逻辑 / L3 防御安全 / L4 生态对标 / L5 测试完备审查。证据：全量 diff 逐段复读 + 295 单测 + ragclosure 42 测试 + 本机临时库迁移冒烟（upgrade/downgrade/upgrade）+ 门禁/pyflakes/secret_scan。

### 审查发现（均已当轮修复）

| 级别 | 位置 | 问题 | 处置 |
|---|---|---|---|
| P2（C-F11 同族） | chat_react.py `_fallback_and_finalize` | finalize 在 saved_normally 置位前执行——若 finalize 抛异常（如 DB 抖动），上层 `_fallback_events` 会再降级出第二段回答（[DONE] 已发出） | saved_normally/finished 先于 finalize 置位 + finalize 包 try/except（内容已送达，异常只影响台账） |
| P2（同族） | chat_stream_core.py `_fallback_events` | saved_normally 仅在 finalize 成功后置位，失败路径状态不完整 | saved_normally 无条件置位（内容已送达），finalize 异常吞掉记日志 |
| P2（同族，含存量） | chat_stream_core.py `serve_from_cache` 缓存命中 / `rebuild_lock_gate` 重读命中 | 两分支 finalize 位于终止事件后且 saved_normally 未置位，同类二次回答风险（前者为存量，后者本轮新增） | 统一补 saved_normally 先置位（与 09-12 C-F11 修复同款口径） |
| P2（结构） | refund.py `process_refund` | 重构后 83 行超 80 门禁 | 拆出 `_load_refund_order`（读单+已退累计短借还），回落门禁内 |

### 逐层结论（要点）

- **L1 执行流**：全部新函数导入/await/类型核对无误；pyflakes undefined=0；ast 全过；FastAPI `int | None` Query 参数与 pydantic v2 兼容。
- **L2 业务逻辑**：refund 三段式重构的 TOCTOU 由 `_settle_refund` 事务内 FOR UPDATE 复验 + 重查已退累计兜住（超退缺口走对账，与充值路径同口径，注释已注明）；槽位 Redis 计数的失败组合矩阵核对（reserve 成功+release 失败=计数泄漏有界，24h TTL 自愈；reverse 组合下溢由 set 0 兜住）；幂等 claim 409 路径早退于所有资源预留，except 清理序列逐状态核对无错配；`_vec_failed` 在改写重试二次召回后取最新态正确。
- **L3 防御安全**：新 SQL 全参数化（迁移为静态字面量清单）；CDC 新端点参数纯 int 且沿用 admin 门；nginx /health 代理无新增暴露面（/health 本为公开探活）；密钥扫描代码目录零命中；迁移 f-string 的表名列名为文件内静态清单，非用户输入。
- **L4 生态对标**：零新依赖——asyncio.wait 排空、引用计数锁、Redis INCR/DECR 均为平台原语复用。
- **L5 测试完备**：新增回归 14 例全部真断言（含"仅 error 输入置 failed"“占位超时返回冲突”"畸形结构走重试"三个行为级用例）；迁移冒烟为真实 DB 实测。**剩余缺口（登记）**：①refund 三段式无单测（支付域仅 opt-in 集成测试，需假渠道夹具，专项）；②CDC (txid,id) 游标无 API 级测试（admin 门 + PG 依赖，专项）；③并发租约/claim 的高并发行为仅靠逻辑推演，无压力级测试。

### 结论：**可发布**

（条件不变：生产需重建镜像 + `alembic upgrade head` 应用 m4c5d6e7f8a9/n5d6e7f8b9c0/o6e7f8a9b0c1 三个新迁移；embedding 服务部署为独立决策 #2A。）

## 2026-09-14 逐行重审轮（主人指示"重审，逐行审阅"——方法升级：不再以 diff 为索引抽查，改为整函数/整文件复读 + 全调用方契约核对）

分 8 组逐行过完全部改动文件（agent 主链 / 检索链 / router+simple_task / services 七文件 / core 五文件 / models+payment+cdc / 迁移×5+nginx×2+CI / frontend×3+tests×3）。逐组要点与发现：

| 组 | 逐行核对要点 | 发现 |
|---|---|---|
| 1 agent 主链 | `_finish_text_answer` 尾部引用的 `sf` 在循环首轮必赋值；`_prepare_task_run` 丢弃无消费的 final_query 与原行为等价；begin_request 在任务协程内自建（创建路由无活跃 trace，contextvar 隔离正确）；`_refund_quota_if_unproduced` 读的 result_buf 与 fail_task 写的 hash 字段是不同键，互不覆盖 | [P3 存量] react_steps._consume_llm_stream:53/63 ContentStreamGuard 重复构造（后者遮蔽前者，无害，登记不动） |
| 2 检索链 | kb_expand 全文=原代码忠实搬迁；tools 无残留引用（_PIN_RE/_ART_HEAD_RE/_chinese_to_arabic 全部随迁）；`_vec_failed` 读的 `_LAST_EMBEDDING` 在每次 `_recall_two_ways` 返回前无条件 set，改写重试后取最新态，无跨请求残留（task 上下文隔离）；adapter 探针包装顺序（先 emb 后 recall）与原函数的动态名字解析核对成立 | [P3] kb_expand docstring 函数名漂移——已修；[登记] what-if 模式（patch_embedding）会覆盖 emb 采集探针，vec_failed 恒 False——该模式本身在测 embedding 替换，语义自洽 |
| 3 router/simple_task | handle_simple_task 引用的 7 个原语经 re-export 全部可达；历史契约 import（router.safe_set/spawn，测试钉住）保留；fast_paths 零召回分支触发条件实测核对：`{"results": []}` 经 `_has_data` 判 False 正确落入新提示词分支 | 无 |
| 4 services | `finalize_answer` 全部 10 个调用点逐个核状态序——**发现系统性缺陷族**：6 处（含 4 处存量）在 finalize 之后才置 saved_normally，finalize 抛异常（DB/Redis 抖动）会让 `_fallback_events` 在 [DONE] 后再降级出第二段回答 | **[P1] 单点根治**：finalize_answer 本体整体 try/except（台账收口失败记 warning 不上抛）——一处修，10 个调用点全受益；与上一轮的 3 处局部置位叠加为双保险 |
| 5 core | `_KeyedLock` 并发时序推演（等待者持 refs 期间不会被 pop；pop 后新来者建新锁但旧持有者临界区已结束）；drain 对长驻 renew 循环的超时取消正确（monitor/maintenance 是手工句柄不在注册表，无双杀）；claim 409 路径早退于一切资源预留，无清理负担 | 无 |
| 6 models/payment/cdc | refund 三段式全流程复读：`_load_refund_order` 的"订单不存在"走外层 except → 未打渠道 → 安全删幂等键 → 可重试 ✓；`_invoke_channel_refund` 后的异常路径全部落"保留键转人工" ✓；CDC 游标新旧语义隔离、cursor 回显正确 | 无 |
| 7 迁移/nginx/CI | n5d6/o6e7 冒烟已真库实测；nginx 两份的插入块均在 server{} 上下文（limit_conn 锚点）；/health 代理的调用方（gw_publish 10090）行为变化即修复目标本身 | [登记] baseline downgrade 的 DROP EXTENSION 在"扩展由超级用户创建"的库上会权限失败——仅影响 downgrade base（冒烟/拆库场景），生产 upgrade 路径不受影响 |
| 8 frontend/tests | vue-tsc/vitest 全绿 | **[P2 本轮引入] RagConsoleView「查看请求」**：事件详情按钮只 set selectedRequest，但详情面板在 requests 标签页下渲染，停在 incidents 页点击无反应——openRequest 补 `activeTab='requests'`，已修 |

### 重审终验

规范门禁无违规；pytest 295 passed；ragclosure 42 passed；pyflakes undefined=0；vue-tsc 0 错；
vitest 32 passed；secret_scan app/frontend/src 干净。

### 结论：**可发布**（条件与上轮相同：重建镜像 + 3 个新迁移；逐行轮新增修复均无迁移面变化）

## 2026-09-14 逐行重审轮·第二批（主人指示"继续查、记台账、不走回头路"——对前两轮未整文件读过的 13 个文件补全全文逐行，一次走完）

### 本轮发现（按严重度，均已处置）

| 级别 | 位置 | 发现 | 处置 |
|---|---|---|---|
| **P1（迁移）** | o6e7f8a9b0c1 | 初版静态清单**漏 8 列**：invoices.issued_at、knowledge_chunks.created_at、payment_orders.paid_at/refunded_at/expire_at、tenants.created_at、audit_logs.created_at、message_ratings.created_at（初版只盘了基线 created_at/updated_at 形态的列，漏了可空时间列与后续迁移建的表；旧冒烟只抽查 users.created_at 未暴露） | 全量重新清点（grep 全迁移 naive TIMESTAMP）补全为 23 列；冒烟 v2 升级后断言 **public 零 naive 时间列残留** ✓，downgrade/upgrades 三步重跑通过 |
| P1（缺陷族单点根治） | chat_stream_ctx.finalize_answer | 逐点核对全部 10 个调用点：6 处（4 处存量）在 finalize 之后才置 saved_normally——finalize 异常会在 [DONE] 后触发二次降级回答 | finalize_answer 本体整体 try/except（台账失败记 warning 不上抛），一处修全族；与上批 3 处局部修复叠加双保险 |
| P2（本轮引入） | RagConsoleView openRequest | 事件详情「查看请求」只 set selectedRequest，详情面板在 requests 页渲染，停 incidents 页点击无反应 | openRequest 补 `activeTab='requests'` |
| P2（存量同族） | RagConsoleView openIncident | 概览页点事件行同样跨页无反应（与本轮修复的 openRequest 同族） | openIncident 补 `activeTab='incidents'` |
| P2（存量漏网） | chat_fast_paths._run_recommend_tools | 推荐通道用裸 create_task（全仓 spawn 化的同型漏网）：生成器被断连 GeneratorExit 早闭时 tasks 局部引用随帧销毁，在途任务成孤儿 | 改 spawn（持引用 + 异常认领） |
| P3 | router._build_simple_prompt | 人格格式化本地 try/except 变体与 safe_format_prompt 完全重复 | 统一走 chat_support.safe_format_prompt |
| P3 | kb_expand docstring | 函数名漂移（article_index vs _article_index） | 已修 |
| P2 登记 | tools.search_knowledge | **存量架构**：整个检索（含低置信改写重试的 LLM 调用与 2-5s 本地重排）持一条池连接——慢重排高并发下可挤压连接池（池 max=50，与 refund 09-11 P1 同型，但检索侧改造面大：管线跨模块重组，无该缝测试覆盖） | 登记专项，不在审查轮动刀 |
| P3 登记 | react_steps._consume_llm_stream:53/63 | ContentStreamGuard 重复构造（无害） | 登记 |
| 登记 | baseline downgrade | DROP EXTENSION 在"扩展由超级用户创建"的库上会权限失败——仅影响 downgrade base 拆库场景，生产 upgrade 路径不受影响 | 登记 |

### 台账（逐文件结论，一次走完不再回头）

| 文件 | 结论 | 文件 | 结论 |
|---|---|---|---|
| router.py（全文） | 过；格式化去重 | retrieval_trace.py（全文） | 过（legs 兼容/落库/span 核对） |
| runner.py（全文） | 过；result_buf 与 hash 键分离确认 | chat_fast_paths.py（全文） | 过；spawn 修复 |
| tools.py（全文） | 过；检索持连接登记 | main.py（全文） | 过（drain 时序在 _stop 之后正确） |
| kb_expand.py（全文） | 过；docstring 已修 | baseline 迁移（全文） | 过；扩展/冗余索引编辑复核 |
| simple_task.py（全文） | 过 | f0a1（全文） | 过 |
| react_steps.py（全文） | 过；双 guard 登记 | n5d6/o6e7（全文） | 过；漏列补全+冒烟 v2 |
| sub_agents.py（全文） | 过 | nginx.conf.lite/.local（全文关键段） | 过 |
| ragclosure schema/adapter（全文） | 过 | code-rules.yml（全文） | 过 |
| cdc routes/schema（全文） | 过 | ragAdmin.ts（全文） | 过 |
| core×4 + quota（全文） | 过 | RagConsoleView（全文） | 过；两处跨页修复 |
| models/schemas、payment/models（全文） | 过 | RagOpsChatView（全文） | 过 |
| refund.py（全文终态） | 过 | test_payment_idempotency / test_20260914_audit_fixes / test_search_pipeline 改动区 | 过（_tm 桩目标核对：tools 内符号打桩路径有效） |
| chat_fallback/chat_react/chat_stream_core/chat_stream_ctx/agent_tasks/rag_monitor（全文） | 过 | prompts/me.json | 过（无手机号、无重复段） |

### 重审终验（第二轮）

规范门禁无违规；pytest 295 passed；ragclosure 42 passed；pyflakes undefined=0；
vue-tsc 0 错；vitest 32 passed；迁移冒烟 v2（upgrade head → 零 naive 列断言 → downgrade base → upgrade head）通过。

### 结论：**可发布**（发布条件不变；本批含迁移列清单修正，部署时以 o6e7f8a9b0c1 最新版为准）

## 2026-09-14 逐行重审轮·第三批（主人指示"审详细点、可只记日志不修"——补全前两轮剩余的头尾段与三个测试文件全文，按行号证据详记）

### 补读范围（到此全部改动文件的头尾段无一遗漏）

main.py:1-95/296-370、refund.py:1-55、nginx.conf.lite:1-39/118-142、nginx.conf.lite.local:1-38/95-141、
baseline 迁移:100-112、RagOpsChatView:1-30/359-420、RagConsoleView 样式段:464-580、
test_search_pipeline.py 全文（301 行）、test_agent_security_fixes.py 全文（357 行）、
test_agent_task_lifecycle.py 全文（293 行）、prompts/me.json（解析级验证）。

### 本批发现（按授权，存量项只登记不改）

| 级别 | 位置 | 发现（行号证据） | 处置 |
|---|---|---|---|
| P3 登记 | main.py:329-335 | 监控中间件 endpoint 标签回退 `request.url.path`——404/异常请求的原始路径会进 Prometheus 标签，随机路径可造成基数膨胀（正常路由命中时用 route.path 无此问题） | 登记：可加"仅 route 命中才打点，未命中统一记 unmatched" |
| P3 登记 | main.py:44-58 | `_ensure_admin_users` bootstrap-only 语义（改 env 不改已有口令）——既有"待主人确认"项，本轮逐行复核语义与注释一致 | 维持待确认 |
| P3 登记 | deploy/nginx.conf.lite:5 vs nginx.conf.lite.local:5 | WAF map 变量漂移：lite 用 `$document_uri`（解码后路径），lite.local 用 `$request_uri`（含 query 原文）——两者特征集相同但匹配面不同；与既有"简易 WAF 不能作为唯一防线"登记同族 | 登记：统一为 $document_uri 更优（解码后匹配），随 WAF 专项 |
| P3 登记 | RagOpsChatView.vue:60 | `requestCache` Map 无上限——admin 专属页、列表仅 20 条，实际增长有界 | 登记（可不修） |
| 通过 | refund.py:28-52 `_decide_refund` | 逐行复核：白名单门→状态门→abs 基数→`requested is None` 显式判断（注释点明 Decimal("0") falsy 陷阱）→上限门，纯函数无副作用 | 通过 |
| 通过 | main.py:80-89 | 关键表清单 16 张含监测四表+stage_spans，缺表 fail loudly（与 init_db 契约一致） | 通过 |
| 通过 | test_agent_security_fixes.py 5 个新测试 | 逐测试推演桩路径：①runner 锁超时测试经 mm=None→`get_context if mm is not None`→cacheable=True→二次 acquire None→runner 命名空间 finish_task 桩记录"服务繁忙"（确定性，不依赖网络）；②cache_ctx 异常测试命中我批的 try 内构建+`_fail_task_sanitized`(task_id,content) 两参签名；③降级计费测试——react_steps 函数级 from-import 在每次调用时解析，monkeypatch ls.record_token_usage 必然生效；④`_emit_simple_result` 拆至 simple_task 后函数内 from-import 同理命中 tm 桩 | 5 个测试全部"为正确的原因通过" |
| 通过 | test_agent_task_lifecycle.py 7 个测试 | ①/quota 回滚两测试的桩均打在 agent_tasks 命名空间全局，与本轮 503/409 改造后的调用形态一致；②chat 门禁测试的 `_get_limits`/`reserve_used_questions` 桩与我改后的全局查找匹配；③react 取消计费测试经 done=True 早退在 max_steps 尾之前，不受收尾改动影响；④HashRedis CAS 语义与 task_manager 两段 Lua 对齐 | 通过 |
| 通过 | test_search_pipeline.py 全文 | 两处 `_tm`（tools 模块）打桩在调用期经模块全局解析有效；`_NEIGHBOR_INDEX` 桩正确指向 kb_expand（唯一移动物）；`_vec_failed` 新增不触碰任何断言（contextvar 默认 None→True 仅入 5 元组）；299 行 method 精确断言与现实现一致 | 通过 |
| 通过 | nginx 头尾 | WAF map→server 级 if→limit_conn→路由序（=/metrics 精确匹配优先于 /）逐段核；/health 代理位于 location / 之前语义正确（nginx 最长前缀匹配，顺序无关但可读性好） | 通过 |
| 通过 | RagOpsChatView 头尾 | imports 全部在用；v-html 唯一两点（MessageBubble/本页）均走 renderMarkdown=DOMPurify；enter.exact 发送与 shift+enter 换行并存 | 通过 |
| 通过 | me.json | 解析级：无 11 位手机号、无重复文案段、JSON 结构完好 | 通过 |

### 第三批终验

规范门禁无违规；pytest 295 passed；ragclosure 42 passed；vue-tsc 0 错；vitest 32 passed（均为改动后复跑）。
本批**零代码改动**（按授权全部为登记），终验沿用第二批修复后的状态。

### 三批累计结论：**可发布**

逐行审阅至此对全部 54 个改动文件 + 7 个新文件完成整文件级覆盖；三批共修复 13 处（P1×3：迁移漏列、
finalize 缺陷族、上批 await 类），登记 9 处存量/边界（P2×2：检索持连接、WAF 变量漂移；P3×7）。
发布条件不变：重建镜像 + 应用 m4c5d6e7f8a9 / n5d6e7f8b9c0 / o6e7f8a9b0c1（23 列版）。

## 2026-09-15 code-review-skill 全量评审（覆盖前序“可发布”结论）

### 2026-09-15 生产级修复实施

全量审查发现的 11 项阻塞和 25 项重要问题已完成代码修复与本地验证：
支付尝试状态机、timestamptz 统一、消费退款禁令、RAG 处理水位与全腿故障、
验证码原子校验、前端账号隔离、跨块敏感词、工具注入边界、知识库扩召权限和
核心并发问题均已收口。

验证证据：后端 `360 passed`；前端 `39 passed` 且生产构建通过；规范门禁
`app mcp_assets scripts` 通过；密钥扫描通过；迁移链
`upgrade head -> downgrade base -> upgrade head` 通过；本地开发库已到
`r9c0d1e2f3a4`。独立复核后补修了退款 attempt 金额漂移、渠道成功但订单过期
卡单、取消任务被记成功、规则重放不更新旧 verdict、任务侧 answer trace 缺失、
消息重试重复插入和前端请求竞态。未接真实支付渠道，未做生产发布。

### 2026-09-15 未覆盖文件全量补充审查

按修复日志基线排除已明确覆盖文件后，对 54 个未被文件名覆盖的生产文件逐行复核，
不含 tests。四个独立代理按 core/services、Agent/RAG、payment/routes/console、
frontend/desktop 分域审查，主线程复核并补修 P0/P1。

- P0：本轮未发现。
- P1：路由包 eager import、数据库连接预算/命令超时、RAG incident 刷新、
  地图路线 500/空成功、终端控制字符注入、控制台标签缺失、绑定手机号假失败、
  Electron 设置窗导航与 IPC 隔离。
- P2：数据库池参数、密码哈希兼容、JSON 工具行为、readiness、安全头、Cookie
  代理协议、OTEL 日志、RAG 内容读取顺序/上限、渠道流水号、metrics 比较、
  worker 就绪状态、CLI 阈值有限性、前端主题/geo/评分类型、桌面健康重定向等
  已修或登记。

`rag_console_api.py`、`rag_console_worker.py` 已审：metrics 鉴权改常量时间比较，
worker readiness 增加任务存活检查，后台任务异常退出写入日志并暴露不健康状态。

依据 `awesome-skills/code-review-skill`（本地 HEAD `4850184dfd7765ea7b3c5c4e9b6cf8e0815ee7cb`）
对全仓做全量静态覆盖与高风险链路逐行复核。完整报告见
`code review/2026-09-15-code-review-skill-全量评审.md`。

本轮新增结论：

- **[blocking] B-01**：`payment` 消费单允许普通用户自助退款，已交付 token 可
  “先消费后退款”，形成免费额度漏洞。
- **[blocking] B-02**：两条召回腿同时失败时 RC-1 和 RC-1b 都不产出 verdict，
  控制台会把检索全故障判为健康。
- **[important] I-01**：RC-1b 把正常单腿零命中误报为 high 级单腿故障。
- **[important] I-02**：deferred 结算路径继续把余额扣成负数并写负金额，违反
  09-12 已落地的资金约定。
- **[important] I-03**：任务创建/恢复失败不回滚已累加的日请求计数。
- **[important] I-04 至 I-09**：控制台 Cookie 登出与令牌暴露、健康探测动作不可用、
  部署验收版本过期、终态分支漏 finalize、OAuth IP 限流可伪造、前端开发依赖漏洞。

终验证据：`pytest` 307 passed；`ragclosure` 42 passed；`vitest` 32 passed；
Vue 生产构建通过；桌面端 10 passed；规范门禁通过；AST 318 文件通过；
应用代码 Bandit 无 high；生产 npm/桌面依赖审计为零；运行中网关、控制台和 worker
健康探针通过。

未验证项：本地 `tests/integration -m integration` 本次 1 skipped、1 deselected，
迁移链冒烟未在本轮本地环境完成；SQL 静态告警已人工判定为静态白名单/参数化片段，
但尚未做真实 SQL 注入验证。

修正结论：在本报告 B-01、B-02 关闭前，仓库不能按生产可发布处理；前序
“代码级 P0/P1 已清零/可发布”的结论仅适用于其当时审查范围，不适用于本轮新增证据。


## 2026-09-15 全量重审收敛批次（dev-review 5 层，第二轮后只修不增）

依据 `code review/2026-09-15-全量重审-dev-review-5层.md`（两轮全量，P0=0，P1=19），
按"完全有把握才动"原则修复 4 项，每项钉一个回归测试：

- **B-06**：`app/payment/refund.py` 全额判定 `>= locked["amount"]` 未取 `abs()`，
  负数消费单部分退款被误标 refunded、剩余额度锁死 → 改 `>= abs(...)`。
  测试 `tests/unit/test_20260915_payment_refund_fix.py`。
- **B-09**：`ragclosure/rules.py` RC-2 用外部 `position` 当下标可越界 IndexError
  → 越界时跳过规则（对齐 schema 容错契约）。
  测试 `tests/unit/test_20260915_review_fixes.py`。
- **B-18**：`app/services/chat_stream_core.py` `_fallback_events` ctx=None 时
  `ctx.req` AttributeError（原被 except 兜住、行为不变，属可靠性清理而非 P1，
  报告定级已更正）→ ctx=None 直接产出兜底文案。
  测试 `tests/unit/test_20260915_review_fixes.py`。
- **B-01**：`alembic/versions/q8b9c0d1e2f3_users_phone_unique.py` 建唯一索引前
  无去重回填，存量重复 phone 会中断迁移链 → 先按 phone 保留最近更新账号、
  其余置 NULL，再建索引（对齐 n5d6e7f8b9c0 防御性回填做法）。
  离线 `alembic upgrade head --sql` 验证生成 SQL 正常。

未修（明确不碰）：B-02（CF-Connecting-IP 信任链涉及两套部署形态，无把握）、
B-03/B-04/B-05/B-07/B-08/B-11/B-12 及测试欠账批次，留待专项。

终验证据：`pytest` 351 passed（348+3 新增）；`ragclosure` 42 passed；
迁移链离线 SQL 生成通过。未接真实支付渠道，未做生产发布。

### 2026-09-15 收敛批次第二批（再修 2 项）

- **B-03**：`app/main.py` 监控中间件未匹配路由时用原始路径作 Prometheus
  endpoint label（未认证可构造任意路径撑爆基数）→ 归一为固定 "unrouted"，
  真实路由仍按 route.path 计数。
- **B-05（部分）**：`app/services/rag_monitor.py` run_cycle 的监测 Gauge
  原在事务内打点，提交失败/回滚时诊断丢失但 Gauge 已推进，RagMonitorStale
  最多被掩盖一个周期 → 打点移到事务提交之后。backfill 行不打指标的另一半
  （需新增指标，属设计决策）未修，留待专项。

回归测试：`tests/unit/test_20260915_review_fixes.py`（unrouted label 固定、
Gauge 提交后才推进 / 提交失败不推进）。
终验：`pytest` 354 passed（351+3）；`ragclosure` 42 passed。

### 2026-09-15 收敛批次第三批（再修 2 项简单 P2）

- **前端 error 复位**：`frontend/src/views/RagOpsChatView.vue` refresh 成功后
  `error.value = ''`——此前一次刷新失败后 UI 永久卡在错误页，15s 轮询恢复
  成功用户也看不到（对齐 RagConsoleView 同款）。回归测试
  `frontend/tests/rag-ops-chat.test.ts`（fake timers 驱动轮询验证清除）。
- **退款金额上限**：`app/payment/models.py` RefundRequest.amount 补
  `le=999999.00`，与充值侧 RechargeRequest 对称，防超大数进业务层。
  回归测试 `tests/unit/test_20260915_payment_refund_fix.py`（越界拒绝、
  边界值放行）。

终验：`pytest` 355 passed（354+1）；前端 `vitest` 39 passed（38+1）。

### 2026-09-15 收敛批次第四批（再修 2 项）

- **journal.py 重复定义**：`app/cdc/journal.py` `load_checkpoint` 定义两遍
  （227 行独立实现 + 257 行委托 load_checkpoint_pair），后者运行时遮蔽前者。
  与 init_db 的历史 Bug #9 同款模式。删除前者——生效语义（原子水位对单次
  读取）不变，纯死代码清理。已有 test_p0_p1_fixes 覆盖存留实现。
- **改密请求限长**：`app/routes/auth.py` ChangePasswordRequest 两个密码字段
  补 max_length=256（与登录请求对称；bcrypt 72 字节截断之外无增益，
  超长串只膨胀日志/内存）。回归测试在
  `tests/unit/test_20260915_review_fixes.py`。

本轮评估后放弃的项：schema.py timeout 改 _as_bool（注释明示"非空描述串
也算异常信号"是刻意设计，改动破坏语义）、_ensure_admin_users 重启复权
（可能是刻意的保底管理员设计，需产品决策）。

终验：`pytest` 356 passed（355+1）；`ragclosure` 42 passed。

### 2026-09-15 收敛批次第五批（再修 2 项）

- **journal.py 死代码清理**：删除 `append()`（单条写入，生产只用
  `append_batch`）与 `load_checkpoint_txid()`（零调用方）。中途曾连带删
  `_write_line`，被 test_journal_rotation_no_file_per_write 拦下（该测试
  直接驱动滚动行为）——已恢复，验证"只删零引用代码"纪律的必要性。
- **project_kb 负 LIMIT 兜底**：`_project_keyword_fill` 的
  `LIMIT top_k - len(results)` 在 results 已满/超量时为负（PG 负 LIMIT =
  无限制，退化成全表兜底）→ `max(0, ...)` + 为 0 即跳出。当前调用序
  不可达，属防御性加固。回归测试验证两分支（不发查询 / LIMIT 恒正）。

终验：`pytest` 357 passed（356+1）；`ragclosure` 42 passed。

### 2026-09-15 收敛批次第六批（按 code-review-skill 全量评审报告修 2 项）

先逐项核对该报告发现清单现状：B-01/B-02/I-01/I-02/I-03/I-05/I-06/I-08
均已在"生产级修复实施"及其后批次关闭。本轮修剩余 2 项：

- **I-07 残留（穿透占位分支漏 finalize）**：
  `app/services/chat_stream_core.py` serve_from_cache 的 __EMPTY__ 穿透占位
  分支发送 answer_complete 后未调 finalize_answer——会话历史、日请求计数、
  answer trace 缺失（同函数缓存命中分支 2026-09-12 已修，此分支漏网）。
  补 saved_normally + finalize(write_cache=False)（繁忙提示不得入缓存）。
  测试 test_serve_from_cache_empty_placeholder 强化：断言 finalize 被调且
  write_cache=False。
- **前端契约类型（报告 suggestion）**：`frontend/src/api/ragAdmin.ts`
  `ungrounded_citations: number[]` → `string[]`（后端
  chat_fast_paths.find_ungrounded_citations 返回条文号字符串数组，已验证；
  当前前端未消费该字段，纯类型修正零运行时影响）。

未修：I-04（Cookie 登出/令牌出 JSON，需前后端协议设计）、I-09（npm audit
在镜像源不可用，无法验证）。

终验：`pytest` 357 passed；`ragclosure` 42 passed；前端 `vitest` 39 passed、
`vue-tsc` 无错。

### 2026-09-15 收敛批次第七批（code-review-skill 报告漏网清理，2 项）

该报告全量发现逐项核对完毕：B/I 级、RC-4d 跳过建议、监控标签 nit 均已关闭
（第六批已修 I-07 残留与 ragAdmin.ts 类型）。本批修"次要问题"区剩余 2 项：

- **check_cherry_mcp.py 裸 except**（登记 scripts/修复日志.md:165 待修复）：
  收窄为具体异常类型并输出失败原因，不再吞任意异常。py_compile 通过。
- **spawn() 同步失败协程泄漏**（报告 suggestion）：无事件循环时
  `get_running_loop()` 抛错，调用点已创建的协程未消费会产生
  "never awaited" 告警 → spawn 内显式 `coro.close()` 后 re-raise。
  回归测试断言异常照抛且协程已关闭。

报告剩余未修（按授权不处理）：I-04（Cookie 登出需协议设计）、I-09
（npm audit 镜像源不可用）、install() 85 行重构、RagConsoleView 可达性。

终验：`pytest` 358 passed（357+1）；`ragclosure` 42 passed。

## 2026-09-15 未覆盖生产代码全量逐行审查（最终）

本轮先按历史修复日志排除已覆盖文件，再对剩余核心/业务代码做逐文件逐行复审；
不审测试代码，不做无关修复。共由四个独立代理分域复核 core/services、
Agent/RAG、payment/routes/console、frontend/desktop。

修复的 P1：

- `app/routes/__init__.py` 包级 eager import 改为惰性隔离，控制台不再被业务路由拖入。
- `app/core/db.py` 连接池默认值按多进程部署收敛，并增加 SQL command timeout。
- `app/services/rag_incidents.py` 规则重放更新 verdict 时同步刷新 incident。
- `app/routes/map_amap.py` 外部接口异常归一化、畸形公交字段防御、无可用路线不再假成功。
- `ragclosure/report.py` 终端报告剥离 ANSI/控制字符。
- `frontend/src/utils/ragLabels.ts` 补齐后端真实路由/阶段/状态中文映射。
- `frontend/src/components/BindPhoneModal.vue` 修复绑定成功但 profile 刷新失败时的假失败。
- `desktop/src/windows.js`、`main.js` 设置窗口导航隔离与 IPC sender 校验。

修复或登记的主要 P2：

- RAG 请求内容 JSONB 标量拆字符、answer trace 决胜排序、retrieval 读取上限和 chunk
  顺序恢复。
- 支付 attempt 结果写回加 processing 前置，模拟渠道流水号改 UUID。
- metrics Bearer 改常量时间比较，worker readiness 检查后台任务存活。
- CLI 阈值 nan/inf 校验，admin error 查看审计，密码坏哈希容错，JSON 工具参数语义一致。
- 控制台/桌面主题存储、Geo adcode、评分类型、健康重定向、挂载错误可见化。

未关闭项：

- 跨实例 LLM 并发限制仍是进程级，需要 Redis 分布式闸或网关层限流决策。
- 真实支付渠道、真实回调、真实短信和 Cloudflare Tunnel 生产链路未做环境级验收。
- 部分历史日志中的脚本、部署和测试专项登记仍属于非本轮范围。

终验：`pytest` 360 passed；前端 `vitest` 39 passed；前端普通构建和控制台构建通过；
规范门禁通过；迁移链通过；本地数据库 head=`r9c0d1e2f3a4`。未接真实渠道，
未做生产发布。

## 2026-09-15 Serena 静默与 Batch 1 本地单实例

Serena 反复拉起：

- 根因是 Codex 对 stdio MCP 按会话启动，每个线程各自生成一份 Serena 进程树。
- 已改为连接固定本地服务 `http://127.0.0.1:8765/mcp`；新增登录时后台启动任务
  `Serena Codex MCP`，脚本为 `C:\Users\PC\.serena\start_codex_mcp_server.ps1`。
- 已关闭 GUI 日志窗口和 Dashboard 自动打开；重复触发启动任务后仍只有 1 个监听端口。
- 旧 stdio 实例及其语言服务子进程已清理，只保留单一共享 HTTP 服务。

Batch 1 本地单实例验收：

- 只启动 `agent_gateway`、`rag_console`、`rag_console_worker`、`postgres`、`redis`，
  未启动 gateway2/3/4 和监控栈。
- 网关 `/health`、`/ready`，控制台和 worker `/ready` 均返回 200；网关容器健康状态为
  `healthy`。
- 控制台真实浏览器登录通过，管理员状态接口返回 200，页面无前端运行时错误。
- 数据库迁移 head=`r9c0d1e2f3a4`，RAG trace/incident/action 关键表和字段检查通过。
- RAG CDC schema 校验修复后，worker 成功成为 leader，不再循环告警。
- 缓存边界已确认：RAG 语义缓存在 `semantic_cache`，旅游城市缓存是
  `map:city_cache:*`；本轮未清理任何缓存。
- 本轮未运行高成本 RAGAS 全量评测，也未推送云端。

## 2026-09-15 Batch 2 本地 RAG 告警与修复闭环

- `scripts/run_rag_drills.py --allow-write` 五类规则演练全部通过：
  `RC-1`、`RC-6`、`RC-8`、`RC-4b`、`RC-4c`。
- 修复动作租约恢复演练通过：执行中 worker 崩溃后可由过期租约重新抢占，
  `attempt_count=2`，最终状态 `succeeded`。
- L1 审批闭环通过：控制台 API 创建 `clear_exact_cache` 后为
  `waiting_approval`，审批后转 `queued`，执行结果 `succeeded`，
  测试 query 不存在，实际删除数 0。
- 报警配置通过：`promtool check rules` 检查 26 条规则成功；
  Alertmanager 配置检查成功；`RagHighSeverityVerdict` 规则单元测试通过。
- 常驻 worker 报警指标链通过：插入本地演练 trace 后重启 worker，
  `/metrics` 出现 `rag_verdicts_total{code="RC-1",severity="high"} 1.0`。
- 浏览器验证通过：控制台显示 5 个活跃事件、1 个待审批动作，
  页面无运行时错误和横向溢出。

待确认的逻辑问题：

- incident 已 `resolved` 后，历史 `waiting_approval` / `proposed` / `queued`
  动作不会自动取消。审批接口会拒绝终态事件的动作，因此这些动作既不能执行，
  又会持续显示为待处理，只能人工取消。

## 2026-09-15 本地账号收敛与 Batch 3 低成检索评测

账号收敛：

- PostgreSQL `users` 实际仅有 `133****8241` 一个账号，无 `Fengfeng` 数据。
- `Fengfeng` 的残留来自 Chrome 保存密码（5 条）和旧压测脚本；Chrome 项已清除，
  脚本已改为只读取当前管理员配置。
- 管理员密码已按主人要求重置并同步 `D:\桌面\.env`，真实密码不写入日志。
- 重置后控制台登录、用户信息、管理员状态接口均返回 200。

Batch 3 检索评测：

- 评测集：民法典 holdout 45 条，关闭 LLM 查询改写，仅本机 embedding + CPU rerank。
- 评测前只清理 RAG `semantic_cache`，`map:city_cache:*` 保持 8 条未动。
- `KB_RERANK_TOP=15`：recall@5=`0.8407`，recall@10=`0.8741`，
  precision@5=`0.2267`，precision@10=`0.1200`，MRR=`0.8495`，
  平均延迟=`3168ms`，零命中 `CC079/CC080/CC083`。
- `KB_RERANK_TOP=20`：recall@5/10 不变，recall@20=`0.8815`，
  precision@20=`0.0633`，MRR=`0.8606`，平均延迟=`4268ms`。
- 增益仅来自 CC140 的 MRR 提升和 CC150 的第 16-20 位补召回；
  每轮额外约 1.1 秒延迟，暂不修改线上默认 `KB_RERANK_TOP=15`。
- 单例追踪：CC083 的正确法条在 trgm 第 3 位，但未进入 RRF 候选池；
  `KB_RERANK_TOP=40` 时被 CE 排回第 4 位。CC079、CC080 的 trgm/vector
  前 20 均无正确法条，主要是口语词与法条术语断层。
- 证据报告：`ragclosure/reports/batch3-holdout-top15.json`、
  `ragclosure/reports/batch3-holdout-top20.json`。

## 2026-09-15 民法典依据契约与 Qwen 主模型切换

产品契约：

- 民法典回答只使用 `search_knowledge` 检索结果；联网搜索、项目知识和其他工具
  即使被模型误调用也会在共享工具漏斗拒绝。
- 不要求覆盖所有相关法条，只回答与用户问题最直接相关的一条或少数几条。
- 零召回时不调用生成模型，直接返回“知识库未检索到可支持回答的民法典条文”。
- 有召回时先缓冲生成结果，校验引用的每个条文号都出现在检索依据中；校验失败
  不向用户发送模型原文，改用无依据提示。
- 民法典链路的主模型失败时不再走通用知识兜底，只返回知识库/生成服务不可用提示。

模型配置：

- 主力模型槽位改为百炼可用模型 `qwen3.7-flash`。
- 兜底模型保持 `deepseek-flash`。
- `.env`、`app/core/config.py` 默认值和 `.env.example` 已同步。
- 直接调用百炼验证 `qwen3.7-flash` 返回 200。

验证：

- 新增民法典依据契约测试，覆盖工具边界、零召回不调用模型、依据外引用拦截。
- 真实 API 联调：民法典问题正常检索并引用检索条文；非民法典问题由硬路由直接拒答。
- 后端全量回归 `367 passed`。

## 2026-09-15 代表用例召回复核与修复

口径：不考虑一条问题对应的全部法条，只判断“最直接相关的核心法条”是否进入
最终 top5；不使用 precision 或不加权的模糊总分。

样本：7 条代表用例，覆盖正常单法条、多法条、口语缺口、候选池截断和已知难例。

修复前（`KB_RERANK_TOP=15`，关闭 LLM 改写）：

- 核心法条 top5 命中 `4/7`。
- `CC079`、`CC080`：trgm 与 vector 前 15 均未召回核心法条。
- `CC083`：trgm 第 3 名已命中，但 RRF 第 18 名被 `RERANK_TOP=15` 截断。

本轮修复：

- `KB_RERANK_TOP` 默认与本地 `.env` 调整为 `20`，最终输出仍由 `top_k` 控制。
- 映射表新增“聘金 → 借婚姻索取财物 民法典第一千零四十二条返还”。
- 映射表新增“空调外机 → 不动产相邻关系 排放噪声 排除妨碍 民法典第二百九十四条”。
- Qwen 查询改写超时从 5 秒提高到 30 秒，`max_tokens` 从 80 提高到 256，
  避免 Qwen3.7-flash 的 reasoning token 吃完预算后返回空 content。

修复后（`KB_RERANK_TOP=20`，关闭 LLM 改写，再跑同一 7 条）：

- 核心法条 top5 命中 `7/7`。
- `CC079` final@5，`CC080` final@3，`CC083` final@4。
- 真实 API 联调 `CC079` 回答已引用《民法典》第二百九十四条并通过依据校验。
- 该结果仅代表小样本诊断，不宣称全量召回率。

证据：`ragclosure/reports/2026-09-15-representative-core-hit.json`。

待产品确认：

- 用户直接问“噪声污染”时，现有环境法纠正映射仍可能先于民法典检索拦截；
  需要确认是否将“邻居/相邻关系/排除妨碍/停止侵害”等民法典意图列为优先例外。

## 2026-09-15 召回并行与重排候选策略优化

分段计时（预热后两条代表用例）：

| 阶段 | CC079 | CC083 |
|---|---:|---:|
| trgm | 36.6ms | 35.0ms |
| embedding | 149.3ms | 176.8ms |
| vector | 9.1ms | 12.5ms |
| RRF | 0.03ms | 0.04ms |
| CE 重排 | 3594.8ms | 4801.4ms |

结论：数据库双路召回本身只有几十毫秒；主要瓶颈是 CPU CrossEncoder 重排。

优化：

- trgm 与 embedding 改为 `asyncio.gather` 并行启动，减少串行等待。
- 不再固定把 RRF 前 20 条全部送 CE；候选池 8 时按
  `RRF 2 + trgm 3 + vector 3` 分配，按 chunk_key 去重后重排。
- 20 条单标准法条复核：候选池 5 条覆盖 18/20，8 条覆盖 19/20，
  13 条也仅 19/20；因此生产候选池收敛到 8。
- 20 条最终核心 recall@5=0.95，precision@5=0.19，MRR=0.8042，唯一 miss 为 CC070。
- 预热后代表性重排耗时降到约 0.9~2.8 秒；CC083 从约 4.8 秒降到约 2.8 秒。
- 控制台 `grounding` 阶段名称从“依据校验”改为“依据外引用”，避免把
  `ungrounded_citations=0` 误读成“没有依据校验”。

缓存修复：

- 原缓存键包含 `conversation_id`，并且有历史时完全跳过缓存，导致重复提问实际上
  很难命中。
- 生产缓存上下文改为“用户名 + 人格 + 画像指纹”，不再绑定会话 ID；有历史时仍
  跳过缓存，避免上下文追问被错误复用。
- 双会话实测：第一次 6409ms 并完成检索，第二次 `cache_hit` 566ms，
  未再执行检索，回答一致。

验证：后端 `369 passed`；前端 `39 passed`；控制台构建通过。

## 2026-09-15 Qwen 思考模式延迟修复

控制台截图中的 `simple_fast_path` 并不代表只做缓存命中；它仍会执行
民法典检索、CrossEncoder 重排和一次 LLM 格式化。

根因：

- `qwen3.7-flash` 默认开启思考模式，简单问题会先输出大段
  `reasoning_content`，完成大量 reasoning tokens 后才输出正文。
- 截图时段请求的首包约 20 秒，不代表检索慢；对应 trace 中检索仅
  `783ms` 到 `6878ms`，其余主要是思考输出。
- 重启后的首个请求还会有约 5 秒的本地 CrossEncoder 冷加载。

修复：

- 新增 `LLM_ENABLE_THINKING=0`，qwen 系用户对话默认关闭思考模式。
- 统一 qwen 请求选项，覆盖流式对话、意图分类、摘要和地图等调用。
- 快速通道增加 `generation` span，控制台可分别查看检索与模型耗时。

端到端验证：

- 关闭思考后网关热态首包约 `2.1~2.5s`，模型阶段约 `1.1s`。
- 直连百炼关闭思考后首正文约 `0.46~0.79s`。
- 重启后首个请求约 `7.8s`，其中主要是 CrossEncoder 冷加载。

## 2026-09-15 Batch 5：召回质量与控制台评测展示

控制台问题：

- 概览此前只有空召回数和重排降级数，没有 recall@5、precision@5、MRR 和零命中用例。
- 控制台已新增最近一次 holdout 评测面板，并显示运行期 `RERANK_TOP`
  是否被 Redis 临时覆盖。

发现：

- L1 演练曾把运行期 `RERANK_TOP` 写为 `5`，后续评测虽传 `15`，实际仍被
  Redis 覆盖成 5，造成 recall@5/10/20 完全相同。
- 本地 Ollama 未启动，vector 腿持续失效，控制台产生 `RC-1b`。
- Batch 3 后新增的大窗口候选配比切掉了 RRF 第 9~15 位，导致质量回退。
- `CC083` 的“赊销合同/职务代理”与法条术语存在确定性断层。

修复：

- 恢复大窗口 `RRF top-N`，小窗口继续保留来源配比。
- 启动本地 `shaw/dmeta-embedding-zh`，恢复 vector 召回腿。
- 新增“赊销合同 → 职务代理/执行工作任务/不得对抗善意相对人/第一百七十条”映射。
- 生产默认 `KB_RERANK_TOP` 从 `8` 调整为 `15`。
- 控制台显示评测指标、零命中用例和运行期覆盖状态。

Holdout 45 条最终结果：

- recall@5：`0.9407`
- recall@10：`0.9630`
- precision@5：`0.2533`
- MRR：`0.9037`
- 平均 / P95 延迟：`3285ms / 4400ms`
- recall@10 零命中：无

相对 Batch 3：

- recall@5：`0.8407 -> 0.9407`
- recall@10：`0.8741 -> 0.9630`
- MRR：`0.8495 -> 0.9037`
- 零命中：`CC079/CC080/CC083 -> 0`

全链路漏斗（45 条、63 个金标法条）：

- trgm 召回：`51/63`
- vector 召回：`57/63`
- 两路并集：`63/63`
- RRF 融合：`63/63`
- 重排候选窗口：`59/63`
- 最终消费窗口：随 RERANK_TOP 从 12 提升到 15，`58/63 -> 59/63`

结论：embedding 和映射不再是主要缺口，剩余 4 个金标法条在 RRF 排名
15 以后，需要继续优化“单腿高排名但 RRF 被稀释”的候选选择策略。

### 全流程埋点与回答层消费修正

- 发现检索评测按 top10 统计，但回答层 `dispatch_tool` 仍只向模型注入
  `top_k=5`，导致已召回的 6~10 位法条在生成阶段再次丢失。
- 新增 `RAG_ANSWER_TOP_K=10`，回答层改为消费 top10；提示词仍要求只回答
  最直接相关条文，不代表必须逐条复述。
- 新增在线子阶段 span：`recall`、`recall_retry`、`fusion`、`rerank`、
  `generation`，请求详情可拆出各阶段耗时和召回数量。

真实请求热态基线：

- recall：`156ms`，trgm/vector 各 15 条，vector 正常。
- fusion：`<1ms`，融合池 27 条。
- rerank：热态约 `2.3s`，15 条候选。
- generation：`1737ms`，无 reasoning。
- 总首包：约 `4.2s`；冷启动首请求会额外承担 CrossEncoder 加载。

剩余已知瓶颈：

- `_keyword_fill` 仍处于“候选配额已满、关键词按整句匹配”的历史失效状态。
- recall@10 仍非 100%，剩余 4 个金标法条需要针对单腿高排名候选做定向融合。
- `bge-reranker-base` 的排序能力有限；更强模型在 CPU 上延迟过高。

### 远端 embedding A/B 与迁移

保留 `dmeta-768` 不变，新增百炼 `text-embedding-v4@768` 临时表 A/B：

| 模型 | recall@5 | recall@10 | MRR |
|---|---:|---:|---:|
| `shaw/dmeta-embedding-zh` | 0.9407 | 0.9630 | 0.9093 |
| `text-embedding-v4@768` | 0.9519 | 0.9852 | 0.8841 |

远端 embedding 以很小的 MRR 代价换取显著召回提升。已新增
`EMBEDDING_PROVIDER`、`embedding_model` 标记和旧向量备份表，并用
`scripts/reembed_knowledge_base.py` 原子重嵌入 1260 条知识块。

生产表实测：

- vector 腿：`57/63 -> 61/63`
- 两路并集、RRF、候选、最终输出：`62/63`
- recall@5：`0.9296`
- recall@10：`0.9926`
- MRR：`0.8791`
- 平均 / P95：`3084ms / 4062ms`

剩余唯一缺口为 `CC150` 的金标 `第一千零六十二条`，两条召回腿都没有命中。
这说明下一步应从“全局调权重”转向“提高 embedding 对未见表达式的泛化”，
或扩充该场景的确定性映射；不需要立即承担微调模型的工程成本。

### RRF 权重与 CE blend 实验

在 `text-embedding-v4@768` 和 `TOP=15` 下：

| 配置 | recall@5 | recall@10 | MRR |
|---|---:|---:|---:|
| trgm=0.4, blend=0.6 | 0.9296 | 0.9926 | 0.8791 |
| trgm=0.3, blend=0.6 | 0.9296 | 0.9926 | 0.8791 |
| trgm=0.3, blend=0.8 | 0.8630 | 0.9926 | 0.8850 |

结论：降低 trgm 权重没有改变指标；提高 CE blend 只能小幅提升 MRR，
但会显著损害 recall@5，不能采用。

CE 分数诊断（Recall@5 未满用例）：

- `CC061` 金标 `1075` 最终第 7、CE `0.9985`；Top5 中最低 CE 为
  `0.9180`。这是 RRF blend 压低了高 CE 金标，不是模型能力不足。
- `CC099` 金标 `604` 最终第 6、CE `0.9981`；Top5 最高 CE 为
  `0.9931`。同样属于融合排序问题。
- `CC065` 金标 `171` 最终第 6、CE `0.2356`；与 Top5 有明显差距，
  属于 CE 能力边界。
- `CC150` 金标 `1062` 未进入候选，属于 embedding/召回缺口，不是 CE。

因此当前优先级应是“候选后处理 + 高质量重排”，而不是继续调 RRF 权重。

单腿保护带离线扫描补充：

- 用 text-v4 的 45 条 CE 快照扫 `leg_top=3/5/8` 与 margin `0~0.02`。
- 只有 `margin=0.02` 能提升 `CC099` 一个案例，整体 recall@5/10 不变。
- 因此保护带保持默认关闭，不进入生产默认链路。

## 2026-09-16 RAG 中控台：事件排序、自动/半自动修复与撤销回滚

南提出三项：未处理高风险事件置顶、已处理事件最新在前、补齐自动/半自动
修复链路、误提交可回退。本轮按「先本地验证再谈部署」落地。

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-16 | `app/services/rag_incidents.py` | P2 | 事件列表无运营优先级，高危未处理事件可能沉底 | 未处理优先；未处理按严重度再按最近出现时间；已处理按 `resolved_at/closed_at/last_seen_at` 倒序；API 补 `closed_at` | 已修 |
| 09-16 | `app/services/rag_recommendations.py` | P1 | 事件只有文本建议，没有可执行的自动/半自动修复链路 | 新增规则到动作的确定性映射，输出 `auto`/`approval`/`manual` 三类建议 | 已修 |
| 09-16 | `app/services/rag_monitor.py` | P1 | 新 verdict 落库后不创建任何动作 | 事务内对新事件调用 `auto_plan_l0_actions`，只自动落 L0；历史回填跳过 | 已修 |
| 09-16 | `app/services/rag_actions.py` | P1 | 已成功动作无法人工回退 | 新增 `rollback_action`，仅成功且注册回滚处理器的动作可回滚，并输出 `rollback_available` | 已修 |
| 09-16 | `app/routes/rag_admin.py` | P1 | 控制台缺推荐查询与人工回滚接口 | 新增 `GET /incidents/{id}/recommendations`、`POST /actions/{id}/rollback`，均限管理员并写审计 | 已修 |
| 09-16 | `frontend/src/views/RagConsoleView.vue` | P2 | 事件列表平铺、已处理排序口径错误 | 拆「未处理/已处理」两组并按处理时间倒序；概览最近事件同口径 | 已修 |
| 09-16 | `frontend/src/components/RagIncidentDetail.vue` | P1 | 事件详情无修复入口，误提交无法回退 | 新增「推荐修复」卡片与「撤销刚才提交」 | 已修 |
| 09-16 | `frontend/src/components/RagActionDetail.vue` | P1 | 已成功动作无人工回滚入口 | 条件满足时显示「回滚此动作」，二次确认后调用回滚接口 | 已修 |

验证证据：

- 后端 RAG 单测 `70 passed`（含新增推荐/排序/回滚用例）。
- 前端 `npx vitest run` `45 passed`，`npm run build:console` 通过。
- `scripts/verify_rag_console.py` 只读验收 `ok=true`，四个网关与控制面
  六个服务健康检查均为 200。
- 本地真实浏览器演练：事件页分组与排序正确（未处理 6 条在前、已处理 2 条
  在后）；RC-1b 建议「健康探测 / 自动执行」可一键提交；撤销后动作
  `#11` 变为「已取消」；`adjust_rerank_top` 成功动作显示回滚按钮，
  L0 只读动作不显示。
- 部署动作：重建 `agent_gateway-gateway:latest` 并重启 gateway/gateway2/
  gateway3/gateway4/rag_console/rag_console_worker，全部回到 healthy。

未执行（待南确认）：真实点击「回滚此动作」会把运行期 `RERANK_TOP` 恢复到
动作前基线（当前为 15，动作基线为 8），会影响已调优的检索配置，故本轮只
验证入口与权限，不实际触发。

## 2026-09-16 文件超限整改（600 行上限）

南要求砍掉超限文件。全仓扫描 `app/`、`frontend/src/`、根入口脚本后，按
「拆出内聚模块 + 原处 re-export」方式处理，保持既有调用路径不变。

| 日期 | 文件 | 拆分前 | 拆分后 | 拆出模块 |
|---|---|---:|---:|---|
| 09-16 | `app/agents/tools.py` | 742 | 540 | `app/agents/kb_rerank.py`（228） |
| 09-16 | `app/services/rag_actions.py` | 611 | 519 | `app/services/rag_action_handlers.py`（107） |
| 09-16 | `app/routes/rag_admin.py` | 680 | 447 | `app/routes/rag_admin_status.py`（256） |
| 09-16 | `frontend/src/views/ChatView.vue` | 601 | 599 | 合并同组件重复 `<style scoped>` |

拆分原则：只搬代码不做行为变更；被拆函数只依赖同块常量与模块状态时才搬，
否则保持原位。`tools.py` 的召回块因被 4 个测试文件直接 patch 模块属性，
本轮不拆，只拆无外部 patch 的重排块。

连带改动：`tests/unit/test_rag_admin.py` 的 `_REPORTS_DIR` patch 目标改为
常量新归属模块（唯一一处测试改动，常量随实现搬家导致原 patch 失效）。

验证证据：

- 全仓扫描超限文件数 `0`。
- 后端单测 `377 passed`；失败 3 项为聊天链路既有夹具缺 `selected_model`，
  与本次拆分无关（拆分前后失败集合完全一致）。
- 前端 `45 passed`，`npm run build` 与 `npm run build:console` 通过。
- 镜像重建并重启六个服务全部 healthy；
  `scripts/verify_rag_console.py` 只读验收 `ok=true`，健康检查均 200。
- 容器内 `app.agents.kb_rerank`、`app.routes.rag_admin_status`、
  `app.services.rag_action_handlers` 均可正常导入。

遗留（未修）：`app/agents/tools.py::_search_two_legs` 130 行，超过函数 80 行
上限。该函数是召回主链路，含并发 gather、诊断埋点与降级分支，拆分需单独立项。

## 2026-09-16 超长函数拆分（80 行上限）

上一轮遗留的函数超限本轮处理完毕，全仓文件与函数两项上限均已达标。

| 日期 | 位置 | 拆分前 | 拆分后 | 拆出内容 |
|---|---|---:|---:|---|
| 09-16 | `app/agents/tools.py::_search_two_legs` | 130 | 59 | `_recall_and_fuse`（70）、`_expand_and_guard`（22） |
| 09-16 | `app/agents/router.py::handle_simple_task` | 85 | 73 | `_build_simple_answer` |

扫描结论：`app/`、`frontend/src/` 无超 600 行文件；`app/` 无超 80 行函数；
官方门禁 `scripts/check_code_rules.py app mcp_assets scripts` 通过。

### 拆分过程中抓到并修掉的真实回归

拆重排层时 `kb_rerank.py` 漏 `import asyncio`，`_rerank_local` 的
`asyncio.to_thread` 抛 `NameError` 被既有 `except` 吞掉，**精排静默降级为纯
RRF**，单测全绿。发现方式是对容器内真实调用 `search_knowledge` 并核对返回的
`method` 标签（`trgm+vector+rrf` vs `+local_rerank`）。

修复：补导入 + 全仓 `pyflakes` 未定义名扫描归零 + 新增回归测试
`test_rerank_local_wiring_survives_module_split`（摘掉导入即失败，已验证）。

验证证据：

- `check_code_rules.py` 通过；`pyflakes app scripts` 未定义名 0 条。
- 后端单测 `378 passed`（+1 新回归测试）；失败 3 项为聊天链路既有夹具缺
  `selected_model`，与本轮无关。
- 容器内真实检索：`search_knowledge("合同解除的条件")` 返回 5 条，
  `method = trgm+vector+rrf+local_rerank`；`_probe_health` 返回 200/passed。
- 镜像重建重启，六个服务 healthy；`verify_rag_console.py` `ok=true`。

## 2026-09-16 收尾：后端单测全绿

前两轮固定红灯的 3 项（`test_civil_grounding_contract`、
`test_stream_safety_contracts`）根因查明并修复，不是夹具问题本身：

`app/services/chat_fast_paths.py:266` 的埋点 `"model": ctx.selected_model`
直接取属性，ctx 由调用方构造，缺字段即抛 `AttributeError`，被
`simple_fast_path` 的兜底 `except` 吞掉，**整条快速通道退化为降级回复**。
生产 `ChatStreamCtx` 有默认值所以线上没暴露出症状，但这是真实缺陷，不是
测试写错。

修复：改 `getattr(ctx, "selected_model", "")`，与同函数内已有的
`getattr(ctx, "username", "")` 同口径——埋点缺标签不该打断主流程。

验证证据：

- 后端单测 `381 passed`，**0 失败**（此前固定 3 失败）。
- `check_code_rules.py` 通过；`pyflakes` 未定义名 0 条。
- 镜像重建重启，六个服务 healthy；`verify_rag_console.py` `ok=true`，
  健康检查均 200。

## 2026-09-20 全量后端审读轮（对标成熟产品；当日点单后已修 20 项，其余待拍板/待部署）

六路并行审计（API 面/认证/聊天链路/支付/RAG/核心设施），主代理对全部 P0 与关键 P1 逐条读源码核实。全文与证据见 `docs/code-review-2026-09-20/后端全量审读-对标成熟产品-2026-09-20.md`（含 7 项待人工确认）。

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-20 | auth.py:95-97 + nginx.conf:73 + compose:105 | **P0** | CF-Connecting-IP 可伪造：RT-1 后信任判据="对端私网即信其头"，nginx 不清洗该头，10190 唯一非回环端口——登录锁/短信日限/oauth 限频可绕，反可锁受害者；测试无伪造负例 | nginx `proxy_set_header CF-Connecting-IP "";` + 10190 回环绑定（生产入流方式待确认） | 待修 |
| 09-20 | refund.py:25,44 | P1 | 用户退款主流程零可达：allow_consumption_refund=True 仅单测传，路由恒 400（09-15/09-19 两轮在案未闭环） | 管理员入口传 flag 或路由 410 | 待修 |
| 09-20 | refund.py:265-273 | P1 | 派生幂等键吞合法重复部分退款（违背 Stripe"不带 key 不去重"语义） | 仅显式 header 建键 | 已修（PAY-2 批次C：删派生键，仅显式 Idempotency-Key 建键；test_20260920_audit_batchCD 复验） |
| 09-20 | attempts.py:52-85 + routes.py:93/149 | P1 | attempt 唯一索引竞态抛 UniqueViolationError 非 ValueError，except 漏接→资金接口 500 | catch IntegrityError 转 ValueError | 已修（PAY-3 批次C：catch IntegrityConstraintViolationError 转 ValueError，日志只记类型名；batchCD 复验） |
| 09-20 | attempts.py:106 + refund.py:114,185 | P1 | 退款额度反查 3 次无索引全表聚合（metadata->>'original_order_no'），扣费每行在膨胀表 | 实体化列+部分索引（走迁移） | 待修 |
| 09-20 | admin_routes.py:165 + alert.rules.yml | P1 | 对账=唯一资金异常探测器却无调度/指标/告警，不平只 warning | 维护循环挂调度+Counter+规则 | 待修 |
| 09-20 | chat_stream_ctx.py:121 + chat_stream_core.py | P1 | SSE 额度只扣不退（rollback 仅任务路径有，同一决策只落一半）；上游抖动白扣免费额度 | cleanup_stream 按零产出补 rollback | 已修（CHAT-1 批次A：cleanup_stream 零产出补 rollback；test_20260920_audit_batchA 复验） |
| 09-20 | runner.py:292→309 + react_steps.py:78 | P1 | 任务路径把 DFA 拦截产物写语义缓存（_finish_text_answer 默认 write_cache=True，前缀拼接绕过终检）——09-11 同族修复漏改兄弟入口 | write_cache=not blocked 一行 | 已修（CHAT-2 批次A：write_cache=not blocked；batchA 复验） |
| 09-20 | chat_fallback.py:175 | P1 | Flash 降级空全文只发裸 [DONE]：置 _yielded=True 使 chain 兜底分支永不触发，流缺 answer_complete | 删该行 | 已修（CHAT-3 批次A：删行，空流交由 chain 补 answer_complete；batchA 复验） |
| 09-20 | chat_react.py:253 / react_steps.py:199 | P1 | 工具异常原文 str(result) 进 LLM 上下文，SSE 副本却过 sanitize_error_text（口径不一致，key 在 URL 里） | 两处过 sanitize_error_text | 已修（CHAT-4 批次A：两处统一 sanitize_error_text；batchA 复验） |
| 09-20 | react_steps.py:265-331 | P1 | 降级链三份保护集各异：任务侧不持 llm_semaphore，故障风暴绕过并发闸 | 补 semaphore；中期收敛单一路由器 | 已修（CHAT-5 批次A：任务侧降级补 llm_semaphore；单一路由器留 P2） |
| 09-20 | router.py + chat_stream_core.py + chat_travel_flow.py | P1 | 旅行推荐没有多轮承接：首轮缺出发地仍先烧四工具，下一轮“梧州”被关键词路由打进 chat 零工具；天气/路线/美食全由模型裸编 | 会话级待补槽状态 + 推荐意图继承 + 澄清轮零工具 | 已修（TRIP-1：目的地/出发地槽位状态存 Redis TTL；首轮缺槽直接追问不跑工具；“梧州”恢复四 Agent 并生成规范查询；新增 6 项回归，相关 64 项通过） |
| 09-20 | conversation_profiles.py + memory_manager.py + memory.py + conversation.py + chat_fast_paths.py + chat_react.py | P1 | 旅行助手没有会话级用户画像和多轮上下文管理：姓名/人数/出发地/目的地/预算/真实天气无法按历史会话独立保存，15 轮后上下文无压缩，天气工具结果也未回写画像 | 新增 `conversation_profiles` 迁移与 CRUD API；每轮自动提取字段，旅行补槽和天气工具结果写回；每会话独立画像，前端提供查看/编辑/删除；15 轮内保留原始上下文，第 16 轮起滚动压缩，用户问题摘要保留前 100 字、助手内容保留前 200 字；天气事件补 `name` 供前端识别 | 已修（PROFILE-1；新增会话画像 9 项与前端 5 项回归，相关测试及构建通过；真实库迁移待数据库可用时执行） |
| 09-20 | task_manager.py:83-99 | P1 | TASK_TIMEOUT 只在轮询时判定，断轮询后生成协程无截止→僵尸吃 token | 生成侧 wait_for 或 sweeper | 已修（CORE-1 批次F：抽出 mark_task_timeout 共享 CAS，生成侧 timeout_deadline 闹钟到点终结+跨实例取消标记，finally 撤闹钟；test_20260920_audit_batchF 复验） |
| 09-20 | main.py:100-140 | P1 | 后台循环退出只 warning 无指标无重启 | *_loop_stopped_total+告警 | 已修（CORE-2 批次F：background_loop_stopped_total{loop,reason} + _watch_background_loop 探针 + alert BackgroundLoopStopped；重启/续跑仍不做） |
| 09-20 | semantic_cache.py:237 + rag_action_handlers.py:103 | P1 | 缓存下线只清 worker 进程内 LRU，4 网关副本 L0 不受影响→毒答案最长 15 分钟 | Redis pub/sub 或 L0 键带 epoch | 待修 |
| 09-20 | stream_utils.py:81-82 | P1 | breaker.guard() 包 semaphore 外层：HALF_OPEN 探针排队期间全量请求被秒拒（下游健康） | 换嵌套顺序 | 已修（INFRA-2 批次F：先 semaphore 后 guard；test_20260920_audit_batchF 复验进入顺序断言） |
| 09-20 | phone.py:153-175 | P1 | 短信限流 INCR/DECR 竞态产生无 TTL 永久键→计数永久累积→永久 429 自锁 | 单条 Lua 原子化 | 已修（AUTH-1 批次B：单条 Lua INCR+EXPIRE+回滚原子化；test_20260920_audit_batchB 复验单往返与键名） |
| 09-20 | safety_filter.py:194-200 | P1 | fail-closed 新面：整段归一化在 deadline 检查之前，超长输入阻塞事件循环数秒 | 入口截断或循环内查 deadline | 已修（AUTH-2 批次B：_normalize_capped 分段查 deadline，超时 fail-closed；batchB 复验 20k 字符等价与超时放行拒绝） |
| 09-20 | middleware/auth.py:282 + auth_cookies.py | P1 | 控制台 HttpOnly cookie 全站通用（任意路由接受、path=/、x-forwarded-proto 可伪） | token aud=console + path 收敛 | 待修 |
| 09-20 | main.py:361 | P1 | SessionMiddleware 无 https_only/samesite，OAuth state（回调唯一 CSRF 防线）可明文携带 | https_only=True | 已修（AUTH-4 批次B：SESSION_COOKIE_SECURE 生产 True + SessionMiddleware https_only=True/samesite=lax；batchB 复验） |
| 09-20 | phone.py:129-131 | P1 | 注册 display_name/email/extra 无长度无 schema，extra 原样入库回吐（同仓 users.py 已限 4096） | 加上限 | 已修（AUTH-5 批次B：128/254/4096 上限对齐 users.py；batchB 复验边界） |
| 09-20 | config.py:110,117 | P1 | 限流阈值占位（user_qps 2000/日 5 万）+ 无 IP 级全局闸=实际无门禁 | 按容量定值+全局闸（阈值需拍板） | 待修 |
| 09-20 | v2.py:104-117 | P1 | 任务长轮询零限流：wait=1 挂 60s、单请求 120 次 Redis 轮询 | BLPOP/pubsub+端点并发闸 | 待修 |
| 09-20 | map_amap.py:11 + map_api.py:557 | P1 | 高德 HTTPStatusError 未捕获→500 且异常文本含带 key URL 入日志 | catch httpx.HTTPError→502 固定文案 | 已修（API-3 批次C：catch ValueError/httpx.HTTPError→502 固定文案，日志只记 err_type；test_20260920_audit_batchCD 复验 key 不外泄） |
| 09-20 | file_upload.py:100 | P1 | 文档解析错误原文透传用户+进 Redis/LLM 上下文（09-19 同类漏改路径） | 固定文案 | 已修（API-4 批次C：固定文案返回，原文只进 warning 日志；batchCD 复验） |
| 09-20 | map_api.py:326-346 | P1 | 今日新建面：city 自由文本无校验进 LLM prompt，产物入全员 15 天共享池（跨用户投毒面） | geocode 校验+池条目过滤 | 待修 |
| 09-20 | rag_monitor.py:412-421 | P1 | 巡检自动解决裸 SQL 不置空 active_key（全表 UNIQUE）→同规则复发永不重开，治理永久静默 | 改调 transition_incident | 已修（RAG-1 批次E：_delete_stale_verdicts 改 CTE 回收 incident_id，无残余判据后统一走 transition_incident；test_20260920_audit_batchE 复验不再出现裸 UPDATE） |
| 09-20 | rag_quality_gate.py:49 + Dockerfile:54 | P1 | 质量防线加载镜像里已不存在的 tests/ 文件→生产 L1/L2 自动修复必 FileNotFoundError（现容器是否 cp 带过待确认） | 脚本+标签进白名单或外置卷 | 待修 |
| 09-20 | tools.py:370,397 | P1 | _LAST_LEGS 改写重试不回填→第二轮 embedding 故障静默，trace 残缺当事实喂 RC-1b/1c | 重试后统一重设 | 已修（RAG-3 批次E：重试后统一重设 _LAST_LEGS 并补 vec_failed 属性；test_20260920_audit_batchE 复验腿元组） |
| 09-20 | rag_change_sets.py:341 | P1 | apply 不重跑条号冲突校验，审批-执行窗口可致同条号双真 valid 并存进召回 | apply 事务内重跑 | 已修（RAG-4 批次E：knowledge_add 分支 apply 事务内先 validate_against_db；batchE 复验拒绝/放行两路） |
| 09-20 | document_parser.py:297 | P1 | DocxDocument 解压无总量上限（AG-1 只封 media）→zip bomb 打爆 1GB 容器 | 校验解压字节与压缩比 | 已修（RAG-5 批次E：DocxDocument 前预检解压总字节 200MB + 压缩比 60；batchE 复验零字节包与超比包均被拒） |
| 09-20 | rag_actions.py:548-554 | P1 | _try_action_lock 抛错时已 acquire 的 lock_conn 不释放→连接池耗尽 | except 释放 | 已修（RAG-6 批次E：except 分支释放 lock_conn；batchE 复验恰一次 release） |
| 09-20 | compose:27-92 + cdc/worker.py:337 | P1 | 4 副本共 mount 一个 cdc_journal，_lock_ok 检查在写入后、无 fencing→双写/乱序 | fencing epoch 或目录隔离 | 待修 |
| 09-20 | compose:254,274 | P1 | PG shared_buffers=1GB 占容器 1536m 的 67%（建议 25%），effective_cache_size 超上限 2 倍 | 384MB/1GB 或提限额（宿主内存待确认） | 待修 |
| 09-20 | journal.py:132 + test_p0_p1_fixes.py:173 | P1 | 滚动测试打的是生产已不调用的 _write_line，真链路 _rotate_if_needed 零覆盖 | 测试改打 _write_lines_durable | 已修（INFRA-5 批次F：删死代码 _write_line，滚动回归改打 _write_lines_durable，注释漂移一并更正；test_p0_p1_fixes 复验 15 通过） |
| 09-20 | cdc/worker.py:235,158 | P1 | 清理 txid<$1 连带删存量 txid=0 未消费事件（与交付报告口径相反）；死分支残留 | 存量计数待确认，先补捞再清 | 待修 |

P2 共 26 项集中登记于报告 P2 区（L4 对标：python-jose 停维可两行换 PyJWT、四处手写分布式锁可收编 redis-py 内置 Lock 删 ~40 行、三份降级链收敛；结构：map_api.py 580 行拆分、routes/ 里的 service 迁 services/；观测：3 个 payment_* 指标零打点；L5：**全仓零端点级测试**、SSE 事件序无通用夹具）。

结论：P0 清零 + PAY-1/CHAT-1/CHAT-2 修复前不具备公网生产发布条件；本地验收运行不受阻。

修复进度（2026-09-20 当晚）：按"先修可以直接修的，不允许臆测"点单，批次A~F 已修 20 项
（CHAT-1~5、AUTH-1/2/4/5、API-3/4、PAY-2/3、RAG-1/3/4/5/6、INFRA-2/5、CORE-1/2），
回归测试 tests/unit/test_20260920_audit_batch{A,B,CD,E,F}.py 共 54 项，全量 pytest
645 passed / 2 failed（两红为另一会话 new45 标注预存项，不在本轮范围）。
仍待修 15 项：P0-1（CF-IP 伪造，需生产入流事实）、PAY-1/4/5、AUTH-3、API-1/2/5、
RAG-2、INFRA-1/3/4/6、refund 路由 reachability——均需人工拍板或生产数据确认。
结构变化：AUTH-1 短信限流脚本外移至 app/routes/sms_lua.py（phone.py 曾破 600 行门禁）；
CORE-1 任务阶段拆分至 app/agents/task_stages.py（经 getattr 晚绑定回读 runner 私有，
保证既有 monkeypatch 测试继续生效）。当晚全量复跑仍为 645 passed / 2 failed。
部署（09-20 深夜）：工作树 app/ 已 docker cp 至 agent_gateway/2/3/4 并滚动重启，
4 副本 healthy、6 关键文件容器内外 md5 一致、启动日志无异常；容器内 alembic current
已在 head（z4a5b6c7d8e9），未跑迁移；alert.rules.yml 经 prometheus /-/reload 热加载，
规则 28 条含 BackgroundLoopStopped；网关 10086 /metrics 已见新指标。
agent_gateway_lite 按此前拍板暂不同步。
