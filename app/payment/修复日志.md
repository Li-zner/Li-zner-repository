# 修复日志 — app/payment

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | refund.py | 债务 | process_refund 81 行超限（豁免名单历史债） | 抽出 _settle_refund 事务结算（退款单+原单联动+退钱+流水） |

## 2026-09-07 审查批次修复（第 5 批，配合 app/修复日志.md 实施记录）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | refund.py + routes.py | P0 | 充值单退款再加一次钱（双倍入账），且无频率限制，「充值→退款→再充值」可无限刷余额 | _decide_refund 加订单类型门：禁止用户自退充值单（决策落地：另一方案"扣回"会在款已消费时造负余额黑洞）；/refund 按用户限频 5 次/小时。消费单金额为负被既有剩余额度门拦住，语义不变 |
| 09-07 | _ledger.py | P2 | _record_transaction 引用未定义 order_no（NameError 地雷）、无调用方却 re-export 公开 | 删除，service re-export 同步移除 |
| 09-07 | service.py + db_maintenance.py | P2 | recover_stale_processing 扫描一个从不存在的状态（死代码） | 删除函数与维护调用点 |
| 09-07 | deferred.py | P2 | 钱包不存在的毒丸单永久重试，每维护轮刷 error | Redis 失败计数，连续 5 轮作废（failed）止血留痕 |

回归：tests/unit/test_20260907_batch_fixes.py（第 5 批 3 用例：充值单拒退/限频/毒丸计数）

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | refund.py | P2 | 充值单拦截是黑名单式（`order_type == "recharge"` 即拒）：若 order_type 出现空值/未知新类型且 amount 为正，仍会走退款加钱路径 | 改白名单式（仅已知可退类型放行）；当前列由迁移管理非空，风险低 | 待修复 |
| 09-07 | routes.py | P3 | _refund_rate_limit 的 INCR 与 EXPIRE 之间进程崩溃会留无 TTL 键（键含窗口号自然隔离，量级每用户每小时最多 1 个，仅崩溃场景） | 可改 SET NX EX 一步原子化 | 待修复（低优先） |

复审通过项：P0 充值单自退修复与决策记录一致（拒退+限频 5 次/小时；except HTTPException 先于泛捕获，429 可穿透）；毒丸单计数 Redis 故障 fail-open 返回 0 不误杀（注释明示语义）；_void_order 带 status='pending' 守卫（deferred 两处调用均安全）；_record_transaction / recover_stale_processing 删除核实（NameError 地雷/死代码，全仓无残留调用）。

## 2026-09-09 全目录审查轮（支付域代理深审 + 主代理复核）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | （跨域，主登记在 app/services 日志） | **P0** | 计量扣费系统性旁路：agent 后台任务路径全量、SSE 降级链、map_api 两端点、简单任务格式化、工具内部调用——统一扣费入口 deduct_token_cost 全仓仅 1 个调用点（llm_streaming:108） | 本轮已收口：runner/简单任务/降级链/map_api 四路接 bill/record_token_usage；工具内部调用口径待主人拍板（见 services 日志） | ✅ 部分已修 09-09 |
| 09-09 | service.py:482-491 | P2 | 扣费系统异常 fail-open 且失败无指标：payment_deduct_total 定义了 status 标签但只打 "success"，ValueError/异常/延后三条失败路径零打点——系统故障期消费零计费仅 error 日志 | 失败分支补 labels(status="failed").inc() | 待修复 |
| 09-09 | main.py:112 + deferred.py:23 | P2 | 待结算补偿 24h 一轮：占位扣费单最长滞后 24h 才入流水（deferred 注释宣称"周期调用"） | settle_pending_deductions 拆独立短周期循环（如 5 分钟） | 待修复 |
| 09-09 | service.py:62 | P2 | 充值幂等键未含用户维度（清单明确要求）：跨用户撞同一 X-Idempotent-Key 误报"订单创建中"（资金不串户，回查带 AND user_id，仅撞键干扰） | key 拼接 username | 待修复 |
| 09-09 | admin_routes.py:157-165 | P2 | 对账接口恒写 status='balanced'，无与 transaction_logs 的任何核对逻辑——"对账"只汇总不校验 | 与流水逐类求差，差异写异常状态 | 待修复 |
| 09-09 | services/chat_react.py:13 | P2 | `from ..payment.service import deduct_token_cost` 死导入（实际经 record_token_usage 扣费），误导排查 | 删除 | 待修复 |

**存量核验**：refund.py 黑名单式拦截 **仍在**（:30-31 仅拒 recharge；09-07 已叠加状态门+剩余额度门，可达面收窄，维持 P2 登记）；llm_streaming:108 裸 create_task **本轮已随 bill_token_usage 重构改走 spawn** ✅；agent_tasks 计数先于幂等 **仍在**（P2）；账务原子性（锁/事务/幂等/流水衔接）四条写路径过检 ✅。

## 2026-09-10 修复：模拟支付退款请求幂等

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | refund.py、routes.py | P1 | 模拟退款无请求级幂等，响应丢失后的重试可重复退余额 | 支持 `X-Idempotent-Key`；无 key 时按用户+订单+金额+原因生成稳定键；处理中 409，成功后重放原结果 | 已修 |

说明：本项目为模拟支付，不建设真实渠道查询、回调验签和资金 outbox；以上仅保证内部余额与模拟渠道调用不重复。

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | refund.py | P2 | 退款拦截黑名单式（== "recharge" 即拒）：未知新 order_type 且金额为正会漏进加钱路径（09-07 登记） | 改白名单 `_REFUNDABLE_ORDER_TYPES = {"payment"}`；测试夹具同步补 order_type | 已修 |
| 09-10 | service.py | P2 | 充值幂等键未含用户维度，跨用户撞 X-Idempotent-Key 误报"订单创建中"（09-09 登记） | 幂等键拼接 user_id | 已修 |
| 09-10 | service.py | P2 | 扣费失败路径零打点：ValueError/系统异常/延后三条失败路径只有 success 有指标（09-09 登记） | 三分支分别补 status=failed/failed/deferred 打点 | 已修 |
| 09-10 | deferred.py + main.py | P2 | 待结算补偿随 24h 维护循环，占位扣费单最长滞后 24h 才入流水（09-09 登记） | 新增 settlement_loop 5 分钟独立循环接入 lifespan（与维护循环内调用并存无害，结算幂等） | 已修 |
| 09-10 | admin_routes.py | P2 | 对账接口恒写 status='balanced'，无任何核对逻辑（09-09 登记） | 订单侧成功净额与 transaction_logs 逐类求和对比，不一致写 abnormal 并 warning，响应透出 status | 已修 |
| 09-10 | routes.py | P3 | _refund_rate_limit 的 INCR 与 EXPIRE 非原子（09-07 登记） | 改 Lua 原子 INCR+EXPIRE；单测假 Redis 同步 eval 语义 | 已修 |
| 09-10 | services/chat_react.py 死导入 | P2 | `from ..payment.service import deduct_token_cost` 死导入（09-09 登记） | 复核确认已随 09-10 计量收口删除 | 已修（核实） |


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | admin_routes.py | **P1** | 09-10 对账修复的恒等式错误：tx_net=recharge+consume+refund 三类相加对比订单 success_amount，符号约定不同——当天有任何消费/退款必判 abnormal，对账无甄别力 | `_compare_daily_ledger` 分类型逐一比对（充值对充值/消费对消费/退款对退款），拆分独立函数 | 已修 |
| 09-11 | service.py | P1 | PaymentFailureHigh 告警永不触发：payment_orders_total 只定义未埋点 | _settle_recharge 的 success/failed 两分支补打点（导入 core.metrics 单例防重复注册） | 已修 |
| 09-11 | refund.py | P2 | 结算提交后写幂等键失败 → except 删幂等键 → 客户端重试本地双退（订单状态机与累计退款上限兜底） | 登记：仅事务确证回滚才删幂等键；提交后写失败改告警保留键 | 登记 |


## 2026-09-12 凌晨清欠轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | refund.py | P2 | 结算提交后写幂等键失败会被 except 删除——客户端重试可本地双退（09-11 登记 B-F11） | 幂等键写失败改告警保留（钱已退不可重放）；仅未进结算的异常才安全删除 | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | refund.py:40、121、service.py:414 | P0 | 消费订单金额为负，退款却按 order.amount - refunded_sum 计算；实测 amount=-5 直接判“已全额退款”，所有消费单都无法正常退款 | 可退余额和累计退款统一使用绝对额，负号仅作为资金方向 | ✅ 已修 09-12（复核：abs 基数修复与注释在 _decide_refund） |
| 09-11 | refund.py:178 | P1 | 退款处理持有数据库连接期间执行最长 10 秒的渠道 I/O，慢渠道可占满连接池 | 读订单后释放连接，渠道成功后再短事务复验并结算 | ✅ 已修 09-14：重构为三段——短借还读订单/已退累计 → 释放连接后渠道 I/O → 独立短借还结算（_settle_refund 事务内 FOR UPDATE 复验 + 重查已退累计，TOCTOU 已兜） |
| 09-11 | admin_routes.py:139 | P1 | 消费订单为负金额，流水侧使用绝对值，对账比较符号不一致，正常消费也会判 abnormal | 按订单类型统一符号后再核对 | ✅ 已修 09-12（按类型符号统一，见根日志清欠轮） |
| 09-11 | admin_routes.py:174 | P1 | 对账统计、流水核对和结果写入未在一致快照或事务内完成，并发支付会生成错误对账 | 使用 REPEATABLE READ 或固定快照后写对账结果 | 已修（09-12 事务包裹，见下） |
| 09-11 | refund.py:200 | P2 | 渠道已成功但本地结算异常时仍会删除幂等键；模拟渠道无资金副作用，但真渠道会重复退款 | 退款意图先持久化，渠道成功后不得删幂等键，进入人工恢复 | 修复不完整 |
| 09-11 | service.py:143 | P2 | 充值订单创建后幂等键写 Redis 失败只告警；窗口内重试可能形成第二笔待支付订单 | 用 DB 唯一幂等记录或 outbox，不依赖 Redis 单点写入 | 登记 |
| 09-11 | models.py:13 | P2 | subject、reason、order_no 缺少长度上限，可造成 DB、内存和日志膨胀 | 为请求字符串设置合理 max_length 和请求体上限 | ✅ 已修 09-14：subject=128 / order_no=64 / reason=200 / payment_method=32 |


## 2026-09-12 外部复核修复轮（P0×1 + P1×2 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | refund.py:40,121、service.py:414 | **P0** | 消费订单金额为负（扣费为负记录），退款按 amount - refunded_sum 恒为负 → 所有消费单判"已全额退款"无法退款 | 可退基数与比较统一取绝对额，负号仅表方向；测试同步（消费单 -5 可退 5） | 已修 |
| 09-12 | refund.py:178 | P1 | 持 DB 连接期间执行 10s 渠道 I/O，慢渠道占满连接池 | 核实：渠道调用在 `async with pool.acquire()` 之外（事务内仅锁行结算）——登记复核结论 | ✅ 已修 09-14：重构为三段——短借还读订单/已退累计 → 释放连接后渠道 I/O → 独立短借还结算（_settle_refund 事务内 FOR UPDATE 复验 + 重查已退累计，TOCTOU 已兜） |
| 09-12 | admin_routes.py:139 | P1 | 对账消费订单为负、流水绝对值，符号不一致正常消费判 abnormal | 两侧统一按金额绝对额比对 | ✅ 已修 09-12（按类型符号统一，见根日志清欠轮） |
| 09-12 | admin_routes.py:174 | P1 | 对账统计/核对/写入非一致快照，并发支付错账 | api_admin_reconcile 统计/核对/写入包同一 REPEATABLE READ 事务；分类型比对单测（balanced/abnormal/历史负数兼容） | 已修 |
| 09-12 | refund.py:200 | P2 | 渠道成功但本地结算异常仍删幂等键，重试双退 | 已修：渠道成功后幂等键保留转人工；未结算异常才删 | 已修 |
| 09-12 | service.py:143 | P2 | 幂等键写 Redis 失败只告警，重试可能形成第二笔订单 | 登记：DB 唯一幂等记录或 outbox（架构级） | 登记 |
| 09-12 | models.py:13 | P2 | subject/reason/order_no 缺长度上限 | 登记：请求字符串补 max_length | ✅ 已修 09-14：subject=128 / order_no=64 / reason=200 / payment_method=32 |
# 修复日志 — app/payment

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | refund.py | 债务 | process_refund 81 行超限（豁免名单历史债） | 抽出 _settle_refund 事务结算（退款单+原单联动+退钱+流水） |

## 2026-09-07 审查批次修复（第 5 批，配合 app/修复日志.md 实施记录）

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | refund.py + routes.py | P0 | 充值单退款再加一次钱（双倍入账），且无频率限制，「充值→退款→再充值」可无限刷余额 | _decide_refund 加订单类型门：禁止用户自退充值单（决策落地：另一方案"扣回"会在款已消费时造负余额黑洞）；/refund 按用户限频 5 次/小时。消费单金额为负被既有剩余额度门拦住，语义不变 |
| 09-07 | _ledger.py | P2 | _record_transaction 引用未定义 order_no（NameError 地雷）、无调用方却 re-export 公开 | 删除，service re-export 同步移除 |
| 09-07 | service.py + db_maintenance.py | P2 | recover_stale_processing 扫描一个从不存在的状态（死代码） | 删除函数与维护调用点 |
| 09-07 | deferred.py | P2 | 钱包不存在的毒丸单永久重试，每维护轮刷 error | Redis 失败计数，连续 5 轮作废（failed）止血留痕 |

回归：tests/unit/test_20260907_batch_fixes.py（第 5 批 3 用例：充值单拒退/限频/毒丸计数）

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | refund.py | P2 | 充值单拦截是黑名单式（`order_type == "recharge"` 即拒）：若 order_type 出现空值/未知新类型且 amount 为正，仍会走退款加钱路径 | 改白名单式（仅已知可退类型放行）；当前列由迁移管理非空，风险低 | 待修复 |
| 09-07 | routes.py | P3 | _refund_rate_limit 的 INCR 与 EXPIRE 之间进程崩溃会留无 TTL 键（键含窗口号自然隔离，量级每用户每小时最多 1 个，仅崩溃场景） | 可改 SET NX EX 一步原子化 | 待修复（低优先） |

复审通过项：P0 充值单自退修复与决策记录一致（拒退+限频 5 次/小时；except HTTPException 先于泛捕获，429 可穿透）；毒丸单计数 Redis 故障 fail-open 返回 0 不误杀（注释明示语义）；_void_order 带 status='pending' 守卫（deferred 两处调用均安全）；_record_transaction / recover_stale_processing 删除核实（NameError 地雷/死代码，全仓无残留调用）。

## 2026-09-09 全目录审查轮（支付域代理深审 + 主代理复核）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | （跨域，主登记在 app/services 日志） | **P0** | 计量扣费系统性旁路：agent 后台任务路径全量、SSE 降级链、map_api 两端点、简单任务格式化、工具内部调用——统一扣费入口 deduct_token_cost 全仓仅 1 个调用点（llm_streaming:108） | 本轮已收口：runner/简单任务/降级链/map_api 四路接 bill/record_token_usage；工具内部调用口径待主人拍板（见 services 日志） | ✅ 部分已修 09-09 |
| 09-09 | service.py:482-491 | P2 | 扣费系统异常 fail-open 且失败无指标：payment_deduct_total 定义了 status 标签但只打 "success"，ValueError/异常/延后三条失败路径零打点——系统故障期消费零计费仅 error 日志 | 失败分支补 labels(status="failed").inc() | 待修复 |
| 09-09 | main.py:112 + deferred.py:23 | P2 | 待结算补偿 24h 一轮：占位扣费单最长滞后 24h 才入流水（deferred 注释宣称"周期调用"） | settle_pending_deductions 拆独立短周期循环（如 5 分钟） | 待修复 |
| 09-09 | service.py:62 | P2 | 充值幂等键未含用户维度（清单明确要求）：跨用户撞同一 X-Idempotent-Key 误报"订单创建中"（资金不串户，回查带 AND user_id，仅撞键干扰） | key 拼接 username | 待修复 |
| 09-09 | admin_routes.py:157-165 | P2 | 对账接口恒写 status='balanced'，无与 transaction_logs 的任何核对逻辑——"对账"只汇总不校验 | 与流水逐类求差，差异写异常状态 | 待修复 |
| 09-09 | services/chat_react.py:13 | P2 | `from ..payment.service import deduct_token_cost` 死导入（实际经 record_token_usage 扣费），误导排查 | 删除 | 待修复 |

**存量核验**：refund.py 黑名单式拦截 **仍在**（:30-31 仅拒 recharge；09-07 已叠加状态门+剩余额度门，可达面收窄，维持 P2 登记）；llm_streaming:108 裸 create_task **本轮已随 bill_token_usage 重构改走 spawn** ✅；agent_tasks 计数先于幂等 **仍在**（P2）；账务原子性（锁/事务/幂等/流水衔接）四条写路径过检 ✅。

## 2026-09-10 修复：模拟支付退款请求幂等

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | refund.py、routes.py | P1 | 模拟退款无请求级幂等，响应丢失后的重试可重复退余额 | 支持 `X-Idempotent-Key`；无 key 时按用户+订单+金额+原因生成稳定键；处理中 409，成功后重放原结果 | 已修 |

说明：本项目为模拟支付，不建设真实渠道查询、回调验签和资金 outbox；以上仅保证内部余额与模拟渠道调用不重复。

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | refund.py | P2 | 退款拦截黑名单式（== "recharge" 即拒）：未知新 order_type 且金额为正会漏进加钱路径（09-07 登记） | 改白名单 `_REFUNDABLE_ORDER_TYPES = {"payment"}`；测试夹具同步补 order_type | 已修 |
| 09-10 | service.py | P2 | 充值幂等键未含用户维度，跨用户撞 X-Idempotent-Key 误报"订单创建中"（09-09 登记） | 幂等键拼接 user_id | 已修 |
| 09-10 | service.py | P2 | 扣费失败路径零打点：ValueError/系统异常/延后三条失败路径只有 success 有指标（09-09 登记） | 三分支分别补 status=failed/failed/deferred 打点 | 已修 |
| 09-10 | deferred.py + main.py | P2 | 待结算补偿随 24h 维护循环，占位扣费单最长滞后 24h 才入流水（09-09 登记） | 新增 settlement_loop 5 分钟独立循环接入 lifespan（与维护循环内调用并存无害，结算幂等） | 已修 |
| 09-10 | admin_routes.py | P2 | 对账接口恒写 status='balanced'，无任何核对逻辑（09-09 登记） | 订单侧成功净额与 transaction_logs 逐类求和对比，不一致写 abnormal 并 warning，响应透出 status | 已修 |
| 09-10 | routes.py | P3 | _refund_rate_limit 的 INCR 与 EXPIRE 非原子（09-07 登记） | 改 Lua 原子 INCR+EXPIRE；单测假 Redis 同步 eval 语义 | 已修 |
| 09-10 | services/chat_react.py 死导入 | P2 | `from ..payment.service import deduct_token_cost` 死导入（09-09 登记） | 复核确认已随 09-10 计量收口删除 | 已修（核实） |


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | admin_routes.py | **P1** | 09-10 对账修复的恒等式错误：tx_net=recharge+consume+refund 三类相加对比订单 success_amount，符号约定不同——当天有任何消费/退款必判 abnormal，对账无甄别力 | `_compare_daily_ledger` 分类型逐一比对（充值对充值/消费对消费/退款对退款），拆分独立函数 | 已修 |
| 09-11 | service.py | P1 | PaymentFailureHigh 告警永不触发：payment_orders_total 只定义未埋点 | _settle_recharge 的 success/failed 两分支补打点（导入 core.metrics 单例防重复注册） | 已修 |
| 09-11 | refund.py | P2 | 结算提交后写幂等键失败 → except 删幂等键 → 客户端重试本地双退（订单状态机与累计退款上限兜底） | 登记：仅事务确证回滚才删幂等键；提交后写失败改告警保留键 | 登记 |


## 2026-09-12 凌晨清欠轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | refund.py | P2 | 结算提交后写幂等键失败会被 except 删除——客户端重试可本地双退（09-11 登记 B-F11） | 幂等键写失败改告警保留（钱已退不可重放）；仅未进结算的异常才安全删除 | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | refund.py:40、121、service.py:414 | P0 | 消费订单金额为负，退款却按 order.amount - refunded_sum 计算；实测 amount=-5 直接判“已全额退款”，所有消费单都无法正常退款 | 可退余额和累计退款统一使用绝对额，负号仅作为资金方向 | ✅ 已修 09-12（复核：abs 基数修复与注释在 _decide_refund） |
| 09-11 | refund.py:178 | P1 | 退款处理持有数据库连接期间执行最长 10 秒的渠道 I/O，慢渠道可占满连接池 | 读订单后释放连接，渠道成功后再短事务复验并结算 | ✅ 已修 09-14：重构为三段——短借还读订单/已退累计 → 释放连接后渠道 I/O → 独立短借还结算（_settle_refund 事务内 FOR UPDATE 复验 + 重查已退累计，TOCTOU 已兜） |
| 09-11 | admin_routes.py:139 | P1 | 消费订单为负金额，流水侧使用绝对值，对账比较符号不一致，正常消费也会判 abnormal | 按订单类型统一符号后再核对 | ✅ 已修 09-12（按类型符号统一，见根日志清欠轮） |
| 09-11 | admin_routes.py:174 | P1 | 对账统计、流水核对和结果写入未在一致快照或事务内完成，并发支付会生成错误对账 | 使用 REPEATABLE READ 或固定快照后写对账结果 | 已修（09-12 事务包裹，见下） |
| 09-11 | refund.py:200 | P2 | 渠道已成功但本地结算异常时仍会删除幂等键；模拟渠道无资金副作用，但真渠道会重复退款 | 退款意图先持久化，渠道成功后不得删幂等键，进入人工恢复 | 修复不完整 |
| 09-11 | service.py:143 | P2 | 充值订单创建后幂等键写 Redis 失败只告警；窗口内重试可能形成第二笔待支付订单 | 用 DB 唯一幂等记录或 outbox，不依赖 Redis 单点写入 | 登记 |
| 09-11 | models.py:13 | P2 | subject、reason、order_no 缺少长度上限，可造成 DB、内存和日志膨胀 | 为请求字符串设置合理 max_length 和请求体上限 | ✅ 已修 09-14：subject=128 / order_no=64 / reason=200 / payment_method=32 |


## 2026-09-12 外部复核修复轮（P0×1 + P1×2 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | refund.py:40,121、service.py:414 | **P0** | 消费订单金额为负（扣费为负记录），退款按 amount - refunded_sum 恒为负 → 所有消费单判"已全额退款"无法退款 | 可退基数与比较统一取绝对额，负号仅表方向；测试同步（消费单 -5 可退 5） | 已修 |
| 09-12 | refund.py:178 | P1 | 持 DB 连接期间执行 10s 渠道 I/O，慢渠道占满连接池 | 核实：渠道调用在 `async with pool.acquire()` 之外（事务内仅锁行结算）——登记复核结论 | ✅ 已修 09-14：重构为三段——短借还读订单/已退累计 → 释放连接后渠道 I/O → 独立短借还结算（_settle_refund 事务内 FOR UPDATE 复验 + 重查已退累计，TOCTOU 已兜） |
| 09-12 | admin_routes.py:139 | P1 | 对账消费订单为负、流水绝对值，符号不一致正常消费判 abnormal | 两侧统一按金额绝对额比对 | ✅ 已修 09-12（按类型符号统一，见根日志清欠轮） |
| 09-12 | admin_routes.py:174 | P1 | 对账统计/核对/写入非一致快照，并发支付错账 | api_admin_reconcile 统计/核对/写入包同一 REPEATABLE READ 事务；分类型比对单测（balanced/abnormal/历史负数兼容） | 已修 |
| 09-12 | refund.py:200 | P2 | 渠道成功但本地结算异常仍删幂等键，重试双退 | 已修：渠道成功后幂等键保留转人工；未结算异常才删 | 已修 |
| 09-12 | service.py:143 | P2 | 幂等键写 Redis 失败只告警，重试可能形成第二笔订单 | 登记：DB 唯一幂等记录或 outbox（架构级） | 登记 |
| 09-12 | models.py:13 | P2 | subject/reason/order_no 缺长度上限 | 登记：请求字符串补 max_length | ✅ 已修 09-14：subject=128 / order_no=64 / reason=200 / payment_method=32 |


## 2026-09-12 深夜补充（主人拍板：金钱记录去负号）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | service.py:422、deferred.py:59 | 约定变更 | 扣费单（order_type='payment'）金额存负数（instant -actual_deduct / deferred -cost_amount），与充值/退款单符号约定不一致，读取方需特判 | 两处 INSERT 改存正数金额（abs 兼容存量负数行）；钱包余额运算仍为减法、可为 0（模拟模式继续服务，主人后续再改）；对账/退款 abs 逻辑保留兜底 | 已修 |

## 2026-09-12 晚：主人验收「金钱怎么还是负数」三连修

| 日期 | 位置 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | service.py:_apply_token_deduction | **P1** | 余额不足按剩余扣的守卫带 `before_balance > 0`——余额一旦 ≤0 该分支被跳过，全额照扣越扣越负（实测 -4.67→-5.09）；主人钱包 215.09 消费 > 210 充值即穿透产物 | 守卫改两段：≤0 直接零扣不写订单/流水/余额；0<余额<应扣才按剩余扣（扣到 0 为止）。deduct_token_cost 对零扣费如实返回 deducted=False | 已修 |
| 09-12 | alembic b9c0d1e2f3a4 | 数据 | payment_orders 历史扣费单 132 行负数金额，订单列表显示负钱 | 迁移翻正为绝对额（方向由 order_type 表达）；本地库已应用 | 已修 |
| 09-12 | user_wallets 数据 | 数据 | 主人钱包余额 -5.09（上款穿透 bug 的存量产物） | 一次性 UPDATE 归零（bug 产物修正，非资金操作）；流水历史 after_balance 保持原样不篡改 | 已修 |

## 2026-09-15 code-review-skill 全量复核

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | refund.py:25,36-52 + routes.py:130-150 + query.py:111-155 | **P0** | `payment`（token 消费单）在 `_REFUNDABLE_ORDER_TYPES` 内，普通用户可在订单列表拿到 `order_no` 后自助退款；已交付的模型服务不会回滚 | 用户可“先消费再退款”，形成免费 token 额度漏洞 | 禁止自助退已交付消费单；仅允许失败/未交付补偿或管理员审批，补滥用路径回归 | 待修复 |
| 09-15 | deferred.py:139-151 | P1 | 余额小于等于 0 时 `actual_deduct` 仍为全额，继续写钱包负值；同时订单金额写 `-actual_deduct`，违反 09-12 “扣费单金额统一存正数”约定 | 待结算路径可与即时扣费产生相反的资金语义，余额被扣成负数，退款/对账/账单显示异常 | 复用即时扣费三态：`balance<=0` 实扣 0 且不动账；不足时只扣余额；订单金额存正数；补三个边界回归 | 待修复 |

本轮测试缺口：现有没有 deferred 结算金额符号、零余额和非正余额保护的行为测试。

## 2026-09-15 修复实施结果

- `B-01`：支付与账务时间统一 aware UTC，`_to_iso` 不再产生双时区字符串。
- `B-02`：普通用户自助退已交付消费单默认拒绝；人工补偿需代码显式开启。
- `B-03`：deferred 非正余额实扣 0，订单金额统一存正数。
- `B-04`：新增 `payment_attempts` 状态机和恢复循环；渠道结果先落库，本地账务同事务结算。
- 独立复核补修：退款复用 attempt 时以持久化金额为准，渠道成功但订单过期仍补结算，
  渠道失败结果先落库再抛错；`payment_attempts.user_id` 随用户名迁移。
- 验证：支付精准测试通过；本地数据库迁移到 `r9c0d1e2f3a4`；未接真实渠道。

### 2026-09-15 未覆盖文件逐行复核

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `app/payment/attempts.py` | P2 | 渠道结果更新无 processing 前置，可能覆盖终态 attempt | 已修：WHERE status='processing' 并校验 UPDATE 行数 |
| `app/payment/channels.py` | P2 | 模拟渠道流水号秒级时间+随机数有碰撞概率 | 已修：改为 UUID 片段 |
