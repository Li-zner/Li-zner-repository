# 修复日志 — app/middleware

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-05 | auth.py | P1 | get_cached_user 5 分钟缓存与额度变更不联动（配合 core/quota 修复） | 键前缀改单源引用 |
| 09-05 | auth.py | P2 | refresh 轮换存在并发重放窗口（检查黑名单与写入非原子） | SET NX 原子抢占 + 签发失败回滚自己的占位 |
| 09-05 | circuit_breaker.py | P2 | HALF_OPEN 允许多个并发试探压垮恢复中的下游 | 单探针语义（_probing 标志） |

登记不修（理由见审查报告）：封禁 5 分钟缓存延迟（已声明接受）；熔断器/取消信号多实例不共享（单实例部署）。

回归测试：tests/unit/test_core_p1_fixes.py

## 2026-09-07 审查批次修复

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-07 | auth.py | P2 | 改密后旧 refresh token 最长 30 天仍有效（routes/auth.py change_password 配套） | refresh_access_token 校验 iat 早于 auth:pwd_changed:{username} 即 401 |

## 2026-09-07 复审轮（仅审查登记，未修复）

复审通过项：refresh 侧 pwd_changed 校验链路两端核实——routes/auth.py 改密写标记（TTL=REFRESH_TOKEN_EXPIRE_DAYS*86400，与 refresh 有效期同源同值），access/refresh token 签发均带 iat，`iat < changed` 比较语义正确；未改密用户（标记不存在）不误伤。

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | （关联 routes/auth.py change_password） | P2 | 吊销标记的写入端无 try：Redis 故障时 500 且标记静默丢失——读取端（本目录）无问题 | 详见 app/routes/修复日志.md 复审轮同项 | ✅ 已修（复核：routes/auth.py change_password 已带 try + 降级语义） |

## 2026-09-09 全目录审查轮

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | auth.py:209-221 | P2 | revoke_refresh_token 吞掉一切异常仅 debug 日志：Redis 故障时"假登出"（登出返回成功但 refresh token 未吊销）——与存量 change_password 标记无 try 同族（无声安全降级），代码位置不同单列 | 区分"token 无效"（幂等 OK）与"Redis 异常"（warning + 响应提示降级） | ✅ 已修 09-12（区分返回 + logout 响应透出 revoked，见根日志清欠轮） |

**存量核验**：change_password 的 Redis 吊销标记写在 DB 更新后且无 try（09-08 登记）**仍在**；JWT 验签（jose）/密码（bcrypt）/refresh 轮换 SET NX+失败回滚/改密 iat 比较链路过检 ✅。

## 2026-09-10 修复：会话安全、并发租约与熔断状态机

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | auth.py | P1 | refresh token 重放只拉黑当前 jti，盗用者已签发的同族后代 token 仍可继续刷新 | access/refresh 默认使用一致 family；检测到旧 refresh 重放时撤销整个 token family | 已修 |
| 09-10 | auth.py | P1 | 已禁用账号仍可用历史 refresh token 换取新 access token；旧 access token 在改密后仍可继续访问 | refresh 校验 is_active；access 校验签发时间与改密标记；Redis 状态不可用时拒绝旧 token 并要求重登 | 已修 |
| 09-10 | rate_limit.py | P1 | 并发槽位使用固定窗口计数，释放接口不校验持有者，其他请求可误释放槽位；fail-open 还会生成可释放槽 | 改 Redis Hash token 租约，释放与续期校验 token；长请求后台续租；fail-open 使用不可释放的 bypass 标记 | 已修 |
| 09-10 | circuit_breaker.py | P1 | CLOSED 状态下单次成功未重置连续失败计数；HALF_OPEN 探针取消后可能错误恢复或放开多探针 | 成功清零连续失败；探针取消回 OPEN；新增 guard 上下文统一管理状态迁移 | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | auth.py revoke_refresh_token | P2 | 吞掉一切异常仅 debug 日志：Redis 故障时"假登出"（09-09 登记） | 区分"token 无效"（幂等 False）与"Redis 异常"（warning + False），返回值经 /logout 响应 revoked 字段透出 | 已修 |
| 09-10 | routes/auth.py change_password 吊销标记 | P2 | 吊销标记写 DB 更新之后且无失败处理（09-07 登记） | 复核确认已随 09-10 修复批前置到 DB 更新前 + 失败 503 | 已修（核实） |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | auth.py:252、294 | P1 | get_cached_user 缓存 is_active、角色和权限 300 秒，禁用或降权后旧缓存仍可继续访问 | 权限变更主动失效缓存，或增加 session version 并在每次鉴权校验 | 登记（复核 09-14）：全仓无 role/is_active 变更入口（仅手动 DB 操作），缓存失效暂无代码接线点；上限 300s TTL。引入用户管理端点时必须同步失效 user:info:*（quota.invalidate_user_cache 已有） |
| 09-11 | rate_limit.py:100、147、215 | P1 | QPS、并发、日用量在 Redis 异常时 fail-open 或返回 0，限流和计量会同时失效 | 受保护业务默认 fail-closed，或提供 PG 降级计数并告警 | 登记（维持，09-12 复核）：可用性取舍需主人拍板——Redis 故障时全链路本已不可用，fail-closed 只是把 500 换成 429 |
| 09-11 | rate_limit.py:176 | P2 | renew_concurrent 只检查 Hash 字段存在，不检查租约过期时间，迟到的续期请求可重新激活过期槽位 | 续期 Lua 校验当前 expire_at 仍大于 Redis 服务端时间 | ✅ 已修 09-12（复核：续期 Lua 已校验 expire_at > Redis 服务端 TIME） |


## 2026-09-12 外部复核修复轮（P2×1 修，P1×2 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | rate_limit.py:176 | P2 | 迟到续期可复活过期槽位（HEXISTS 不校验 expire_at） | 续期 Lua 校验 expire_at > 服务端 TIME 才续 | 已修 |
| 09-12 | auth.py:252,294 | P1 | get_cached_user 300s 内 is_active/降权旧缓存仍可访问 | 登记：权限变更主动失效缓存或 session version（涉缓存架构，单独批次） | 登记 |
| 09-12 | rate_limit.py:100,147,215 | P1 | Redis 异常时 QPS/并发/日计量 fail-open | 登记：受保护业务 fail-closed 或 PG 降级计数（架构决策，待拍板） | 登记 |
