---
description: Cursor-Wiki AI 写入放权范围与安全红线
alwaysApply: true
---

# AI 写入放权

本 Vault 中，AI 按路径分级行使写入权。未列出的路径：先问再动。

## 绿区 — 自主写入（无需确认）

| 路径 | 允许操作 |
|------|----------|
| `20-AI-Memory/**` | 新建、追加、小幅整理；优先追加而非覆盖 |
| `00-Input/**` | 捕获碎片、草稿、剪藏 |

## 蓝区 — 指令即可（用户任务明确时直接写，无需 Diff 预览）

| 路径 | 触发条件 |
|------|----------|
| `40-Resources/**` | 「记录」「总结」「整理成笔记」 |
| `30-Context/**` | 项目背景、上下文、模板实例（非 `当前基调-*.md`） |
| `20-Projects/**`、`30-Areas/**` | 明确属于某项目/领域的笔记 |

## 黄区 — 需用户明确授权

| 路径 | 条件 |
|------|------|
| `10-Rules/**` | 用户说「改规则」「对齐结构」「更新放权表」 |
| `30-Context/当前基调-*.md` | 用户说「切换基调」并指定严肃/轻松 |
| 任意已有笔记的大幅改写 | 先输出修改计划，用户确认后执行 |

## 红区 — 禁止（除非用户逐文件点名）

- `.obsidian/**`
- 删除、清空文件夹、移除 YAML frontmatter
- 根目录新建文件（`AGENTS.md`、`.cursor/**` 除外）
- 本 Vault 内新建可执行代码项目；代码写入 `C:\Users\PC\cursor code\<项目名>\`

## 执行优先级

1. `10-Rules/05-AI-Write-Scope.md`（放权表）
2. `10-Rules/全局约束.md`
3. `10-Rules/01-Core-Identity.md`

冲突时：红区 > 黄区 > 本表路径分级。

## 笔记格式

新建笔记遵循 `10-Rules/03-Note-Anatomy.md` 的 YAML 与 Obsidian 语法。
