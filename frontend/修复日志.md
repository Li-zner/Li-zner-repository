# 修复日志 — static（前端）

| 日期 | 级别 | 问题 | 修复 |
|---|---|---|---|
| 09-05 | P2 | GitHub 用户名冲突（?error=github_username_taken）走通用 alert 显示原始错误码，无引导；且该访客无账号，现有绑定弹窗（依赖登录态）不可用 | 特判该错误码，弹窗引导改用手机号登录（zh/en 双语） |
| 09-05 | P1 | 绑定手机号后端已支持改绑用户名（username -> phone_{手机号}）并签发新 token 对，前端不消费——旧 JWT sub 失效导致用户被静默登出 | 提交成功后写入新 access/refresh token；renamed 时展示"登录名已更新为手机号"提示 |

| 09-05 | index.html + vendor/ | P1 | LLM 回答经 marked.parse 直插 innerHTML 且无消毒：经 web_search/文件内容注入的原始 HTML 可执行（LLM 应用标准 XSS 面）；且 marked 走 CDN 未锁版本无 SRI（供应链 + 国内可用性） | vendor 自托管 marked@12.0.2 + DOMPurify@3.1.6（同源、锁定版本）；两处渲染点输出统一过 DOMPurify.sanitize |
| 09-05 | index.html | P2 | 交易流水 tx.remark 进 innerHTML 模板；改名后 travel_username 不同步；30s 余额轮询定时器随反复登录堆叠 | 流水改 DOM+textContent；绑定成功同步 username；轮询挂单例守卫 |
| 09-05 | map.html | P1 | /api/map/recommend 内容为 LLM 生成，foods/spots 直插 innerHTML；收藏/历史面板把用户输入 city 拼进 onclick 属性（self-XSS 面） | 三处改 DOM 构建 + textContent + addEventListener（与 index.html 同标准） |

| 09-05 | index.html | P2 | 登录后定位逆地理调用未带 Authorization（另一处 initGeolocation 已带，不一致） | 补 Bearer 头 |

| 09-05 | 全部 | 债务 | index.html 4469 行 / map.html 1477 行上帝文件（豁免名单翻案：主人批准低风险拆分） | 三件套拆分：css/app.css(1249)+js/i18n.js(428)+js/app.js(2409)；map.css(374)+js/map.js(1034)；index.html 剩 407 行纯壳、map.html 剩 149 行。IIFE 闭包原样搬移（零逻辑变更），echarts@5.5.1 一并 vendor 化（原 CDN 未锁版本）。新上限登记：app.js 2409 行（单闭包，进一步拆分需先建前端测试）。**浏览器实测**：双页加载全 200，i18n 切英文端到端、DOMPurify+marked 消毒链（hasH1+onerror 剥离）、echarts 全局与 inline onclick 依赖的 4 个全局函数全部完好 |

关联：app/routes/修复日志.md（后端用户名方案）、alembic/versions/修复日志.md（github_id 迁移）

# 审查发现 — 2026-09-07 逐文件夹审查·第6批（frontend）

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| P2 | src/utils/sessions.ts:15-17 + stores/auth.ts | 本地会话只按人格隔离不按用户隔离：共用浏览器换账号登录，前一用户的聊天记录对后一用户完全可见；登出也不清 sessions 与定位缓存 | 待修：storage key 拼入用户标识，登出时清态 |
| P2 | src/api/chat.ts:46 | ChatRequest.user 字段塞的是 conversationId（后端该字段注释称"真实用户名由鉴权注入"）——两端语义错位的遗留兼容字段，后端不消费 | 待修：后端删字段或前端传空，别让误导性注释存活 |
| P2 | views/LoginView.vue:26 + 后端 phone.py | 手机号注册 agree 前端默认 true，后端"请阅读并同意用户协议"校验形同虚设（合规面） | 待修：默认 false |
| P2 | stores/chat.ts deleteQA/regenerate | 本地删除问答不联动服务端 conversation_memories；regenerate 以同 conversation_id 重发，旧上下文仍在——用户以为删了，AI 还记得 | 语义确认（产品决策：是否需要服务端删除接口） |
| P3 | views/LoginView.vue:100-101 | GitHub 回调 loginWithTokens 内部已 saveTokens，外面又显式 saveTokens 一次（无害冗余） | 待修 |
| 备注 | api/http.ts / api/sse.ts / api/chat.ts | 401 单飞刷新、响应形状防御、SSE 跨 chunk 攒帧、token 离开地址栏后 history.replaceState 抹除——实现质量好 | 通过 |

## 2026-09-07 复审轮（仅审查登记，未修复）

复审通过项：locales 的 default_pwd_hint 双语文案与后端默认密码随机化配套（不再宣传 123456789，改引导短信验证码登录）；register 响应 default_password_hint 恒 False 的键形兼容核实。上一轮第 6 批 4 项 P2 待修不变（本轮未动前端代码）。

## 2026-09-08 全目录覆盖轮（src 全量逐行 + 配置；无新增 P0/P1/P2，仅登记，未修复）

XSS 面干净（唯一 v-html 走 renderMarkdown=marked+DOMPurify，eslint 豁免仅 MessageBubble）；401 单飞刷新正确且 SSE 复用同一把锁；无 console 打 token；无硬编码后端地址/密钥。

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | src/views/ChatView.vue | P3 | 614 行超 600 行上限（新超标文件；static/js/app.js 2409 行豁免不覆盖此处） | 合并两段 scoped style 或抽独立样式文件 | 登记 |
| 09-08 | src/styles/app.css | P3 | 1271 行（纯 CSS，最低优先） | 拆分或豁免登记 | 登记 |
| 09-08 | api/http.ts:16、locales/index.ts:7 vs enums/index.ts:15-24 | P3 | StorageKey 枚举与本地字面量双轨（gw_access_token/travel_lang 两处维护，漂移风险） | http.ts/locales 改引用 StorageKey 或删枚举死成员 | 登记 |
| 09-08 | api/auth.ts:18 + stores/auth.ts:17 | P3 | api 层 login 内部已 saveTokens，store 又存一次（第 6 批 LoginView 双写的同类新实例）；且与 phone.ts 约定相反 | 统一"api 不落存储、调用方存" | 登记 |
| 09-08 | views/LoginView.vue:191-193 | P3 | GitHub 按钮 `<a href>` 内嵌 button @click 双重导航 | 去掉 goGithub 只留 href | 登记 |
| 09-08 | components/WalletModal.vue:20 | P3 | 幂等键 setup 期只生成一次，充值失败后改金额重试仍复用同一 X-Idempotent-Key，后端按 key 去重会返回旧单（幂等语义误用） | 每次 goPay 重新生成 | 登记 |
| 09-08 | views/MapView.vue:226 | P3 | loadProvinceMap 未检查 resp.ok，GeoJSON 拉取失败 → onMounted 未处理 rejection，地图白屏无提示 | 补 ok 检查+降级提示 | 登记 |
| 09-08 | SessionList.vue:33、stores/chat.ts:85、SettingsModal.vue:66、MapView.vue:225/452、TxModal.vue:52、useUserLocation.ts:95 | P3 | 硬编码中文文案绕过 i18n，而对应键（keep_one_session/new_chat_title/map_recent 等）两语言包已定义零消费；file_too_large/unsupported_file_type 亦无消费（客户端无上传预检） | 改用既有键；补上传预检 | 登记 |
| 09-08 | package.json | P3 | 依赖全部 ^ 范围（lockfile v3 兜底可复现）；@vue/test-utils 装了但全仓零 import | 删多余依赖 | 登记 |
| 09-08 | index.html:13 | P3 | meta Cache-Control 对文档本身不兑现（sw.js network-first 已兜底，风险低） | 后端下发真实响应头 | 登记 |
| 09-08 | MapView.vue:49/119/146/152 | P3 | map_history/map_favorites 同样全局 localStorage 键跨用户可见、登出不清（第 6 批 sessions 隔离问题的同类扩展） | 随该登记项一并修 | 登记 |

locale 键集比对：zh-CN 与 en-US 各 244 键，差集为空；default_pwd_hint 两边键名一致、文案配套。

## 2026-09-09 全目录审查轮（无新 P0/P1；地图缩放修复核验通过）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | src/api/http.ts:27-30 | P2 | JWT 对（含 refresh token）存 localStorage，XSS 可窃取（:4-6 已注释声明 demo 形态；主 XSS 注入面 LLM markdown 已 DOMPurify 消毒，可利用面小） | 升生产时改 httpOnly Cookie | 登记待升级 |
| 09-09 | src/views/ChatView.vue:401 | P2 | 文件上传无前端类型/大小预检（input 无 accept），全靠后端白名单（升级 09-08 同项 P3 为 P2 证据链） | 加 accept + file.size 预检 | 待修复 |
| 09-09 | src/views/MapView.vue:226-227 | P2 | loadProvinceMap 无 resp.ok 检查无兜底：DataV 失败整页静默白图（确认并细化 09-08 P3：onMounted 中 Promise 拒绝无人接） | 仿 drillToCity 补 ok 检查 + try/catch 可见提示 | 待修复 |
| 09-09 | src/views/MapView.vue:334 | P2 | 跨 768px resize 只切 zoomFactor 不重缩放视图：比例读数与实际脱节（不构成限幅绕过） | resize 时同比缩放 geo.zoom | 待修复 |
| 09-09 | src/api/chat.ts:52 | P2 | 流式 401 刷新最终失败不清 token 不跳登录，与 http.ts 语义不一致 | 刷新失败分支补 clearTokens + 回调 | 待修复 |
| 09-09 | src/views/ChatView.vue:290 | P2 | 额度徽标条件 `(remaining_questions ?? 0) >= 0` 恒真死逻辑 | 删恒真子条件或改 > 0 | 待修复 |
| 09-09 | src/views/MapView.vue:248 | P2 | 下钻后 300ms 强制弹气泡与用户中途操作竞态 | 回调内校验 currentLevel 与手动关闭态 | 待修复 |
| 09-09 | stores/chat.ts:166 等 3 处 | P2 | 死代码：regenerate 无调用方；MessageBubble 从未 emit @suggest；LoginView 在 loginWithTokens（内部已 saveTokens）后重复 saveTokens | 删除 | 待修复 |

**地图缩放修复核验（09-06/09-07 两轮提交）✅ 正确且不可绕过**：ZOOM_STEP_MAX=1.08 单事件限步在 georoam 生效（:216）；总范围 [0.5f, 5f] 先于限步、两 clamp 顺序保证不变量不可磨穿（越界需 lastGeoZoom>5.4f，不可能）；zoomFactor 来自 innerWidth 恒非 0 无除零；滑杆直控有意绕过阻尼（显式控件合理）；off/on 无重复注册。**清单过检**：v-html 唯一使用点已 DOMPurify 消毒 ✅；无硬编码密钥（高德 key 全走后端代理，e2e 账号走 env 且空则 skip）✅；SW gw-shell-v2 版本常量 + activate 清旧 + API/SSE 不拦截 ✅；390px 视口无明显问题 ✅；console 无敏感输出 ✅。

## 2026-09-10 正式聊天链路修复

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | api/auth.ts、stores/auth.ts、LoginView.vue | P0 | GitHub 回调长期令牌经 URL 传递 | 前端改读一次性 `oauth_code`，调用后端原子兑换后再落本地 token | 已修 |
| 09-10 | stores/chat.ts、api/conversation.ts、SessionList.vue | P1 | 删除会话只删本地，服务端消息与画像仍保留 | 先调用后端删除接口，成功后再删本地；失败保留本地并提示 | 已修 |
| 09-10 | stores/chat.ts | P1 | 删除问答或重新生成仍复用旧 conversationId，模型能读到已删除上下文 | 删除问答和重新生成时切换新会话分支 ID | 已修 |
| 09-10 | views/ChatView.vue、components/WelcomePanel.vue、components/RatingModal.vue | P1 | ChatView 622 行超文件上限 | 抽出欢迎面板与评分弹窗，ChatView 降至 583 行 | 已修 |
| 09-10 | components、locales、utils/sessions.ts | P2 | 非必要 emoji 污染状态与导出文本 | 删除状态、附件、角色标签等装饰 emoji；保留关闭、箭头、评分星等必要控件符号 | 已修 |
| 09-10 | tests/chat-store.test.ts、tests/oauth-exchange.test.ts | P1 | 删除、分支、OAuth 兑换缺少回归 | 新增 5 项单测，前端测试总数 23 | 已修 |
| 09-10 | locales/zh-CN.ts、locales/en-US.ts | P2 | 定位、文件说明、最近浏览仍有装饰性 emoji | 删除非必要 emoji；关闭、菜单、评分星等交互符号保留 | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | views/ChatView.vue | P2 | 文件上传无前端类型/大小预检（09-09 登记）：大文件传完才被后端拒绝 | input 加 accept 白名单；onFilePicked 先查扩展名（对齐后端 ALLOWED_EXTENSIONS）与 20MB 大小，超限本地提示不发请求 | 已修 |
| 09-10 | views/MapView.vue | P2 | loadProvinceMap 无 resp.ok 检查无兜底：DataV 失败整页静默白图（09-09 登记） | 补 ok 检查 + try/catch + loadError 状态条与"重试"按钮（可见提示） | 已修 |
| 09-10 | views/MapView.vue | P2 | 跨 768px resize 只切 zoomFactor 不重缩放视图：比例读数与实际脱节（09-09 登记） | resize 检测倍率变化按比例重缩放 geo.zoom 并同步读数 | 已修 |
| 09-10 | views/MapView.vue | P2 | 下钻后 300ms 强制弹气泡与用户中途操作竞态（09-09 登记） | 回调内校验仍处城市级、标题未变且气泡未手动关闭才弹出 | 已修 |
| 09-10 | api/chat.ts | P2 | 流式 401 刷新最终失败不清 token 不跳登录，与 http.ts 语义不一致（09-09 登记） | 刷新失败分支补 notifyUnauthorized()（http.ts 新导出：clearTokens + 兜底回调） | 已修 |
| 09-10 | views/ChatView.vue | P2 | 额度徽标 `(remaining_questions ?? 0) >= 0` 恒真死逻辑（09-09 登记） | 改 > 0（剩余 0 时不再显示空额度徽标） | 已修 |
| 09-10 | stores/chat.ts、views/ChatView.vue | P2 | 死代码三处（09-09 登记）：regenerate 无调用方、MessageBubble 从未 emit @suggest、LoginView 重复 saveTokens | regenerate 删除（连带 deleteQA+send 语义由用例钉住）；@suggest 监听与 sendDirect 删除；LoginView 重复 saveTokens 已随 09-10 OAuth 改造消失（现仅手机登录路径单次 save） | 已修 |

**验证**：vue-tsc --noEmit 0 错误；vitest 23 passed（chat-store 用例同步改造）。


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | stores/chat.ts:102 | P1 | 流式进行中可删除当前会话：服务端记忆被删而流仍在写，产生孤儿会话（对照 switchSession/switchPersona 均有守卫） | deleteSession 加 `streaming && id===currentSessionId` 守卫；新增 vitest 回归 | 已修 |
| 09-11 | views/MapView.vue | P2 | 单文件 608 行超 600 行门禁 | 登记：CITY_LIST/MUNI 常量与收藏/历史存取抽至 utils | 登记 |
| 09-11 | views/MapView.vue 等 5 处 | P2 | 硬编码中文绕过 i18n（'中华人民共和国'、加载失败提示、重试/收藏/历史按钮），英文界面夹杂中文 | 登记：补双端词典键 | 登记 |
| 09-11 | components/SettingsModal.vue:66 | P2 | 角色文案硬编码中文 | 登记：t('role_admin')/t('role_user') 双端补键 | 登记 |
| 09-11 | 多处死代码 | P2 | formatWeather/getUserFile/amapRoute/SseEventType/StorageKey.AutoPrompt/store error 态零消费 | 登记：删除或接线 | 登记 |
| 09-11 | api/http.ts + enums | P2 | token 键名字面量与 StorageKey 枚举重复定义（拼写漂移风险） | 登记：http.ts 改引枚举 | 登记 |
| 09-11 | api/auth.ts + stores/auth.ts | P2 | 登录令牌双重落盘（API 层与 store 各 saveTokens 一次） | 登记：落盘职责归 store 一处 | 登记 |
| 09-11 | components/TxModal.vue | P2 | `t('tx_'+type) || fallback` 死分支（vue-i18n 缺键返回键名）+ 时间格式硬编码 zh-CN | 登记：te() 判存在；locale 传入 fmtTime | 登记 |
| 09-11 | views/ChatView.vue:390 | P2 | 多附件仅显示第 1 个文件名，实际全部发送 | 登记：v-for 渲染全部或限单文件 | 登记 |
| 09-11 | stores/chat.ts:201 | P2 | 纯文件首条消息生成空会话标题（query 为空串未用兜底文案） | 登记：标题取 sendQuery | 登记 |
| 09-11 | views/MapView.vue:128 | P2 | 换一换推荐失败静默无反馈 | 登记：rejected 分支补提示 | 登记 |
| 09-11 | views/MapView.vue:321 | P2 | 流式期间"去这里"切人格静默失效（switchPersona 内 streaming 早退无返回值），autoPrompt 落错人格 | 登记：switchPersona 返回 boolean，失败禁用或提示 | 登记 |
| 09-11 | views/LoginView.vue | P2 | a 嵌套 button 双重导航；手机登录无 busy 防重连点 | 登记：去外层 a 或去 @click；补 busy ref | 登记 |
| 09-11 | views/ChatView.vue:116 | P2 | profile 瞬时失败（5xx/网络抖动）把持有效 token 用户踢回登录页 | 登记：仅 401 跳登录 | 登记 |


## 2026-09-12 凌晨清欠轮（09-11 登记 P2 全修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | views/MapView.vue | P2 | 608 行超 600 门禁（09-11 登记） | CITY_LIST/MUNI/loadList/saveList 抽至 utils/mapData.ts，降至 586 行 | 已修 |
| 09-12 | MapView/SettingsModal/TxModal | P2 | 7 处硬编码中文绕过 i18n + 角色文案（09-11 登记） | map_title_country 复用已有键；新增 map_load_failed/retry/map_favorites_short/map_history_short/map_switch_busy/role_admin/role_user 双端键；TxModal te() 判存在 | 已修 |
| 09-12 | views/MapView.vue:128,308 | P2 | 换一换失败静默（D-F11）；流式中"去这里"切人格静默失效（D-F12） | rejected 分支设 loadError 提示；switchPersona 返回 boolean，被拒时提示 | 已修 |
| 09-12 | views/ChatView.vue | P2 | 多附件仅显示第 1 个文件名（D-F9）；profile 非 401 失败踢登录（D-F14） | 文件名 join 全量展示；仅 ApiError 401 跳登录 | 已修 |
| 09-12 | stores/chat.ts | P2 | 纯文件首条消息空标题（D-F10） | 标题源取 send_file_query 兜底文案 | 已修 |
| 09-12 | api/http.ts + enums | P2 | token 键名字面量与枚举重复定义（D-F6） | http.ts 改引 StorageKey 枚举 | 已修 |
| 09-12 | api/auth.ts + stores/auth.ts | P2 | 登录令牌双重落盘（D-F7） | API 层删 saveTokens，落盘归 store | 已修 |
| 09-12 | views/LoginView.vue | P2 | a 嵌套 button 双重导航；手机登录无 busy 防重（D-F13） | 去内层 button；phoneLoginBusy 防重 + 按钮 :disabled | 已修 |
| 09-12 | 死代码 | P2 | formatWeather/getUserFile/amapRoute/StorageKey.AutoPrompt 零消费（D-F5） | 全部删除；SseEventType 采纳进 stores/chat.ts 事件判断（不删改用） | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | utils/sessions.ts:15、stores/chat.ts:73 | P0 | 本地会话键只有 personaId，不含账号；换账号登录后可直接读取上一账号的完整会话 | 使用不可变 user_id 分区存储，登录/登出/切换账号时清理或迁移 | 待修复 |
| 09-11 | api/http.ts:29 | P1 | access_token 与 refresh_token 均存 localStorage，任意同源脚本可读取长期刷新令牌 | 生产改为 HttpOnly/Secure/SameSite Cookie，或短期 access 放内存 | 待修复 |
| 09-11 | api/http.ts:61、102 | P1 | 刷新单飞仅限单标签页；多窗口同时刷新会互相踩失效并 clearTokens，可能清掉刚成功的新令牌 | 使用 Web Locks 或 BroadcastChannel 跨窗口协调，锁内重读最新令牌 | 待修复 |
| 09-11 | api/sse.ts:16、api/chat.ts:79 | P1 | [DONE] 被解析为 null，和忽略行/坏 JSON 无法区分；连接不关闭时聊天永久停留流式状态且无空闲超时 | 增加显式 done 信号，收到后取消 reader，并设置流空闲超时 | 待修复 |
| 09-11 | utils/sessions.ts:31、stores/chat.ts:137 | P1 | 会话持久化整体覆盖数组，没有 storage 同步、版本号或合并；多窗口并发修改会丢更新 | 按会话增量写入，或监听 storage 并做版本校验合并 | 待修复 |
| 09-11 | enums/index.ts:19、utils/mapData.ts:21、composables/useUserLocation.ts:25 | P1 | 地图历史、收藏、用户定位使用固定全局键，换账号后仍可读取上一账号数据 | 所有用户相关键加入 user_id，并按登出策略清理 | 待修复 |
| 09-11 | stores/chat.ts:179、views/ChatView.vue:202 | P1 | deleteQA 没有 streaming 守卫，生成中修改消息和 conversationId，回答会写入废弃分支 | 流式中禁用或排队删除和分支切换 | 待修复 |
| 09-11 | views/ChatView.vue:132、147 | P1 | 切换人格会清空 uploads 但不取消上传 Promise，完成回调仍可把旧人格文件写入新人格 | 绑定人格或请求序号，过期结果丢弃，并用 AbortSignal 取消 | 待修复 |
| 09-11 | components/SessionList.vue:57、SettingsModal.vue:59、views/MapView.vue:446 | P1 | 关键操作使用 div/span click，无键盘语义；弹窗无 dialog 角色、焦点锁定和 Escape 关闭 | 使用原生 button/dialog，或成熟焦点管理组件 | 待修复 |
| 09-11 | styles/app.css:45、styles/adapt.css:69 | P1 | --text-muted 在浅色背景约 3.0-3.2:1，却被 11-13px 文本使用，不满足 WCAG AA | 加深颜色并校验三套主题，增加对比度检查 | 待修复 |
| 09-11 | package.json:33 | P1 | 开发依赖 vitest 2.1.9/Vite 相关版本命中 critical/high 漏洞，威胁开发机和 CI | 升级到修复版本并重跑测试；生产依赖可保持单独审计 | 待修复 |
| 09-11 | stores/auth.ts:21 | P2 | 令牌先落盘，loadProfile 失败会显示登录失败但刷新后仍有登录态 | 失败回滚令牌，或拆分明确登录状态机 | 登记 |
| 09-11 | components/TxModal.vue:22、views/MapView.vue:252 | P2 | 快速切换筛选或地图层级时旧请求可覆盖新结果 | 统一请求序号和 AbortController | 登记 |
| 09-11 | components/SettingsModal.vue:66 | P2 | role_admin/role_user 词典缺失且关闭缺失警告，界面会显示键名 | 补齐中英文词典并启用类型化消息 schema | 登记 |
| 09-11 | vite.config.ts:28、api/http.ts:125 | P2 | as never 绕过 Vite 类型校验；request<T> 无条件信任 JSON 形状 | 使用 vitest/config 的 defineConfig，并在边界做运行时校验 | 登记 |
| 09-11 | tests/sse-framer.test.ts:12、tests/chat-stream.test.ts:10 | P2 | 测试伪流必然关闭，未覆盖 DONE 后不断连、多窗口刷新、账号隔离、上传竞态和焦点管理 | 增加对应单测和 Playwright 多标签页场景 | 登记 |
| 09-11 | utils/sessions.ts:61 | P2 | 导出文件直接拼接原始 HTML/Markdown，在允许原始 HTML 的查看器中可执行恶意内容 | 导出前中和原始 HTML，或明确标记不可信内容 | 登记 |


## 2026-09-12 外部复核修复轮（P0×1 + P1×8 中 3 修 5 登记 + P2×3 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | utils/sessions.ts:15、stores/chat.ts:73 | **P0** | 本地会话键只有 personaId 不含账号——换账号登录直接读取上一账号完整会话 | storageKey 加 owner（username）维度；store 传参；未登录退 shared 键 | 已修 |
| 09-12 | stores/chat.ts:179 | P1 | deleteQA 无 streaming 守卫，生成中改消息和 conversationId | 流式中 return（与 deleteSession 守卫同款） | 已修 |
| 09-12 | api/http.ts:61,102 | P1 | 多窗口同时刷新互相踩失效并 clearTokens | 登记：Web Locks/BroadcastChannel 跨窗口协调（涉存储架构，单独批次） | 登记 |
| 09-12 | api/http.ts:29 | P1 | 双 token 均存 localStorage 可被同源脚本读取 | 登记：生产改 HttpOnly Cookie（架构级，单独批次） | 登记 |
| 09-12 | api/sse.ts:16、api/chat.ts:79 | P1 | [DONE] 解析为 null 与坏 JSON 不可区分；无空闲超时 | SSE_DONE 哨兵单例（=== 判定）+ 流截断检测（answer_complete/[DONE] 皆缺抛 502，内容完整时容忍）+ 60s 空闲看门狗 reader.cancel；vitest 回归×3 | 已修 |
| 09-12 | utils/sessions.ts:31 | P1 | 会话持久化整体覆盖无版本合并，多窗口丢更新 | 登记：增量写入/版本校验（架构级，单独批次） | 登记 |
| 09-12 | enums+mapData+useUserLocation | P1 | 地图历史/收藏/定位固定全局键跨账号可读 | 登记：与 gw_sessions 同批加 owner 维度（依赖本轮 P0 的用户标识接线） | 登记 |
| 09-12 | views/ChatView.vue:132,147 | P1 | 切人格清空 uploads 不取消上传 Promise，旧人格文件写入新人格 | 登记：请求序号+AbortSignal（涉异步生命周期，单独批次） | 登记 |
| 09-12 | SessionList/SettingsModal/MapView | P1 | 关键操作无键盘语义、弹窗无 dialog 角色/焦点锁定/Escape | 登记：可访问性专项（WCAG 批次） | 登记 |
| 09-12 | styles/app.css 等 | P1 | --text-muted 对比度 3.0:1 不满足 AA | 登记：加深颜色+三主题校验（设计批次） | 登记 |
| 09-12 | package.json:33 | P1 | vitest 2.1.9 等开发依赖命中 critical/high 漏洞 | 登记：升级到修复版本重跑测试（依赖批次） | 登记 |
| 09-12 | stores/auth.ts:21 | P2 | 令牌先落盘 loadProfile 失败仍留登录态 | 登记：失败回滚令牌（与 D-F7 同批） | 登记 |
| 09-12 | TxModal/MapView | P2 | 快速切换旧请求覆盖新结果 | 登记：请求序号+AbortController | 登记 |
| 09-12 | SettingsModal.vue:66 | P2 | role_admin/role_user 词典缺失显示键名 | 已修（本轮 D-F4 补齐双端键） | 已修 |

## 2026-09-12 晚：主人验收反馈修

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | locales zh/en + SessionList.vue | 交互 | 历史对话四按钮中文全称（置顶/重命名/导出/删除），会话名过长时挤压覆盖 | 按主人定的口径改单字/首字母：顶·改·出·删（en: P·R·E·D）；置顶态以主色+加粗区分；悬停仍有完整 tooltip | 已修 |
| 09-12 | ChatView.vue personaOptions | 交互 | unified 兜底项被误标「旅行」，与后端 travel「旅行规划助手」重复出现——主人见到的四个助手实为三个 | 删除 unified 幻影选项；旧会话残留 unified 时标签回落到首选项（行为等同默认 travel） | 已修 |

## 2026-09-14 全量只读审计：控制台展示链

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-14 | views/RagConsoleView.vue:346-360 | P1 | 事件详情只显示 title/cause/action，不渲染 `verdicts[].evidence`、`patch` 和 request_uid | 控制台无法解释“为什么触发 RC-4b”，只能猜 | 增加证据表、规则明细、关联请求跳转 | ✅ 已修：详情面板新增「样本证据」区（最多 5 条，每条含 code/severity 徽标 + query 摘要 + evidence 列表 + action + patch + 「查看请求」跳转 openRequest）；api/ragAdmin.ts 补 `RagVerdictSample` 类型 |
| 09-14 | views/RagOpsChatView.vue:200-208 | P2 | 运维会话上下文只带事件标题、规则和状态，不带 evidence | 模型回答依赖全局最近 20 条，目标事件证据可能丢失 | incident 上下文补 evidence/patch | ✅ 已修：contextQuestion 随上下文回带最多 3 条样本（query + evidence 前 3 条 + patch JSON） |

### 前端通过项

- `MessageBubble.vue` 与 `RagOpsChatView.vue` 的 `v-html` 均经 `utils/markdown.ts`
  的 DOMPurify 消毒。
- 前端全库未发现新增硬编码 API Key、云 AccessKey 或密码。

## 2026-09-15 code-review-skill 全量复核

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | stores/auth.ts:43-48 + api/http.ts:24-52 + app/routes/auth.py:103-147 | P1 | 控制台 Cookie 模式下 `getRefreshToken()` 恒为空，登出不会调用 `/api/logout`，只删 localStorage；同时登录/刷新 JSON 仍返回令牌 | 服务端 refresh token 未吊销、Cookie 未清理，HttpOnly 保护被响应体绕过 | 增加控制台专用 Cookie 登录/刷新/登出契约，登出必须服务端吊销并清 Cookie，响应体不再回传令牌，补 E2E | 待修复 |
| 09-15 | views/RagConsoleView.vue:152-166 | P1 | `probe_health` 动作前端提交空参数，后端默认地址又是 worker 容器内的 `127.0.0.1:10086` | 控制台“健康探测”按钮通常必失败 | 提供目标选择/URL 参数，固定默认内网服务名，展示执行结果 | 待修复 |
| 09-15 | views/RagConsoleView.vue:286,318,352 | P2 | 可点击 `<tr>` 没有 keyboard/role/focus 语义 | 键盘用户无法打开请求或事件详情，不满足生产可访问性 | 改原生 button/可聚焦行，补 focus-visible 与键盘验证 | 待修复 |
| 09-15 | api/ragAdmin.ts:64 | P2 | `ungrounded_citations` 类型写为 `number[]`，后端实际返回中文条文号字符串数组 | 契约类型漂移，后续消费该字段易误判 | 改为 `string[]` 或统一后端返回格式并补类型测试 | 待修复 |

## 2026-09-15 修复实施结果

- 认证后等待 profile 完成再加载会话、地图历史和收藏。
- 认证态禁止回退 shared 存储，流式持久化绑定发送时账号。
- Cookie 模式登出调用服务端登出；刷新重试后再次 401 会清理登录态。
- SSE 看门狗按每次 chunk 重置；定位结果跨账号返回时丢弃。
- RAG 控制台写操作串行化，详情请求防止旧响应覆盖。
- RAG 运维请求切换加请求序号，发送时快照账号 owner，避免旧响应或旧账号数据覆盖。
- 验证：`npm test` 38 项通过，`vue-tsc --noEmit` 与生产构建通过。

### 2026-09-15 未覆盖文件逐行复核

| 文件 | 级别 | 问题 | 状态 |
|---|---|---|---|
| `frontend/src/utils/ragLabels.ts` | P1 | 路由/阶段/状态映射遗漏真实后端值，控制台显示未知项 | 已修：补齐 cache/task/react/config 等映射 |
| `frontend/src/components/BindPhoneModal.vue` | P1 | 绑定已成功但 profile 刷新失败会显示“操作失败” | 已修：绑定成功即成功，profile 异步刷新；发送码加 busy 和输入上限 |
| `desktop/src/windows.js` / `main.js` | P1 | 设置窗口缺少导航隔离，IPC 未校验 sender | 已修：设置窗口禁导航/新窗口，IPC 仅接受设置窗口 sender |
| `frontend/src/views/ConsoleLoginView.vue`、`console-main.ts` | P2 | 控制台挂载失败无可见错误 | 已修：挂载异常写 title/data 属性 |
| `frontend/src/stores/ui.ts`、`utils/geo.ts`、`api/user.ts`、`main.ts` | P2 | 主题/存储/Geo adcode/评分类型/错误采集防御不足 | 已修：白名单、存储降级、adcode 校验、optional 字段、初始化错误数组 |
| `desktop/src/health.js` | P2 | 健康检查跟随重定向，偏离目标策略 | 已修：`redirect: error` |
| `desktop/src/config_store.js` | P2 | `close_to_tray` 配置生产代码未消费 | 待专项：删除死配置或接入窗口关闭行为 |
| `frontend/src/console-router.ts` | P2 | 路由守卫信任本地认证标记，不作为安全边界 | 待专项：接服务端 profile/session 校验 |
| `desktop/src/preload/setup.js`、`setup.js`、`url_policy.js` | 无 | 未发现比上述更严重的可复现问题 | 已审 |
| `frontend/src/api/files.ts`、`api/map.ts`、`api/payment.ts`、`api/personas.ts`、`App.vue`、`ConsoleApp.vue`、`utils/reasoning.ts` | 无 | 未发现比上述更严重的可复现问题 | 已审 |

## 2026-09-15 有把握批次（用户手修后复核剩余 15 项，修 8 项）

先对用户手修后的代码核对 20 项遗留：已修 4（tools 向量错位、task_manager 取消事件清理、
chat store 单例竞态、_ensure_admin_users bootstrap-only）、部分修 1（XFF 末跳）、未修 15。
本批修有把握的 8 项：

- B-12：router.py _format_simple_answer 的 wait_for 超时捕获后降级
  _fallback_tool_view（原先 TimeoutError 冒泡成整任务 error，工具结果已在手）。
- B-11：simple_task.py _emit_simple_result 返回 finish_task 的 CAS bool
  （失败返 None）；router.py 检查后跳过记忆/语义缓存写入——取消竞态不再污染
  记忆/缓存，与 runner 主路径同契约。
- users.py：RateMessageRequest 三字段补 max_length（4096/4096/128）。
- map_api.py：RouteRequest departure/destination 补 max_length=128（地址串
  会拼进 LLM prompt）。
- db_maintenance.py：压缩节流从取模改为下一暂停点比较——单对话 msg_count
  跨过 BATCH_SIZE*5 整数倍时取模恒非 0、节流整轮失效。
- orchestrator.py：add_phase2_review 与 phase1 同口径消毒（当前 phase2 无
  消费方，防御性）。
- test_payment_recharge.py：_patch_charge 永久模块属性赋值改
  monkeypatch.setattr（消除跨文件共享状态泄漏）。
- pytest 基建：requirements-dev.txt 加 pytest-timeout==2.3.1（已安装），
  pytest.ini 加 timeout = 120（CI 挂死保护）。

回归测试：B-11 两例（CAS 失败返 None / handler 不写 artifacts）、B-12 一例
（超时降级数据视图），并入 test_20260915_review_fixes.py。

明确不修（需设计决策/被 Hook 拦）：B-02 CF 头、B-04 lock_timeout（安全扫描
拦截分区名拼接误报，绕行需重写白名单 SQL）、B-05b backfill 指标、B-07/B-08
支付恢复终态、kb_expand list 形态、reasoning_guard 强指纹、answer_trace
username 哈希口径、auth_cookies XFP、/route QPS 口径。

终验：pytest 372 passed（timeout 生效）；前端 vitest 39 passed。

## 2026-09-16 RAG 运营控制台布局与证据面板

- 概览顶部改为待处理告警带，优先展示活跃事件、待审批动作、空召回和高延迟，
  其余容量/运行指标下移。
- 最近动作支持点击并打开审计详情。
- 15 秒轮询刷新列表后，同步刷新当前选中的请求、事件和动作详情。
- 请求详情新增错误码、两路召回命中、相似度、融合/重排状态、未依据引用和反馈。
- 动作详情补充申请原因、审批人、环境、配置版本和冷却截止时间。

验证：`vue-tsc` 通过，控制台生产构建通过；前端 `42 passed`。

## 2026-09-16 RAG 中控台事件分组排序、推荐修复与撤销回滚

南反馈三项中控台问题：未处理高风险事件要排最前、已处理事件最新在处理组
头部、没有任何自动/半自动修复入口、误点提交后无法回退。

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-16 | `RagConsoleView.vue` | P2 | 事件列表平铺展示，未处理与已处理混杂，看不到最紧要项 | 事件页拆成「未处理事件 / 已处理事件」两组：未处理按严重度再按最近出现时间，已处理按处理时间（`resolved_at`/`closed_at`/`last_seen_at` 取最大）倒序；概览「最近事件」改用同一排序 | 已修 |
| 09-16 | `RagConsoleView.vue` | P2 | 已处理事件排序把 `resolved` 无条件排在 `closed` 之前，刚关闭的事件会沉到旧的已解决事件下面 | `compareHandledIncidents` 改为纯处理时间倒序 | 已修 |
| 09-16 | `api/ragAdmin.ts` | P3 | `RagIncident` 类型缺 `resolved_at`/`closed_at`，前端无法计算处理时间 | 补齐字段类型，并新增 `getRagIncidentRecommendations`、`rollbackRagAction`、`RagIncidentRecommendation`、`RagAction.rollback_available` | 已修 |
| 09-16 | `RagIncidentDetail.vue` | P1 | 事件详情只有文本建议，没有可点的修复动作 | 新增「推荐修复」区：展示动作、模式（自动执行/需要审批/人工处理）、风险等级和参数；一键「生成并执行」或「提交审批」 | 已修 |
| 09-16 | `RagIncidentDetail.vue` | P1 | 误点提交后无法回退，只能切到动作页手动取消 | 提交成功后就地显示「撤销刚才提交」，仅在 `proposed/waiting_approval/queued` 可撤销，状态以服务端返回为准 | 已修 |
| 09-16 | `RagActionDetail.vue` | P1 | 已成功动作没有人工回滚入口 | 「回滚」区展示回滚方案，`rollback_available && status='succeeded'` 时显示「回滚此动作」，二次确认后调用回滚接口并刷新审计 | 已修 |

测试补充（`tests/rag-console-view.test.ts`）：

- 事件列表分组排序：断言未处理 3 条在前、已处理 2 条在后，且已处理按处理
  时间倒序（09:30 关闭的事件排在 09:00 解决的事件之前）。
- 推荐修复链路：断言渲染推荐卡片、调用 `planRagAction`、出现「撤销刚才
  提交」并调用 `cancelRagAction`、人工建议不出现执行按钮。
- 人工回滚：断言二次确认后调用 `rollbackRagAction` 并重新拉取动作详情。

验证证据：

- `npx vitest run` 全量 `45 passed`。
- `npm run build:console` 通过（`vue-tsc --noEmit` + vite 生产构建）。
- 真实浏览器演练：事件页「未处理事件 6 / 已处理事件 2」分组正确；打开
  RC-1b 事件出现「健康探测 / 自动执行」推荐卡；点「生成并执行」后动作数
  1→2 并出现「撤销刚才提交」，点撤销后回到 1 且显示「动作 #11：已取消」；
  动作 #6（`adjust_rerank_top`，已成功）显示「回滚此动作」，动作 #9
  （L0 `probe_health`）回滚区显示「未定义」且无按钮。

## 2026-09-16 ChatView 文件超限整改

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-16 | `views/ChatView.vue` | P3 | 601 行，超过 600 行上限 | 合并两段相邻的 `<style scoped>` 块（同一组件、同一作用域，拆成两段无意义），601 → 599 行 |

验证：`npm run build` 通过。
