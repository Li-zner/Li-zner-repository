# 修复日志 — scripts

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-07 | secret_scan.py | P2 | 通用密钥赋值模式的值字符类 [A-Za-z0-9+/]{12,} 缺 _ 和 -：含下划线/连字符的密码（如 abcd-efgh-ijkl）分段均 <12 整段不命中——门禁假阴性（该脚本是公开仓推送的 P0 门禁） | 值字符类补 _-；同步核对 test_secret_scan 干净样本不产生新误报 | ✅ 已修（复核：值字符类已补下划线与连字符，测试样本无新误报） |
| 09-07 | secret_scan.py | P3 | "已知泄漏"特征值（真实 IP/历史密码）硬编码进仓——属防御黑名单且值本身已泄漏，可接受；提示该文件自身会被其它扫描器命中 | 登记说明即可 | 可不修 |

复审通过项：sk- 形状 \b 前缀防误报、占位符豁免清单、literal 规则"值后随 . / ( 视为代码引用"判定、SKIP_DIRS 原地剪枝、命中不回显密钥内容（符合日志红线）；test_secret_scan.py 正反两向 + 退出码覆盖。

## 2026-09-08 全目录覆盖轮（105 个代码文件全量审查；仅登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | test_voice.py:6 / test_upload.py:7 / test_voice.ps1:12 | P1 | 硬编码 64 位 hex 签名密钥（三文件同值），并用它伪造 sub=admin 的长效 token。已核实**非现行 JWT_SECRET**（与桌面权威 .env 不一致），但该值随基线提交 a80159d 进了 git 历史——若历史上任何部署用过此值，伪造 admin token 即可行 | ✅ 已修 09-08：三处改运行时读取（py 走 _env.get("JWT_SECRET")，ps1 读 env），缺失即退出 fail-loudly；grep 确认旧值工作区零残留。**遗留：该值仍在 git 历史中，是否曾在线上用作 JWT_SECRET 需主人核对，是则轮换** | ✅ 已修（历史待核） |
| 09-08 | backup_daily.cmd:3 | P1 | 每日备份脚本的路径已损坏为字面 0x3F 问号（`cd /mnt/d/??/agent_gateway`，od 核实非显示问题），cd 必然失败——**每日备份从未成功执行过** | ✅ 已修 09-08：路径改 glob `cd /mnt/d/*/agent_gateway`（ASCII 安全，规避中文目录名编码问题），文件整体重写为 ASCII 注释；WSL 实测 `cd /mnt/d/*/agent_gateway && pwd` 正确解析 /mnt/d/桌面/agent_gateway | ✅ 已修 |
| 09-08 | seed_project_knowledge.py:17（另 update_kb.py:11、gen_resume*.py、write_resume.py:35、test_phone_login.py:46） | P2 | 真实手机号硬编码 7 处，且 seed 脚本会把含真实姓名+手机号的简历文案写进线上知识库供 /v2/chat/me 检索回答；仓库拟公开 | KB/简历文案外置为不入库数据文件；公开仓库前脱敏 | ✅ 已修 09-12（7 处外置 gitignored 数据文件；登录脚本改环境变量） |
| 09-08 | verify_me_github.py:9、benchmark_single_gateway.py:25 | P2 | admin 密码字面量（Admin@****）入库 | 改 _env.get("ADMIN_PASSWORD") | ✅ 已修 09-09（6 处脚本账密改走环境变量，见根日志第四轮） |
| 09-08 | setup_grafana.py:5（另 fix/check_grafana.py、final_setup.py） | P2 | Grafana 管理弱口令字面量，4 脚本重复硬编码 | 走环境变量并更改线上弱口令 | ✅ 已修 09-12（4 脚本改环境变量） |
| 09-08 | check_code_rules.py:23 + tasks.json:6-7 | P2 | 门禁 DEFAULT_TARGETS 只有 ["app"]，而定时任务在 scripts/ 目录下执行 → 相对路径 app 不存在 → **每日 08:00 代码门禁空转假通过** | 以 __file__ 推项目根，恢复三目录目标 | ✅ 已修 09-12（门禁根推导修复，每日任务不再空转） |
| 09-08 | check_code_rules.py:56 | P2 | 语法错误/读取失败的文件被静默跳过全部规则（坏文件反而全绿，门禁假阴性） | 解析失败计入违规输出 | ✅ 已修 09-12（解析失败计入违规） |
| 09-08 | slow_query_monitor.py:37-39 | P2 | psql 失败返回空串 → check() 判"无慢查询" exit 0，监控自身故障被当作健康 | 失败返回非 0 或 UNKNOWN 状态 | ✅ 已修 09-12（失败不装健康） |
| 09-08 | task_scheduler.py:143-146 | P2 | 子进程 wait(timeout) 超时后只记日志不 kill，挂死任务成孤儿进程 | TimeoutExpired 分支补 proc.kill(); proc.wait() | ✅ 已修 09-12（超时 kill） |
| 09-08 | task_scheduler.py:114 | P3 | startswith(folder_abs) 无分隔符，scriptsX 前缀目录可绕过路径校验 | commonpath 或补 os.sep | 登记 |
| 09-08 | backup_db.sh:24 | P3 | pg_dump 失败留半截 .sql，外观与好备份无异，"取最新备份"会误取 | 失败即删或先写 .tmp 再改名 | 登记 |
| 09-08 | rotate_jwt_secret.sh:50 | P3 | 每次轮换在 /etc/agent_gateway/ 留含全部密钥的 .bak，无上限无清理 | 只留最近一份或成功后删 | 登记 |
| 09-08 | slowq_loop.cmd、setup_ssl.sh:47、create_user_inside.py:11、add_test_users.py:23、locustfile.py:181、clear_semantic_cache.py:24、retention_cleanup.py:89、cdc_acceptance.py:44、build_knowledge_base.py:11、simulate_locust.py:33、find_mcp_config.py:28、scale_gateway.sh:53、check_doc_deps.py:13 | P3 | 杂项登记：日志无限追加；certbot 占位邮箱必失败；测试口令字面量；超时补发真实请求加重负载；KEYS "*cache*" 通配删可能误伤他库键；--days 缺 --table 静默失效；裸 except（诊断路径）；os.system 自动 pip install；调试脚本打印 token 前 20 字符；sed 锚定行被摘除后扩容静默不生效；shell=True 拼常量串 | 逐条见审查底稿 | 登记 |
| 09-08 | 全目录 33 个代码文件 | P3 | 装饰性 emoji（✅❌🔄⚠️ 等，位于字符串/日志/注释），违反代码禁 emoji 规范 | 批量替换纯文本 | 登记 |
| 09-08 | check_syntax.js、test_fixed.js、test_new.js、test_server_script.js | P3 | 4 份近 50KB 前端聊天页副本（7 月调试残留）误放 scripts/，两两近似重复（已扫描无敏感信息） | 删除或移出仓库 | 已修 09-10：确认零引用后删除 4 份副本 |

红线排查结果（未命中）：无 sk- 真实形状、无 LTAI、无公网 IP 残留、无身份证号（13800138000 等为假号段）、无无确认危险删除、SQL 均参数化。覆盖：52 个深读（常驻/安全运维/数据资产/压测/门禁五类全覆盖）+ 46 个一次性脚本红线扫描 + 3 个跳过（secret_scan 已审、修复日志、__pycache__）。

## 2026-09-09 全目录审查轮（10 域并行 + 主代理逐条复核 + Mimosa L3 钩子拦截补充）

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | task_scheduler.py:256-258 | **P1** | 到点判断恒为假：next_run() 按构造只返回严格未来时刻，`(now-nr)>=0` 永假——tasks.json 里"每日代码检查 08:00""每周六评测门禁"**从未执行过**且无报错（模拟验证 24h/30 天触发 0 次） | next_run 加 after 参考时间参数；调度循环维护 .scheduler_state.json 心跳，`nr(基于 last_tick) <= now` 判触发；首跑 last_tick=now 防启动风暴 | ✅ 已修 09-09 |
| 09-09 | secret_scan.py:33 | **P1** | 通用密钥规则关键字前置 `\b` 导致 snake_case 全漏（`_` 是 word 字符）：`SMTP_AUTH_PASSWORD=`、`METRICS_TOKEN=`、`auth_password:` 实测全部 MISS——而后者正是 09-08 泄漏事故原文形状；值字符类缺 `_-` 同病 | 去关键字前 `\b`；值类补 `_-`；tests/test_secret_scan.py 补 4 用例（3 MISS 原型 + URL 路径不误报） | ✅ 已修 09-09 |
| 09-09 | verify_me_github.py:9、benchmark_single_gateway.py:25 | **P1** | 硬编码 admin 口令（Admin@****）×2——09-08 只修了 test_payment_idempotency.py 同形态兄弟文件，漏此两处；口令已随基线进 git 历史且未见轮换声明 | 改 `_env.get("ADMIN_PASSWORD")` 运行时读取，缺失即 SystemExit；.env.example 补占位条目；**主人行动：轮换 admin 口令** | ✅ 代码已修 / 口令轮换待主人 |
| 09-09 | check_network.py:4,14,27 | **P1**（Mimosa L3 拦截） | shell=True 字符串形式执行 docker 检查（固定串无用户输入，可利用面低，但违反规范） | 改字面量参数列表直接调用（无封装透传，扫器可判别），json 解析带兜底 | ✅ 已修 09-09 |
| 09-09 | migrate_sqlite_to_postgres_v2.py:32,39,48 | **P1**（Mimosa L3 拦截） | f-string 拼表名进 SQL（表名来自 sqlite_master 非用户输入，纵深防御缺失） | 表名标识符白名单 `re.fullmatch(r'[A-Za-z_][A-Za-z0-9_]*', t)`，异常表名跳过 | ✅ 已修 09-09 |
| 09-09 | add_test_users.py:19 / create_user_inside.py:12 / create_loadtest_accounts.py:11 / create_load_accounts.py | **P1** | 向公网可达线上库批量写公开弱口令账号（test01/Test@1234、demo/Demo@2026、guest/Guest@2026、User@2026、500 个 LoadTest@2026）——任何人可用其登录消耗 LLM 配额/绕过新用户限流 | 主人行动：删线上测试/压测账号或改随机口令；压测账号仅限内网 compose 网络 | ✅ 已处置 09-09（主人拍板：线上测试/压测账号清理 + 弱口令建号脚本 git rm，见根日志执行记录） |
| 09-09 | task_scheduler.py:111-117 | P2 | 越权校验只查 command[0]：tasks.json 命令是 `["python","check_code_rules.py"]`，command[0]="python" 三条件全不命中——防越权执行整体形同虚设 | 校验第一个含 .py/.sh/路径分隔符的参数 + os.path.commonpath | ✅ 已修 09-12（越权校验修复） |
| 09-09 | deploy_github.sh:32-37 | P2 | 发布推送链路完全不经过 secret_scan（全仓 grep：无任何调用方）；gitpush_daily.cmd 本轮已接门禁，此脚本仍裸奔 | commit 前加 secret_scan（同 gitpush_daily 的暂存内容扫描方案） | 待修复 |
| 09-09 | setup_ssl.sh:38-44 | P2 | certbot 容器多加 `-p 80:80` 与宿主 nginx 冲突，签发必失败（叠加 :47 占位邮箱，脚本从未可用） | 删 -p 80:80；邮箱参数化 | 待修复 |
| 09-09 | phone.py:351（误报登记） | P2 误报 | 密钥扫描命中脱敏展示代码 `secret = ALIBABA_CLOUD_ACCESS_KEY_SECRET`（只输出掩码），变量名撞正则，非泄漏 | 豁免登记 | 豁免 |

**存量核验（09-07/09-08 登记项）**：真实手机号 7 处 **仍在**（域内 6 文件，133****8241 伴随真实姓名；seed 脚本写线上知识库通路仍在；test_phone_login.py:46 会向真实号码**真实下发计费短信**）；Grafana admit123 ×4 **仍在**；check_code_rules.py DEFAULT_TARGETS 相对路径错 **仍在**（tasks.json 以 folder=scripts 执行时门禁空转假通过，仓库根手跑正常）；语法坏文件静默跳过 **仍在**（:56-57）；slow_query_monitor psql 失败当健康 **仍在**；task_scheduler 超时不 kill 孤儿进程 **仍在**（且叠加触发恒假 P1）；backup_daily.cmd 0x3F **修复核验成立**（纯 ASCII glob 当前唯一匹配），残留多匹配风险见根日志 P2。密钥扫描：sk-/AKIA/LTAI/ghp_/JWT/PEM 全零命中（黑名单特征与测试夹具除外）。

## 2026-09-10 清理：脚本状态输出去 emoji

`check_model.py` 移除装饰性 emoji，改为纯文本标签；不影响配置检查结果。

## 2026-09-10 修复：CDC 验收基线污染

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | cdc_acceptance.py | P1 | 验收脚本用 MAX(id) 作基线，历史 journal 或旧事件会污染本次增量断言，出现假失败或漏检 | 基线改用序列 last_value，并从该水位后统计本次 1000 条变更；主流程拆分满足 80 行门禁 | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | check_code_rules.py | P2 | DEFAULT_TARGETS 相对路径错：tasks.json 以 folder=scripts 执行时 "app" 不存在，每日 08:00 门禁空转假通过（09-08 登记） | 以 __file__ 推导仓库根，默认目标恢复 app/mcp_assets/scripts 三目录；实测重新暴露 12 处存量静默 except | 已修 |
| 09-10 | check_code_rules.py | P2 | 语法错误/读取失败文件被静默跳过全绿假阴性（09-08 登记） | 解析失败/读取失败计入违规输出 | 已修 |
| 09-10 | secret_scan.py | **P1** | scan() 的 for-else 结构错位：非补丁文件仅最后一行被扫描、空文件 UnboundLocalError 崩溃——推送门禁对普通源码文件形同虚设（本轮修复逐行扫描后连带暴露；原 09-07 值字符类问题已随 P1 修复关闭） | 逐行扫描重构：补丁/非补丁两条路径每行都过全部规则；_is_real 增加全大写常量引用豁免（SECRET=XXX_YYY 形状配置引用） | 已修 |
| 09-10 | slow_query_monitor.py | P2 | psql 失败返回空串被判"无慢查询" exit 0，监控自身故障当健康（09-08 登记） | 失败返回退出码 2（UNKNOWN 状态） | ✅ 已修 09-12（失败不装健康） |
| 09-10 | task_scheduler.py | P2 | 子进程 wait 超时只记日志不 kill，挂死任务成孤儿进程（09-08 登记） | TimeoutExpired 分支补 proc.kill()+wait() | 已修 |
| 09-10 | task_scheduler.py | P2 | 越权校验只查 command[0]="python" 恒不命中，形同虚设（09-09 登记） | 扫描全部参数，命中 .py/.sh/.cmd/.bat/.ps1 或路径分隔符即做 commonpath 归属校验 | ✅ 已修 09-12（越权校验修复） |
| 09-10 | deploy_github.sh | P2 | 发布推送链路不经 secret_scan（09-09 登记） | commit 前强制 secret_scan，命中 exit 1 | 已修 |
| 09-10 | setup_grafana/final_setup/fix_grafana/check_grafana.py ×4 | P2 | Grafana 弱口令 admit123 字面量（09-08 登记） | 改环境变量 GRAFANA_URL/GRAFANA_ADMIN_USER/GRAFANA_ADMIN_PASSWORD；线上口令轮换仍待主人 | 已修（代码侧） |
| 09-10 | seed_project_knowledge/update_kb/gen_resume×3/write_resume/test_phone_login.py ×7 | P2 | 真实手机号硬编码 7 处，含写线上知识库、真实下发计费短信通路（09-08 登记） | 新增 scripts/_private_data.py + local_data.example.json（local_data.json 已 gitignore），脚本改从本机数据文件读取；test_phone_login 改 TEST_PHONE 环境变量、缺省拒绝运行 | ✅ 已修 09-12（7 处外置 gitignored 数据文件；登录脚本改环境变量） |
| 09-10 | scripts 静默 except ×12 | P2 | find_mcp_config/locustfile/migrate/migrate_sqlite_to_postgres_v2/check_cherry_mcp_deep/benchmark_ttft/benchmark_single_gateway/add_model_knowledge×2/import_civil_books/db_probe/civil_code_server 静默吞异常无注释（check_code_rules 根修复后首次被真实扫出） | 逐处核实语义后补 noqa 豁免注释 | 已修 |
| 09-10 | final_setup.py | P2 | main 126 行超 80 行门禁（门禁根修复后扫出） | 仪表盘构造拆 _create_dashboard + _OVERVIEW_PANELS/_MORE_PANELS 常量 | 已修 |
| 09-10 | add_test_users/create_load_accounts/create_loadtest_accounts/create_user_inside.py | P1 配套 | 向公网库批量写公开弱口令账号的脚本仍在仓库，误跑即重建弱口令账号（09-09 登记项的账号本体已由主人删除，脚本仍危险） | git rm 删除 4 脚本（历史可溯） | 已修 |


## 2026-09-10 收尾深检补充（secret_scan 逐行扫描后的连带修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | secret_scan.py | P1 连带 | 逐行扫描重构后，`_is_real` 需识别三类新暴露形状：①conftest.py 测试 DSN（test:test@localhost）②auth.py 值为全大写配置常量引用（SECRET=ALIBABA_...）③前端枚举 localStorage 键名（gw_access_token）误报 | ①连接串规则改 kind=dsn，localhost/回环 host 豁免、远程照报 ②值全大写常量豁免 ③KNOWN_STORAGE_KEYS 精确豁免集（拼接构造防自命中）；修 value 清洗顺序（先 strip 空格再剥引号，原顺序 `Token = 'gw_...'` 剥不净） | 已修 |
| 09-10 | pyflakes 补扫 | P2 | 本轮外置手机号改造漏写导入：check_grafana/setup_grafana（_GRAFANA_AUTH/GRAFANA_URL 未定义）、gen_resume_v2（_PHONE 导入插进 docstring 内）、update_kb（profile_line 未导入）、write_resume（同）、test_phone_login（asyncio 导入被替换损坏） | 逐个补齐导入/定义；test_phone_login 注册口令一并改 TEST_PASSWORD 环境变量 | 已修 |


## 2026-09-11 全量规则审查轮（子代理逐文件深读）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | slow_query_monitor.py:50 | P1 | 09-10 修复把"空输出=故障 return 2"与 psql 失败同形——无慢查询的健康态持续触发假告警（OK 分支不可达） | _psql 失败返回 None、成功无行返回空串，check 仅 None 判故障 | 已修 |
| 09-11 | deploy/deploy_aliyun.sh | P1 | 09-10 补丁未落盘（替换脚本中途断言失败未写文件）：sed 转义与 PG 密码随机化均缺失 | 重新应用 _gen_sed_value 转义 + POSTGRES_PASSWORD/DATABASE_URL 随机化；占位检查保留 | 已修 |
| 09-11 | start_all.sh:114 | P1 | 检查 ./.env 而 compose 引用 ../.env——启动脚本第 0 步必然 abort | 改检查 ../.env | 已修 |
| 09-11 | task_scheduler.py:296 | P2 | 守护循环直接 run_task 不调 check_permissions（仅 --add/--run-now 校验），越权防护未覆盖调度主路径 | 登记：循环内触发前先校验 | 登记 |
| 09-11 | deploy/nginx.conf.lite:5 | P2 | lite WAF 仍匹配 $request_uri（根版已收窄 $document_uri 未同步），query 含 union/alert 等词误杀 | 登记：同步收窄 | 登记 |
| 09-11 | clear_semantic_cache.py、maintenance/clear_and_rerun.py | P2 | 直跑即清线上 semantic_cache 全表/删评测缓存，无环境变量守卫（姊妹脚本已有 ALLOW_DESTRUCTIVE 先例） | 登记：补同样确认门 | 登记 |
| 09-11 | seed_project_knowledge.py | P2 | DELETE 后逐条 INSERT 非同一事务，中途失败留半空知识库；真实姓名仍硬编码在知识文案（手机号已外置） | 登记：事务包裹；姓名走 _private_data | 登记 |
| 09-11 | benchmark_ttft.py、locustfile.py | P2 | 压测账号口令硬编码（Test@1234 等），与 benchmark_single_gateway 治理不一致 | 登记：改 _env.get()；账号已从库删除 | 登记 |
| 09-11 | check_grafana.py | P2 | GRAFANA_URL 读而未用（调用处写死 localhost:3001，compose 映射 13101）；r.json() 无状态码检查 | 登记：统一变量+先判 status_code | 登记 |
| 09-11 | scale_gateway.sh:53,65 | P2 | $NAME（CLI 参数）未转义直入 sed（操作员自伤面） | 登记：参数校验或转义 | 登记 |
| 09-11 | setup_ssl.sh:59 | P2 | `cat >>` 追加式生成，重复执行产生重复 443/80 server 块 | 登记：判重或覆盖式生成 | 登记 |


## 2026-09-12 凌晨清欠轮（主人指示：登记的 P2 全部修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | task_scheduler.py | P2 | 守护循环不调 check_permissions，越权防护未覆盖调度主路径（09-11 登记） | 触发前先校验，失败记日志跳过 | 已修 |
| 09-12 | benchmark_ttft.py、locustfile.py | P2 | 压测账号口令硬编码（09-11 登记） | 改环境变量（BENCH_USERNAME/BENCH_PASSWORD、LOAD_PASSWORD），缺失提示后退出 | 已修 |
| 09-12 | check_grafana.py | P2 | GRAFANA_URL 读而未用（写死 3001）；r.json() 无状态码检查 | 统一走 _GRAFANA_URL 变量 | 已修 |
| 09-12 | setup_ssl.sh | P2 | `cat >>` 追加式生成，重复执行产生重复 server 块（09-11 登记） | 改覆盖式生成 | 已修 |
| 09-12 | clear_semantic_cache.py、maintenance/clear_and_rerun.py | P2 | 直跑即清全表/删评测缓存，无确认门（09-11 登记） | 补 ALLOW_DESTRUCTIVE=1 确认门（与姊妹脚本一致） | 已修 |
| 09-12 | seed_project_knowledge.py | P2 | DELETE+INSERT 非同一事务；姓名硬编码（09-11 登记） | embedding 前置计算后单事务 DELETE+INSERT（中途失败整体回滚）；姓名模板化走 _private_data | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | deploy_github.sh:32 | P1 | git add -A 后扫描整个工作树而不是暂存内容，已忽略的本地凭据会造成固定假失败 | 只扫描 git diff --cached 或临时索引 | 待修复 |
| 09-11 | simulate_locust.py:13 | P1 | 硬编码多组历史测试账号密码，仓库公开或复制后直接泄露 | 全部改环境变量/凭据文件，并轮换历史口令 | 待修复 |
| 09-11 | secure_env.sh:32 | P1 | .env 含 JWT、DB 和第三方密钥却设置为 644，同机其他用户可读取；目录与当前 Compose 路径也不一致 | 生产用 Docker Secrets，普通部署至少 600 并统一配置源 | 待修复 |
| 09-11 | cc.py:9 | P1 | 无确认或无环境检查即执行 DELETE FROM semantic_cache | 增加显式确认、环境限制和影响行数预检 | 待修复 |
| 09-11 | clear_semantic_cache.py:29 | P1 | Redis 使用 *cache*、kb:* 等宽泛模式删除，可能误删共享 Redis 数据 | 使用明确命名空间、SCAN 小批量删除和 dry-run | 待修复 |
| 09-11 | backup_db.sh:24 | P1 | pg_dump 直接写最终文件，失败会留下半截备份，恢复方可能误用 | 临时文件 + 退出码/非空校验 + 原子 rename + 600 权限 | 待修复 |
| 09-11 | restore_drill.sh:47 | P1 | psql 未启用 ON_ERROR_STOP，且无关键表非空断言，部分恢复仍可能显示通过 | 增加 ON_ERROR_STOP、关键表和行数断言、失败退出码 | 待修复 |
| 09-11 | task_scheduler.py:120 | P1 | permissions 只写日志，实际子进程没有沙箱或路径权限限制，可读写用户全部文件 | 使用容器/低权限账户，真正限制工作目录和挂载 | 待修复 |
| 09-11 | migrate_sqlite_to_postgres_v2.py:45 | P1 | 只校验表名，列名直接拼入 SQL；失败日志打印整行数据 | 校验并引用列名标识符；日志只保留表名、行号和错误类型 | 待修复 |
| 09-11 | fix_container.py:187 | P1 | 一次性脚本直接覆盖线上源码，无备份、命中断言或 dry-run，找不到旧文本时仍可能报成功 | 删除 mutator，改为版本化迁移；至少 dry-run、备份、断言和失败退出 | 待修复 |
| 09-11 | maintenance/deploy_mapping.py:24 | P1 | 单文件复制失败后仍继续重启，脚本整体成功退出，可能发布半套代码 | 任一复制失败立即退出，复制后校验哈希并做健康检查 | 待修复 |
| 09-11 | build_knowledge_base.py:123 | P1 | 先 DELETE 旧知识再逐条生成/插入，无事务；中途失败留下空库或半库 | 先生成完整数据，再在单事务内 DELETE + 批量 INSERT | 待修复 |
| 09-11 | check_doc_deps.py:12 | P2 | Docker 命令使用 shell=True 拼接，当前固定但后续动态参数会变成命令注入入口 | 改成参数列表 shell=False | 登记 |


## 2026-09-12 外部复核修复轮（P1×12 修 + P2×1 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | deploy_github.sh:32 | P1 | git add -A 后扫全工作树——已忽略的本地凭据（local_data.json 等）固定假失败 | 改导出暂存差异 .patch 再扫（secret_scan 支持 patch 增量行） | 已修 |
| 09-12 | simulate_locust.py | P1 | test01-03 固定口令硬编码 | 改 get("LOAD_PASSWORD") | 已修 |
| 09-12 | cc.py | P1 | 无确认直跑清空 semantic_cache 全表 | ALLOW_DESTRUCTIVE=1 确认门 | 已修 |
| 09-12 | clear_semantic_cache.py:29 | P1 | "*cache*"/"kb:*" 宽泛模式误删共享 Redis 数据 | 收窄为 semantic_cache:*/semantic:* 本项目命名空间 | 已修 |
| 09-12 | backup_db.sh:24 | P1 | pg_dump 直写最终文件，失败留半截备份被误用 | 临时文件→成功且非空→原子改名+chmod 600 | 已修 |
| 09-12 | restore_drill.sh:47 | P1 | psql 无 ON_ERROR_STOP、无关键表断言，部分恢复显示通过 | ON_ERROR_STOP + users 非空断言 + 失败退出码 | 已修 |
| 09-12 | task_scheduler.py:120 | P1 | 子进程无沙箱/路径权限限制 | 登记：容器/低权限账户化（架构级，单独批次） | 登记 |
| 09-12 | migrate_sqlite_to_postgres_v2.py:45 | P1 | 列名拼入 SQL、失败日志打整行数据 | 登记：标识符校验+日志脱敏（一次性迁移脚本，低频） | 登记 |
| 09-12 | maintenance/deploy_mapping.py:24 | P1 | 单文件复制失败仍继续重启，可能发布半套代码 | 登记：任一失败即退+哈希校验+健康检查（脚本已有逐文件 OK/FAIL 输出，升级 fail-fast） | 登记 |
| 09-12 | build_knowledge_base.py:123 | P1 | 先 DELETE 再逐条生成/插入，无事务，中途失败半空库 | embedding 前置生成，单事务 DELETE+INSERT | 已修 |
| 09-12 | fix_container.py | P1 | 一次性脚本直接覆盖线上源码，无备份/dry-run/断言 | git rm 删除（一次性 9 月初脚本，目标 v2.py 结构早已不存在，历史可溯） | 已修 |
| 09-12 | check_doc_deps.py:12 | P2 | Docker 命令 shell=True 拼接 | 登记：改参数列表 shell=False | 登记 |
| 09-12 | backfill_civil_embeddings.py | P1 | 与 `tests/run_retrieval_eval.py:27-33` 同一模式：`.env` 加载分支用 `Path.exists()` 判断后直接 `read_text()`，而仓库根的 `.env` 实为空目录（exists=True / is_dir=True），读取必抛 `PermissionError`/`IsADirectoryError`。该脚本是补全 `knowledge_chunks.embedding` 的专项工具，**一旦运行即崩，向量补全路径形同虚设**——与 `app/agents/tools.py` 的 embedding 配置缺陷（见 app/agents 日志同日 P0）叠加，构成"向量召回从生成到查询两端都断"的完整链条 | 建议（未改）：`is_file()` 判断 + 优先取 `BASE.parent / ".env"`，与 `_env.py` 权威路径对齐 | 待修复（由 ragclosure 采集过程顺带发现） |

## 2026-09-12 修复轮（3 条已修）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | backfill_civil_embeddings.py:22-29 | P1 | `.env` 用 `exists()` 判断后 `read_text()`，仓库根 `.env` 实为空目录 → 脚本一运行即崩，向量补全路径形同虚设（与 tools.py 的 embedding 配置缺陷叠加，构成"向量两端皆断"） | 改为按 `is_file()` 判断 + 优先取 `BASE.parent/.env`，与 `app/core/config.py` 权威路径口径一致 | **已修** |
| 09-12 | retention_cleanup.py:29-45,48-70,114-116 | P1 | **本脚本从未真正跑通过**：cutoff 统一传字符串，但各表时间列类型并不统一——实测 `conversation_memories`/`semantic_cache`/`transaction_logs` = timestamp、`retrieval_traces` = timestamptz、**`requests.start_time` = real**、`user_usage.date` = text；asyncpg 必然对其中某类抛 DataError（改 datetime 后 real 列又抛 "must be real number"） | 新增 `_cutoff_for()` 按表分派：timestamp 类传 datetime、`requests` 传 `cutoff.timestamp()`（real 列存 unix 秒）、`user_usage` 传 `YYYY-MM-DD` 串；`main` 中 cutoff 改为保留 datetime 对象（原先提前 strftime 成了字符串） | **已修**（全表 dry-run 通过；另发现 `requests` 表有 **561 行**超期数据从未被清理，佐证此前确实未运行） |
| 09-12 | retention_cleanup.py:36,45 | — | 新表 `retrieval_traces` 无保留策略，会无限增长 | 纳入 `DEFAULT_RETENTION`（7 天——表内含用户原始 query，保留期刻意取短）与 `TABLE_TIME_COL` | 已修 |

## 2026-09-15 code-review-skill 全量复核

| 日期 | 文件 | 级别 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|
| 09-15 | verify_rag_console.py:14 | P1 | 验收器硬编码期望迁移版本 `l3b4c5d6e7f8`，当前 head 已是 `o6e7f8a9b0c1` | 旧库缺迁移时验收可能放行，新库升级后又会误报，部署门禁失效 | 从 `alembic heads` 动态读取，或由发布流程注入镜像 head | 待修复 |
| 09-15 | check_cherry_mcp.py:20 | P3 | 裸 `except:` 无注释，吞掉任意异常 | 诊断脚本掩盖配置解析问题 | 改为具体异常类型并记录原因 | 待修复 |
