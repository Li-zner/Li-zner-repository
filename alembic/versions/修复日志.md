# 修复日志 — alembic/versions（新增迁移）

| 日期 | 迁移 | 级别 | 说明 |
|---|---|---|---|
| 09-04 | b1c2d3e4f5a6_cdc_events_monthly_partition.py | P1 | cdc_events 改按月 RANGE 分区：单事务原子重建（rename->建分区表->迁数据->行数校验->删旧表），主键扩 (id, created_at)，序列校准，预建 [-3,+2] 月分区 + DEFAULT 兜底；downgrade 完整还原 |
| 09-04 | c2d3e4f5a6b7_audit_logs_table.py | P1 | 补建 audit_logs 审计表（此前 core/audit.py 一直写不存在的表，审计静默全失败）+ 查询索引 |
| 09-05 | d9e0f1a2b3c4_users_github_id.py | P0 | users 缺失列补齐（email/avatar_url/display_name/extra/updated_at——全新环境启动即崩的隐患）+ github_id 身份键（部分唯一索引），支撑 GitHub 登录防接管与手机号改名回访 |

链路：... -> f0a1b2c3d4e5 -> b1c2d3e4f5a6 -> c2d3e4f5a6b7 -> d9e0f1a2b3c4 (head)
云端部署由容器启动命令自动执行 alembic upgrade head。

## 2026-09-08 全目录覆盖轮（仅审查登记，未修复）

| 日期 | 迁移 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | d9e0f1a2b3c4_users_github_id.py:37-39 | P1 | downgrade 不成对：upgrade 加 6 列（display_name/email/avatar_url/extra/updated_at/github_id），downgrade 只回滚 github_id+索引，其余 5 列残留 | ✅ 已修 09-08：downgrade 补齐 5 列 DROP COLUMN IF EXISTS，与 upgrade 完全对称（py_compile 通过；未跑真库回滚，下一轮空库往返冒烟时覆盖） | ✅ 已修 |
| 09-08 | 76a1f09d2ac8_baseline_schema.py | P3 | 13 张表 created_at/updated_at 全为 timestamp without time zone（UTC 与否依赖 DB 会话时区；仅 cdc_events 后续迁 timestamptz） | 参照 f0a1b2c3d4e5 模式分批迁移 | ✅ 已修 09-14：迁移 o6e7f8a9b0c1 全部 naive 时间列（15 列，含 d9e0 补的 users.updated_at）静态 ALTER 为 timestamptz（USING AT TIME ZONE 'UTC'），本机临时库 upgrade/downgrade/upgrade 冒烟通过 |
| 09-08 | 76a1f09d2ac8 | P3 | downgrade 删 13 表但不删 pg_trgm/vector 扩展；同参 ivfflat 索引建了两个（冗余写放大） | 补 DROP EXTENSION IF EXISTS；删其一 | ✅ 已修 09-14：downgrade 末尾 DROP EXTENSION vector/pg_trgm（downgrade base 后实测扩展残留=0）；删除冗余 idx_project_knowledge_embedding（与 idx_knowledge_embedding 同列同 opclass） |
| 09-08 | f0a1b2c3d4e5:22-26 | P3 | downgrade timestamptz→timestamp 无 USING，非 UTC 会话回滚会平移存量值 | 加 `USING created_at AT TIME ZONE 'UTC'` | ✅ 已修 09-14 |
| 09-08 | a7b8c9d0e1f2:21-29、f6a7b8c9d0e1:30 | P3 | 业务数据 DML 混入迁移（建唯一索引前置清理/毒缓存清空）——理由已注释、downgrade 本就无法恢复被删行，属固有 | 维持现状 | 登记 |

复审通过项：迁移链单链无分叉；b1c2d3e4f5a6 分区迁移（原子重建/行数守卫/序列校准）为链中质量最高；a1b2c3d4e5f6 播种 id=1 不同步序列的缺陷已被链上 d8e9f0a1b2c3 显式修复；c3d4e5f6a7b8 downgrade 不对称已注释声明策略；app/cdc/worker.py 的分区 DDL 属迁移文档载明的运行时维护，非绕过 Alembic。

## 2026-09-09 全目录审查轮

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | （缺迁移，新增 e1a2b3c4d5e6） | **P1** | knowledge_chunks.permission 列脱离迁移管理：agents/tools.py:157 检索 SQL 引用该列（`COALESCE(permission,'{}')='' OR permission && $3`），baseline 建表 DDL 无此列，全新环境 `alembic upgrade head` 后首查即 UndefinedColumnError、检索主流程不可用（线上靠历史手工 ALTER 侥幸；与 d9e0 users 缺列同类，users 修了这张表漏了） | 新增 e1a2b3c4d5e6_knowledge_chunks_permission.py：`ADD COLUMN IF NOT EXISTS permission TEXT[] NOT NULL DEFAULT '{}'`（幂等兼容线上手工列，类型与 tools.py 数组用法对齐），downgrade 对称 DROP IF EXISTS | ✅ 已修 09-09（未在真库执行，下次部署 alembic upgrade head 时覆盖） |

**13 迁移对称性复核表（09-09）**：76a1 ✅ / a1b2 ✅ / b2c3 ⚠️ downgrade 重加 used_tokens（有意兼容，无害）/ c3d4 ❌ 有意（phone 列不回滚已注释声明）/ d4e5 ✅ / e5f6 ✅ / f6a7 ✅ / a7b8 ✅（去重 DELETE 不可逆固有）/ d8e9 ✅ / f0a1 ✅（无 USING 风险已登记 P3）/ b1c2 ✅（本轮重点复核：rename 让位→按 f0a1 后形态重建→迁数→setval→DROP 无残留）/ c2d3 ✅（drop 丢审计固有）/ d9e0 ✅（09-08 修复复核通过）。单链无分叉；head 原 d9e0f1a2b3c4 → 本轮后 e1a2b3c4d5e6。

## 2026-09-10 新增迁移：CDC 事务顺序水位

| 日期 | 迁移 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | a8b9c0d1e2f3_cdc_events_txid.py | P0 | cdc_events 只按 BIGSERIAL id 推进会漏掉后提交的较小 id，短事务先提交、长事务后提交时消费者可能永久跳过事件 | 增加 txid 并回填；触发器写入 txid_current()；消费顺序改为 (txid,id)，等待事务低于当前快照 xmin 后再落盘 | 已修；本地已升级到该 head |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 迁移 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | f6a7b8c9d0e1_semantic_cache_ctx.py:33 | P1 | upgrade 允许多个 cache_ctx 共用 query_hash，downgrade 直接恢复 UNIQUE(query_hash)，存在重复数据时回滚必失败 | 降级前按 query_hash 去重或明确要求人工预处理 | ✅ 已修 09-12（降级前每组 query_hash 保留最新一条；09-14 冒烟 downgrade base 实测通过） |
| 09-11 | c3d4e5f6a7b8_users_active_permissions.py:21 | P1 | upgrade 新增 phone，downgrade 未删除，降级后的 schema 与目标版本不一致 | 对本迁移引入的 phone 对称 DROP，并修正列归属注释 | ✅ 已修 09-12（复核：对称 DROP 与归属注释已在） |
| 09-11 | a8b9c0d1e2f3_cdc_events_txid.py:13 | P2 | txid 回填后未设置 NOT NULL，旁路写入 NULL 会使事件永远不满足 (txid,id) 游标 | 回填后 SET NOT NULL，并补约束回归 | ✅ 已修 09-14：新迁移 n5d6e7f8b9c0（防御回填 0 后 SET NOT NULL，冒烟实测 NOT NULL=True） |

## 2026-09-15 修复实施结果

- 新增 `o6e7f8a9b0c1`：支付、画像等 naive 时间列统一迁 `timestamptz`。
- 新增 `p7e8f9a0b1c2`：`payment_attempts` 渠道尝试状态机。
- 新增 `p7a8b9c0d1e2`：RAG 处理状态和 incident 请求去重表。
- 新增 `q8b9c0d1e2f3`：`users.phone` 部分唯一索引。
- 新增 `r9c0d1e2f3a4`：`conversation_memories.message_uid` 幂等索引。
- `f0a1b2c3d4e5` 显式按 UTC 解释存量时间。
- 验证：本地升级到 `r9c0d1e2f3a4`；临时库 `upgrade head -> downgrade base -> upgrade head` 通过。

## 2026-09-20 新增迁移：会话级用户画像

- 新增 `z5a6b7c8d9e0`：`conversation_profiles`，主键
  `(user_id, conversation_id)`，画像 JSONB、消息计数与时间戳。
- 用途：每个历史会话独立保存画像，支持读取、编辑、删除；天气快照记录抓取时间。
- 验证：`python -m alembic heads` 返回单一 head `z5a6b7c8d9e0`；迁移文件通过
  `compileall`。全链离线 SQL 生成受既有 `f0a1b2c3d4e5` 的连接时区守卫限制，
  未在本轮替代真库迁移验证。
