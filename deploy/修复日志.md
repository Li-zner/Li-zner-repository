# 修复日志 — deploy

| 日期 | 文件 | 级别 | 问题 | 修复 |
|---|---|---|---|---|
| 09-04 | docker-compose.lite.yml | P1 | 容器启动不跑迁移，新表/分区需手工 docker exec | command 改为 sh -c "alembic upgrade head && uvicorn ..."（幂等，失败退出由 restart 拉起重试） |

| 09-07 | P1 | nginx.conf.lite | /health 在 nginx 层硬编码 return 200 不代理后端：gw_publish.py 的发布健康检查打在 10090 → 恒 200，网关容器挂了照样"健康"，发布门禁被架空（与镜像一致性校验互为补充但本身失效） | ✅ 已修 09-14：/health 改 proxy_pass gateway_cluster（connect 2s / read 3s），发布健康检查打真实后端 |
| 09-07 | P2 | nginx.conf.lite | $is_attack WAF 正则只查 URI，POST body 不检（注入防护实际靠后端 sanitize），且个别正常 URL 可能误伤 403 | 定位说明：轻量缓解而非防线 |
| 09-07 | P2 | publish_lite.py | 是 gw_publish.py 的过时副本（LOCAL_TGZ=gw_lite_0815af2.tar.gz、argv[4] 死代码、健康检查 WARN 不阻断均在）——双脚本漂移，权威版在 ~/.zcode/cli/gw_publish.py（已修） | 待修：删除或同步 |

## 2026-09-07 复审轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 内容 | 状态 |
|---|---|---|---|---|
| 09-07 | publish_lite.py | 关闭 | 上一行"待修：删除或同步"已随本工作树删除落地（git D）；全仓核实无存活代码引用（仅 Bug日志.md 历史记录提及，属文档非 import） | 已关闭 |

## 2026-09-08 全目录覆盖轮（仅审查登记，未修复）

| 日期 | 文件 | 级别 | 问题 | 建议修复 | 状态 |
|---|---|---|---|---|---|
| 09-08 | .server.env:3-5 | P1 | 真实云服务器 root 明文密码落盘仓库目录（39.108.***.*** / root）。已核实未被 git 追踪、历史无泄漏，但属真实凭据明文 + root 密码登录形态 | 修复需服务器侧操作（仅主人可做）：生成 SSH 密钥对并 ssh-copy-id 上服务器 → 验证免密登录 → 服务器禁用密码登录（sshd PasswordAuthentication no）→ 删除本文件。本地无安全动作可代做 | **待主人服务器操作** |
| 09-08 | deploy_aliyun.sh:21-29,46-48 | P2 | 必填项（ADMIN_PASSWORD/DEEPSEEK_API_KEY）未填仍继续部署并打印"部署完成"；健康检查失败被 `\|\| echo` 吞掉不置失败退出码 | 占位残留即 exit 1；健康检查失败 exit 1 | ✅ 已修 09-12（占位残留即失败；健康检查失败 exit 1） |
| 09-08 | deploy_aliyun.sh:19-26 | P2 | sed 替换未转义，密码含 `\|`/`&` 时损坏 .env.lite | 转义或改用 python 写入 | ✅ 已修 09-12（sed 转义 + PG 密码随机化重新应用） |
| 09-08 | .env.lite.example:17 | P2 | 部署脚本只随机化 SESSION/JWT，POSTGRES_PASSWORD 保持公开默认值 agent_pass（compose 不暴露 PG 端口缓解） | 部署脚本同法随机生成并同步 DATABASE_URL | ✅ 已修 09-12（PG 密码同法随机化并同步 DATABASE_URL） |
| 09-08 | Dockerfile.lite:14 | P2 | `COPY static/ ./static/`，但 git 只追踪 static/index.html、user-agreement.html、.gitkeep——static/assets/（前端构建产物）、icons、manifest 未入库，干净环境构建 lite 镜像有 HTML 无 JS/CSS，白屏 | 构建产物入库，或 lite 也走多阶段构建 | ✅ 已修（复核：Dockerfile.lite 已是前端多阶段构建，COPY --from=frontend-build dist → static/，构建产物不入库） |
| 09-08 | nginx.conf.lite.local | P3 | 与 nginx.conf.lite 漂移：缺 /metrics、/test-otel 的 404 屏蔽两段（现靠应用层 fail-closed 兜底，非漏洞） | 同步补齐 | ✅ 已修 09-14：同步 /metrics、/test-otel 404 屏蔽两段 |

复审通过项：docker-compose.lite.yml（迁移前置+健康依赖+无多余端口暴露）、nginx.conf.lite（除已登记的 /health 硬编码 200 外无新漏项）。

## 2026-09-09 全目录审查轮

| 日期 | 文件 | 级别 | 问题 | 修复方法 | 状态 |
|---|---|---|---|---|---|
| 09-09 | deploy_aliyun.sh:46-48 | **P1** | 健康检查失败仅 `curl \|\| echo` 兜底、退出码 0——部署失败被当成功（绕过前段 set -euo pipefail；细化 09-08 同项 P2 并升级） | 循环后终判复查，仍不健康 `exit 1` | ✅ 已修 09-09（bash -n 通过；占位残留 exit 1 部分未动，维持 09-08 登记） |
| 09-09 | 修复日志.md:21（本文件自身） | **P1** | 本日志被 git 追踪且公开披露真实服务器 IP + root 用户名 + "仅密码登录"事实（.server.env 本体未入库但同信息随日志分发） | 主人行动：服务器改 SSH 密钥登录 + sshd 禁密码 + 轮换口令后，本行脱敏（git 历史另行处理） | 待主人拍板 |
| 09-09 | .env.lite:11,23 | P2 | 真实 DeepSeek `sk-` 与高德 key 明文于工作区（核实未被 git 追踪、`git log -S/-G` 历史零命中） | 主人确认后轮换；改占位/环境变量注入 | 待主人拍板 |

**本轮核验通过项**：alertmanager SMTP 链端到端闭环（../.env → start_all.sh → monitoring/ 挂载 → auth_password_file，容器 Up）；prometheus authorization 移入 scrape_config 后 promtool 通过、容器恢复 Up（crash-loop 27h 解除）。

## 2026-09-10 修复：lite 部署模型默认值漂移

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | .env.lite.example | P2 | 示例配置仍使用过时的 `deepseek-v4-flash`，与代码默认值和部署环境不一致，新环境按示例部署可能请求错误模型 | 示例默认改为 `deepseek-flash`，与 config.py 和测试口径统一 | 已修 |

## 2026-09-10 修复：local lite 账号收敛

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | .env.lite、postgres_lite | P1 | lite 环境保留遗留 `Fengfeng` 管理员，配置账号与 full 环境不一致，无法用目标账号登录 | 配置改为目标管理员账号；事务清理遗留账号关联数据；重建目标账号并复验登录 200，最终仅一行管理员账号 | 已修 |

## 2026-09-10 全量清欠轮（主人指示：历史登记未修复项一并修复）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-10 | deploy_aliyun.sh | P2 | 必填项（ADMIN_PASSWORD/DEEPSEEK_API_KEY）占位残留仍继续部署并打印"部署完成"（09-08 登记） | 占位残留循环检测，命中 exit 1 fail-fast | 已修 |
| 09-10 | deploy_aliyun.sh | P2 | sed 替换未转义，密码含 \|/& 时损坏 .env.lite（09-08 登记） | 新增 _gen_sed_value 转义函数，值中 \|/& 均转义后再替换 | 已修 |
| 09-10 | deploy_aliyun.sh + .env.lite.example | P2 | POSTGRES_PASSWORD 保持公开默认值 agent_pass（09-08 登记） | 部署时 openssl rand 随机生成并同步 DATABASE_URL | 已修 |
| 09-10 | deploy.sh（仓库根） | P2 | 旧一键部署脚本对 4 实例同时 --force-recreate，ci.sh 已替代但仍可误用（09-09 登记） | git rm 删除（历史可溯） | 已修 |

**待主人拍板（维持登记）**：①deploy/.env.lite 真实 DeepSeek sk- 与高德 key 轮换（供应商控制台操作）；②本日志及 .server.env 历史披露的服务器 IP/root 口令脱敏（需先完成 SSH 密钥登录切换与口令轮换）；③HTTPS 证书签发激活（服务器侧跑一次 setup_ssl.sh，配置已备齐）。


## 2026-09-11 全量规则审查轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-11 | alert.rules.yml:88,100 | **P1** | 两条告警永不触发：LLMErrorRateHigh 引用的 llm_requests_total{status="error"} 全仓零埋点；PaymentFailureHigh 引用的 payment_orders_total 只定义未打点——监控双哑火 | LLM 侧：answer_via_models/_stream_react/runner fallback 三处异常出口补 status='error' 打点；支付侧：_settle_recharge success/failed 两分支补打点（导入 core.metrics 单例） | 已修 |
| 09-11 | alertmanager.yml:11 | P2 | 真实 QQ 邮箱地址硬编码（授权码已走挂载文件；仓库拟公开属 PII） | 登记：改环境变量/挂载注入 | ✅ 已修 09-12（ALERT_EMAIL 环境变量注入） |
| 09-11 | docker-compose.yml | P2 | redis:7-alpine 与 pgvector:pg15 浮动 tag；tempo user: root | 登记：固定 minor 版本；tempo 改专用用户+卷属主 | 部分已修 09-12（tempo 去 root）；redis:7-alpine/pgvector:pg15 minor 钉版维持登记（需查 registry 确认具体 tag 后再钉，防钉到不存在标签破坏构建） |
| 09-11 | deploy/Dockerfile.lite:17 | P2 | `COPY tests/` 把测试打进生产镜像（根 Dockerfile 已删，lite 未同步） | 登记：同步删除 | 登记 |
| 09-11 | autoscale.sh bind-mount | 待核实 | nginx.conf 单文件 :ro bind-mount 下 `sed -i`（临时文件+rename）可能致容器内仍指旧 inode，扩缩容 reload 加载旧 upstream——需 `docker exec nginx_lb cat` 对比宿主实测 | 待运行时确认（潜在 P1） | 待核实 |


## 2026-09-12 凌晨清欠轮

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | docker-compose.yml | P2 | redis:7-alpine 浮动 tag（09-11 登记） | 固定 redis:7.2-alpine；pgvector:pg15 已是 major 钉版维持 | 已修 |
| 09-12 | docker-compose.yml | P2 | tempo user: root（09-11 登记） | 移除（tempo 镜像默认 UID 10001，named volume 首挂继承属主）——**明早验收第一项：确认 tempo 容器 Up**，若权限报错回滚该行 | 部分已修 09-12（tempo 去 root）；redis:7-alpine/pgvector:pg15 minor 钉版维持登记（需查 registry 确认具体 tag 后再钉，防钉到不存在标签破坏构建） |
| 09-12 | alertmanager.yml + start_all.sh + compose + .env.example | P2 | 真实 QQ 邮箱硬编码（09-11 登记） | to: 改 ALERT_EMAIL_PLACEHOLDER 占位；start_all.sh 从 ../.env 的 ALERT_EMAIL 生成挂载副本 monitoring/alertmanager.yml；compose 改挂载副本；.env.example 补 ALERT_EMAIL 键 | 已修 |
| 09-12 | scripts/autoscale.sh | P2 | sed -i（rename 语义）在单文件 bind-mount 下可能致容器内旧 inode，扩缩容静默失效（09-11 审查"待运行时确认"的代码侧防御） | nginx_add/nginx_remove 改重定向写回（cat tmp > 原文件保 inode），两处 | 已修 |

## 2026-09-11 全量审查复核发现（只登记，不修改）

| 日期 | 文件 | 级别 | 问题 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| 09-11 | deploy_aliyun.sh:17 | P0 | 每次部署都覆盖 deploy/.env.lite 并重新随机 POSTGRES_PASSWORD/DATABASE_URL；已有 PG 数据卷不会同步换密，重复部署可能直接失联 | 已有配置禁止覆盖；随机凭据和 ALTER ROLE 必须同一迁移流程 | ✅ 已修 09-12（已有 .env.lite 不覆盖，防换密失联） |
| 09-11 | docker-compose.lite.yml:12 | P0 | gateway 只有 image 没有 build，部署脚本却执行 compose build；干净主机 dry-run 返回 No services to build 后仍继续，启动必失败 | 增加 build.context/dockerfile，或只拉取固定镜像 | ✅ 已修 09-12（补 build 段） |
| 09-11 | deploy_aliyun.sh:57、nginx.conf.lite:39 | P1 | 健康检查命中的是 Nginx 静态 /health，网关、PG、Redis 故障时仍可返回 200，部署可能误报成功 | 检查网关就绪接口、容器状态和数据库连接 | 待修复 |
| 09-11 | .env.lite.example:5 | P1 | 模板未声明 ADMIN_USERNAME/ADMIN_PHONE，干净部署默认创建 admin，与单手机号管理员目标不一致 | 增加必填管理员字段并移除默认 admin 兼容路径 | 待修复 |
| 09-11 | Dockerfile.lite:17 | P2 | 精简生产镜像仍复制整个 tests/，增加体积并可能带入敏感测试数据 | 只复制运行模块，测试使用独立镜像 | 登记 |


## 2026-09-12 外部复核修复轮（P0×2 + P1×2 全修 + P2×1 登记）

| 日期 | 文件 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | deploy_aliyun.sh:17 | **P0** | 重复部署覆盖 .env.lite 并重新随机 POSTGRES_PASSWORD/DATABASE_URL，既有 PG 数据卷不同步换密——重部署即失联 | 已有 .env.lite 沿用不覆盖（SKIP_ENVGEN），随机化仅首次；换密须 ALTER ROLE+同步流程人工执行 | 已修 |
| 09-12 | docker-compose.lite.yml:12 | **P0** | gateway 无 build 段而 deploy 脚本执行 compose build——干净主机 "No services to build" 后启动必失败 | 补 build.context/dockerfile | 已修 |
| 09-12 | deploy_aliyun.sh:57、nginx.conf.lite:39 | P1 | 健康检查命中 Nginx 静态 /health，网关/PG/Redis 故障仍 200 | 登记：改查网关就绪接口+容器状态（涉健康检查语义，部署后验证） | 登记 |
| 09-12 | .env.lite.example:5 | P1 | 模板缺 ADMIN_USERNAME/ADMIN_PHONE 必填字段 | 登记：补必填字段并移除默认 admin 兼容（涉部署迁移，单独批次） | 登记 |
| 09-12 | Dockerfile.lite:17 | P2 | 精简镜像仍复制 tests/ | 登记：只复制运行模块 | 登记 |

## 2026-09-12 晚：本地测试环境启动事故（WinNAT 端口保留 + Redis RDB 版本漂移）

> 起因："打开本地测试"时全栈起不来。三因叠加，逐一根治。

| 日期 | 位置 | 级别 | 问题 | 修复 | 状态 |
|---|---|---|---|---|---|
| 09-12 | docker-compose.yml 全部宿主端口 | P1 | Windows WinNAT 动态保留段 10004-10103（Docker 引擎重启后重排，netsh excludedportrange 可查）吞掉 10089-10095 宿主绑定，容器重建必被拒（"forbidden by access permissions"）；无管理员权限无法改 winnat | 宿主端口整体迁移 100xx→1019x：nginx 10090→10190、gateway 10092→10192、gateway2 10089→10189、gateway3 10093→10193、gateway4 10094→10194、lite.local 10095→10195；start_all.sh/ci.sh/benchmark×2/测试×6 同步；config.py CORS 默认白名单补 10189/10190。deploy_aliyun.sh 面向 Linux 生产无 WinNAT，维持 10090 | 已修 |
| 09-12 | docker-compose.yml redis | P1 | 09-12 早间把浮动标签 7-alpine 钉到 7.2-alpine（第 79 行），但该卷 dump 已被浮动标签实际拉的 7.4 写成 RDB v12——7.2 加载必败崩溃循环（"Can't handle RDB format version 12"） | 钉版改 redis:7.4-alpine：与盘上 v12 匹配且同为 7.x 系，数据零丢失。教训：钉版本前先看盘上数据的写入版本 | 已修 |
| 09-12 | monitoring/alertmanager.yml | P2 | 生成文件缺失期间 Docker 把 bind-mount 源建成了空目录，alertmanager 重挂报 "not a directory" | rmdir 空目录，按 start_all.sh 生成逻辑重出 alertmanager.yml + smtp_auth_password | 已修 |