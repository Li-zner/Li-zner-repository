---
description: "agent_gateway 项目强制编码规范——所有 AI 代码生成必须遵守"
globs: app/**/*.py, scripts/**/*.py, mcp_assets/**/*.py
alwaysApply: true
---

# agent_gateway 编码强制规范

> 本项目规则来源于真实事故：`app/routes/v2.py` 曾出现 1145 行上帝函数
> （chat_stream_v2），导致无法单测、无法审查、修改高风险。以下规则为硬约束。

## 一、结构硬约束（违反 = 必须重构，不得以"先跑通"为由跳过）

1. **单个函数 ≤ 80 行**。超过必须拆分：主流程只做编排，具体步骤拆成私有辅助函数。
2. **单个文件 ≤ 600 行**。超过必须拆分模块（routes/ 只放路由与参数解析，业务逻辑下沉 service/ 或 agents/）。
3. **禁止上帝函数**：一个函数只做一件事。如果函数名是"流程名"（如 chat、generate、handle），
   里面超过 3 个职责段（缓存、路由、工具、记忆、降级……），必须拆。
4. **新增功能优先拆新函数，禁止在现有函数尾部追加**。追加超过 20 行必须问：这属于谁？
5. 重复代码 ≥ 3 处必须抽公共函数（如 SSE 的 `yield "data: [DONE]"` 封装）。

## 二、代码风格

6. 所有函数/类必须：类型标注（含返回类型）+ docstring（一句话做什么 + Args/Returns）。
7. 配置一律走环境变量 / `_env.py`，禁止硬编码密钥与魔法数字。
8. 错误处理：可恢复错误返回结构化 `{"error": ...}`，不静默吞异常（except: pass 必须注释原因）。
9. 保持现有注释；删除代码前先确认调用方。

## 三、修改纪律（AI 生成代码前的强制步骤）

10. 修改任何函数前，先通读该函数全部代码，理解上下文再动手。
11. 改动影响面 > 2 个文件时，先向用户说明拆分方案，确认后再写。
12. 流式/长任务代码：必须有超时与取消路径（客户端断连处理）。
13. 异步代码禁止同步阻塞调用（time.sleep、同步 requests）。

## 四、AI 交叉检查清单（每次生成代码后必须自查）

14. 输出前逐条自查并报告：
    - [ ] 每个函数 ≤ 80 行？
    - [ ] 没有在现有函数尾部追加新逻辑？
    - [ ] 类型标注 + docstring 齐全？
    - [ ] 没有重复代码 ≥ 3 处？
    - [ ] 密钥/配置没有硬编码？
    - [ ] 异常处理符合第 8 条？
15. 自查不通过 → 先重构再交付，不得说"可以稍后优化"。

## 五、测试

16. 关键逻辑（缓存、锁、限流、解析）必须配 pytest 测试，放 tests/ 下。
17. 重构后必须跑通相关测试或至少验证原行为不变。
