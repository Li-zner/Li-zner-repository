# 业务定义（人类填写）

> 定义「这个项目做什么」。AI 据此设计领域模型与核心流程。

## 1. 产品定位

- 目标用户：`[待定]`
- 核心价值：`[待定]`
- 竞品/参照：`[待定]`

## 2. 领域模型（核心实体）

| 实体 | 关键字段 | 关系 | 备注 |
|---|---|---|---|
| `[待定]` | `[待定]` | | |
| `[待定]` | `[待定]` | | |

## 3. 核心流程（写清楚状态流转）

### 流程一：`[待定]`
```
[待定] → [待定] → [待定]
```
- 边界/异常：`[待定]`

### 流程二：`[待定]`
```
[待定]
```

## 4. 非功能要求

- 并发规模预估：`[待定]`（如 100 QPS → 需要多实例 + Nginx）
- 数据量预估：`[待定]`
- 可用性要求：`[待定]`（如 99.5%）
- 多语言/多租户：`[待定]`

## 5. 明确「不做」的（防止 AI 过度设计）

- `[待定]`（例：本阶段不做多租户、不做消息队列）
